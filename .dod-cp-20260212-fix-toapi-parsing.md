# DoD - 修复 ToAPI 响应解析 - 处理嵌套的 data 结构和 SUCCESS 状态映射

## 验收标准

### 功能验收
- [ ] 视频生成完成后，立即显示"已完成"状态（不再停留在"排队中"）
      Test: manual:提交视频生成任务，观察状态变化，验证从 queued → in_progress → completed 的转换
- [ ] 进度条正确显示 100% 而不是 0%
      Test: manual:视频完成后查看进度条，应显示 100%
- [ ] 视频 URL 正确提取，可以点击播放
      Test: manual:点击已完成的视频，验证能正常播放
- [ ] ToAPI 返回 SUCCESS 状态时，轮询停止
      Test: manual:观察 Console Network 标签，验证轮询在视频完成后停止

### 技术验收
- [ ] `createVideoGeneration` 函数正确解包 `{code, data}` 结构
      Test: manual:查看代码，验证有 toApiResponse.data 的解包逻辑
- [ ] `getTaskStatus` 函数正确解包 `{code, data}` 结构
      Test: manual:查看代码，验证有 toApiResponse.data 的解包逻辑
- [ ] `mapToAPIResponse` 函数正确处理大写状态值（SUCCESS/FAILED/PROCESSING）
      Test: manual:查看代码，验证 statusMap 包含大写 key
- [ ] `mapToAPIResponse` 函数正确解析进度字符串 "100%" → 100
      Test: manual:查看代码，验证有 parseInt(progress.replace('%', '')) 逻辑
- [ ] `mapToAPIResponse` 函数从 `fail_reason` 字段提取视频 URL
      Test: manual:查看代码，验证有 fail_reason → result.video_url 的映射

### 测试验收
- [ ] 构建成功，无 TypeScript 编译错误
      Test: manual:npx vite build 成功
- [ ] 部署到香港服务器后功能正常
      Test: manual:访问 https://autopilot.zenjoymedia.media/ai-video 验证

### 回归验证
- [ ] 之前修复的"点击后空白"问题未复现
      Test: manual:点击"开始生成视频"按钮后，应立即显示"正在提交任务..."加载提示
