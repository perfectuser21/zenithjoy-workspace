server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # N8N REST API proxy (HK N8N on localhost)
    location /api/n8n/ {
        proxy_pass http://host.docker.internal:5679/api/v1/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # N8N Webhook proxy (HK N8N on localhost)
    location /api/n8n-webhook/ {
        proxy_pass http://host.docker.internal:5679/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Feishu auth backend (US server via Tailscale)
    location /api/feishu-login {
        proxy_pass http://100.71.32.28:3002/api/feishu-login;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SPA 路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location /assets {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript;
}
