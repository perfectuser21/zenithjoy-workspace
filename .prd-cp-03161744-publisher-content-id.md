# PRD: publisher 支持 --content-id 从 works 表查内容发布

## 目标

创建编排脚本 `scripts/publish-by-content-id.sh`，打通：创作→NAS→DB（已有）→ 发布→publish_logs（本次）。

## 范围

- 新建 `scripts/publish-by-content-id.sh`
- 新建 `scripts/__tests__/test_publish_by_content_id.sh`（DoD 验证测试）

## 不做什么

- 不修改现有 publish-kuaishou.cjs 等 publisher 脚本
- 不做前端 UI

## 成功标准

- 脚本接受 --content-id + --platform 参数
- 查询 zenithjoy.works 获取 nas_path
- 发布后写入 zenithjoy.publish_logs
