# DoD: WorkDetailPage ?tab URL 参数修复

[BEHAVIOR] Test: manual:chrome: 访问 /works/:id?tab=pipeline → 直接显示「全链路」Tab（不再默认进编辑 Tab）
[BEHAVIOR] Test: manual:chrome: 访问 /works/:id → 显示「编辑」Tab（默认行为不变）
[BEHAVIOR] Test: manual:chrome: 页面不再白屏
