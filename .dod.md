# DoD: 棒3b-2 探针读回经 ssh 在 MMV 跑 + videos_readback 口径修正

- [x] [BEHAVIOR] Test: tests/services/phone-adb-controller/__tests__/workflow-result.test.mjs — stage delivery 经 `ssh -o ConnectTimeout=20 <WFR_PROBE_HOST>` 跑 `set -a; source ~/.credentials/zenithjoy-db.env; set +a; cd <WFR_PROBE_DIR> && node verify-step.mjs --stage/--run-tag/--line-key/--word`（形状同 batch2.sh:55），stdout 末行 JSON 合进 result.probes；本机 node 不再拉起 verify-step
- [x] [BEHAVIOR] Test: tests/services/phone-adb-controller/__tests__/workflow-result.test.mjs — WFR_PROBE_HOST / WFR_PROBE_DIR 可覆盖，--word 中文与单引号照单引号包裹；ssh 超时/非 JSON/无 probes 数组 → probes=[] + WFR_WARN + exit 0 且回执照发；discovery 不 ssh；远端噪音行只取末行 JSON
- [x] [BEHAVIOR] Test: tests/services/phone-adb-controller/__tests__/checks-social-keyword-leadgen.test.mjs — videos_readback 与 line_key_not_null 同 target/同表/同 WHERE（`harvest_batch = '$RUN_TAG'`，不带 line_key）；resolveLineKey profile 名→路由键、认不出原样；WFR_PROBE_STAGES 兜底闸 == YAML stage 集合
- [x] [BEHAVIOR] Test: manual:bash .github/workflows/scripts/smoke/phone-adb-controller-smoke.sh — 层 29 改为 ssh 形状守卫（WFR_PROBE_HOST 默认 mmv / 远端命令形状 / resolveLineKey / README 落点）
- [x] README 基座 1/7 第 4 步部署清单改 MMV `~/.openclaw/leadgen-scripts/` 落点，xian-m4 / M1 不再需要探针文件与 zenithjoy-db.env
- [x] MMV 实跑三条 observed（TAG auto09252230：videos_readback == line_key_not_null 行数）
- [x] CI 全绿
