# DoD: Stop Hook 双钥匙状态机

## P0 验收清单（必须全部通过）

### ✅ DoD-1: Stop Hook 核心逻辑修复

**检查项**：
- [ ] Line 93-99: 检查 `.dev-lock` 存在（而不是 `.dev-mode`）
- [ ] 增加 `.dev-lock` 在但 `.dev-mode` 不在的处理分支
- [ ] 该分支输出明确错误信息并 exit 2
- [ ] 增加 sentinel file 检查（三重保险）

**测试**：
```bash
# 创建 .dev-lock 但删除 .dev-mode
echo "dev_lock" > .dev-lock
rm -f .dev-mode
echo '{}' | bash ~/.claude/hooks/stop.sh
# 预期：exit 2，输出包含 "dev-lock 存在但 dev-mode 缺失"
```

### ✅ DoD-2: 所有 block 强制阻止退出

**检查项**：
- [ ] Line 68: `exit 0` → `exit 2`（并发锁失败）
- [ ] Line 79: `exit 0` → `exit 2`（fallback 锁失败）
- [ ] Line 250: `exit 0` → `exit 2`（PR 未创建）
- [ ] Line 294: `exit 0` → `exit 2`（CI 失败）
- [ ] Line 304: `exit 0` → `exit 2`（CI 进行中）
- [ ] Line 314: `exit 0` → `exit 2`（CI 状态未知）
- [ ] Line 339: `exit 0` → `exit 2`（Step 11 未完成）
- [ ] Line 350: `exit 0` → `exit 2`（PR 未合并）

**测试**：
```bash
# 检查所有 {"decision": "block"} 后面都是 exit 2
grep -A 1 '"decision": "block"' ~/.claude/hooks/stop.sh | grep -c 'exit 2'
# 预期：8（8 处 block）
```

### ✅ DoD-3: 删除锁文件时同时删除 sentinel

**检查项**：
- [ ] Line 104-106: 同时删除 `.dev-lock` + sentinel（cleanup_done 提前退出）
- [ ] Line 145: 同时删除 `.dev-lock` + sentinel（15 次重试失败）
- [ ] Line 189-190: 同时删除 `.dev-lock` + sentinel（分支不匹配）
- [ ] Line 330: 同时删除 `.dev-lock` + sentinel（工作流完成）

**测试**：
```bash
# 检查所有 rm -f 都包含 .dev-lock
grep 'rm -f.*DEV_MODE_FILE' ~/.claude/hooks/stop.sh
# 预期：所有删除都包含 DEV_LOCK_FILE
```

### ✅ DoD-4: Step 3 Bash 原子写入

**检查项**：
- [ ] 删除 Write 工具调用
- [ ] 使用 `mktemp` + `mv -f` 原子写入 `.dev-lock`
- [ ] 使用 `mktemp` + `mv -f` 原子写入 `.dev-mode`
- [ ] 使用分支级别文件名（`.dev-lock-{branch}`）
- [ ] `.dev-lock` 创建失败 → exit 1
- [ ] `.dev-mode` 创建失败 → 警告但继续
- [ ] 创建 sentinel file（`.git/hooks/cecelia-dev-{branch}.sentinel`）

**测试**：
```bash
# 模拟 Step 3 创建逻辑
BRANCH="test-branch"
DEV_LOCK_TMP="$(mktemp .dev-lock.XXXXXX)"
echo "dev_lock" > "$DEV_LOCK_TMP"
mv -f "$DEV_LOCK_TMP" ".dev-lock-$BRANCH"
[[ -f ".dev-lock-$BRANCH" ]] && echo "✅ 原子写入成功"
```

### ✅ DoD-5: cleanup.sh 不删除状态文件

**检查项**：
- [ ] Line 392: RUNTIME_FILES 不包含 `.dev-mode`
- [ ] Line 466-485: 只标记 `step_11_cleanup: done`
- [ ] Line 466-485: 不删除 `.dev-mode` / `.dev-lock`
- [ ] 增加警告信息："状态文件将由 Stop Hook 自动删除"

**测试**：
```bash
# 检查 RUNTIME_FILES 不包含 .dev-mode
grep -A 20 'RUNTIME_FILES=' ~/.claude/skills/dev/scripts/cleanup.sh | grep -c '\.dev-mode'
# 预期：0（不包含）
```

### ✅ DoD-6: .gitignore 防止污染

**检查项**：
- [ ] `.gitignore` 包含 `.dev-mode*`
- [ ] `.gitignore` 包含 `.dev-lock*`
- [ ] `git rm --cached .dev-mode .dev-lock` 执行成功

**测试**：
```bash
# 检查 .gitignore
grep -E '\.dev-(mode|lock)' .gitignore
# 预期：至少 2 行
```

## P1 验收清单（推荐完成）

### ✅ DoD-7: 分支级别锁文件（并发隔离）

**检查项**：
- [ ] Step 3 使用 `.dev-lock-{branch}` 而不是 `.dev-lock`
- [ ] Stop Hook 检查 `.dev-lock-{current_branch}`
- [ ] cleanup.sh 删除 `.dev-lock-{branch}`

**测试**：
```bash
# 模拟并发会话
echo "dev_lock" > .dev-lock-cp-A
echo "dev_lock" > .dev-lock-cp-B
[[ -f .dev-lock-cp-A && -f .dev-lock-cp-B ]] && echo "✅ 并发隔离成功"
```

### ✅ DoD-8: Sentinel file 三重保险

**检查项**：
- [ ] Step 3 创建 `.git/hooks/cecelia-dev-{branch}.sentinel`
- [ ] Stop Hook 检查 sentinel 存在但 `.dev-lock` + `.dev-mode` 都不存在 → exit 2
- [ ] Stop Hook 完成时删除 sentinel

**测试**：
```bash
# 模拟同时删除
echo "dev_workflow_active" > .git/hooks/cecelia-dev-test.sentinel
rm -f .dev-lock .dev-mode
echo '{}' | bash ~/.claude/hooks/stop.sh
# 预期：exit 2，输出 "sentinel 存在但状态文件丢失"
```

## 集成测试

### 测试场景 1：正常工作流

```bash
# 1. 创建分支 + 状态文件
git checkout -b test-workflow
bash ~/.claude/skills/dev/steps/03-branch.md  # 模拟 Step 3

# 2. 验证文件存在
[[ -f .dev-lock && -f .dev-mode && -f .git/hooks/cecelia-dev-test-workflow.sentinel ]]

# 3. 模拟 Stop Hook 检查
echo '{}' | bash ~/.claude/hooks/stop.sh
# 预期：exit 2（未完成，阻止退出）

# 4. 标记完成
sed -i 's/step_11_cleanup: pending/step_11_cleanup: done/' .dev-mode

# 5. 再次检查
echo '{}' | bash ~/.claude/hooks/stop.sh
# 预期：exit 0（完成，允许退出），文件被删除
```

### 测试场景 2：`.dev-mode` 创建失败

```bash
# 1. 只创建 .dev-lock
echo "dev_lock" > .dev-lock

# 2. Stop Hook 检查
echo '{}' | bash ~/.claude/hooks/stop.sh
# 预期：exit 2，输出 "dev-lock 存在但 dev-mode 缺失"
```

### 测试场景 3：cleanup.sh 时序

```bash
# 1. 创建状态文件
echo "dev\nstep_11_cleanup: pending" > .dev-mode
echo "dev_lock" > .dev-lock

# 2. 运行 cleanup
bash ~/.claude/skills/dev/scripts/cleanup.sh test-branch

# 3. 验证文件仍存在
[[ -f .dev-mode && -f .dev-lock ]] && echo "✅ cleanup 未删除状态文件"

# 4. 验证标记完成
grep -q "step_11_cleanup: done" .dev-mode && echo "✅ cleanup 已标记完成"
```

## 完成标准

- ✅ 所有 P0 验收清单通过
- ✅ 所有集成测试通过
- ✅ 无 lint 错误
- ✅ 文档更新（11-cleanup.md）
