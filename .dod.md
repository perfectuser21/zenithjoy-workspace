# DoD: zenithjoy-dashboard 仓库初始化

## 验收清单

### 必须通过
- [ ] `npm install` 无错误
- [ ] `npm run build` 无 TypeScript 错误
- [ ] 构建产物在 dist/ 目录
- [ ] GitHub 仓库可访问

### 代码质量
- [ ] 无 @cecelia 命名空间残留（除 stubs）
- [ ] vite.config.ts 不依赖 monorepo 路径
- [ ] package.json 名称为 @zenithjoy/dashboard

## QA 决策

参考：本仓库为纯前端项目，初始化阶段无需复杂测试。

测试类型：手动验证构建
覆盖范围：构建成功即可
