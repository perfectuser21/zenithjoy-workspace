## 全平台发布脚本清单汇总（2026-03-26）

### 根本原因

本次 CI L3 Code Gate 失败一次：`picomatch <=2.3.1` 高危 ReDoS 漏洞（pre-existing）。
Task Card 中使用了 `ls`/`git` 等非白名单 manual: 命令，被 bash-guard.sh 拦截两次。

### 下次预防

- [ ] 写 Task Card DoD 测试命令时，直接用 `manual:node -e "..."` 替代 `ls/cat/git/grep` 等 shell 命令
- [ ] 新建分支前先运行 `npm audit --audit-level=moderate --omit=dev`，pre-existing 漏洞应在 Task Card 里明确记录为 PRESERVE 条目
- [ ] CI 白名单：`node / npm / npx / curl / bash / psql`，其他命令须通过 `bash -c "..."` 包装
