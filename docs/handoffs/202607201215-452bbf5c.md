# Handoff:line04 刀A——overlay pywebview 确定性供给+消灭静默降级+判据焊进 golden path

- task_id: 452bbf5c-88c0-4793-b177-223e7d66b073
- verdict: PASS(PR #1429 已合并,main squash cf 见 PR;后续补丁 PR 见 artifacts)
- decision_refs: c7022118(安装框架方向拍板) / 145014a4(修层 evaluator 必跑纪律) / 9202c14e(部署链禁 warning 降级) / fc17d9eb(判据折进 golden path 不养散装)

## 完成(done)

1. **供给链**:pywebview==6.2.1 锁版进 build-install-pack.sh WHEEL_PKGS + 两份 requirements.txt;非 Windows 交叉打包拆 WHEEL_PKGS_BASE + PYWEBVIEW_CLOSURE(--no-deps 显式闭包 8 包,防宿主 marker 求值坑,proxy_tools 无 wheel 用 --no-binary 放行);pip 预装失败硬红 exit 1+3 次退避重试;site-packages 对 pywinauto/webview 双目录硬断言。
2. **客户机自修复**:preflight.ts `installPywebview`(清华→官方双源回退,从不 throw,失败冒泡 `pywebview_install_failed`);overlay.ts start() 在 `pywebview_missing` 时经 `_repairFuncs` 自动补装再复检——覆盖"core 换目录"事故场景(agent 重启即触发)。
3. **消灭静默降级**:OverlayHandler.getStatus() 状态机(9 种 reason 含 user_closed/circuit_open/overlay_script_missing/module_down)→ line04 健康 IPC `overlay` 字段 → module-manager `healthReport['line04-overlay']`(lineId 来源隔离;模块重启超限终态同步置 module_down 防陈旧绿灯)→ 心跳 module_status(服务端 agents.module_status jsonb 已持久化);diag 成功态无条件覆写。
4. **看板**:ModuleHealthPage 加 "Line04 框框浮窗" 列;诊断页(AdminCustomersPage)动态遍历自动显示。
5. **闸门**:agent-installpack dryrun verify 对打包产物 python-embedded 硬断言 `import webview`;wechat-cs-e2e 探针去 `|| true` 转硬+锁 6.2.1;**GP-4 Step 3k 供给链四处断言**(WHEEL_PKGS/requirements/installPywebview/红灯上报,拆任一即红)。
6. **版本全同步**:core 2.0.87 / module 1.0.148(manifest×2 + 服务端 required_version + 4 个 smoke 版本锚 + lock)。
7. **三发变异 proven-to-fire**:sprints/07201020-overlay-pywebview-supply/evidence/proven-to-fire.md(删 WHEEL_PKGS→红/注释上报→红/删 requirements→红,均还原绿)。

## 未完成(not_done,范围外)

- 主动告警(红灯持续→飞书/企微推送)——用户拍板另立小刀
- 存量散装 overlay smoke(line04-ai-overlay-*)折并 GP-4
- follow-up 留档:PYWEBVIEW_CLOSURE 传递依赖未锁精确版+Step 3k 未锚 CLOSURE(M3)/downloadDir 跨进程竞态/start() 无重入/syncModules 升级中缺 overlay 止损/rog 探针共享 conda 无 venv/respawn 路径 getStatus 盲区(M2)/首报 not_started 红闪 ≤30s(M1)

## next_steps

1. **真机轻量 evaluator(merge 后验收,145014a4 修层纪律)**:等 rog OTA 至 core 2.0.87+module 1.0.148,断言心跳 `module_status['line04-overlay']` 出现且看板"Line04 框框浮窗"列真实变色;红灯路径可临时移除 pywebview 看红→autoRepair 补装→绿。
2. **API 部署后重启中台**(mmv/hk-vps)使 required_version=1.0.148 下发生效(memory 惯例 feedback_module_bump_needs_mw_redeploy)。
3. **确认 installpack 出包**:合并后首次 dispatch 因 PS5.1 非 ASCII em-dash 解析崩(本 PR 修复);修复合并后需再 dispatch 一次 agent-installpack 确认 build+publish 全绿、2.0.87 包上 COS。
4. 逐台客户机核实 overlay 影响面(handoff_0720 遗留任务③)+ 刀B(安装框架 PrepPRD)+ 刀C(框框内容重设计,建议走 GP 提案对抗)。

## data_sources

- 设计: docs/superpowers/specs/2026-07-20-overlay-pywebview-supply-design.md
- 计划: docs/superpowers/plans/2026-07-20-overlay-pywebview-supply.md
- 变异证据: sprints/07201020-overlay-pywebview-supply/evidence/proven-to-fire.md
- diag: C:\Users\Public\overlay-diag.json(现成功态也覆写)

## artifacts

- PR #1429(主体,已合并) + 本修复 PR(installpack PS ASCII + 本 handoff)
- branch: cp-0720101623-overlay-pywebview-supply / cp-0720121500-installpack-ascii-fix
