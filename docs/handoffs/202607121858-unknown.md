# Handoff：Stage2 派发排除 rejected 视频 + dm_assignments cancelled CHECK 约束修复

- task_id: unknown（Brain task 注册失败，本次未获得有效 task_id，跳过 DB 写，仅写本镜像文件）
- initiative_id: null
- journey_id: Line02 智能获客（内容判定门槛 sprint 07120952 的衍生 bugfix）
- verdict: PASS
- created_at: 2026-07-12T10:58:00.000Z

## 完成（done）
- Stage2 派发查询（`apps/api/src/routes/acquisition.ts`）新增 `judgment_status != 'rejected'` 过滤：真机验收发现已被判定门槛标记为 rejected 的视频仍会被派发到 Stage2 抓评论，现已阻断。未采用 `= 'matched'` 的严格过滤，因为当前真实采集主链路从未调用 `/judge-video`，严格过滤会导致 Stage2 派发永久归零（比原 bug 更严重的回归）；改用排除法仅堵住"rejected 视频不该进 Stage2"这一具体缺陷。
- `dm_assignments` 表 `chk_dm_assign_status` CHECK 约束补充 `'cancelled'` 状态值（新迁移 `apps/api/db/migrations/20260712_dm_assignments_cancelled_status.sql`）：真机验收复现 `acquisition-dispatch.ts` 的 `rescoreLead()` FR-8 降级取消逻辑（`outreach_eligible` 变 false 时 UPDATE `dm_assignments SET status='cancelled'`）直接违反此前两次迁移遗漏的约束，生产环境该代码路径此前会硬报错。
- 两个 bug 均按 TDD 先写 failing test 再修复：bug1 为 `apps/api/src/routes/acquisition.test.ts` 内新增单测断言 SQL 含 `judgment_status != 'rejected'`；bug2 为新增真 Postgres 集成测试 `apps/api/tests/integration/p2-line02-content-judgment/dm-assignments-cancelled-status.integration.test.ts`（查询 `pg_get_constraintdef` 断言约束定义含 `'cancelled'`），已注册进 `test-registry.yaml`（CI 的 Orphan Test Check 首次因未注册而失败，已修复）。
- PR #1243 已合并（squash merge，CI 全绿）。

## 未完成 / 显式范围外（not_done）
- 判决闸客户端接线未完成：真实采集主链路（Android agent/服务端）目前从未调用 `/judge-video`，导致 `judgment_status` 永远停留在默认值 `pending`，判定门槛功能实际上未在生产路径生效。这是本次真机验收暴露的更深层缺口，规模远超本次两个 bugfix，需要单独立 PrepPRD。
- 原 PrepPRD（`sprints/07120952-line02-content-judgment-gate/prep-prd.md`）5 条 windows_wechat 验收标准中，"图文帖判定链路"和"视频帖判定链路"两条仍为 BLOCKED/FAIL 状态，需等上述客户端接线完成后才能重新验收。
- 会话中发现的其他明文密钥泄露（OpenAI/OpenRouter/飞书/抖音/LICENSE_HMAC/BETTER_AUTH 等，散落在 `com.zenithjoy.api.staging.plist` 及多个 `.bak.*` 备份文件中）未处理，用户仅授权处理最初报告的单个 Anthropic key（已 chmod 600 锁定权限，真正轮换需用户手动在 console.anthropic.com 操作）。

## 下一步（next_steps）
- 把"判定门槛客户端接线"（Android agent 或服务端在真实采集流程中调用 `/judge-video`，使 `judgment_status` 能真正从 pending 流转）立为新的 /dev 路径 C（大功能）PrepPRD，完成后重跑 5 条 windows_wechat 真机验收标准。
- 视用户是否授权，评估是否扩大安全整改范围到其余散落明文密钥（OpenAI/OpenRouter/飞书/抖音/LICENSE_HMAC/BETTER_AUTH），并完成 Anthropic key 的真正轮换（console.anthropic.com 手动操作，非 CLI 可达）。

## 数据源（data_sources）
- `sprints/07120952-line02-content-judgment-gate/prep-prd.md`
- `apps/api/src/routes/acquisition.ts`（Stage2 派发查询）
- `apps/api/src/services/acquisition-dispatch.ts`（FR-8 rescoreLead 降级取消逻辑）
- `apps/api/db/migrations/20260712_dm_assignments_cancelled_status.sql`
- `test-registry.yaml`（`line02-content-judgment-dm-assignments-cancelled-status` 条目）

## 决策引用（decision_refs）
- CLAUDE.md Bug Fix 流程（/dev + systematic-debugging，failing test 先行，regression test 永久留 CI）

## 产物（artifacts）
- pr_urls: ["https://github.com/perfectuser21/zenithjoy-workspace/pull/1243"]
- sprint_dir: null
- branch: cp-07121839-content-judgment-stage2-wiring
- docs: []
