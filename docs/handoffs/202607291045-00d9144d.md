# Handoff：fix(agent-android): 安卓Path2账号扫描OPEN_PANEL_FAILED——个人页导航检查改轮询等待

- task_id: `00d9144d-972f-4554-8cb0-2880bf5be7dc`
- journey_id: `afa6abca-53c0-4815-8594-b7fb81ca547f`（客户智能获客路径）
- verdict: **PASS**

## 完成
- 根因定位：`openSwitchAccountPanel()` 点我tab后单次 `delay(1500L)` 检查过早，个人页渲染慢于1500ms时误判 `OPEN_PANEL_FAILED`，3次CLEAR_TOP重试系统性命中同一时序窗口全部失败（真机复现证据：07-28 staging `agent_scan_failures` 表，2台干净设备 afc3505c/f182e64e 共3次，两台失败 tree_dump 均证实"我，按钮"节点其实一直存在但从未进入个人页）
- 写 failing regression test `DeviceAccountScanServiceSwitchEntryPollTest.kt`（源码静态断言写法，同仓库无 Mockito/Robolectric），commit-1 验证修复前两条断言均红
- 修复代码：对齐同文件 `readMyProfile()` 已验证过的轮询等待模式，最多轮询4次每次 `delay(800L)` 重新取根节点查"切换账号"锚点，commit-2 验证测试全绿（account包+全模块）
- 判据回流 `golden-path-2-smoke.sh`（Step 30后加TODO标记，说明真机UIA时序CI跑不到、等价断言指向单测、待真机通道复跑确认）
- PR #1523 已合并（zenithjoy-workspace，squash merge）

## 没完成
- 未做真机复跑验证——CI环境无真实Android设备，轮询等待策略修复本身无法在CI里验证是否真的解决了真机上的问题，需要下次真机复测确认
- PRD里提到的设备8346ab56的子模式（已到个人页、切换账号节点找到、点击后面板不渲染，6次）未处理——已判定为人为操作噪音排除在本次判据外，但若真机复测这个子模式依然独立稳定复现，可能是另一个未修的bug
- handoff_0729记忆里提到的另一个任务"前台放弃安卓获客任务(暂停/放弃控制)"（Brain任务 `de4bf363`）本次未涉及，仍是 queued

## 下一步
- 安排一次真机复测（原PRD建议的 afc3505c/f182e64e 两台设备优先），确认 `OPEN_PANEL_FAILED` 不再复现，并把 smoke.sh 里的 TODO 标记转正
- 若真机复测仍发现"找到切换账号节点但点击后面板不渲染"这个子模式独立稳定复现，需要新开一个bug PrepPRD单独诊断（不是本次修的这个根因）

## 数据源
- `sprints/07290930-android-open-panel-failed-navigation-bug/prep-prd.md`
- `services/agent-android/app/src/main/kotlin/com/zenithjoy/agent/account/DeviceAccountScanService.kt`
- `services/agent-android/app/src/test/kotlin/com/zenithjoy/agent/account/DeviceAccountScanServiceSwitchEntryPollTest.kt`
- `zenithjoy_staging.agent_scan_failures` 表（hk-vps docker exec zenithjoy-db-postgres）

## 决策引用
- `2db1c180-0900-4781-9254-7f4776cd7a4c`

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1523
- sprint_dir: `sprints/07290930-android-open-panel-failed-navigation-bug`
- branch: `cp-07291021-fix-android-openpanel-failed`
