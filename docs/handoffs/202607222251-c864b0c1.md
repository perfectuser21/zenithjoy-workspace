# Handoff：fix(staff-hub): 飞书邮箱字段兼容 enterprise_email 兜底

**Verdict**: PASS
**Task ID**: c864b0c1-5fd2-48d4-85fb-732c46c64040

## 完成
- 定位Staff Hub飞书登录'email为空'长期未解问题的第二个候选根因：飞书authen/v1/access_token接口除email外还有独立的enterprise_email(企业邮箱)字段
- TDD修复：新增红测试(enterprise_email兜底场景，email为空但enterprise_email有值时期望200实际403)→实现fetchFeishuUserByCode兜底逻辑(email||enterprise_email)→apps/api staff.test.ts 21/21全绿
- tsc --noEmit + eslint均通过，PR #1461已合并(auto-merge squash)

## 没完成
- 未确认这就是根治：用户尚未回复其测试飞书账号本身('我的'→设置→账号)是否绑定了邮箱(email或enterprise_email任一)；若该账号两个字段在飞书侧都为空，此修复不会生效，需要换一个已绑邮箱的测试账号验证
- 本次未做后端生产环境promote(apps/api属于human-gated promote-prod-hk.yml，需用户手动确认触发)

## 下一步
- 等待用户确认测试飞书账号是否绑定邮箱；若绑定了，需触发promote-prod-hk.yml把这次修复推到生产，再让用户重新扫码登录验证email是否非空
- 若用户确认账号确实没绑邮箱，需要换一个已绑邮箱的飞书账号做验收测试，而非继续在字段兼容上下功夫

## 数据源
- apps/api/src/routes/staff.ts (fetchFeishuUserByCode / POST /feishu-login)
- apps/api/src/routes/__tests__/staff.test.ts (feishu-login测试组)
- Memory: 无独立handoff记录此问题此前的多轮迭代，详见本session对话历史(golden_path/staff-hub飞书登录调试)

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1461
- 分支: cp-07222232-feishu-enterprise-email-fallback
