# Handoff：Stage2 抓评论主动两层等待 —— line02 获客端到端首次全程打通

task_id=unknown（对话直连 /dev 路径 A，未注册 Brain task）
verdict: PASS ｜ PR https://github.com/perfectuser21/zenithjoy-workspace/pull/1658 (merged)
branch: cp-08190928-stage2-comment-await ｜ GP-Anchor: line02/keyword_acquisition keep-green

## 完成

1. **根治 Stage2 抓评论恒 0 条**（agent-android 2.1.25）
   `handleVideoUrlOpened` 是纯事件回调，`rootInActiveWindow ?: return` + `findVisibleCommentButton ?: return`
   拿不到就静默丢弃、零重试。深链刚拉起抖音时详情页未渲染完，这一次探测必然扑空，watchdog
   随后在**没打开评论面板的详情页**上抓取，稳定 0 条。改为与 startCollect 相同的两层等待
   （awaitAppForeground 消厂商插屏 + awaitNode 等评论按钮，24×500ms），等不到报
   NO_COMMENT_BUTTON/NO_WINDOW 不再静默交 0 条；删 handleVideoUrlOpened /
   startVideoUrlOpenTimeout 两条旧路径；findVisibleCommentButton 无界全树 BFS →
   NodeTreeFlattener 带上限遍历（它现在是 awaitNode 的 finder，每轮都调用，必须廉价）。

2. **守卫** `.github/workflows/scripts/__tests__/stage2-comment-await.test.sh`（4 条断言），
   已接入 ci-l1-process.yml 显式清单。proven-to-fire：commit-1 时 FAIL=4，实现后 PASS=4，
   弄坏再红、还原再绿。

3. **真机端到端首次全程打通**（四号机 192.168.1.96，关键词「装修」）
   修复前：extracted 0 comments ×2 → lead_count_raw=0
   修复后：lead=4 → grade 精准×2/感兴趣×1 → outreach_eligible=true → dm_assignments queued×2
   「10w预算，精装修三室两厅」被 DeepSeek 判精准并自动开私信闸。

## 纠正的错误认知（重要，别再走回头路）

- **`outreach_eligible` 不是人工复核闸**。`rescoreLead` 在评论回填事务里自动调用
  （acquisition.ts:1346/1371），规则 `highestGrade === '高意向' || '精准' → true` 全自动。
  backfill-null-grade-leads.ts 里那句「人工复核后」注释只属于那个脚本，不代表主链路。
  本次实测已证明它会自己变 true。**曾据此立项"拆闸门"，是错的，已撤销。**
- 08-18 之前采回 EDG 电竞视频不是判定链坏，是**搜索没成功时 agent 在推荐流上采**；
  判定正确拒绝了它们，评论打分正确给「其他」，闸门正确不开——全链每一环都在做对的事。

## 没完成 / 遗留

- **两个包共用同一个 agent_id**（prod com.zenithjoy.agent 与 e2e com.zenithjoy.agent.e2e
  在四号机上都是 e017953c-…），中台派单时互抢。e2e 包的无障碍服务此前从未启用，因此
  **CI 名义上 SMOKE_APP_PKG=com.zenithjoy.agent.e2e，实际一直由 prod 2.1.21 抢先接单执行**。
  本次为验证已把四号机临时改成 e2e 独占（摘掉 prod 三个无障碍服务）。**需拍板：恢复双包
  还是固化 e2e 独占；并给两个包分配不同 agent_id。**
- 小黄（荣耀 MAA-AN00）需人在「启动管理」手动开后台（MagicOS 冻结，adb 治不了）
- 小白（realme RMX3478）无障碍仅 2/6，需人在设置里手动开（ColorOS 拒绝 adb 写）
- `publish_date` 全空（表有字段，代码从没写过）；抖音搜索筛选器方案未做
- MAX_ENRICH_LEADS=5 写死：每视频只挖前 5 条评论的抖音号，超出的直接留空
- CardCountSettleTest.kt（采集张数波动话题的半成品，引用不存在的函数）暂存于本 session
  scratchpad，未进仓

## 下一步

1. 给 prod / e2e 两个包分配不同 agent_id，消除互抢；并确认 CI 真正测的是哪个包
2. 用合并后的 main 跑一次真机 CI，确认 lead>0 成为 CI 可见判据（本次是手动派任务验证的）
3. 办公室两台（小黄/小白）人工点完系统开关后复验

## 数据源

- services/agent-android/app/src/main/kotlin/com/zenithjoy/agent/collect/DouyinCollectService.kt
  （startStage2Collect / findVisibleCommentButton）
- services/agent-android/app/src/main/kotlin/com/zenithjoy/agent/uia/NodeAwait.kt（两层等待原语）
- apps/api/src/services/acquisition-dispatch.ts:252-332（rescoreLead → outreach_eligible 规则）
- apps/api/src/services/comment-grading.ts（DeepSeek 评论分档）
- apps/api/src/services/acquisition-collect.ts:168-197（settleCollectTask：videoDone 只数落章）
- zenithjoy.acquisition_leads / acquisition_lead_comments / dm_assignments
