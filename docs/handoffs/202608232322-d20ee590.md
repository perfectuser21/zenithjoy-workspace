# Handoff：AI on-call 加抖音UI识别参考知识(STEP_KNOWLEDGE) + gpt-5.6-terra不稳定切gpt-5.4-mini

- task_id: d20ee590-9e88-40e0-b640-5d41b4a6fb03
- verdict: PASS
- journey_id: line02/customer_smart_acquisition
- created_at: 2026-08-23T15:23:24.000Z

## 完成
- 新增 STEP_KNOWLEDGE(Record<step,string[]>) + renderStepKnowledge(step)，按 step 精确注入 locate/extract/extract_list 三个 prompt 构造函数，只命中匹配 step 不做 value-stream 通用兜底(用户明确拍板的架构)
- 首个知识条目覆盖 scan_me_tab：抖音底部导航栏(首页/朋友/消息/我)共享同一 view_id 模板、靠 content_desc 判定、相邻未读数角标(id=.../1u_,desc=-)易被误选
- locator-assist.test.ts 新增用例验证 step 精确命中/不相关 step 不命中
- rpa-locator-assist-smoke.sh Section 8：用真实生产 bug 的树结构(首页/消息共享 0qm + 角标 1u_ + 我)做永久回归断言，真调用模型验证
- 深查 gpt-5.6-terra 不稳定根因：nonce 差分测试法确认 prompt_tokens 异常膨胀(336→8262)/SSE 格式泄漏/超时，2/5~3/10 异常率；同账号同端点 gemini-2.5-flash-official 0/4 异常，证明是渠道/模型级问题非 TOAPIS 平台级
- 查 TOAPIS 公开 pricing 端点选出 gpt-5.4-mini：input/output 双轴比 gemini-2.5-flash 更便宜(0.1125/4 vs 0.12/8.33)、平台第三大真实用量(5401次/天)、8/8 正确率(含难任务)、支持 reasoning_effort:none
- ASSIST_MODEL/GRADING_MODEL/COMMANDER_MODEL/csLlm 默认模型统一切 gpt-5.4-mini(locator-assist.ts/comment-grading.ts/content-judgment.ts/wechat-draft.ts + env-registry.ts 注释同步)
- 顺手修复无关阻塞：hk-vps cloudflared tunnel QUIC 握手超时导致 staging 502，重建容器加 --protocol http2 修复，staging/生产健康检查均恢复200

## 没完成
- STEP_KNOWLEDGE 目前仅覆盖 scan_me_tab 一个 step，其余 step(私信/评论/采集链等)的已知坑尚未沉淀成条目，需在后续踩坑时逐步补充(不做批量預填)
- PR#1726 尚未 promote 到生产(hk-vps prod)，生产仍跑此前 PR#1725(bounds-fallback修复)已promote的版本，尚未含本次 STEP_KNOWLEDGE + gpt-5.4-mini
- 尚未用 PR#1725+PR#1726 完整叠加后的代码在真机上再跑一轮强制失败闭环验证(用户反复要求的'百分百跑通'尚未在完全修复后的最终栈上验证过)

## 下一步
- 1) 用 promote-prod-hk.yml 把 main(含PR#1726)promote到生产hk-vps
- 2) 用已构建的 cp-08232124-forced-failure-verify2 e2e 测试包(或重新构建含1726改动的新包)在真机上对生产再跑一轮强制失败闭环测试，拿到'bounds fallback+STEP_KNOWLEDGE+稳定模型'三者叠加后的最终证据
- 3) 更新 MEMORY.md：补 bounds-fallback bug(PR#1725)、STEP_KNOWLEDGE架构决策及用户原话依据、gpt-5.6-terra不稳定发现、cloudflared QUIC/HTTP2 修复(可并入 staging_nginx_stale_ip_502_fleet_heartbeat.md 或新建)
- 4) 清理两条一次性强制失败测试分支(cp-08232010-forced-failure-verify, cp-08232124-forced-failure-verify2)及其 worktree

## 数据源
- apps/api/src/services/locator-assist.ts (STEP_KNOWLEDGE定义处)
- apps/api/src/services/comment-grading.ts / content-judgment.ts / wechat-draft.ts (模型默认值)
- apps/api/src/env-registry.ts (OPTIONAL_ENV reason 注释)
- .github/workflows/scripts/smoke/rpa-locator-assist-smoke.sh Section 8
- TOAPIS GET /api/pricing, GET /v1/models (模型选型依据)

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1726
- branch: cp-08232144-ui-knowledge-ref
