# Handoff：朋友圈点赞链路真机打通（read-moments-list/open-moment-card-by-text/like-current-card）

**verdict**: PASS  ·  **task_id**: fb4a02b2-4524-4bcd-afd6-8bb10cc00d83  ·  **branch**: cp-09171557-moments-like-repost

## 完成
- wechat-moments-adb 新增 `read-moments-list`（朋友圈摘要读取，纯数据不判断）、`open-moment-card-by-text`（按文案定位点开具体一条）、`like-current-card`（点赞+核验）三个命令，全部在真实客户账号（周宁@燕文物流）上真机验证通过。
- 重写并修复 `open-contact-moments`：改用「···→聊天信息→单头像」路径规避一对一聊天头像歧义，加装身份核验安全闸（vision_judge 核对落地资料页姓名），复测确认正确定位到目标客户而非误触自己头像。
- **真机发现并修复关键坑**：单条动态详情页存在两种布局——A) 直接可见「赞 评论 详情」三按钮条；B)「详情」页赞/评论按钮折叠进日期旁「···」图标，需先展开悬浮条才能点赞。最初实现靠 `resolve_coord` 对「赞」的成功/失败判断走哪条路，真机复现过一次误判：视觉模型在目标不可见时仍"蒙"出一个坐标（碰巧点开了「···」），被误判为直接命中布局A，实际没点到真正的赞按钮。改为显式 `vision_judge` 状态判定（悬浮条是否已可见）决定要不要先展开，修复后重跑通过，点赞列表正确出现自己头像。
- `wechat-moments-controller-smoke.sh` 新增层6/7：新命令面存在性检查 + `bar_visible` 显式状态判定护栏（防止同类误判回归）。
- `android-moments-like`/`android-moments-repost`/`android-moments-publish` 三个独立 skill 已在此前会话拆分完成并合并（zenithjoy-skills PR#198）。

## 没完成 / 范围外
- 跟圈（repost）无新增设备端命令——按已确认的架构设计，跟圈机械动作层面完全复用 `open-contact-moments`/`read-moments-list`/`open-moment-card-by-text`（浏览判断）+ `compose-photo-post`/`publish`（发布），无需重复实现；改写/内容判断发生在 AI 编排层，不在设备脚本层。
- 跟圈真实业务阻塞未解：内部暂无第二个配置好的朋友圈销售账号可作为内容源（四台机另三台跑抖音获客），需主理人安排账号源后才能进入真实 VALIDATE（已记录在 `android-moments-repost/SKILL.md`）。
- 点赞客户分层判断未落库表——主理人此前拍板「先不动库表，口头维护」，本次未新建任何分层数据结构，AI 现场读内容判断安全性。
- 未搭建 moments-like-tick.sh 自动化定时调度——点赞天然需要 AI 现场读内容做判断，不适合像发布那样纯队列轮询调度，暂以 AI 交互式调用 wechat-moments-adb 命令的方式使用。
- 复盘（数据日报/优化）完全未开始，是五件套里唯一还没设计的一环。
- Brain API `PATCH /tasks/:id` 完成态回写返回 `accepted:false reason:pr_not_found`（task 未能转为 completed，目前状态 blocked，result 字段已正确写入 merged/pr_url/handoff）——疑似该任务创建时缺少可交叉核验的 pr 关联元数据，非本次代码问题，留给下次遇到同类任务时排查。

## 下一步
- 跟圈：等主理人安排真实内部销售账号源后，用本次验证过的浏览命令链 + compose-photo-post/publish 组合跑通首次真实 VALIDATE。
- 点赞：视使用频率决定是否需要包一层轻量调度（如每天固定时段 AI 主动巡检几个重点客户），目前靠交互式调用已可用。
- 复盘：需要单独走 /capability 判定它在 GP 体系里的归位，再进入 PrepPRD。

## 数据源
- `services/wechat-moments-controller/wechat-moments-adb`（核心控制器，本次改动的唯一文件）
- `services/wechat-moments-controller/README.md`（坐标协议+命令表）
- `~/perfect21/zenithjoy-skills/android-moments-like/SKILL.md`
- `~/perfect21/zenithjoy-skills/android-moments-repost/SKILL.md`
- `~/perfect21/zenithjoy-skills/android-moments-publish/SKILL.md`
- `product-map/generated/product-map.json` 的 `line04/moments_interaction` GP 定义

## 决策引用
- 578643bb-258a-477f-bcc5-621f71a6ae94
- aedcfab4-03d5-4ea0-82c2-8818cc2159c5

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1856
