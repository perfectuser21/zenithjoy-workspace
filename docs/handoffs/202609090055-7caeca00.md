# Handoff：line01 刀1 作品→发布任务派发接缝（PR#1791）

- **task_id**: 7caeca00-d5dd-4d31-8412-d05ef077803a · **verdict**: PASS
- **journey**: line01 安卓端多平台自动发布（24987ee5）· golden_path c5c0e259
- **PR**: https://github.com/perfectuser21/zenithjoy-workspace/pull/1791（已合并，squash b8c9d1ab）

## 完成

1. **POST /api/contents/:id/publish**：作品（contents）按平台拆 publish_tasks，统一发布包（标题/文案/storage_key）进 payload；事务内 CAS 幂等（堵 TOCTOU 连点双发）；平台白名单+去重；限流。
2. **GET /api/publish-tasks** + **GET /:id/package**：执行器发现/领取作业单，领取时现签素材 COS URL（TTL 1h，payload 不存 URL 防过期）。
3. **getQueuedTasks 排除 content_publish**（IS DISTINCT FROM）：旧 Windows/安卓 agent 心跳不误领，NULL 与既有 task_type 全保留。
4. **smoke 进 CI 基线**：content-publish-dispatch-smoke.sh（8 步，含真调心跳负例、跨租户 404），并登记为 customer_first_success 的 smoke_files 验收面。
5. **staging 真验通过（主理人专属号 ZJ-E-ALEX5211，tenant b0058fb7）**：传素材建作品→一键拆 3 平台任务→重复派发 409→领包标题文案原样带出→COS 签名 URL 真下载 HTTP 200 内容逐位一致→心跳负例 0 混入。
6. **P0 顺手止血**：deepseek-v4-flash 渠道再坏（真调返回 C2PA 二进制垃圾）、gpt-5.4-mini 渠道 #159 403——评论分档默认模型切 gpt-5.6-terra（先例 PR#1719），staging 已生效。

## 没完成

- **生产 API 未 promote**：生产评论分档此刻仍用坏渠道（grade 全 NULL），**疑与 0902 起零线索连败相关**——promote 由主理人放行（注意 promote-prod-hk 缺 migration 步骤的已知坑，生产库也还没有 materials/contents 表）。
- 记债：smoke Step7 补 agent_id 断言加固；旧 dashboard /api/publish/tasks 列表会混入 content_publish 行（只读噪音）。

## 下一步

1. 刀2：Notion 编排台双向同步（作品自动长行→主理人填标题文案勾平台→状态"发"触发派发→回执写回）。
2. 刀3：执行器消费——android-publish skill 家族接 GET /api/publish-tasks 领单协议发真机；那 6 个 skill 仍在 worktree 未提交（0830 待办）。
3. 刀4：生产 promote（migrations 差集核对 + API），主理人放行。
4. 渠道恢复后评估分档模型切回（判据：真调返回正常 completion 且 gp2 Step23b 连续绿）。

## 数据源

- spec：docs/superpowers/specs/2026-09-08-content-publish-dispatch-design.md
- plan：docs/superpowers/plans/2026-09-08-content-publish-dispatch.md
- PrepPRD：sprints/09082250-content-publish-dispatch/prep-prd.md
- 实现：apps/api/src/routes/publish-dispatch.ts
- 决策：933240ae（刀1）/ 56d7b36f+f5616812（尊享号）/ golden_path c5c0e259

GP-Anchor: none(docs)
