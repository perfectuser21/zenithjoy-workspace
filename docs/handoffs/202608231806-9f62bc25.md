# Handoff：extract_list 多值提取模式——收官扫号链最后一个 AI 保底覆盖点

- task_id: 9f62bc25-dc59-4ab7-a23e-5aaca2d60589
- verdict: PASS

## 完成
- 新增 `extract_list` 多值提取模式（PR#1723 已合并），补上 `locate`/`extract` 都表达不了的场景——"读一整份列表"（如切换账号面板里所有已登录昵称）：
  - `locator-assist.ts`（中台）：`buildExtractListPrompt`/`parseExtractListAnswer`/`askExtractList` + `requestLocatorAssist` 新分支，`LocatorAssistResult` 新增 `extractedValues?: string[]`
  - 关键设计：`values: []`（空数组）是合法答案（AI 确认没有账号），跟 `values: null`（AI 读不出来）语义分开判定——避免"确认没有"被误判成"问不出来"
  - `agent-burner.ts`：透传 `mode=extract_list`，响应新增 `extracted_values`
  - `LocatorAssistClient.kt`：`parseExtractListResponse`/`requestExtractListBlocking`，跟 `extract` 平行实现
  - `DeviceAccountScanService.kt`：`readAccountListFromPanel()` 在 `tv_nickname` 这个 id 找不到时，先问 AI 读一遍全部昵称，读不到/AI 不可用再判死
- `rpa-locator-assist-smoke.sh` 扩展了真实 `gpt-5.6-terra` 调用验证：给定含 2 个账号昵称的树，AI 必须把两个都列出来（不是只抽一个）——本地真调已确认该 prompt 完全按预期工作
- apps/api 全量单测 272 files / 2144 tests 绿，`tsc --noEmit` 无报错
- **全仓库 15 个真实等待失败点（`NodeAwait.classifyFailure` 调用点）现已全部（15/15）接上 AI 保底**——这条从 0822 晚开始的 AI on-call 横切件铺满工作线到此收官（起点见 `docs/handoffs/202608230300-ai-oncall-crosscut-full.md`，中间经 PR#1717/1721/1723 三刀）

## 没完成
- 真机验证：待装机验证扫号链读账号列表的 `extract_list` AI 保底实际生效（本次只有代码 review + CI 真调验证 + 单测，没有真机装机）
- 刀3周报固化（`rpa_locator_assist` 病历按机型×版本×步骤聚类，人工拍板固化答案进定位器代码发版）——仍是待办，本刀没做
- TOAPIS 渠道 #58（`deepseek-v4-flash`）欠费仍未修，现在全部 AI on-call 调用默认走临时切换的 `gpt-5.6-terra`

## 下一步
- 找机会做真机装机验证，把 15 个覆盖点在真实设备上跑一遍确认无退化
- 视野拉远：这条"失败分类→AI保底→验证闸→病历→周报固化"的骨架，目前只服务抖音一个软件；等第二个软件 RPA 立项时，才是把它抽成"跨软件通用底座"的正确时机（当前不做，避免只有一个真实用例就强行抽象）

## 数据源
- `apps/api/src/services/locator-assist.ts`、`apps/api/src/routes/agent-burner.ts`
- `services/agent-android/app/src/main/kotlin/com/zenithjoy/agent/uia/LocatorAssistClient.kt`
- `services/agent-android/app/src/main/kotlin/com/zenithjoy/agent/account/DeviceAccountScanService.kt`
- `services/agent-android/app/src/main/kotlin/com/zenithjoy/agent/uia/FailureClassifier.kt`（PR#1717）

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1723
- branch: `cp-08231742-extract-list-mode`（已合并，worktree 已清理）
