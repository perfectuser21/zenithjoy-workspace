# Handoff: fix(deploy): nginx /api/materials 无斜杠请求不再301降级到http

**Verdict**: PASS
**task_id**: unknown（Brain `/api/brain/tasks` 注册时报 `routing_evidence_unavailable`，未能拿到 task_id，仅写此镜像，未写 DB）

## 完成
- 根因确认：deploy/nginx.staging.conf 与 deploy/nginx.conf 的 location /api/materials/（带尾斜杠）对不带斜杠的 GET /api/materials 触发 nginx 自动 301 补斜杠，Location 头 scheme 降级为 http（该层只 listen 80），浏览器当 mixed content 拦截，axios 表现为无信息量 Network Error
- 本地用 docker nginx:1.27-alpine 加载真实配置文件复现（proxy_pass 换占位端口隔离），二分定位确认是该 location 块本身、与 Express 应用代码无关
- 修复：两份配置各加一条 location = /api/materials 精确匹配块，复用原有大文件超时/体积设置，proxy_pass 到不带尾斜杠的同名后端路径
- 新增回归守卫 .github/workflows/scripts/smoke/nginx-materials-redirect-smoke.sh，proven-to-fire（对修复前配置跑真实报红，对修复后配置跑全绿），已注册进 smoke-baseline.txt
- PR #1875 已合并（baef3920），deploy-dashboard-staging.yml 自动部署成功
- 线上验证：curl https://staging-autopilot.zenjoymedia.media/api/materials 不再是 301/http，而是 Express 正常返回的 401/403；真实 Playwright 登录 AI验收账号后打开 /materials 页面，网络面板显示 GET /api/materials?limit=60 拿到具体的 403 响应而非 Network Error，页面文案也确认不含 Network Error 字样

## 没完成/范围外
- 登录态下素材库页面本身返回业务层 403（NO_TENANT/无租户绑定），怀疑是该验收账号未选择机器/设备绑定导致，与本次 nginx 路由修复无关，未在本次范围内处理

## 下一步
- 如需彻底验收素材库页面功能，需要用真实已绑定机器/租户的账号重新登录测试那个 403 是否消失（不属于本次 nginx bug 范围）

## 数据源
- deploy/nginx.staging.conf
- deploy/nginx.conf
- .github/workflows/scripts/smoke/nginx-materials-redirect-smoke.sh
- PR https://github.com/perfectuser21/zenithjoy-workspace/pull/1875

## 决策引用
- 0ac1eca3-9ea8-4477-8083-ad7bf83e39c1

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1875
- 分支: cp-09191844-nginx-materials-redirect
- sprint_dir: sprints/09191844-nginx-materials-redirect
