# Handoff：Brain派发死循环三源根治（cecelia#4468）

- task_id: e0e7954d-115a-4a6f-81c3-671c604d2092
- journey_id: e6f803f2-8c48-4cce-a7a1-5b1bda5e9c29（工厂 · F1 开发闭环）
- verdict: PASS
- created_at: 2026-07-30T05:36:43.000Z

## 完成
- cecelia#4468已合并(v1.267.135已部署上线)：A cecelia-run.sh解析软链(_SELF_PATH循环readlink)——经~/bin软链调用时launcher拼成//scripts/claude-launch.sh每次派发exit 127秒挂，#4327引入；B brain-deploy.sh成功路径无条件drain-cancel——drain持久化working_memory被新容器恢复，每次自动部署后派发永久瘫痪(0729 16:48/0730 10:21两次实证)；C transient requeue计数上限5+quarantine自动释放计数上限2
- 真机验证：根因A端到端生效——修复后cecelia-run真正拉起claude进程(此前从未到达)；宿主~/bin/cecelia-run软链目标(主checkout)已ff到含修复版本，dry-run输出真实launcher路径
- 排障过程处置：两次手动drain-cancel恢复派发；3个过期重复ci_patrol任务清理；拉会(3d6d35d2)cancelled/整理测试账号(c6930857)人工quarantined止血；主cecelia checkout ff同步
- 过程教训(已付学费)：git reset --mixed到新main后git add -A把工作树里旧版的他人新合并文件(gp-ledger整个feature 19文件)误提交成回退，靠diff清单核对+checkout origin/main逐一恢复救回——重写历史必须先核对diff只含本刀文件；brain版本bump必须同步DEFINITION.md两处(facts-check闸)

## 未完成
- ⚠️下一层洋葱(已追加进Issue cc28d1af)：①bridge research任务的claude以'Not inside a trusted directory'exit 1(cwd信任配置，launcher/cecelia-run需为headless会话处理信任)——修好路径后暴露的真实阻塞，研究类任务仍无法完成；②failure_count累到50仍未触发quarantine——checkShouldQuarantine阈值路径有洞；③根因D的post-swap drain-cancel真机验证未闭环(本次部署是合并前旧脚本启动的，下次部署才是首次真跑)
- ci_patrol日报任务bd8ae91b仍queued排队中(若ci_patrol派发也走同一bridge路径，可能同样撞信任问题)
- 本Brain task因evaluator gate停留in_progress(headed session惯例)

## 下一步
- 立项修'untrusted directory'信任配置bug(Issue cc28d1af追加项①)——这是research/bridge任务恢复的真正拦路虎
- 排查checkShouldQuarantine阈值为何50次不触发(追加项②)
- 下次任意cecelia PR合并部署后确认tick/status.draining自动为false(根因D首次真机验证)

## 数据源
- packages/brain/scripts/cecelia-run.sh(_SELF_PATH)
- scripts/brain-deploy.sh(post-swap drain-cancel)
- packages/brain/src/quarantine.js(两个上限)
- Issue cc28d1af(完整病理+追加发现)

## 产物
- PR: https://github.com/perfectuser21/cecelia/pull/4468
- 分支: cp-07301119-fix-dispatch-loop
