# Handoff：批量混剪 S4——高清成片渲染+内容安全Gate（四步骨干收官）

task_id: 73a82e3c-97b3-4ab4-a27e-2e6ba14a0957
journey_id: bb4f2154-d5d3-4836-af11-aeeaa3c2e8c8（批量混剪 GP f6f96e17）
verdict: PASS

## 完成
- mashup-render-ffmpeg.ts：多段素材 scale+letterbox 统一到 1920x1080 后 filter_complex concat，真机验证（proposal-v2.md A2 断言分辨率要求）
- mashup-render.ts：下载素材 → ffmpeg 合成 → 抽帧 → Gemini 内容安全+水印双审（复用 TOAPIS 代理，水印检测折进同一次调用，不新增第三方账号）→ fail-closed（A1）：安全/水印任一非通过，export_url/download_url 恒为空
- 复用既有 contents 表（不新建平行表），补 safety_check_status/watermark_check_status/export_url/download_url/source_candidate_id 五个字段，对齐 A1 断言原文
- 新增端点 POST /api/mashup/candidates/:id/render
- 新增 mashup-render-smoke.sh：真库+真 ffmpeg 端到端验证，本地对真 Postgres 跑通过才推送
- 过程中修复两个真 bug：①CodeQL js/tainted-format-string（candidateId 来自 URL 路径参数拼进 console.error 首参）；②CI 的 API Test job 无 ffmpeg 环境，补 hasFfmpeg() 探测 + it.skip 降级（同 video-frame-extract.test.ts 口径）
- PR #1865 已 squash 合并（dc94dd67），CI 全部 12 项必需 gate 绿，auto-merge 自动完成
- **至此批量混剪 GP f6f96e17 四步骨干（S1 素材打标签 → S2 槽位模板分配 → S3 语义检索候选生成 → S4 渲染+安全Gate）全部落地，端到端可用**：客户上传素材 → 自动打标签 → 选模板分配槽位 → 浏览候选方案 → 选定 → 渲染出 1080P 成片，内容安全 fail-closed 把关

## 未完成（显式范围外，非遗漏）
- 声音克隆（CosyVoice）：零凭据，Gate1 要求的逐次授权 UI/consent 表也未做——这是业务/法务决策项，不是技术缺口
- 阿里云超分：ALIYUN_VIAPI_ACCESS_KEY_ID 真凭据已就位但未接入，留后续 PR
- 剪映草稿导出（J9）：全库无集成，本刀恒走 FFmpeg 直出降级分支（J9 设计上唯一实现的分支，非遗漏）
- 候选生成阶段的内容安全轻量初筛（J6）：未单独做，安全审核只在 S4 末端 Gate 做一次
- **前端 UI 全线缺失**：S2/S3/S4 都只有后端 API，客户目前无法通过界面操作，需要前端团队/后续 sprint 补上模板选择器+候选网格+成片下载页面

## 下一步
1. 补前端 UI（模板选择、候选网格浏览、成片下载）——这是当前离"客户能自己点着用"最近的缺口
2. 阿里云超分接入（真凭据已就位）
3. 声音克隆 Gate1 授权文书+逐次勾选流程——需要主理人对法务文本拍板后再排期
4. Gate 0 结论=B 的降级路径（补拍 Seedance/HappyHorse）如果后续拿到账号，回 mashup-slot-assignment.ts 加真实调用

## 数据源（下一个大脑接手要看的）
- sprints/09182210-batch-mashup-proposal/proposal-v2.md（Golden Path 全量定义，A1/A2 断言、Gate1 声音克隆授权要求）
- decisions 表 98d1fab1（Gate0 实测结论 + 技术选型）
- apps/api/db/migrations/20260919_030000_mashup_export_gate.sql
- apps/api/src/services/mashup-render-ffmpeg.ts
- apps/api/src/services/mashup-render.ts
- apps/api/src/routes/mashup.ts

## 决策引用（Brain decisions 表）
- 98d1fab1：批量混剪 Gate0 实测结论=B + 语义检索/补拍/声音克隆/超分技术选型

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1865
- branch: cp-09191316-batch-mashup-s4-render（已合并，worktree 已清理）
- sprint_dir: sprints/09182210-batch-mashup-proposal
