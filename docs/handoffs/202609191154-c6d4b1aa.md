# Handoff：批量混剪 S2——槽位模板分配（钩子/产品/证据/CTA）

task_id: c6d4b1aa-7d0e-4662-917c-e0433c116cce
journey_id: bb4f2154-d5d3-4836-af11-aeeaa3c2e8c8（批量混剪 GP f6f96e17）
verdict: PASS

## 完成
- 新建 mashup_templates / mashup_runs / mashup_slot_assignments 三表 + 内置标准四槽位模板种子（钩子/产品/证据/CTA）
- mashup-slot-assignment.ts：从 S1 已打标签素材按标签重叠打分分配到槽位，贪心不重复用同一素材
- 三态落地，均已真库验证：assigned（已分配）/ reshoot_skipped（必填槽位缺素材且补拍账号零凭据，决策 98d1fab1，J4 降级分支）/ unfilled（选填槽位缺素材，非异常）
- 新增端点：GET /api/mashup/templates、POST /api/mashup/runs、GET /api/mashup/runs/:id，鉴权/租户隔离口径对齐 materials.ts，已补限流（CodeQL js/missing-rate-limiting 拦下 3 条 high）
- 新增 mashup-slot-assignment-smoke.sh（真库真调用三条确定性路径），已入 smoke-baseline.txt
- product-map.yaml 补 batch_mashup step2 + 新 smoke_files；product-map T3 快照断言（精确匹配 smoke_files/steps）同步更新
- PR #1861 已 squash 合并：https://github.com/perfectuser21/zenithjoy-workspace/pull/1861

## 未完成（显式范围外，非遗漏）
- 补拍（Seedance/HappyHorse）真实调用未实现——全库零凭据，Gate0 实测结论=B。当前 assignSlots 恒走 reshoot_skipped 分支；凭据到位后只需在 assignSlots 里加真实调用+失败仍回落同一分支，调用方看到的状态语义不变
- 槽位分配结果的前端展示 UI 未做——本刀只交付后端服务+API，客户目前只能通过接口拿到分配结果

## 下一步
1. S3：语义检索候选生成引擎——用本地开源 embedding 模型（@xenova/transformers，决策 98d1fab1 已选型，因 TOAPIS/Gemini 代理零 embedding 模型访问权限）做候选生成 + 候选级去重（proposal-v2.md J10）
2. S4：FFmpeg 高清合成渲染 + 内容安全 Gate（J7，fail-closed）+ 声音克隆（CosyVoice 零凭据，走降级分支）+ 阿里云超分（ALIYUN_VIAPI_ACCESS_KEY_ID 真凭据已就位，可真实接入）
3. 待办：给 S2 分配结果补前端展示页（客户当前只能拿到 JSON，看不到"槽位分配结果"）

## 数据源（下一个大脑接手要看的）
- sprints/09182210-batch-mashup-proposal/proposal-v2.md（Golden Path 全量定义，四步骨干+判定点表+外部依赖降级路径表）
- decisions 表 98d1fab1（Gate0 实测结论 + embedding/补拍/声音克隆/超分技术选型）
- apps/api/db/migrations/20260919_010000_mashup_slot_templates.sql
- apps/api/src/services/mashup-slot-assignment.ts
- apps/api/src/routes/mashup.ts

## 决策引用（Brain decisions 表）
- 98d1fab1：批量混剪 Gate0 实测结论=B + 语义检索/补拍/声音克隆/超分技术选型

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1861
- branch: cp-09191120-batch-mashup-s2-slot-templates（已合并，worktree 已清理）
- sprint_dir: sprints/09182210-batch-mashup-proposal
