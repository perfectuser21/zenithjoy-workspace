# Handoff：OpenClaw 信号桥·件3 phonectl CLI（三件套收官，PR#1771）

- task_id: `b41720f5-a6c0-4c9f-9935-87d46434fcaf` ｜ verdict: **PASS** ｜ 2026-09-04

## 完成

- `scripts/openclaw/phonectl.sh`：HK OpenClaw 网关 exec 白名单薄壳，8 action 参数映射转发到中台设备指令桥（件2 契约 `POST /api/devices/:agentId/actions`）。504 响应体的 `outcome:"unknown"` 原样透传，调用方自行判断是否用 `--idempotency-key` 重取结果，不擅自重试。
- 17 个 `node --test` 单测全绿，TDD 途中修 3 个真实 bug：测试自身用 `spawnSync` 导致同进程 mock server 死锁（改异步 `spawn`）；脚本内 `jq` 的 `// empty` 把 JSON `false` 当假值放过（`data.ok=false` 场景会静默 exit 0）；`$ACTION` 紧跟全角括号触发 bash 5.3 词法误判为变量名一部分。
- 接进 `ci-l3-code.yml` 新 job `OpenClaw Scripts Test`，不复刻 detect-review-issues.js 半年孤儿测试的教训（本次顺手也把那个坑修了，见下）。
- **决策 `7a4c0369` 三件套全部完成**：件1（PR#1762，设备端 8 原语）+ 件2（PR#1765，中台桥端点）+ 件3（PR#1771，本次）。

## 副产品（同一轮工作流中顺手清偿的债）

- `detect-review-issues.js` 否定句误判第 4 次复发修复 + 回归测试接进 CI（PR#1769）
- gp2 smoke curl 超时统一 60s（PR#1766）
- 安卓版本号 bump 至 2.1.48，解版本二义（PR#1768）
- `agent-registry` 快速重连竞态修复（随件2 PR#1765）
- commander/评论分档 TOAPIS 渠道切换排障（随件2）

## 没完成

- **部署到 HK 网关**（运维手动步骤，非代码任务）：`phonectl.sh` 复制到能 exec 的 node（与 `zenithjoy-api-prod` 同 hk-vps docker 网络，不需要额外遥控器机器）+ `chmod +x` + 配 `ZENITHJOY_INTERNAL_TOKEN` + `openclaw approvals allowlist add --agent main "phonectl*"`
- **端到端首验**：OpenClaw 发指令 → 中台 → 机队手机真 tap（先机队小黄/小粉，不碰客户机）

## 下一步

1. 运维手动部署 phonectl 到 HK 网关（见上）
2. 机队单机端到端首验
3. 验证通过后才谈客户机灰度（决策 `7a4c0369` 原文顺序）

## 数据源

`scripts/openclaw/phonectl.sh`、`apps/api/src/routes/devices.ts`

## 决策引用

`7a4c0369`（本方向，三件套已全部完成）

## 产物

PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1771 ｜ 分支: `cp-09042136-openclaw-phonectl`
