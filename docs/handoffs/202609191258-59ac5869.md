# Handoff：批量混剪 S3——语义检索候选生成引擎

task_id: 59ac5869-b413-4e94-aad0-fc89253ab426
journey_id: bb4f2154-d5d3-4836-af11-aeeaa3c2e8c8（批量混剪 GP f6f96e17）
verdict: PASS

## 完成
- embedding.ts：本地开源模型 @xenova/transformers（多语言 384 维）做句向量，pipeline 单例缓存，真机验证语义判别力（相关文本相似度 0.82 vs 不相关 0.13）
- mashup-candidate-generation.ts：对 S2 判定 assigned 的槽位用语义相似度重排全体已打标签素材，Top-K + beam search 枚举多个槽位填充组合，score 降序 + J10 候选级去重（Jaccard 阈值 0.8）+ targetCount 截断（默认 50，硬上限 300，proposal-v2.md A4）
- S2 判定 reshoot_skipped/unfilled 的槽位原样透传（该槽位所有候选留空），不重新尝试
- 新增端点：POST /api/mashup/runs/:id/candidates（触发生成）、GET .../candidates（按分数查看候选池）、POST /api/mashup/candidates/:id/select（客户选定候选，S3 终态）
- materials.embedding 缓存列（首次用时懒计算写回）+ mashup_candidates 表 + mashup_runs.selected_candidate_id
- 新增 mashup-candidate-generation-smoke.sh：真本地模型 + 真库端到端验证。本地对真 Postgres 跑通时抓到两个真 bug 并修复：①`$RUN_ID` 后紧跟全角括号在某些 bash/locale 组合下被误判成变量名一部分导致 unbound variable；②@xenova/transformers 传递依赖的嵌套 sharp@0.32.6 在 CI linux 环境缺原生二进制导致 release-selfcontained-smoke 失败，用 npm 嵌套定向 override 清掉冗余副本
- npm audit：@xenova/transformers 链带的 protobufjs（critical）+ onnx-proto/onnxruntime-web（high）登记进 audit-gate.sh ALLOWLIST（已建 issue 1db295ce 跟踪，威胁模型=只解析固定模型名的 HuggingFace Hub 文件，不解析用户可控数据）
- PR #1863 已 squash 合并（49b89bf2），CI 全部 12 项必需 gate 绿，auto-merge 自动完成

## 未完成（显式范围外，非遗漏）
- 候选缩略图/低清代理预览未做——proposal-v2.md Step3 提到的"候选预览 UI"本刀只交付后端候选数据（材料 ID 填充），前端网格选择器未建
- 候选生成阶段的内容安全初筛（J6）未做——proposal 定义为 S4 末端 Gate 之前的轻量初筛，本刀暂缓，留给 S4 一起做双闸

## 下一步
1. S4：FFmpeg 高清合成渲染 + 内容安全 Gate（J7 fail-closed，含候选初筛 J6）+ 声音克隆（CosyVoice 零凭据，走降级分支）+ 阿里云超分（ALIYUN_VIAPI_ACCESS_KEY_ID 真凭据已就位）+ 剪映草稿导出（J9 降级 FFmpeg 直出）
2. 候选网格选择器前端 UI（S3 现在只有 API，客户拿不到可视化的"一屏候选方案"）

## 数据源（下一个大脑接手要看的）
- sprints/09182210-batch-mashup-proposal/proposal-v2.md（Golden Path 全量定义，J10 候选级去重、A4 候选量软断言）
- decisions 表 98d1fab1（Gate0 实测结论 + embedding/补拍/声音克隆/超分技术选型）
- apps/api/db/migrations/20260919_020000_mashup_candidates.sql
- apps/api/src/services/embedding.ts
- apps/api/src/services/mashup-candidate-generation.ts
- apps/api/src/routes/mashup.ts

## 决策引用（Brain decisions 表）
- 98d1fab1：批量混剪 Gate0 实测结论=B + 语义检索/补拍/声音克隆/超分技术选型

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1863
- branch: cp-09191212-batch-mashup-s3-candidates（已合并，worktree 已清理）
- sprint_dir: sprints/09182210-batch-mashup-proposal
