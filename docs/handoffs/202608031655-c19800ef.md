# Handoff：line02安卓验收规程SSOT — 发版验收双表刀1

- task_id: `c19800ef-3c53-476f-8595-e88b9e758fc6`
- journey_id: `afa6abca-53c0-4815-8594-b7fb81ca547f`（客户智能获客路径）
- verdict: **PASS**
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1586（已合并，squash）
- 分支: `cp-08031605-line02-acceptance-spec-ssot`
- sprint目录: `sprints/08031511-line02-acceptance-spec-ssot/`

## 完成

- 新建 `acceptance-spec/line02-android.yaml`：line02安卓员工验收14步×4格判据唯一真相源（含 `verifiable_by` 三值分类：`machine_db`/`machine_visual`/`human_only`），ajv2020 schema校验（缺 `t`/`na` 或缺 `verifiable_by` 直接报具体路径错误）
- `scripts/acceptance-spec/{lib,cli}.mjs`：从yaml生成员工验收网页，逐字节复用既有交互逻辑（判定按钮/症状选择题/证据快捷键/结论生成），消除"网页即规程"双写
- CI drift-check（`acceptance-spec-contract` job）锁定生成产物与规程文件一致
- TDD RED→GREEN两次commit + 真机浏览器点击回归验证 + 部署 `docs.zenjoymedia.media/line02-android-acceptance-sop/` 替换手工版
- PR过程修了4类CI问题：test-registry.yaml漏注册测试文件、PR标题缺`[CONFIG]`标签（改了.github/workflows/）、CodeQL `js/bad-tag-filter`连续3轮（最终改用indexOf切片替代正则匹配script/style标签，从规则命中的模式类别里跳出去根治，不是单纯调正则）

## 没做（显式声明范围外）

- 刀2 evaluator发版验收机器打表（读同一份yaml跑断言，逐格输出结构化结果）— 未开始
- 刀3 对比页（机器表+人表四象限）+ report复活 — 未开始
- 刀4 稳定性档（随机扰动+N遍重跑）— 未开始，需先在decisions登记主理人拍板
- **Brain任务记账gap**：本任务PATCH到`completed`被拒（`reason:pr_not_found`）——本PR是"插队"手动创建、未走harness generator流水线，PR标题/commit没有嵌入`[task_id]`方括号标记，Brain的PR匹配校验找不到关联，任务状态卡在`in_progress`未能转`completed`。**不影响真实代码已合并的事实**，纯粹是Brain侧记账没跟上。下次类似手动插队场景，建议PR标题带上`[<task_id>]`方括号以便Brain正常收尾。

## 下一步

1. 立项刀2：evaluator新增发版验收模式，读 `acceptance-spec/line02-android.yaml` 跑断言（预估现有 `golden-path-2-smoke.sh` 可覆盖约28~30格 `machine_db` 格），逐格输出结构化结果到 `harness_attempts.result`（复用现有checks数组形态，补格子编号字段），证据死规则：禁止只落 `/tmp`
2. 刀2完成后才能做刀3对比页，把机器表和员工表叠合看四象限

## 数据源（下一个大脑要加载的文件/表/键）

- `acceptance-spec/line02-android.yaml`
- `acceptance-spec/line02-android.schema.json`
- `scripts/acceptance-spec/lib.mjs`
- `scripts/acceptance-spec/cli.mjs`
- `docs/superpowers/specs/2026-08-03-line02-release-acceptance-dual-table-prd.md`
- `sprints/08031511-line02-acceptance-spec-ssot/prep-prd.md`

## 决策引用

- decision `ada7d5b1-a179-42e3-bd45-0a5ad69f5c1d`：line02安卓验收规程改为YAML SSOT

## 产物

- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1586
- 部署入口（员工使用）：`docs.zenjoymedia.media/line02-android-acceptance-sop/`
- sprint目录: `sprints/08031511-line02-acceptance-spec-ssot/`
- 分支: `cp-08031605-line02-acceptance-spec-ssot`
