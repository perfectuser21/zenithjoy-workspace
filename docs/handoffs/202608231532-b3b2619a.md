# Handoff：抽 FailureClassifier —— 失败类型保底门禁统一

- task_id: b3b2619a-ad7c-43d3-8ea0-5d8b128877b4
- verdict: PASS

## 完成
- 新增 `FailureClassifier.shouldAssist(failure: WaitFailure): Boolean`（`services/agent-android/app/src/main/kotlin/com/zenithjoy/agent/uia/FailureClassifier.kt`），把「这次等待失败值不值得问一次 AI 保底」从私信/采集链 8 个 call site 里散落的隐性判断收口成唯一可测的纯函数
- 迁移 6 处已手写 `failure != WaitFailure.NO_ROOT` 判断（dm_entry/dm_message_input/dm_search_entry/dm_search_input/collect_comment_button/collect_search_entry）到统一函数，零行为变化
- 补齐 2 处此前漏写门禁的点（dm_send_button 全程无门禁、dm_search_input 重试耗尽分支会对 NO_ROOT 也去问 AI）——纯粹减少无意义调用，不改变任何已验证路径的正常行为
- Android agent versionCode 46→47（2.1.42→2.1.43）
- PR#1717 已合并（TDD 两段式 commit：先写 `FailureClassifierTest.kt` 三用例，再实现+迁移）

## 没完成
- 剩余 ~34 个 RPA 动作点（采集链剩余点约 10 个、扫号链 `DeviceAccountScanService.kt` 全部约 7 个点）尚未用 `FailureClassifier` 接线，本 PR 刻意只做抽取+零回归迁移，不新增任何点的接线
- 途中意外发现并解决了一个不相关的紧急阻塞：TOAPIS `deepseek-v4-flash` 渠道 #58 上游欠费导致 3 个 baseline smoke 全局性失败（见另一交接单 `202608231515-bca4fb05.md`），修复后本 PR 的 CI 才转绿

## 下一步
- 用 `FailureClassifier` 批量接线采集链剩余点（开视频/切视频tab/取分享链接/评论抽取/点头像等）
- 用 `FailureClassifier` 接线扫号链全部动作点（目前一个都没接）
- 待 MediaProjection 截图授权在真机开启后，验证视觉接线（D8）在有授权情况下的真实纠偏效果（此前受控实验只验证了 fail-open 正确退回盲赌）
- 刀3周报固化：`rpa_locator_assist` 病历按机型×版本×步骤聚类，人工拍板固化答案进定位器代码发版

## 数据源
- `services/agent-android/app/src/main/kotlin/com/zenithjoy/agent/uia/FailureClassifier.kt`
- `services/agent-android/app/src/main/kotlin/com/zenithjoy/agent/collect/DouyinCollectService.kt`、`DouyinDmOutreachService.kt`

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1717
- branch: `cp-08231127-failure-classifier`（已合并，worktree 已清理）
