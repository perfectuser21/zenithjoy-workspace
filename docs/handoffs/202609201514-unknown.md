# Handoff：0920 抖音获客下一代架构（无 n8n + Jev + schema 校验 + Commander）探索归档

> task_id=unknown（本次是主理人 0919-0920 晚间临时口头指示的探索工作，未走 /capability 立项流程，
> Brain 无对应 task 记录，DB 侧 `tasks.result.handoff` 写入跳过，仅写本镜像文件）

## verdict：PASS

## 完成了什么（done）

1. 查清抖音智能获客真实生产现状：n8n 工作流「Social Leadgen V4」仍在 hk-vps 挂 `active`，但生产
   自 2026-09-14 起早已绕开它，真跑的是纯 crontab+shell（`harvest-cron.sh`/`outreach-tick.sh` →
   `douyin-phone-adb`）；发现评论打分/"AI判断理由"这层判定目前生产是假的（`push-leads.js` 硬编码
   模板字符串，非真 LLM 输出）。
2. 设计并真机验证了一套完全不用 n8n 的目标架构：账本(ledger) + Jev(schema归一化) + 纯代码结构
   校验(照抄 `protocol.md` 契约) + Commander兜底，11 个目标阶段**全部至少真机/真实API验证一次**。
3. 中途一次关键纠偏：第一版拿 Jev 置信度分数当 gate 阈值，被主理人当场指出不对——应该拿 schema
   是否合法做判定依据，已重写为 `orchestrator-v2.mjs`。
4. 老实测出评论打分（qualification）三条路线（Jev/Luna/Terra）均不达标（61.1%/55.6%/55.6%），
   明确不能自动放行，路由「待复核」，不再用生产现有的假模板字符串糊弄。
5. 新增3个抖音设备控制器命令（`account-identity`/`tap-video-author`/`preflight-full`），只改
   `douyin-phone-adb.dev-accountid` 开发副本，生产文件全程未动一行。
6. 全部产物已提交进 `zenithjoy-skills` 仓库并合并：PR #205
   （`android-douyin-leadgen-nextgen` skill，含 SKILL.md + 6 个真实脚本 + references/findings.md）。

## 没做完 / 显式排除（not_done）

1. **评论打分（qualification）没有可用方案**——不是没测，是测了三条路线全部不达标，需要下一个
   session 去搞清楚原始 `entered_final_lead` 标注当年的真实判定口径（怀疑比"只有strong_intent"
   更宽松），而不是继续换模型/调prompt。
2. **未进入 VALIDATE 阶段**——11 个阶段各只跑通 1 次，未达"连续零新坑3次、跨≥2台机器"的固化门槛，
   禁止直接切生产 crontab。
3. **Commander 升级路径从未真实执行过**——只验证了触发条件（schema校验不通过会调用 escalate
   函数），真调用 `openclaw cron add` 会在 us-vps 网关建真实 cron 任务，下一个 session 要触发前
   必须先跟主理人确认一次。
4. **快手/小红书/视频号完全没有对应的设备控制器代码**——现在只有三份 skill 文档（判定树+坐标记录，
   给 AI 照着手动操作用，不是可执行代码）。要达到抖音今晚这个成色，工作量是"从零写一个
   `<平台>-phone-adb` 控制器"，跟抖音这条线的历史工作量是同一量级。

## 下一步建议（next_steps）

1. **挑一个平台先做，不要三个同时铺开**（主理人已被告知这个工作量提醒）——建议顺序参考此前
   "先做快手"的口头倾向，但需要主理人在新 session 里再拍板一次。
2. 新 session 起步先读 `android-douyin-leadgen-nextgen/SKILL.md` + `references/findings.md`
   （已随 PR #205 合并进 `zenithjoy-skills` 主分支），不用复述本次探索过程。
3. 评论打分口径问题：找一下原始 `entered_final_lead` 标注是谁/什么流程产出的，核实真实判定阈值，
   再决定是换模型还是调整规则，禁止不查口径就继续盲调 prompt。
4. 若要推进 VALIDATE：至少再拿这套架构在抖音线上完整跑 2 次（不同关键词/不同视频），且换一台
   机器（目前只在 xian-m4 金诺机跑过），零新坑才计入进度。

## 数据源（data_sources，下一个 session 要加载的）

- `android-douyin-leadgen-nextgen/SKILL.md`（架构+11阶段证据表+禁止事项+跨平台工作量提醒）
- `android-douyin-leadgen-nextgen/references/findings.md`（评论打分三轮准确率表/ASR坑/Jev适用边界/protocol.md契约要点/账号身份读取坑）
- `android-douyin-leadgen-nextgen/scripts/*`（6个真实脚本：ledger.mjs / orchestrator-v2.mjs / push-leads-v2.mjs / video_judge.py / harvest-keyword-v3-full.sh / controller-new-commands.patch）
- `agentic-workflow-runtime/references/protocol.md`（Commander protocol v2，schema契约原文）
- xian-m4 `~/.config/openclaw/douyin-account-routes.tsv`（自有账号登记表，`preflight-full`/自有小号过滤都读它）

## 决策引用（decision_refs）

- 0920 主理人当场纠偏：判定依据必须是 schema 合法性，不是 Jev 置信度分数（口头拍板，未走 decisions 表正式登记，此处仅记录）

## 产物指针（artifacts）

- PR: https://github.com/perfectuser21/zenithjoy-skills/pull/205 （已合并）
- branch: `cp-09202307-douyin-leadgen-nextgen`
- sprint_dir: 无（未走 /capability 或 harness 流程）
