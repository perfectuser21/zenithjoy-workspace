# Handoff：批量混剪前端UI——素材/模板选择→槽位分配→候选浏览→渲染下载全流程

task_id: 519e1a16-d9c7-4f36-86d8-d74f6edf99cd
journey_id: bb4f2154-d5d3-4836-af11-aeeaa3c2e8c8（批量混剪 GP f6f96e17）
verdict: PASS

## 完成
- MashupPage.tsx：四步向导单页（/mashup，视频剪辑区新增入口卡片）——选模板+选已识别素材 → 槽位分配结果 → 候选方案浏览 → 成片结果（下载链接或未通过原因，fail-closed）
- mashup.api.ts：7 个函数对齐后端 S2-S4 四个路由的响应形状
- GET /api/materials 补 tag_status/ai_tags 字段暴露（前端筛已识别素材要用）
- **本地真机验证**：起本地 API（真 Postgres + 真本地 embedding 模型）+ Dashboard dev server，真实 sign-up/sign-in + 真实浏览器（Playwright）走完整四步流程，S1-S3 全链路截图确认 UI 正确渲染，S4 因 seed 数据非真实文件按预期走 fail-closed 降级分支
- **真机验证过程中发现并修复 2 个真实生产 bug**（非本 PR 引入，是已存在的）：
  1. materials.api.ts / my-contents.api.ts / mashup.api.ts 三处 getUploadToken 调用了不存在的 /api/account 路由（真实路由是 /api/account/me，PR #261 定的）——素材库、我的作品两个已上线页面此前一直 404
  2. S4 mashup-render.ts 素材下载 fetch() 网络层异常没有 try/catch，会冒泡成裸 500，已仿 material-tagging.ts 的 extractFrame 同款优雅降级
- 顺手修复：批量混剪页面错误提示原本显示 axios 裸报文（Request failed with status code 500），改优先取后端 error.message 真实原因
- PR #1867 已 squash 合并（1364007a），CI 全部 12 项必需 gate 绿，auto-merge 自动完成

## 未完成（显式范围外，非遗漏）
- 候选缩略图/成片视频预览未做——素材/成片只显示文件名，不出缩略图或播放器（与 MaterialsPage 已有的取舍一致：视频缩略图是独立的一件事）
- 声音克隆/超分/剪映导出仍是 S4 遗留的同一批范围外项，UI 层自然也没有对应入口

## 下一步
1. 如果要继续加固：候选/成片缩略图预览、批量混剪历史记录页（客户回看之前生成过的 run/成片）
2. 声音克隆 Gate1 授权文书 + 阿里云超分接入（S4 遗留）完成后，UI 层需要补对应开关/入口

## 数据源（下一个大脑接手要看的）
- sprints/09182210-batch-mashup-proposal/proposal-v2.md
- apps/dashboard/src/pages/MashupPage.tsx
- apps/dashboard/src/api/mashup.api.ts
- apps/api/src/routes/materials.ts（tag_status/ai_tags 暴露）
- apps/api/src/services/mashup-render.ts（fetch 优雅降级修复）

## 决策引用（Brain decisions 表）
- 98d1fab1：批量混剪 Gate0 实测结论=B + 语义检索/补拍/声音克隆/超分技术选型

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1867
- branch: cp-09191416-batch-mashup-ui（已合并，worktree 已清理）
- sprint_dir: sprints/09182210-batch-mashup-proposal
