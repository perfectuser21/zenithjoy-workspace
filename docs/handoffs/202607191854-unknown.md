# Handoff：热键召唤Ctrl+Alt+W替代托盘召唤 + onboarding自检（PR #1410）

- task_id: unknown（本次为交互式 /dev 路径B，Brain task 注册因 task_type 校验拒绝而跳过，未走 headed --task-id 流程）
- journey: Path 4 客户私域 AI 接管（Line04 微信客服）
- verdict: PASS

## done

- 落地 `handoff_0719_line04_2findings_ready_to_ship` 发现1：`services/agent/wechat-rpa/listen_chat.py` 新增 `_summon_wechat_via_hotkey()` 作为真塌自愈召唤主路，verify 驱动（Ctrl+Alt+W 是显示/隐藏 toggle，仅在检测到隐藏/塌缩态才发，发后校验 UIA 树重建，最多重试3次处理奇偶次数），失败降级 `_summon_wechat_from_tray()`（原托盘双击方案），再失败降级重启兜底。
- 落地该 handoff 的 onboarding 前置：`services/agent/wechat-rpa/preflight.py` 新增第9项检测 `check_hotkey_summon` —— 功能自检探针（微信4.x设置存储加密+crc校验+运行时独占锁，程序读不了配置，只能靠"实发一次热键观察窗口显隐响应"判定），探针幂等（翻转后立即还原状态）。
- TDD 四提交（test→impl→test→impl）：`f622b446`/`18d9f807`/`88d781a6`/`b3aeba96`。
- 同步 `services/agent/build-modules/line04/wechat-rpa/`（`diff -r` 与源码一致，`ci-l4-runtime` 强制检查）。
- line04 三版本面（`services/agent/modules/line04/manifest.json` / `services/agent/build-modules/line04/manifest.json` / `apps/api/src/services/walking-skeleton.service.ts` HEARTBEAT_MODULES）同步 bump 1.0.145→1.0.146。
- 新增 `.github/workflows/scripts/smoke/line04-hotkey-summon-smoke.sh`（满足 `lint-feature-has-smoke` 门槛），补进 `smoke-baseline.txt`。
- 补齐两处遗漏的 CHECK_NAMES 计数硬编码（`agent-preflight-hardening-e2e.yml` 两个 job + `tests/test_preflight_lock.py`），8→9。
- PR 标题补 `[CONFIG]` 标签（触碰了 `.github/workflows/`）。
- CI 全绿，squash-merge 进 main（commit `63068f6b`）。

## not_done

- **真机验证未做**：本次新代码（`_summon_wechat_via_hotkey`、`check_hotkey_summon`）尚未在 xian-rog 真机上跑过。遵照 `feedback_no_live_core_swap_for_testing`（绝不为测试顶替 rog 正在服务的 live core），本次改动的真机验证走正常 OTA 通道，未在本次会话内完成。
- Path 4 的 `golden-path-4-smoke.sh` 仍未建（项目已知 gap，本 PR 未改变这一状态）。
- handoff 0719 发现2（音频提示音叫醒 + 降级轮询）未落地——这是下一个更大的改动，未在本次范围内。

## next_steps

1. 等 1.0.146 走 OTA 交付到 xian-rog 后，看一次真机监听日志，确认「真塌自愈(scan快速通道~15s)：先试热键召唤 Ctrl+Alt+W」命中且日志出现「热键召唤成功」（而不是一路掉到托盘召唤/重启）。
2. 真实走一遍绑号流程，触发 `check_hotkey_summon`，确认 onboarding 提示与自检结果（ok/failed）符合预期，尤其验证探针的"发送后还原状态"确实不留副作用（不会把用户正在用的微信窗口意外切走）。
3. 两步验证过、且日志/截图留痕后，再启动 handoff 0719 发现2（音频提示音叫醒，加速器模式 + 自动降级轮询）——该改动更大，动的是监听主循环热区，需谨慎评估。

## data_sources

- `memory/handoff_0719_line04_2findings_ready_to_ship.md`（本次落地的源 handoff，发现1已交付，发现2待做）
- `memory/wechat_monitoring_event_driven_findings.md`（架构大结论：轮询是结构性必需，事件驱动此路不通）
- `memory/feedback_no_live_core_swap_for_testing.md`（真机验证纪律：绝不顶替 live core）
- `memory/feedback_realmachine_fix_validate_yourself.md`（真机验证要自己复现→补丁→验证转绿）
- `services/agent/wechat-rpa/listen_chat.py` 中 `_summon_wechat_via_hotkey`/`_summon_wechat_from_tray`/`_should_fast_heal_hidden_collapsed`
- `services/agent/wechat-rpa/preflight.py` 中 `check_hotkey_summon`/`CHECK_NAMES`/`run_all_checks`

## decision_refs

- 无新增 decision（本次为路径B小改动，判定点/铁律登记发生在原 handoff 讨论阶段，非本次新增）

## artifacts

- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1410
- branch: cp-07191824-line04-hotkey-summon（已合并，未删除远端分支——按项目习惯留存）
- merge commit: 63068f6bcc5f0a85d09d3afbcda95c59a5c1d500
