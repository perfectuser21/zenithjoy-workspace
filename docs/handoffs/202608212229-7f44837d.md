# Handoff：dm_assignments 排期游标越窗后滚雪球式漂移到未来一周

**task_id**: 7f44837d-3e4b-4e22-bfbb-633f9bc7b28b　**verdict**: PASS

## 完成
- 确诊 0821 交付冲刺遗留问题：`dm_assignments` 排期越过 `dm_active_end` 后，同一账号连续候选被逐条前移约 1 天，两条记录在 staging 实锤排到了 8/28、8/29
- 根因：`clampToWindowStart` 只在 `d < startToday` 时才钳制，越窗时刻（如22:05）+1天钟点不变仍 `>= startToday`，函数原样返回超窗时间——用 staging 真实 SQL 数据反推验证，非猜测
- TDD 两次 commit 修复：Step D/Step E 两处调用点滚入次日前先 `setHours(0,0,0,0)`，PR #1695 已合并（CI 全绿，含 tsc/既有测试/冻结合同测试无回归）
- 顺带确认 `dm_per_hour=5` 是自拍保守值（25 个 staging 租户全在默认值，代码无平台风控引用来源），失败原因全是 NO_MATCH/NO_SEARCH_INPUT 等 RPA 错误码，无风控类信号——但从未真机跑过 5/小时以上速度，是否调高仍是零样本判断

## 没完成
- `dm_per_hour` 是否调高：用户拍板暂不动，等更多真机风控数据再定
- 交接单里其余待办未碰：step1 中台可用小号仍是0 / 采集稳定性 / 四号机 adb 不通 / 小白 DHCP 保留 / 失败现场截图未做

## 下一步
- 继续 `docs/handoffs/202608212130-line02-acquisition-delivery-push.md`「五、其余待办」按交付优先级推进：优先 step1 中台可用小号可见性（客户第一眼看到的就是它）
- 若要判断 `dm_per_hour` 能否调高：先让当前 5/小时 pace 再稳定跑几天，观察 `dm_outreach_log.error_code` 有没有出现风控类信号（验证码/账号异常/限流），再决定是否小步试探调到 6-8

## 数据源
- `zenithjoy_staging` 库 `dm_assignments` / `dm_outreach_log`（`error_code`/`foreground_pkg`/`failure_diag` 字段）
- `apps/api/src/services/acquisition-dispatch.ts`（`buildAssignments`/`clampToWindowStart`）
- `docs/handoffs/202608212130-line02-acquisition-delivery-push.md`（上一棒交接单，本次任务来源）

## 决策引用
- c3bd917b-5f22-40f4-b939-793f1eddc315

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1695
- sprint_dir: sprints/08212200-dm-schedule-clamp-window-bug
- branch: cp-08212204-dm-schedule-clamp-fix
