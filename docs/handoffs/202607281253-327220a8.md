# Handoff：作战窗 Agent Panel 刀1：事件总线+看门狗+line04打点+WebView2壳+首次装机仪式

**Verdict**: PASS
**Task ID**: 327220a8-f13c-4dab-b0f6-e280719d79d0
**Journey**: bfeed805-deed-46c3-8624-87f0028101d4

## 完成
- Panel Event Bus(services/agent/src/shared/panel-event-bus.ts)：6事件类型+看门狗90s(可调)+多task灯态max()聚合+最近完成列表50条上限，15个单测全绿
- 业务语言映射(panel-line-labels.ts)：line02→智能获客/line04→智能回复/publish→智能发布，客户视图零内部代号泄露
- desktop-lease-broker fail-closed守卫(panel-rpa-guard.ts)：超时/异常一律视为RPA进行中
- panel-events.jsonl文件tail桥接(panel-events-tail.ts)+本地SSE路由(panel-events-route.ts)+index.ts接线
- line04三处真实打点(listen_chat.py: task_started/_gen_draft、done/failed发送分支)，PII经filter_pii过滤，与现有events.jsonl完全独立文件(单写者约束未破坏)
- 中台panel_events表迁移(按tenant_id隔离)+薄写入端点(/api/panel/events)
- apps/agent-panel三态客户端UI：收起边缘灯带/展开全屏Warroom看板/RPA贴边只读，首次装机仪式(本地标记位仅触发一次)
- apps/agent-panel-host：WPF+WebView2最小原生宿主代码(Ctrl+Alt+Z热键/WS_EX_NOACTIVATE+WS_EX_TRANSPARENT/WebView2缺失Win32 MessageBox兜底)
- golden-path-4-smoke.sh新增Step17面板可见性判据，本地跑通17步全绿
- 10条判定点写入decisions表(category=judgment)：fail-closed默认姿态/看门狗阈值/WebView2渲染悖论/断线重连快照数据源/首次装机仪式等，均经3轮FR GAN对抗深挖+3路Phase2补全(产品/混沌工程/运维)验证
- PR#1488已合并main：修了5项CI闸门失败(agent版本bump/test-pairing补5个测试/orphan-test注册17个文件/npm audit高危漏洞/lint-feature-has-smoke误判修复)+1次rebase解冲突

## 没完成
- C#原生宿主代码(apps/agent-panel-host/)未在本地编译/运行验证——沙箱环境无dotnet SDK，真实运行时行为(窗口创建/热键/WS_EX标志/WebView2渲染)需windows_cloud CI(GHA windows-latest)首次验证
- 刀2(中台聚合SSE+dashboard「实时动态」页)、刀3(客户视图脱敏分级+画像卡合流)均在本刀PrepPRD明确排除范围外，未做
- apps/dashboard若客户也会看到刀2的「实时动态」页，需要按角色分视图(运营者看line代号/客户看业务语言)，刀2 PrepPRD需明确此点——已作为待讨论事项记入本刀PrepPRD
- Brain task状态卡在in_progress未能转completed(PATCH返回reason:pr_not_found，可能是Brain内部PR归属校验逻辑与手动创建的PR不匹配)，result字段已如实写入merged:true+pr_url，功能性完成不受影响

## 下一步
- windows_cloud CI首次跑通后验证C#原生宿主真实行为(窗口创建/热键/WS_EX标志/WebView2渲染)，若有问题需修复
- 刀2点火前：明确apps/dashboard「实时动态」页的客户/运营者视图分级方案
- 刀2/刀3按PrepPRD既定顺序推进（刀2锚定Path2 Step8挖客闭环可见性，刀3锚定Path4+Path1 Step2装客户端体验）

## 数据源
- sprints/07280929-agent-panel-knife1/prep-prd.md
- docs/superpowers/specs/2026-07-22-agent-panel-design.md
- decisions表 category=judgment，topic含"作战窗刀1"的10条
- test-registry.yaml 中 id前缀 agent-panel-* 的17条

## 决策引用
- d6099fd9

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1488
- Sprint dir: sprints/07280929-agent-panel-knife1
- Branch: cp-07281103-agent-panel-knife1
