# Handoff：OpenClaw 信号桥·件1 — agent-android 统一指令处理器（PR#1762）

- task_id: `5b8d0139-f652-4fc9-a76e-a8aa4f4a7c6b` ｜ verdict: **PASS** ｜ 2026-09-04
- 决策：`7a4c0369`（supersede `0a6fd1d4`）：OpenClaw=编排大脑，`com.zenithjoy.agent`=设备执行端，中台桥=唯一信号通路，adb 不进客户机生产链路。

## 完成

- **PR#1762 已合并**：`com.zenithjoy.agent.command` 包 8 文件（CommandProtocol/AutomationLease/CommandQueue/GestureRunner/ScreenshotRunner/TypeRunner/LaunchRunner/CommandExecutor）；WsClient 改造（msgId 透传、sendResult、busy 探针、token 日志脱敏——原先整条含 token 的 wsUrl 打进 logcat）；AgentService 装配（cmd 路由 + realScreenSize 物理分辨率姿势）；6 处原生任务入口 lease 拒单守卫（proven-to-fire）。
- **8 指令回执协议**：screenshot/tap/swipe/type/key/launch/device_info/tree_dump；20 个错误码分永久/瞬时；tap 用 GestureResultCallback 三态（全仓旧姿势会谎报成功）；launch 以前台包名轮询为成功判据（ColorOS 静默拦截前科）；screenshot 回执带 captureW/H+screenW/H 双分辨率（720 缩图 vs 物理坐标错位是 AI 循环第一坑）；msgId 幂等去重含 in-flight。
- **安全**：远程协助开关默认开（Alex 2026-09-04 拍板，保留租户级关闭）；type/launch 白名单只放抖音系；717 JVM 单测绿；smoke 进 android CI 并登记 smoke-baseline。
- **双审查**：spec 合规（前置对抗审查 4 个 P0 全吸收：WsClient 纯下行/ScanMutex 被踩/token 裸奔/手势无回调）；code quality 两轮（C1 displayMetrics→物理分辨率、I1 in-flight 去重、I2 TOCTOU 复查已修）→ APPROVED。
- **顺手修 3 个仓库级 CI 挡门**（都不是本 PR 引入）：dashboard npm audit high（browserslist/fast-uri 升级）；commander 复核超时 20s→45s（实测 TOAPIS gpt-5.4-mini 单字延迟 12.4s，20s 窗口致 gp2 Step8d / rpa-locator 间歇 timeout_保守拒——同根因）；L2 contract 两 job timeout 5→10min（npm ci 冷缓存必超）。

## 没完成（范围外/遗留）

- 件2 中台桥端点 `/api/devices/:id/actions`（鉴权复用 PR#1748 agent license）
- 件3 phonectl CLI（HK OpenClaw 网关 exec 白名单加一条即通）
- 机队真机首验（小黄或小粉单机；截屏授权单机先验，不碰全机队）
- SESSION_EXPIRED 不主动上报（租约惰性过期无自然发射点，已声明裁剪）
- 质量债（下 sprint）：M3 close 后语义 / M4 720 常量双份 / M5 globalAction 兜底 / M6 魔法数 / M8 AgentService 1600 行拆分 / realScreenSize 双份抽共享 util

## 下一步

1. **件2 中台桥**：红线动作（私信/关注/点赞）频控+租户开关在此层代码强制 fail-closed；⚠️ 威胁项：tap 坐标序列可绕行 launch/type 白名单（原语层无业务闸），必须在中台桥层设防
2. **件3 phonectl**：`openclaw approvals allowlist add --agent main "phonectl*"` 即接通
3. **端到端首验**：OpenClaw 对话发指令 → 中台 → 机队手机真 tap → 回执带前台包名

## 数据源

- 设计（含 Research 修正笔记 7 条）：`docs/superpowers/specs/2026-09-04-openclaw-signal-bridge-agent-cmd-design.md`
- PrepPRD（判定点登记表）：`sprints/09041528-openclaw-signal-bridge-agent-cmd/prep-prd.md`
- 实现计划：`docs/superpowers/plans/2026-09-04-openclaw-signal-bridge-agent-cmd.md`

## 决策引用

`7a4c0369`（本方向）、`0a6fd1d4`（被 supersede）、判定点 4 条（tap 三态/launch 前台/screenshot 分类/坐标系对齐）

## 产物

PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1762 ｜ 分支: `cp-09041533-openclaw-bridge-agent-cmd` ｜ sprint: `sprints/09041528-openclaw-signal-bridge-agent-cmd`
