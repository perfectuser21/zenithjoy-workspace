# Handoff：xian-rog 真实Agent(v2.0.88)升级到v2.0.94(含作战窗刀1)，真机截图验证通过

**Verdict**: PASS

## 完成
- 应用户明确指示"真的升级那个正在跑的真实agent(v2.0.88)到新版本"，通过`.active-core`指针自升级机制安全完成真实升级：v2.0.88 → v2.0.94
- 升级过程中发现并修复了一个真实生产缺口（见PR #1517）：核心进程从未拉起过作战窗WPF壳，本sprint此前所有"真机验证"都是schtasks手动拉起的假象，客户真实装机后看不到作战窗
- 用带修复的新代码重新构建 zenithjoy-agent.exe + agent-panel-host(dotnet publish，只能在Windows打包机做)+ agent-panel web内容(vite build)，组装进新的 zenithjoy-agent-v2.0.94 目录（沿用v2.0.88原有.env真实凭据+ffmpeg/python-embedded/node_modules等734MB重资产不变）
- 安全触发升级：写.active-core指针→kill当前监控中的核心进程→start.bat自带的supervise loop自动重启进新版本（此机制专为处理这种重启设计，非破坏性操作）
- **真机截图验证**（PrintWindow真实截图，非DOM文字推断）：ZenithJoyAgentPanel.exe被核心进程真实自动拉起，首次装机仪式真实触发，截图清晰显示"作战窗已上线 · 从现在起你能随时看到AI在做什么"文案 + "智能获客"/"智能回复"业务线泳道（真实业务语言，非line02/line04代号）
- 升级后确认机器整体健康：GitHub Actions self-hosted runner未受影响、微信监听listen_chat.py进程已恢复运行、.active-core正确指向v2.0.94

## 没完成
- 无（本次用户明确要求的真实升级+真机验证已完整闭环）

## 数据源
- xian-rog `C:\Users\asus\.active-core`（当前值 zenithjoy-agent-v2.0.94）
- xian-rog `C:\Users\asus\Desktop\zenithjoy-agent-v2.0.94\`（新真实安装目录）
- `services/agent/src/agent-panel-launcher.ts` / `apps/agent-panel-host/SingleInstanceGuard.cs`（本次修的核心缺口）

## 产物
- PR #1517（缺口修复）+ PR #1519（handoff镜像），均已合并
- 真机PrintWindow截图（本对话内已展示）

## 下一步
- 用户目前把xian-rog当作可直接观察效果的"staging"环境使用；若需正式推production，按用户标准流程（AI只部署staging，用户从staging判断是否放行）由用户决定
- Line02关键词→评论smoke真机测试的预存缺陷已登记issue 14314b94，与本次工作无关，不阻塞
