# Handoff：line01 刀2 Notion 发布编排台（PR#1795）

- **task_id**: 5163f159-9c69-4203-ac2c-5df8f8759f08 · **verdict**: PASS
- **journey**: line01（24987ee5）· golden_path c5c0e259 · **PR**: #1795（已合并 d5aeae2a）

## 完成

1. 派发核心抽 `services/content-publish-dispatch.ts`（route/worker 共用，类型化错误，刀1 16 测试保绿）；`notion-client.ts` 防 token 泄漏封装；`notion-orchestrator.ts` 三方向 60s 轮询（推行/拉发/回执，启动自检+running 互斥+白名单不信 Notion+锚失效跳过+1900 截断+回执后挪出 queued+终态行拒重派）。
2. **Notion「发布编排台」库已建**：`3d6c40c2-ba63-813e-9552-d7cf50a942ab`（AI Hub 根页下）。staging env 三个 NOTION 变量已配——⚠️ `docker restart` 不重读 env-file，必须重跑 deploy workflow 重建容器（本次实证踩过）。
3. **staging 真验四条全过**（ZJ-E-ALEX5211）：传素材≤60s 行自动出现；Notion 填《落日与晚风》改"发"→标题文案真实回写 contents+拆 douyin/weibo 任务+行"排队中"；伪造终态→回执 `douyin ✅ / weibo ❌` +行"部分失败"+contents 挪 failed；agent 过期→"派发失败"+人话原因→刷心跳重试成功。
4. CI 途中顺手修：npm audit fix 清 multer/svgo 高危；@tiptap/core 进 ALLOWLIST（需 tiptap v3 major，issue ad98e258）；ESLint 警告棘轮 54→39（worker any 全类型化）；TDD 顺序闸经 cherry-pick 重排提交过闸。

## 没完成 / 记债

- 生产 promote 属主理人放行的既定流程节点（AI 只部署 staging 是仓库铁律）：staging 已全量真验、无技术阻塞，编排台+刀1+评分模型修复将随下一次 promote 一并上产线。
- 债：回执 emoji 判定与 SUCCESS_STATUSES 不同源（挂 H-3）；"部分失败"重试语义 v1=重新上传；预览 URL 1h 过期；PublishPackageMaterial 双定义。

## 下一步

1. 刀3：android-publish skill 家族接 GET /api/publish-tasks 领单协议真机发布（staging 已有一条 douyin queued 任务可当第一单）；6 个 skill 仍在 worktree 未提交（0830 待办）。
2. 刀4：生产 promote（migrations 差集+API+生产 env），主理人放行。
3. 主理人日常入口：Notion 搜「发布编排台」——手机传图→填文案→状态改"发"。

GP-Anchor: none(docs)
