# Handoff：fix(android): 重启Agent服务按钮真正重试register，不再需要杀进程重开

**Verdict**: PASS
**Task ID**: 4c2d3d1c-a38e-484f-9f7e-d2d4cf7732ff
**Branch**: cp-07201538-android-restart-register-retry

## 完成
- 真机复现并根因定位：register()嵌套在只跑一次的initAgent()里，被agentInitialized标志位永久锁死，导致"重启Agent服务"按钮对已存活Service是no-op
- 抽出独立performRegister()，onStartCommand新增else if重试分支，与agentInitialized解耦
- 补齐performRegister()的catch-all异常兜底（CancellationException显式rethrow+Exception兜底写lastRegisterError），防止重试变成崩溃循环
- 整体审查发现并修复真实并发竞态：加registerCallInFlight(AtomicBoolean.compareAndSet)统一覆盖initAgent()首次调用与按钮重试两条路径，防止并发打两次注册请求非原子写SharedPreferences
- 新增shouldRetryRegister纯函数+4组真值表单测（AgentServiceRegisterRetryTest.kt），先红后绿
- gradle :app:testDebugUnitTest 全量439个测试通过，无回归
- 全部改动经4轮subagent实现+spec/质量双阶段审查，最终整体审查READY TO SHIP
- PR #1433已创建、CI全绿、auto-merge、已合并进main（939d48f1）

## 没完成
- shouldRetryRegister/registerCallInFlight的并发行为契约本身缺少自动化测试（仓库无Robolectric/Mockito，无法实例化真实Service测并发，已在review中记录为已知限制）
- 未按项目CLAUDE.md铁律5把这个纯客户端bug回流进golden-path-2-smoke.sh（该bug是Android Service状态机问题，服务端无感知，golden-path-2-smoke.sh是纯curl脚本测不了；review建议在smoke里留一行注释指向本测试文件说明判据落脚点，未做）
- 本次修复触发的真机验证（用户在staging环境更新新版本APK后实际点击"重启Agent服务"按钮确认状态页变为"已注册"）尚未做——依赖用户获取新版本APK

## 下一步
- 等待Android CI自动构建的新release APK（android-agent-ci.yml，merge到main后自动触发）发布到 http://apk.zenjoymedia.media/install-pack/android/zenithjoy-agent.apk
- 用户在staging测试手机上更新到新版本App后，重新走一次账号扫描失败诊断的原始目标：点"立即扫描"确认能看到真实screenshot_b64/tree_dump诊断证据（这是本session最初的目标，因中途发现的注册bug而插入了本次修复）
- 可选：把本次纯客户端bug的回归判据在golden-path-2-smoke.sh里留一行TODO注释指向AgentServiceRegisterRetryTest.kt，补齐铁律5的留痕要求（不阻塞，review标记为non-blocking）

## 数据源
- docs/superpowers/specs/2026-07-20-android-restart-register-retry-design.md
- docs/superpowers/plans/2026-07-20-android-restart-register-retry.md
- services/agent-android/app/src/main/kotlin/com/zenithjoy/agent/AgentService.kt
- services/agent-android/app/src/test/kotlin/com/zenithjoy/agent/AgentServiceRegisterRetryTest.kt

## 决策引用
- 413a4bdc-bdb2-40b8-a45a-94dffe92fc9f

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1433
- Branch: cp-07201538-android-restart-register-retry
