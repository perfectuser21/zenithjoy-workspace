# Handoff：TOAPIS deepseek-v4-flash 渠道#58 上游欠费——4处 AI 调用临时切走 gpt-5.6-terra

- task_id: bca4fb05-749c-4ef1-951d-8d025468df70
- verdict: PASS

## 完成
- 确诊 PR#1717 CI 里 3 个 baseline smoke（`collect-report-comment-grading-smoke.sh`/`golden-path-2-smoke.sh`/`rpa-locator-assist-smoke.sh`）失败根因：TOAPIS `deepseek-v4-flash` 所在渠道 #58 上游返回 402 Insufficient Balance，无备用渠道；用不相关 PR#1712 交叉验证同样命中，确认是全局性问题、非某个 PR 的回归
- 排除了"用户账户 20691 credits 能解决"这个误解——渠道 #58 是 TOAPIS 自己的上游供应商账户，客户侧的充值/查询入口都碰不到
- 真调探活确认 `gpt-5.6-terra` 健康可用，且它认 `reasoning_effort:'none'`（reasoning_tokens=0）——纠正了此前"该参数只有 deepseek 认"的旧代码注释
- **PR#1719 已合并**：`comment-grading.ts`/`content-judgment.ts`(commander)/`locator-assist.ts`/`wechat-draft.ts` 四处 AI 调用默认模型临时切 `gpt-5.6-terra`，均 env 可覆盖；`env-registry.ts` 同步更新说明 + 新增 `GRADING_MODEL` 登记；`comment-grading.test.ts` 更新模型名断言；apps/api 全量单测 272 files / 2137 tests 绿，`tsc --noEmit` 无报错
- 两处历史决策冲突（comment-grading 2026-07-20 为省钱换 deepseek、wechat-draft 2026-07-03 为速度/成本换 deepseek、content-judgment commander 的跨厂商交叉验证设计）已在对话中向用户明确挑出并获得拍板确认（`gpt-5.6-terra`），不是静默覆盖历史决策

## 没完成
- TOAPIS 渠道 #58 本身未修——需要 TOAPIS 平台自己给上游供应商充值/换绑，非本仓库能解决；这是临时应急切模型，不是根治
- PR#1717（FailureClassifier）截至本次交接仍在等 CI 重跑验证（main 已含 #1719 的修复）
- Brain 任务状态卡在 `in_progress` 未能 PATCH 到 `completed`（疑似已知的状态机问题），`result` 字段已正确写入 `merged:true` + `pr_url`，不影响实际交付

## 下一步
- 回 main 分支，rerun PR#1717（`cp-08231127-failure-classifier`）的 CI，验证 3 个 smoke 随 main 修复转绿后合并
- 渠道 #58 修好后，评估是否把 4 处模型换回 deepseek-v4-flash（降成本）；模型名均已 env 可覆盖，改配置即可，不必改代码
- 持续观察 `gpt-5.6-terra` 在生产真实流量下的判定质量/延迟/成本，尤其 content-judgment commander 的跨模型交叉验证效果是否符合预期

## 数据源
- `apps/api/src/services/comment-grading.ts`、`content-judgment.ts`、`locator-assist.ts`、`wechat-draft.ts`
- `apps/api/src/env-registry.ts`
- decisions 表：`4346c90f`（comment-grading 换 deepseek）/ `c29ab436`（视频判定 commander 交叉验证设计）

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1719
- branch: `cp-08231451-toapis-model-failover`（已合并，worktree 已清理）
