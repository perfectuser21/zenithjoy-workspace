# Handoff：fix(agent-panel): 装机包接线 + 补托盘图标，堵刀1两处遗留缺口

**Verdict**: PASS
**Task ID**: unknown（本次为用户交互式 /dev 手动执行，未预先注册 Brain task；harness evaluator 未走，已登记 issue `d90cdee5` 说明原因）
**Branch**: cp-07281601-agent-panel-pack-tray

## 完成
- 用户要求把最初 PrepPRD（`sprints/07280929-agent-panel-knife1/prep-prd.md`）验收标准跟当前实现逐条重新核对（不看 waiting list），核出两处真实缺口，本 PR 补齐：
  1. **装机包从未包含作战窗**：`services/agent/scripts/build-install-pack.sh` 从未引用过 `apps/agent-panel-host`，客户装的 Agent 实际不含此功能。补 Windows 打包机分支：`dotnet publish AgentPanelHost.csproj -c Release -r win-x64 --self-contained true` + `apps/agent-panel` vite build，网页内容拷进 `agent-panel-host/agent-panel-web/`（exe 同目录，`MainWindow.xaml.cs` 按此相对路径找 index.html）；非 Windows 打包机改为显式 WARN 而非静默跳过；dry-run stub 同步补占位。新增静态契约测试 `build-install-pack-agent-panel.test.sh`（6 项检查全过）
  2. **托盘图标缺失**：PrepPRD Golden Path Step3"按热键或点托盘展开"只做了热键。`AgentPanelHost.csproj` 加 `UseWindowsForms=true`（只借 `NotifyIcon` 一个类）+ `icon.ico`；`MainWindow.xaml.cs` 新增 `SetupTrayIcon()`，点击复用与热键相同的 `ToggleExpanded()`
- CI 首轮报 `CS0104: Application 在 System.Windows.Forms.Application 与 System.Windows.Application 间歧义`（`UseWindowsForms=true` 给整个项目注入了 `global using System.Windows.Forms`）。第二次提交修复：`App.xaml.cs` 基类改完整限定名 `System.Windows.Application`，`MessageBox`/`MessageBoxButton`/`MessageBoxImage` 用 `using` 别名钉死指向 WPF 版本
- 两轮提交后 `agent-panel-host-build.yml`（windows-latest 编译检查）转绿，全部 CI check 通过，PR #1497 squash-merge

## 没完成
- **真机验证完全没做**：本 PR 只解决"代码能不能被打进装机包"，没有在任何真实 Windows 机器上跑过真实（非 dry-run）打包 + 安装 + 运行。窗口是否真的弹出、Ctrl+Alt+Z 热键是否真的生效、托盘图标点击是否真的唤出、RPA 激活时 WS_EX_TRANSPARENT/WS_EX_NOACTIVATE 是否真的生效——全部未经肉眼/真实操作确认
- 这正是用户反复强调的"应该推到 staging 上，我们去测试呀，然后我们去看呀，然后没问题我再推 production"的那一步，本 PR 不代表该要求已满足

## 下一步
- 在 xian-rog（Lead 自检机）上跑真实（非 dry-run）的 `build-install-pack.sh`，确认产出的装机包里真的含 `agent-panel-host/ZenithJoyAgentPanel.exe` + `agent-panel-web/`
- 实际安装该装机包，运行 Agent，肉眼确认：
  - WebView2 窗口能创建并渲染 `apps/agent-panel` 网页内容
  - Ctrl+Alt+Z 热键能收起/展开
  - 托盘图标存在且点击能收起/展开
  - 模拟 RPA 激活（desktop-lease-broker held=true）时窗口是否正确收成贴边 mini 视图、且真的鼠标穿透+不抢焦点
- 全部确认无误后，这才是用户认可的"staging 验证通过"，可以据此判断是否推进 production

## 数据源
- `sprints/07280929-agent-panel-knife1/prep-prd.md`（原始 PrepPRD，本次核对依据）
- `docs/superpowers/specs/2026-07-22-agent-panel-design.md`（原始设计文档，PR#1457）
- `apps/agent-panel-host/`（WPF+WebView2 原生宿主）
- `services/agent/scripts/build-install-pack.sh`（装机包打包脚本）

## 决策引用
- Brain issue `d90cdee5`：本 initiative 的原始功能 PR（#1488）在 evaluator 未执行时被合并（harness 自动化被绕过），本 PR 延续同一路径手动执行

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1497
- Branch: cp-07281601-agent-panel-pack-tray
- 前置 PR：#1488（主功能）、#1489（handoff镜像）、#1491（windows-latest CI编译检查）
