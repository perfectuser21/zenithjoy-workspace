# Handoff：抖音获客线索表字段收敛 + 自有账号/视频去重 + 成功触达真实校验

- task_id: local（本次为交互式 /dev 路径B 会话，未走 Brain headless 任务派发，无真实 Brain task UUID）
- journey_id: line02 智能获客（金诺租户，keyword_acquisition golden path）
- verdict: PASS
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1869（已 merge，2026-09-19T07:44:21Z）
- 分支: cp-09191439-douyin-leads-table-cleanup

## 完成
- 金诺租户抖音获客线索表字段收敛：删「抖音昵称/主页链接」合并列、删「命中关键词」列；「客户昵称」/「抖音号」/「主页链接」独立成列；「评论原文」改名「原始评论」；新增独立「评论作品视频链接」字段（修复 push-raw-comments.js 原漏读 LEAD 行第12列的 bug）。
- 采集侧新增：自有账号名单过滤（`config/own-accounts.json` + `own-accounts-lib.js` + `check-own-account.js`，命中即跳过，命中视频作者则整条视频不再采集）+ 同视频去重（`fetch-seen-videos.js` 查「视频池」已处理视频ID，命中即跳过）。
- 触达侧新增「成功触达」字段：Path 2（Chrome CDP，`douyin-dm-outreach.ts`/`lead-writer.ts`）复用已有 sent/limited/failed 三态给出准确映射；老链路（真机 ADB）降级写"待确认"+透传设备原始输出。
- 新增/更新 4 个 CI 门禁修复：新增 `leadgen-field-cleanup-smoke.sh`（满足 feat: PR 的 smoke 强制要求）、修复 `phone-adb-controller-smoke.sh` 因字段改名产生的断言过期、补 GP-Anchor PR body 声明（`line02/keyword_acquisition keep-green`）。
- 全部改动按 TDD 逐任务提交，本地+CI 双重验证：`node --test` 15/15，`vitest` 9/9，Smoke Glob Runner 全绿。

## 没完成 / 范围外
- **生产飞书表结构变更未执行**（改名/删列/新增列/隐藏视图），这是有真实数据丢失风险的操作，PR 描述里已列清单，需要主理人在飞书里直接操作或另找受控场景执行，不适合本次自动化无人确认执行。
- 老链路（真机 ADB `douyin-phone-adb` 二进制）的"对方未加好友"设备侧真实探测未实现——该二进制不在 zenithjoy-workspace 仓库范围内（真机/独立仓库产物），需要另开任务在对应仓库补。
- 「对标账号」过滤未实现（用户明确确认无此概念/需求，非遗漏）。
- 真机验证（自有账号过滤日志、同视频去重日志）未做，PR Test Plan 里列为待办 checkbox。

## 下一步
1. 主理人在生产飞书表执行结构变更（见 PR #1869 描述里的清单），执行后跑一次真实采集/触达确认新字段写入正确。
2. 真机验证自有账号过滤 + 同视频去重两个日志断言。
3. 悦升租户的对应表（`tblmz52E2GDX0uwu`，与金诺"0916 已补齐 7 个新列对齐"）本轮未动，用户表示要想清楚具体改动点后单独提出。

## 数据源 / 决策引用
- PrepPRD: `sprints/09191439-douyin-leads-table-cleanup/prep-prd.md`
- 设计文档: `docs/superpowers/specs/2026-09-19-douyin-leads-table-cleanup-design.md`
- 实现计划: `docs/superpowers/plans/2026-09-19-douyin-leads-table-cleanup.md`
- Brain strategic-decisions: `3f2bc2b4-c2d7-4e71-a4ed-12814c3be49b`（category=small-change）

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1869
- sprint_dir: sprints/09191439-douyin-leads-table-cleanup
- branch: cp-09191439-douyin-leads-table-cleanup
