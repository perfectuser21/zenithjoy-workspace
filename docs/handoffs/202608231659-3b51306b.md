# Handoff：AI on-call 铺满第二批——扫号链我tab/切号行 + 采集链详情页分享按钮

- task_id: 3b51306b-52ae-4c07-bf29-fc5222df3591
- verdict: PASS

## 完成
- 用 PR#1717 抽出的 `FailureClassifier.shouldAssist()` 门禁接线 3 个此前完全无 AI 保底的动作点（PR#1721 已合并）：
  1. `DeviceAccountScanService.kt`「我tab」——坐标兜底本来就在，AI 先指认真实节点再退坐标兜底，不改变最坏情况行为
  2. `DeviceAccountScanService.kt`「切换账号面板找目标昵称行」——此前无任何兜底、找不到直接判死；切号是账号管理关键动作，选错人风险高
  3. `DouyinCollectService.kt`「详情页分享按钮」(STEP1_detail_not_ready) ——此前无任何兜底、找不到直接判死；命中候选直接当分享按钮用，跳过原 STEP2 查找
- `DeviceAccountScanService.kt` 新增一份 `tryLocatorAssist` 私有实现（与另外两个 Service 各自持有的同名实现一致，延续既有代码结构不引入新抽象）
- 全仓库精确盘点：15 个真实等待失败点（`NodeAwait.classifyFailure` 调用点）中现有 12 个已接线（此前 9 + 本刀 3）
- versionCode 47→48 (2.1.43→2.1.44)

## 没完成（明确排除，非遗漏）
- `DeviceAccountScanService.kt` 读账号列表（`readAccountListFromPanel`，account/573）——这是多值提取场景（一次要读出整个账号列表），现有 `locate`/`extract` 两种 assist 协议都是单值设计，接这里需要先扩展协议（新增多值 extract 模式），不是简单接线，留作独立后续工作
- `DouyinDmOutreachService.kt` 两处（结果页无窗口 / 主页渲染信号未等到）——都是纯环境性 NO_ROOT 判断（树都没有，问 AI 无意义），且后者代码本身刻意设计成"拿不到信号也不阻断"，两处都不该接 AI 保底

## 下一步
- 若要继续铺满，下一个有价值的目标是给多值 extract 协议做设计（服务 `readAccountListFromPanel` 这类"读一整个列表"的场景），之后才能把扫号链最后一个未覆盖点接上
- 真机验证：待装机验证扫号链/详情页分享按钮的 AI 保底实际生效（本次未做真机验证，只有代码 review + CI 编译通过）

## 数据源
- `services/agent-android/app/src/main/kotlin/com/zenithjoy/agent/account/DeviceAccountScanService.kt`
- `services/agent-android/app/src/main/kotlin/com/zenithjoy/agent/collect/DouyinCollectService.kt`
- `services/agent-android/app/src/main/kotlin/com/zenithjoy/agent/uia/FailureClassifier.kt`（PR#1717）

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1721
- branch: `cp-08231639-ai-oncall-wave2`（已合并，worktree 已清理）
