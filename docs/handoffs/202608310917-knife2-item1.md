# Handoff：feat(api): /api/workers POST 支持 agent license 鉴权（与内部 token 二选一）——控制塔第二刀·件1

- task_id: unknown（本次未注册 Brain task，有头 /dev 路径 B 直接开工） · journey: 24987ee5-53a0-4c37-946f-1b749954cac7 · verdict: PASS · PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1748

## 完成
- 新增 `apps/api/src/middleware/worker-agent-auth.ts`：`workerAgentAuth` 从 `X-Agent-License` 取 license_key → 复用 `walking-skeleton.service.validateLicense`（已含 revoked/suspended/expired/no-tenant 判定并直接返回 tenant_id）→ 查 `zenithjoy.agents.tenant_id` → 租户一致才放行；`workerPostAuth` 是内部 token 与 agent license 二选一的入口
- `workers-executor.ts` 的 `router.use(internalAuth)` 换成 `workerPostAuth`；无 license 头或 env 未设置时直接委托原 `internalAuth`，内部 token 路径逻辑一字未改（n8n / creator-api 调用不受影响）
- 状态码：同租户放行 / 无凭据 401 / license 认不出 401 INVALID_LICENSE / revoked·suspended·expired·no-tenant 403 / 跨租户（含 agent.tenant_id 为空）403 TENANT_MISMATCH / agent 不存在 404 / 查询抛错 500 不静默放行
- 守卫（逻辑接缝）：`routes/__tests__/workers-executor.test.ts` 三条路由层契约（201 / 401 / 403）+ `middleware/worker-agent-auth.test.ts` 15 条分支单测；TDD 两段式 commit（bc66aace 红 → 6c1193c6 绿）
- 守卫（环境接缝）：`worker-activity-smoke.sh` 新增 `[4c]` 段，真 API + 真 DB 种同租户/跨租户 license 各推一帧，断言 202/403/401/401；两个前提（服务端已设 ZENITHJOY_INTERNAL_TOKEN、有可用 DB）缺一即显式 SKIP 不假绿
- test-registry 登记 `worker-control-tower-agent-auth-unit`

## 没完成
- **件2 安卓推流接线**（本 PR 范围外，下一步）：`services/agent-android` 的 `ScreenCaptureReal.captureFrame`（已出 JPEG）接循环 POST `/api/workers/<agentId>/frame`，带 `X-Agent-License`；APK 加「上墙」开关；顺带解决金诺 Realme(ColorOS) shell 抓屏被封
- **件3 Windows 桌面捕获**（本 PR 范围外）：`services/agent` 新写桌面捕获模块进 agent 主进程（Node 截屏），随 install-pack 分发；agent 自带签名代码不被 Defender AMSI 拦
- `/tasks/:id/steps`、`/tasks/:id/complete` 仍只认内部 token —— 路径上没有 agent 身份可比对租户归属。若将来 agent 需要自己上报步骤，得先决定用什么绑定（查 worker_tasks.agent_id 反推，或路由改带 agentId）
- 真机验收未做：件2 做好后由安卓 agent 实推一次才算端到端验证
- 悦升手机画面临时脚手架（xian-m4 `~/.zj-wall/`）仍在跑，件2 落地后退役

## 下一步
- 做件2：安卓推流接线（详见 `/Users/administrator/worktrees/cecelia/session-a871bead/CONTROL-TOWER-KNIFE2-HANDOFF.md`）
- 件2 合并后做件3：Windows 桌面捕获
- 件2/件3 任一落地后，用真机推帧跑一次 `/dashboard/workers/:agentId` 实时详情做端到端验收

## 数据源
- apps/api/src/middleware/worker-agent-auth.ts
- apps/api/src/middleware/worker-agent-auth.test.ts
- apps/api/src/routes/workers-executor.ts
- apps/api/src/routes/__tests__/workers-executor.test.ts
- apps/api/src/services/walking-skeleton.service.ts（validateLicense）
- .github/workflows/scripts/smoke/worker-activity-smoke.sh（[4c] 段）
- docs/superpowers/specs/2026-08-30-worker-control-tower-design.md
- docs/handoffs/202608301826-3a2f9b58.md（第一刀）

## 决策引用
- dce74be9（本件，small-change）
- e14297d4（工作机控制塔设计）

## 产物指针
- branch: cp-08310825-worker-agent-frame-auth
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1748
- merge sha: 2f7ccac9

## 备注：CI 两轮修复 + 一次 flake
- Orphan Test Check 报新单测未注册 test-registry（L2 Consistency Gate FAIL 只是它的下游）→ 补注册
- CodeQL js/missing-rate-limiting high：把单测里的 catch-all 当成「做鉴权 + DB 访问却未限流」的路由 → 测试 app 补上与生产同序的 `simpleRateLimit`（限流先于鉴权）
- `E2E Knowledge Hub Path2 (collab notes)` 的 staff-hub CRDT 双人同编 spec 失败一次（B 的文字没同步到 A），与本次改动无交集且该 workflow 此前每次 PR 都绿，重跑即过 —— **flake，非本次引入**
