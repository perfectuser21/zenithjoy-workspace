# Handoff：[Ability Acceptance Phase A] Staff Hub Ability 验收端到端首刀

**Verdict**: PASS（Evaluator PASS + Judge PASS）  
**Task ID**: 30a0c83a-47f4-4151-9636-a8cd2b6f1d7a  
**Sprint Dir**: sprints/07281218-relay-30a0c83a  
**Branch**: cp-07281340-ws-30a0c83a  
**PR**: [#1493](https://github.com/perfectuser21/zenithjoy-workspace/pull/1493)

---

## 完成

### Phase A — product-map 合同 E2E
- `ability_acceptance.status` 从 `proposed` 升级为 `active`（`product-map/product-map.yaml`）
- `scripts/product-map/lib.mjs`：`canonicalize()` 函数 `golden_paths` 保序（三键排序 `[app_id, line_id, id]`）
- `product-map/generated/product-map.json` 重新生成，digest 确定性通过 T6
- 7 个 product-map 单元测试 T1-T7 全绿（CI `ability-acceptance-smoke` job）

### Phase B — API 集成
- 新建 DB 迁移：`apps/api/db/migrations/20260728_ability_acceptance.sql`（4 张表 + CHECK 约束 + tenant 索引）
- 新建路由：`apps/api/src/routes/ability-acceptance.ts`（7 个端点，全 staffGuard）
- 挂载路径：`/api/staff/ability-acceptance`（`apps/api/src/app.ts`）
- `STAGING_VERSION` / `PRODUCTION_VERSION` env 注册（`env-registry.ts`）
- API 单测：`apps/api/src/routes/__tests__/ability-acceptance.test.ts`（含租户隔离 / 幂等 / 输入校验）

### Phase C — Playwright E2E（windows_cloud）
- 新建 E2E spec：`apps/staff-hub/e2e/ability-acceptance.spec.ts`（C1-C5 全 page.route mock）
- 独立 CI workflow：`.github/workflows/e2e-staff-ability-acceptance-windows.yml`（windows-latest，Staff Hub 5175 端口）
- 不进 orphan-consolidation（两个 app 完全分离，ports 5174 vs 5175）
- `test-registry.yaml` 路径更新：`apps/staff-hub/e2e/ability-acceptance.spec.ts`

### Phase D — 部署验收（合同已知豁免）
- Phase D（D1-D4）为生产部署手工验收，合同明确标记 unverifiable，不入 CI

### 前端
- `apps/staff-hub/src/App.tsx`：新增 `ClipboardCheck` 图标 + `/ability-acceptance` 路由 + 历史页路由
- `apps/staff-hub/src/pages/AbilityAcceptancePage.tsx`：版本概览 + 创建/继续 run + 逐条验收 + 提交
- `apps/staff-hub/src/pages/AbilityAcceptanceHistoryPage.tsx`：历史 run 列表 + 明细展开

### CI 结果（head sha=5c6b6282，合并前最终绿态）
- 102 checks：95 success，7 skipped，0 failed

---

## 没完成

- **Phase D 真实部署**：DB 迁移未在 hk-vps/mmv 执行，生产 URL `https://staff.zenjoymedia.media/ability-acceptance` 未验收
- **合同 E2E 脚本 B1-B9**：`sprints/07281218-relay-30a0c83a/e2e-contract.sh` 需 `$API_BASE`/`$DB`/`$STAFF_EMAIL` 在真实服务器上执行

---

## 下一步

1. 在 hk-vps + mmv 两台生产 DB 执行 `apps/api/db/migrations/20260728_ability_acceptance.sql`
2. 部署 Staff Hub 和 API 到 staging（`ci(staff-hub): staging/production 对称部署` 已在 #1499 就绪）
3. 执行 `bash sprints/07281218-relay-30a0c83a/e2e-contract.sh`（需真实 API_BASE + DB DSN）
4. 验收 Phase D D1-D4：公网 URL 可访问 + `\dt acceptance_*` 四表存在
5. 下一个 Path 推进：ability_acceptance 已 active，可启动 Golden Path 下一阶段

---

## 迭代轨迹（fix 轮次）

| 轮次 | Commit | 问题 | 修复 |
|------|--------|------|------|
| fix-1 | `090059fb` | Phase C 无 CI 宿主（CI Rule #5 违规）| 加 e2e-verify.ps1 + 尝试进 orphan-consolidation |
| fix-2 | `1d9f11c5` | orphan-consolidation 跑 Dashboard 5174，spec 测 Staff Hub 5175 | 移 spec 到 `apps/staff-hub/e2e/`，建独立 workflow |
| fix-3 | `5c6b6282` | test-registry 路径仍指向 `apps/dashboard/e2e/` | 更新路径 + merge origin/main |

---

## 数据源

- `sprints/07281218-relay-30a0c83a/contract-draft.md`（27 判定点 + 7 铁律）
- `sprints/07281218-relay-30a0c83a/contract-dod.md`（DoD 完成标准）
- `.harness/verdicts/evaluate-5c6b628.json`（Evaluator PASS 凭证）
- `product-map/product-map.yaml`（ability_acceptance 分类事实）

## 产物

- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1493
- Branch: cp-07281340-ws-30a0c83a（auto-merge 已启用）
- Sprint: sprints/07281218-relay-30a0c83a
- Evaluator verdict file: `.harness/verdicts/evaluate-5c6b628.json`
