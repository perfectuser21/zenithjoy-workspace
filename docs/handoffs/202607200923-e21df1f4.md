# Handoff：Path2 Step7 账号扫描补手动触发通道（3-5秒响应目标，实际约最坏30秒，用户已确认接受该取舍）（PR #1424）

**task_id**: e21df1f4-cced-4853-8478-314e1a510ad4（Brain任务状态被卡在failed终态无法回写，不反映真实结果——PR已真实合并）
**PR**: #1424（已MERGED）
**verdict**: PASS

## 完成
- 根因确认：DeviceAccountScanService唯一触发路径是客户端30-60分钟随机被动定时器，服务端无任何主动催促通道
- API新增 POST /api/acquisition/account-scan/trigger（查在线安卓设备+60秒/租户限流+建publish_tasks）
- Android AgentService.kt新增shouldRouteAccountScan WS任务分支，复用既有ws1心跳(30s)机制
- Dashboard账号管理页加「立即扫描」按钮+60秒本地冷却+离线态提示
- task-by-task review期间抓到并修复3个真实bug：①payload为空导致设备端永远收不到任务 ②/account-scan-result从不闭环publish_tasks导致每30秒心跳无限重复扫描（电量+抖音风控风险）③修复②引入的新回归：非UUID格式request_id（内部定时循环既有路径）会让查询崩溃挂死请求
- whole-branch final review Approved，唯一非阻塞发现（MUTEX_BUSY重投递竞态，自愈，与既有warmup-result同款容忍模式）记为fast-follow
- PR #1424 已合并（过程中撞上GitHub Actions/API平台性503故障导致CodeQL/gitleaks假失败，与代码无关，push空commit重触发CI后转绿）

## 没完成
- MUTEX_BUSY竞态fast-follow未做：手动扫描任务若在设备端遇到已在跑的其它扫描(state!=IDLE)会报busy→标failed，即便随后真扫描完成上报也会被幂等短路吞掉——自愈（下次心跳/下次手动点击即可），非阻塞但建议后续在AgentService加in-flight task_id去重
- qr-bind-result/warmup-result两处同款'未校验UUID直查DB'的脆弱模式未加固（当前无已知非UUID输入源，非活跃bug，记为防御性加固backlog）
- 真机端到端复核未做：本次修复合并后尚未用真实客户账号+真实手机走一遍完整链路验证'立即扫描'按钮到手机真实弹出切换账号面板

## 下一步
- staging部署后找真机验证一次'立即扫描'按钮的完整链路（点按钮→30秒内手机弹抖音切换账号面板→Dashboard显示新账号）
- 视情况决定是否要做MUTEX_BUSY竞态的fast-follow修复

## 数据源
- docs/superpowers/specs/2026-07-20-account-scan-manual-trigger-design.md
- docs/superpowers/plans/2026-07-20-account-scan-manual-trigger.md
- apps/api/src/routes/acquisition.ts（/account-scan/trigger）
- apps/api/src/routes/agent-burner.ts（/account-scan-result）
- services/agent-android/.../AgentService.kt（shouldRouteAccountScan）

## 决策引用
- 029d9d97-e7cd-4430-99f3-9ccdc05710c6

## 产物
- PR: https://github.com/perfectuser21/zenithjoy-workspace/pull/1424
- sprint_dir: sprints/07192358-account-scan-manual-trigger
