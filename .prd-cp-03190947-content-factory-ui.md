---
id: prd-cp-03190947-content-factory-ui
version: 1.0.0
created: 2026-03-19
branch: cp-03190947-content-factory-ui
---

# PRD: ZenithJoy 内容工厂模块

## 背景

内容工厂 Pipeline 已在 Brain 后端实现（API: /api/brain/pipelines），但目前只有 Cecelia 管理台能访问。ZenithJoy 是员工日常操作的前台，需要在这里新增内容工厂入口，让员工能看排期、看进度、做审核。

## 成功标准

1. `ContentFactoryPage.tsx` 存在，包含 KanbanView、ScheduleView、ReviewQueue 三个 Tab 子组件，看板/日历/审核切换正常渲染
2. `navigation.config.ts` 包含 `content-factory` 路由和菜单项「内容工厂」
3. `vite.config.ts` 代理 `/api/brain` 到 `localhost:5221`（Brain API）
4. TypeScript 编译无错误（npx tsc --noEmit 通过）
5. CI 全部通过
