[pytest]
# 禁用 .pytest_cache 写入：本模块有 build-modules/line04/wechat-rpa 打包副本，
# CI L4 Gate / 合同 §E2E 用 `diff -r 源码 vs 打包副本`（仅 exclude *.pyc/__pycache__）强校验一致。
# pytest 默认在 rootdir 写 .pytest_cache，会让「先 pytest 后 diff」的 §E2E 误报源码与打包副本分叉。
addopts = -p no:cacheprovider
