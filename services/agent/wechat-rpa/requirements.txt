# Path 4 — wechat-rpa Python 依赖（Windows 运营机执行）
# Mac mini / Linux CI 开发期不安装也能跑 --dryrun 与纯逻辑单测
# （pywinauto 仅在 scan_unread / reply_in_chat / get_main_window 函数体内 import，顶层不触发）

pywinauto>=0.6.8; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
requests>=2.31.0
