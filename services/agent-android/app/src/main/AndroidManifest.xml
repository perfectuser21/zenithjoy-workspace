<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      Android 11+ 包可见性限制：targetSdk 30+ 查询其他包信息前必须声明 <queries>，
      否则 PackageManager.getLaunchIntentForPackage() 对未声明的包直接返回 null——
      真机验证复现：抖音包明明装着（monkey 能拉起），但代码里拿到的 launch intent
      是 null，症状是"合法失败"而不是抛异常，很容易被误判成别的原因。
    -->
    <queries>
        <package android:name="com.ss.android.ugc.aweme" />
    </queries>

    <application
        android:name=".AgentApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="zenithjoy" android:host="bind" />
            </intent-filter>
        </activity>

        <!-- Bug C：share-intent 接收器。抖音分享面板选本 app 后系统以 ACTION_SEND 拉起，
             无 UI（透明主题 + noHistory + excludeFromRecents），取文案投递后立即 finish。 -->
        <activity
            android:name=".collect.ShareIngestActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <service
            android:name=".AgentService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".collect.DouyinCollectService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/douyin_collect_config" />
        </service>

        <!-- Sprint 07052218 followup — 私信触达执行路径，独立于采集服务的无障碍服务实例 -->
        <service
            android:name=".collect.DouyinDmOutreachService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/douyin_dm_outreach_config" />
        </service>

        <!-- Sprint 07061301-device-account-scan-wiring — 设备账号扫描，独立于采集/触达服务的无障碍服务实例 -->
        <service
            android:name=".account.DeviceAccountScanService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/douyin_account_scan_config" />
        </service>

    </application>

</manifest>
