# CI-only minimal deps（PR-e/5）
# 用途：GitHub Actions Ubuntu runner 上跑 services/creator 的 Python 测试
# 不包含 playwright（体积大、runner 装起来慢、worker 测试不需要浏览器）
#
# 对应 services/creator/requirements.txt 核心子集

fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# API client（worker/server 必需）
httpx>=0.24.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
