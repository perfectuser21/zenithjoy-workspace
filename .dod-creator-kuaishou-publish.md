# DoD - Creator 发布自动化：快手 API 接通

## 验收标准

### 功能验收 ✅

#### 1. 快手 API 认证与初始化
- [ ] 快手 API client 通过认证初始化
      Test: tests/creator/kuaishou.test.ts::test_kuaishou_client_init
- [ ] 从环保凭据读取 APP_KEY 和 APP_SECRET
      Test: manual:echo $KUAISHOU_APP_KEY | grep -q . && echo $KUAISHOU_APP_SECRET | grep -q .
- [ ] 支持 Bearer Token 或 OAuth2 认证（根据快手 API 类型）
      Test: tests/creator/kuaishou.test.ts::test_auth_types

#### 2. 内容格式转换
- [ ] 将数据库中的作品（title + content）转换为快手 API 格式
      Test: tests/creator/kuaishou.test.ts::test_content_format_conversion
- [ ] 支持文本、图片、视频多种内容类型
      Test: tests/creator/kuaishou.test.ts::test_media_type_support
- [ ] 内容长度限制检查（文本 ≤ 快手限制）
      Test: tests/creator/kuaishou.test.ts::test_content_length_validation

#### 3. 发布接口实现
- [ ] 实现 POST /api/works/{work_id}/kuaishou/publish 接口
      Test: manual:curl -X POST http://localhost:8899/api/works/1/kuaishou/publish -H "Content-Type: application/json" -d '{}' | jq '.status'
- [ ] 至少 3 条测试内容成功发布到快手
      Test: manual:verify_in_kuaishou_backend(3)
- [ ] 发布返回快手 POST ID（用于追踪）
      Test: tests/creator/kuaishou.test.ts::test_publish_response_contains_post_id

#### 4. 错误处理与重试
- [ ] 网络超时时自动重试（最多 3 次）
      Test: tests/creator/kuaishou.test.ts::test_retry_on_timeout
- [ ] 认证失败返回 401，并提示检查凭据
      Test: tests/creator/kuaishou.test.ts::test_auth_error_handling
- [ ] 内容违规/审核失败，返回清晰的错误消息
      Test: tests/creator/kuaishou.test.ts::test_content_violation_error
- [ ] 所有异常均被捕获，不导致进程崩溃
      Test: tests/creator/kuaishou.test.ts::test_error_resilience

#### 5. 发布成功/失败日志
- [ ] 发布结果写入 publications 表（status = 'published' or 'failed'）
      Test: tests/creator/kuaishou.test.ts::test_publications_table_update
- [ ] 发布 URL 保存到数据库（platforms.kuaishou.url）
      Test: manual:select url from publications where platform_id = (select id from platforms where code = 'kuaishou') limit 1 | grep -q http
- [ ] 失败原因保存到日志（error_message）
      Test: tests/creator/kuaishou.test.ts::test_error_message_logging
- [ ] PostgreSQL publish_logs 表同步更新（status、platform_post_id、published_at）
      Test: tests/creator/kuaishou.test.ts::test_publish_logs_sync

### 代码质量验收 ✅

- [ ] 所有代码通过 TypeScript 严格类型检查（services/creator 部分）
      Test: npx tsc --noEmit
- [ ] 代码通过 ESLint 检查（无警告）
      Test: npx eslint services/creator/ --max-warnings 0
- [ ] 关键函数有 JSDoc 注释（参数、返回值、可能的异常）
      Test: grep -r '@param\|@returns' services/creator/ | wc -l > 0
- [ ] 单元测试覆盖率 ≥ 80%（快手发布模块）
      Test: npx jest --coverage services/creator/kuaishou 2>&1 | grep -E '(Line|Branch)' | grep -E '(8[0-9]|9[0-9]|100)'

### 集成验收 ✅

- [ ] Creator FastAPI 服务可正常启动（端口 8899）
      Test: manual:curl http://localhost:8899/health | jq '.status' | grep -q 'ok'
- [ ] 快手发布接口与 Creator 数据库正常交互
      Test: manual:curl -s http://localhost:8899/api/works | jq '.[] | select(.id == 1)' | grep -q 'id'
- [ ] 支持批量发布（多篇文章同时发布）
      Test: tests/creator/kuaishou.test.ts::test_batch_publish
- [ ] 避免重复发布（同一篇文章不会发布多次）
      Test: tests/creator/kuaishou.test.ts::test_idempotence

### 安全性验收 ✅

- [ ] API 凭据不硬编码，从环保读取
      Test: manual:grep -r "APP_KEY" services/creator/ | grep -v "\$KUAISHOU" || echo "no hardcode found"
- [ ] 敏感数据（POST ID、URL）不记录到日志
      Test: tests/creator/kuaishou.test.ts::test_no_sensitive_data_in_logs
- [ ] 速率限制遵守快手 API 限制（通常 100 req/min）
      Test: tests/creator/kuaishou.test.ts::test_rate_limiting

### 部署验收 ✅

- [ ] 代码成功合并到 develop 分支
      Test: contract:C1-001 (branch-protection)
- [ ] npm run build 成功编译（无错误）
      Test: npm run build 2>&1 | grep -q "error" && exit 1 || exit 0
- [ ] CI 流程全部通过（DevGate + GitHub Actions）
      Test: contract:C2-001 (github-ci)

## 完成条件

所有 30+ 个验收标准均标记为 ✅，PR 可合并。

## 测试优先级

**P0（必须）**：
- 快手 API 认证成功
- 至少 3 条内容发布成功
- 所有异常被正确捕获

**P1（强烈建议）**：
- TypeScript 类型检查通过
- ESLint 通过
- 单元测试覆盖率 ≥ 80%

**P2（理想）**：
- 数据库事务处理（原子性）
- 性能优化（批量发布<5秒/篇）

## 参考实现

**头条发布实现**（可作为参考）：
- 文件：`services/creator/scripts/publish-to-toutiao.py`
- 关键函数：`async def publish_to_toutiao(title, content, image_path=None)`
- 流程：认证 → 格式转换 → API 调用 → 结果追踪

**Creator 数据库架构**：
- Works 表：`id, title, content, type, created_at, updated_at`
- Publications 表：`id, work_id, platform_id, status, url, published_at`
- Platforms 表：已定义快手 (code='kuaishou')

---

**版本**: 1.0.0
**最后更新**: 2026-03-07
**作者**: repo-lead:zenithjoy (备选方案 A 期间)
