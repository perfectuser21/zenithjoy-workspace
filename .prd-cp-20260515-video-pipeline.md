# PRD: AI 视频剪辑流水线

## 功能描述

客户上传原始视频 + 脚本/标题，系统自动用 Whisper 转录音频、FFmpeg 粗剪（去掉静音/口误段），导出 9:16 竖屏和 16:9 横屏两个版本供下载。

## 成功标准

- POST /api/ai-video/upload 接受 multipart 上传（video + script + logo 可选）
- 创建 DB 记录（platform=local-whisper-ffmpeg）并返回 job_id
- xian-m4 自动开始处理（Whisper 转录 + FFmpeg 粗剪）
- GET /api/ai-video/task/:id 返回进度和下载链接
- 处理完成后两个输出格式均可下载（HTTP 200）

## 需求来源

用户直接需求：上传视频自动剪辑，输出适配主流平台的格式。
