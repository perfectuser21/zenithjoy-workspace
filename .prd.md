# PRD: Phase 1 基础设施（补救合并）

## 背景
PR #34 被过早合并，合并的是 commit 4323404（旧版本，`--max-warnings 0`），Phase 1 改动（`21b672f`）未进入 develop。

## 问题
当前 develop 的质量门禁仍然过弱：
1. CI 允许 100 个 lint warnings（代码持续变乱）
2. 无安全漏洞检查（npm audit）
3. 无测试框架（无法写测试）
4. 无功能清单（不知道哪些功能已跑通）

## 根本原因
质量体系缺失，导致：
- 代码越改越乱
- 改 A 坏 B 无法检测
- 无法保护已完成的功能

## 解决方案（止血三步）

### 1. 修 CI 配置
- Lint warnings: 100 → 0（阻止继续变乱）
- 添加 security job（npm audit）

### 2. 安装测试框架
- vitest + @testing-library/react
- 配置 vitest.config.ts
- 添加测试脚本（但先不写测试）

### 3. 梳理功能清单
- 创建 FEATURES_STATUS.md
- 标记每个功能状态（✅ / 🟡 / ❌）
- 为后续"边做边测"准备

### 4. 修复 lint warnings（79 个）
- 删除未使用的变量/imports（18 个）
- 把 any 改为具体类型（56 个）
- 修复 useEffect 依赖（5 个）

### 5. 修复安全漏洞
- axios: 已修复
- esbuild/vite: 需要升级 vite（可能破坏代码，待评估）

## 影响范围
- `.github/workflows/ci.yml` - CI 配置
- `apps/dashboard/package.json` - 添加测试依赖
- `apps/dashboard/vitest.config.ts` - 新建配置
- `FEATURES_STATUS.md` - 新建功能清单

## 验收标准（Phase 1：基础设施）
1. ✅ CI lint 从 100 → **79** warnings（阻止增加，Phase 2 再降到 0）
2. ✅ CI 有 security job（npm audit）
3. ✅ vitest 已安装并配置
4. ✅ FEATURES_STATUS.md 已创建
5. ✅ 安全漏洞部分修复（axios 已修复，esbuild 待 Phase 2）
6. ✅ 所有 PR 必须通过新的 CI 检查

## Phase 2 计划（下个 PR）
1. 逐步修复 79 个 warnings → 0
2. 评估并修复 esbuild/vite 漏洞
3. 最终目标：CI lint --max-warnings 0
