# PRD: 修复 Stop Hook 双钥匙状态机

## 执行仓库

**⚠️ 重要**：此修复需要在 `cecelia-engine` 仓库执行（`/home/xx/perfect21/cecelia/engine`）

全局 hooks 目录被保护，只能在 engine 仓库修改后部署。

---

## 背景

当前 Stop Hook 机制存在严重漏洞：
1. `.dev-mode` 创建失败（Write 工具 git 污染）→ Stop Hook 放行 → 会话退出
2. `.dev-mode` 被删除（cleanup/手动/AI）→ Stop Hook 放行 → 会话退出
3. cleanup.sh 时序 Bug：先删除 `.dev-mode` 再尝试写入 → 无限循环

## 根因分析

**路径 A：.dev-mode 创建失败**
```
PR #14 错误提交 .dev-mode 到 git
  ↓
Merge 到 develop
  ↓
新分支继承 .dev-mode commit
  ↓
Step 3 用 Write 工具创建 .dev-mode
  ↓
Write 检测到文件在 git 历史中
  ↓
要求先 Read（防止覆盖）
  ↓
Step 3 没有 Read → Write 失败
  ↓
.dev-mode 从未创建
  ↓
Stop Hook 检测不到 → exit 0 → 会话退出 ❌
```

**路径 B：.dev-mode 被删除**
```
cleanup.sh Line 392: RUNTIME_FILES 包含 ".dev-mode"
  ↓
Line 400: rm -f .dev-mode
  ↓
Line 471: sed -i 's/.../step_11_cleanup: done/' .dev-mode
  ↓
写入失败（文件已删除）
  ↓
Stop Hook 检测不到 .dev-mode → exit 0 → 会话退出 ❌
```

---

## 解决方案：双钥匙状态机

### 核心思路

- **Key-1**: `.dev-lock`（硬钥匙，不可绕过）
- **Key-2**: `.dev-mode`（软状态，11 步 checklist）
- **Key-3**: sentinel file（三重保险，防止同时删除）

### 规则

1. **进入 dev**：必须创建 `.dev-lock`（Bash 原子写入，保证不失败）
2. **Stop Hook 放行**：`.dev-lock` 不存在
3. **删除 .dev-mode**：`.dev-lock` 还在 → exit 2
4. **.dev-mode 创建失败**：`.dev-lock` 在 → exit 2
5. **cleanup.sh**：禁止删除 `.dev-mode` / `.dev-lock`
6. **并发隔离**：分支级别锁（可选）

---

## 修改清单

### 📝 修改 1: Stop Hook 核心逻辑

**文件**: `~/.claude/hooks/stop.sh` → engine 仓库的 `hooks/stop.sh`

**位置**: Line 93-106

**当前代码**:
```bash
# ===== 检查 .dev-mode 文件 =====
DEV_MODE_FILE="$PROJECT_ROOT/.dev-mode"

if [[ ! -f "$DEV_MODE_FILE" ]]; then
    # 普通会话，没有 .dev-mode，直接允许结束
    exit 0
fi

# ===== 检查 cleanup 是否已完成 =====
# 优先检查 cleanup_done: true（向后兼容旧版本）
if grep -q "cleanup_done: true" "$DEV_MODE_FILE" 2>/dev/null; then
    rm -f "$DEV_MODE_FILE"
    exit 0
fi
```

**替换成**:
```bash
# ===== 检查 .dev-lock 和 .dev-mode 文件（双钥匙状态机）=====
# v12.9.0: 双钥匙修复 - .dev-lock（硬钥匙）+ .dev-mode（软状态）+ sentinel（三重保险）
CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "")
DEV_LOCK_FILE="$PROJECT_ROOT/.dev-lock"
DEV_MODE_FILE="$PROJECT_ROOT/.dev-mode"
SENTINEL_FILE="$PROJECT_ROOT/.git/hooks/cecelia-dev.sentinel"

# Key-1: .dev-lock（硬钥匙）- 只要它在，就必须走 dev 检查
if [[ ! -f "$DEV_LOCK_FILE" ]]; then
    # 没有 .dev-lock，检查 .dev-mode 是否泄漏
    if [[ -f "$DEV_MODE_FILE" ]]; then
        # .dev-mode 泄漏（.dev-lock 被删除但 .dev-mode 还在）
        echo "" >&2
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >&2
        echo "  [Stop Hook: 状态文件泄漏]" >&2
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >&2
        echo "" >&2
        echo "  ⚠️  .dev-lock 不存在但 .dev-mode 存在（泄漏）" >&2
        echo "  清理泄漏的 .dev-mode 文件..." >&2
        rm -f "$DEV_MODE_FILE" "$SENTINEL_FILE"
        echo "  ✅ 已清理" >&2
        echo "" >&2
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >&2
    fi

    # 检查 sentinel（三重保险）
    if [[ -f "$SENTINEL_FILE" ]]; then
        # Sentinel 存在但 .dev-lock 不存在 → 状态文件被删除
        echo "" >&2
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >&2
        echo "  [Stop Hook: Sentinel 检测到状态丢失]" >&2
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >&2
        echo "" >&2
        echo "  ⚠️  Sentinel 存在但 .dev-lock 和 .dev-mode 都不存在" >&2
        echo "  可能原因：状态文件被误删或同时删除" >&2
        echo "" >&2
        echo "  下一步：重建状态文件或检查清理脚本" >&2
        echo "" >&2
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >&2
        jq -n --arg reason "Sentinel 存在但状态文件丢失，判定为误删，阻止退出" '{"decision": "block", "reason": $reason}'
        exit 2  # ← 强制阻止退出（三重保险生效）
    fi

    # 没有 .dev-lock 且没有 sentinel → 普通会话，允许结束
    exit 0
fi

# .dev-lock 存在，检查 .dev-mode（软状态）
if [[ ! -f "$DEV_MODE_FILE" ]]; then
    # .dev-lock 在但 .dev-mode 不在 → 状态丢失/创建失败/被删除
    echo "" >&2
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >&2
    echo "  [Stop Hook: 状态文件丢失]" >&2
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >&2
    echo "" >&2
    echo "  ⚠️  .dev-lock 存在但 .dev-mode 缺失" >&2
    echo "  可能原因:" >&2
    echo "    - .dev-mode 创建失败（Write 工具 git 污染）" >&2
    echo "    - .dev-mode 被误删（cleanup.sh / 手动 rm / AI）" >&2
    echo "" >&2
    echo "  下一步：重建 .dev-mode 或执行最小检查" >&2
    echo "" >&2
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >&2
    jq -n --arg reason ".dev-lock 存在但 .dev-mode 缺失，阻止退出（状态丢失或创建失败）" '{"decision": "block", "reason": $reason}'
    exit 2  # ← 强制阻止退出（双钥匙核心机制）
fi

# ===== 检查 cleanup 是否已完成 =====
# 优先检查 cleanup_done: true（向后兼容旧版本）
if grep -q "cleanup_done: true" "$DEV_MODE_FILE" 2>/dev/null; then
    rm -f "$DEV_MODE_FILE" "$DEV_LOCK_FILE" "$SENTINEL_FILE"
    exit 0
fi
```

---

### 📝 修改 2: Stop Hook 所有 exit 0 → exit 2

**文件**: `hooks/stop.sh`

**需要修改的位置**（共 8 处）：

1. **Line 68** (并发锁失败):
```bash
# 改前
exit 0
# 改后
exit 2
```

2. **Line 79** (fallback 锁失败):
```bash
# 改前
exit 0
# 改后
exit 2
```

3. **Line 250** (PR 未创建):
```bash
# 改前
exit 0
# 改后
exit 2
```

4. **Line 294** (CI 失败):
```bash
# 改前
exit 0
# 改后
exit 2
```

5. **Line 304** (CI 进行中):
```bash
# 改前
exit 0
# 改后
exit 2
```

6. **Line 314** (CI 状态未知):
```bash
# 改前
exit 0
# 改后
exit 2
```

7. **Line 339** (Step 11 未完成):
```bash
# 改前
exit 0
# 改后
exit 2
```

8. **Line 350** (PR 未合并):
```bash
# 改前
exit 0
# 改后
exit 2
```

**批量替换命令**:
```bash
# 在 engine 仓库执行
cd /home/xx/perfect21/cecelia/engine
sed -i '68s/exit 0/exit 2/' hooks/stop.sh
sed -i '79s/exit 0/exit 2/' hooks/stop.sh
sed -i '250s/exit 0/exit 2/' hooks/stop.sh
sed -i '294s/exit 0/exit 2/' hooks/stop.sh
sed -i '304s/exit 0/exit 2/' hooks/stop.sh
sed -i '314s/exit 0/exit 2/' hooks/stop.sh
sed -i '339s/exit 0/exit 2/' hooks/stop.sh
sed -i '350s/exit 0/exit 2/' hooks/stop.sh
```

---

### 📝 修改 3: Stop Hook 删除时同时删除 sentinel

**文件**: `hooks/stop.sh`

**需要修改的位置**（共 3 处，第 1 处已在修改 1 中完成）：

2. **Line 145** (15 次重试失败):
```bash
# 改前
rm -f "$DEV_MODE_FILE"
# 改后
rm -f "$DEV_MODE_FILE" "$DEV_LOCK_FILE" "$SENTINEL_FILE"
```

3. **Line 189-190** (分支不匹配):
```bash
# 改前
rm -f "$DEV_MODE_FILE"
exit 0
# 改后
rm -f "$DEV_MODE_FILE" "$DEV_LOCK_FILE" "$SENTINEL_FILE"
exit 0
```

4. **Line 330** (工作流完成):
```bash
# 改前
rm -f "$DEV_MODE_FILE"
# 改后
rm -f "$DEV_MODE_FILE" "$DEV_LOCK_FILE" "$SENTINEL_FILE"
```

**批量替换命令**:
```bash
# 方法：用 sed 替换所有 `rm -f "$DEV_MODE_FILE"` 为完整删除
sed -i 's|rm -f "\$DEV_MODE_FILE"|rm -f "$DEV_MODE_FILE" "$DEV_LOCK_FILE" "$SENTINEL_FILE"|g' hooks/stop.sh
```

---

### 📝 修改 4: Step 3 Bash 原子写入

**文件**: `skills/dev/steps/03-branch.md`

**位置**: Line 114-153（创建 .dev-mode 文件 部分）

**删除**: 所有 Write 工具调用和 TaskCreate 调用

**替换成**:

```bash
# ===== 创建 .dev-lock（硬钥匙，必须成功）=====
echo "🔒 创建 .dev-lock..."

# 原子写入：先写临时文件，再 mv（防止竞态）
DEV_LOCK_TMP="$(mktemp .dev-lock.XXXXXX)"
{
  echo "dev_lock"
  echo "branch: $(git rev-parse --abbrev-ref HEAD)"
  echo "session_id: ${CLAUDE_SESSION_ID:-unknown}"
  echo "created_at: $(date -u +%FT%TZ)"
} > "$DEV_LOCK_TMP"

# 原子移动（覆盖旧文件，即使 git 中存在也能成功）
mv -f "$DEV_LOCK_TMP" .dev-lock

if [[ -f .dev-lock ]]; then
    echo "✅ .dev-lock 创建成功（硬钥匙已设置）"
else
    echo "❌ .dev-lock 创建失败，无法继续" >&2
    exit 1
fi

# ===== 创建 sentinel file（三重保险）=====
echo "🛡️  创建 sentinel file..."
mkdir -p .git/hooks
SENTINEL_TMP="$(mktemp .git/hooks/cecelia-dev.sentinel.XXXXXX)"
{
  echo "dev_workflow_active"
  echo "branch: $(git rev-parse --abbrev-ref HEAD)"
  echo "started: $(date -u +%FT%TZ)"
} > "$SENTINEL_TMP"
mv -f "$SENTINEL_TMP" .git/hooks/cecelia-dev.sentinel

if [[ -f .git/hooks/cecelia-dev.sentinel ]]; then
    echo "✅ Sentinel 创建成功（三重保险）"
else
    echo "⚠️  Sentinel 创建失败，但可以继续" >&2
fi

# ===== 创建 .dev-mode（软状态，允许失败）=====
echo "📝 创建 .dev-mode..."

# 原子写入（同样方式）
DEV_MODE_TMP="$(mktemp .dev-mode.XXXXXX)"
{
  echo "dev"
  echo "branch: $(git rev-parse --abbrev-ref HEAD)"
  echo "session_id: ${CLAUDE_SESSION_ID:-unknown}"
  echo "tty: $(tty 2>/dev/null || echo 'not a tty')"
  echo "prd: .prd.md"
  echo "started: $(date -u +%FT%TZ)"
  echo "retry_count: 0"
  echo "step_1_prd: done"
  echo "step_2_detect: done"
  echo "step_3_branch: done"
  echo "step_4_dod: pending"
  echo "step_5_code: pending"
  echo "step_6_test: pending"
  echo "step_7_quality: pending"
  echo "step_8_pr: pending"
  echo "step_9_ci: pending"
  echo "step_10_learning: pending"
  echo "step_11_cleanup: pending"
} > "$DEV_MODE_TMP"

mv -f "$DEV_MODE_TMP" .dev-mode

if [[ -f .dev-mode ]]; then
    echo "✅ .dev-mode 创建成功（软状态已设置）"
else
    echo "⚠️  .dev-mode 创建失败，但 .dev-lock 已设置，可以继续" >&2
    echo "   Stop Hook 会检测到这个情况并阻止退出" >&2
fi

echo ""
echo "✅ 双钥匙状态机已初始化"
echo "   .dev-lock: 硬钥匙（不可绕过）"
echo "   .dev-mode: 软状态（11 步 checklist）"
echo "   sentinel: 三重保险（防止同时删除）"
```

---

### 📝 修改 5: cleanup.sh 不删除状态文件

**文件**: `skills/dev/scripts/cleanup.sh`

**修改 1**: Line 392（从 RUNTIME_FILES 删除 .dev-mode）

```bash
# 改前
RUNTIME_FILES=(
    ".quality-report.json"
    ".prd.md"
    ".dod.md"
    ".prd-${CP_BRANCH}.md"
    ".dod-${CP_BRANCH}.md"
    ".quality-gate-passed"
    ".quality-gate-passed-${CP_BRANCH}"
    ".cecelia-run-id"
    ".cecelia-run-id-${CP_BRANCH}"
    ".layer2-evidence.md"
    ".l3-analysis.md"
    ".quality-evidence.json"
    ".dev-mode"  # ← 删除这行
)

# 改后
RUNTIME_FILES=(
    ".quality-report.json"
    ".prd.md"
    ".dod.md"
    ".prd-${CP_BRANCH}.md"
    ".dod-${CP_BRANCH}.md"
    ".quality-gate-passed"
    ".quality-gate-passed-${CP_BRANCH}"
    ".cecelia-run-id"
    ".cecelia-run-id-${CP_BRANCH}"
    ".layer2-evidence.md"
    ".l3-analysis.md"
    ".quality-evidence.json"
    # .dev-mode 和 .dev-lock 由 Stop Hook 管理，不在此删除
)
```

**修改 2**: Line 466-485（只标记完成，不删除文件）

在 Line 485 后面添加警告：

```bash
if [[ -f "$DEV_MODE_FILE" ]]; then
    # ... 原有的标记逻辑 ...
    
    # 新增警告
    echo ""
    echo -e "   ${YELLOW}注意: .dev-mode 和 .dev-lock 将由 Stop Hook 在工作流完成后自动删除${NC}"
    echo -e "   ${YELLOW}      cleanup.sh 只负责标记 step_11_cleanup: done${NC}"
fi
```

---

### 📝 修改 6: .gitignore 防止污染

**文件**: 每个项目的 `.gitignore`（需要手动添加到所有仓库）

**添加内容**:
```bash
# Dev workflow 运行时文件
.dev-mode
.dev-mode.*
.dev-lock
.dev-lock.*
```

**批量添加脚本**:
```bash
# 在所有项目仓库执行
cat >> .gitignore << 'EOF'

# Dev workflow 运行时文件
.dev-mode
.dev-mode.*
.dev-lock
.dev-lock.*
EOF
```

---

### 📝 修改 7: git 清理已 tracked 的文件

**执行**: 在所有已经 track 了 .dev-mode 的仓库

```bash
# 从 git 索引移除（不删除工作区文件）
git rm --cached -f .dev-mode .dev-lock 2>/dev/null || true

# 提交清理
git add .gitignore
git commit -m "chore: add .dev-mode and .dev-lock to .gitignore, remove from tracking"
```

---

## 部署步骤

### 在 cecelia-engine 仓库执行

```bash
# 1. 切换到 engine 仓库
cd /home/xx/perfect21/cecelia/engine

# 2. 从 develop 创建分支
git checkout develop
git pull
git checkout -b cp-fix-stop-hook-double-key

# 3. 执行所有修改（按上面的修改清单）

# 4. 测试修改（见 DoD 测试部分）

# 5. 提交
git add -A
git commit -m "fix: implement double-key state machine for stop hook

- Add .dev-lock as hard key (cannot bypass)
- Add sentinel file as triple insurance
- Fix cleanup.sh timing bug (don't delete .dev-mode)
- Change all block exit 0 to exit 2
- Use Bash atomic write in Step 3 (avoid Write tool git pollution)

Fixes: .dev-mode creation failure and deletion causing workflow exit"

# 6. Push 并创建 PR
git push -u origin cp-fix-stop-hook-double-key
gh pr create --title "fix: stop hook double-key state machine" \
  --body "完整修复 Stop Hook 双钥匙状态机，统一解决 .dev-mode 创建失败和被删除的问题"

# 7. CI 通过后合并

# 8. 部署到全局
bash deploy.sh  # 部署 hooks 和 skills 到 ~/.claude/
```

---

## 验收标准（DoD）

详见 `.dod.md`

---

## 风险评估

- **高风险**: Stop Hook 是核心循环控制，改错会导致所有 dev workflow 失败
- **缓解**: 
  1. 充分测试（所有 DoD 测试）
  2. 保留原逻辑作为注释
  3. 先在测试分支验证
  4. 可以快速回滚（git revert）

---

## 成功指标

- ✅ `.dev-mode` 创建失败不会导致会话退出
- ✅ `.dev-mode` 被删除不会导致会话退出
- ✅ cleanup.sh 不会删除状态文件
- ✅ 所有 block 都是 exit 2
- ✅ DoD 所有测试通过
