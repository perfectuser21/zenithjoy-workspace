# PRD: scraper 指标同步到 zenithjoy.works（work_id 关联）

## 目标

创建 `scripts/sync-scraper-to-works.sh` 桥接脚本，打通内容自动化流水线最后一环：scraper 采集的平台指标 → zenithjoy.publish_logs.response。

## 范围

- 新建 `scripts/sync-scraper-to-works.sh`
- 新建 `scripts/__tests__/test_sync_scraper_to_works.sh`

## 不做什么

- 不修改现有 scraper JS 脚本
- 不做 schema 变更

## 成功标准

- 脚本可独立运行，指定 --platform 同步该平台的指标
- 匹配逻辑：social_media_raw title + platform → zenithjoy.publish_logs
- 更新 publish_logs.response['metrics'] JSONB
