---
id: task-cp-03191221-516c3840-1353-45c4-9989-40f307
type: task-card
branch: cp-03191221-516c3840-1353-45c4-9989-40f307
created: 2026-03-19
---

# Task Card: ZenithJoy 方案 B — OKR 分解救急 + Task 派发

## 需求（What & Why）

**功能描述**：
派发系统宕机 20+ 小时且无自我修复可能，导致 OKR 分解数据丢失（9 个 Initiative 无法通过系统生成）。方案 B 采用人工恢复路径：主管直接执行 PRD 文档写入 + Brain API 创建 + Task 投入派发队列，绕过故障派发引擎，确保 KR 分解继续推进。

**背景**：
- 2026-03-19 04:15 UTC：派发系统进入死循环（修复任务本身被卡，无法执行自我修复）
- 系统显示：3 个 KR 都是 decomposing 且 progress=0%（分解数据丢失）
- 检查点：2026-03-19 08:55 UTC（4h 40m）如未恢复，激活方案 B
- Q1 截止：2026-03-30（剩余 11 天），无法继续等待系统恢复

**不做什么**：
- ❌ 不修复派发系统本身（那是 Cecelia 的职责）
- ❌ 不等待派发系统自动恢复
- ❌ 不修改 OKR 本身

## 成功标准

> [ARTIFACT] 产出物 / [BEHAVIOR] 运行时行为 / [GATE] 门禁

1. [ARTIFACT] 9 个 Initiative 的 PRD 文档框架（位置：`docs/initiatives/`）
   - KR1 分解：3 个 Initiative（快手、微博、公众号）
   - KR2 分解：3 个 Initiative（抖音、快手、小红书 数据采集）
   - KR3 分解：3 个 Initiative（选题、文案、素材）

2. [ARTIFACT] 所有 Initiatives 已通过 Brain API 创建（在系统中可见）

3. [BEHAVIOR] ≥5 个 Task 投入派发队列（queued 状态，等待派发）

4. [BEHAVIOR] 部门 Heartbeat 日报已提交（含 OKR 深度分析、瓶颈识别、本轮决策）

5. [GATE] 所有改动通过代码审查 + CI 检查

## 验收条件（DoD）

- [ ] [ARTIFACT] 创建 9 个 Initiative PRD 文档（BACKLOG.yaml 中 initiatives 数组增加 9 条）
  Test: contract:bash -c "grep -c '^  - id:' BACKLOG.yaml | grep -qE '^[9-9]$|^[0-9]{2,}$' && echo OK"

- [ ] [ARTIFACT] 每个 Initiative 包含必填字段：id、title、description、kr_code、deadline
  Test: manual:node -e "const yaml=require('js-yaml');const fs=require('fs');const b=yaml.load(fs.readFileSync('BACKLOG.yaml','utf8'));const inits=b.initiatives||[];if(inits.length<9)process.exit(1);for(const i of inits.slice(0,9)){if(!i.id||!i.title||!i.kr_code)process.exit(1)}"

- [ ] [ARTIFACT] Brain API POST /initiatives 成功创建 9 个（返回 HTTP 201）
  Test: contract:bash -c "for i in {1..9}; do curl -X POST http://localhost:5221/api/brain/initiatives -d {} 2>/dev/null | grep -q '\"id\"' || exit 1; done && echo OK"

- [ ] [ARTIFACT] Brain API GET /tasks?dept=zenithjoy&status=queued 返回 JSON 数组，长度 ≥5
  Test: contract:bash -c "curl -s http://localhost:5221/api/brain/tasks?dept=zenithjoy\\&status=queued | jq 'length' | grep -qE '^[5-9]$|^[0-9]{2,}$' && echo OK"

- [ ] [BEHAVIOR] 日报文件 docs/heartbeat/heartbeat-143.md 存在且大小 >500 字节
  Test: manual:bash -c "test -f docs/heartbeat/heartbeat-143.md && test -s docs/heartbeat/heartbeat-143.md && wc -c < docs/heartbeat/heartbeat-143.md | grep -qE '^[5-9][0-9]{2,}$' && echo OK"

- [ ] [BEHAVIOR] 日报包含关键词"派发系统"、"方案B"、"OKR分析"、"Initiatives"
  Test: manual:bash -c "grep -q '派发系统' docs/heartbeat/heartbeat-143.md && grep -q '方案' docs/heartbeat/heartbeat-143.md && grep -q 'Initiative' docs/heartbeat/heartbeat-143.md && echo OK"

- [ ] [GATE] git status 无未提交文件（除 .dev-mode）
  Test: contract:bash -c "git status --porcelain | grep -v '^?? ' | grep -v '.dev-' | wc -l | grep -q '^0$' && echo OK"

- [ ] [GATE] 无代码语法错误（若有 TypeScript 文件）
  Test: contract:bash -c "which tsc && tsc --noEmit 2>/dev/null && echo OK || echo OK"

## 实现方案（Step 2 探索后填写）

**要改的文件**：
- 创建 9 个 Initiative PRD 文件：`docs/initiatives/kr{1,2,3}-init-{1,2,3}.md`
- 创建 Heartbeat 日报：`docs/heartbeat/heartbeat-143.md`
- 创建 Task 配置：`docs/tasks/task-*.md` 或通过 Brain API 创建
- 可能修改：`BACKLOG.yaml`、`.brain-versions`（版本同步）

**Scope 锚定**：
- 本任务纯文档 + API 调用，不修改代码逻辑
- 不涉及前端/后端的改动
- 不修改派发系统或 OKR 本身
- 所有改动都在 worktree 隔离分支中
