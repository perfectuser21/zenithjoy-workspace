# DoD: 修复 AI 视频生成页面白屏问题

## 验收清单

### 1. 平台抽象层实现
- [ ] 创建 `src/api/platforms/base.ts`（定义统一接口）
- [ ] 创建 `src/api/platforms/toapi.ts`（ToAPI 适配器）
- [ ] 创建 `src/api/platforms/index.ts`（平台注册）

**测试**：导入模块不报错

### 2. ToAPI 响应解包修复
- [ ] `toapi.ts` 的 `getTaskStatus` 方法正确解包 `{code, data}` 格式
- [ ] 返回的 task 对象包含完整字段（id, model, status）

**测试**：控制台日志显示解包后的 task 对象有完整字段

### 3. API 层更新
- [ ] 替换 `video-generation.api.ts` 使用平台抽象
- [ ] 导出统一的 `UnifiedVideoParams` 和 `UnifiedTask` 类型
- [ ] 保留 `uploadImage` 功能

**测试**：编译通过，无 TypeScript 错误

### 4. 功能验证
- [ ] 打开 AI 视频页面不报错
- [ ] 点击"开始生成视频"按钮不白屏
- [ ] 显示任务监控界面（任务 ID、平台、进度）
- [ ] 轮询状态正常更新

**测试**：浏览器测试通过

### 5. 部署验证
- [ ] 构建成功（vite build）
- [ ] 部署到香港服务器
- [ ] 生产环境测试通过

**测试**：`npx vite build && rsync`
