---
skeleton: false
journey_type: user_facing
target_environment: windows_cloud
---
# Contract DoD — Sprint: Line02 机器管理模块（管理客户机器 + 机器上绑定/管理抖音号）

**范围**: agents 表加 nickname + machine_role；新增 machines 列表/详情/改名改角色/加号 API（复用 qr-bind 派单 + burner 回写）；前端机器管理页接入智能获客 + 路由；thin（fake-agent 回写，不拉真浏览器）。
**大小**: M

> **验证环境约定**：后端 BEHAVIOR（模式A）打**真实 ZenithJoy API** `API_BASE`（默认 `http://localhost:5200`）+ **真实 Postgres**（PGHOST/PGUSER/PGDATABASE/PGPASSWORD，默认 cecelia，schema `zenithjoy.`）。**不是** Brain 5221（本 sprint 是 ZenithJoy 产品，非 Cecelia harness）。UI 行为见 ## BEHAVIOR:E2E（windows_cloud Playwright，干净 VM 无真后端 → stub，只验 UI）。
>
> **psql 字符串字面量铁律**：`manual:bash -c '...'` 内 psql 的 SQL 字符串字面量**必须**用 `'\''…'\''`（POSIX 单引号转义）；JSON/jq 参数用 `\"`（它们要双引号）。
>
> **接缝诚实标注**：真机扫码（接缝#1）/ 浏览器真 cookie→:5200 租户解析（接缝#2）/ 真心跳驱动 status（接缝#3）= **logic-done-pending**（见 contract-draft.md 接缝清单），本 sprint 只真验逻辑层。

## ARTIFACT 条目

- [ ] [ARTIFACT] migration 给 zenithjoy.agents 加 nickname + machine_role（main/sub，幂等）
  Test: manual:bash -c 'f=$(ls apps/api/db/migrations/*agents*nickname*machine_role*.sql 2>/dev/null | head -1); [ -n "$f" ] || f=$(grep -rl "machine_role" apps/api/db/migrations/ | head -1); [ -n "$f" ] || { echo FAIL; exit 1; }; grep -q "nickname" "$f" && grep -q "machine_role" "$f" && grep -Eq "main.*sub|sub.*main" "$f" || { echo "FAIL: migration 缺 nickname/machine_role/枚举"; exit 1; }; echo OK'

- [ ] [ARTIFACT] 新增 machines 路由文件，挂载在 /api/agent(agentRouter) 之前
  Test: manual:bash -c 'test -f apps/api/src/routes/agent-machines.ts || { echo "FAIL: 路由文件缺"; exit 1; }; node -e "const c=require(\"fs\").readFileSync(\"apps/api/src/app.ts\",\"utf8\"); const m=c.indexOf(\"/api/agent/machines\"); const a=c.indexOf(\"app.use(\x27/api/agent\x27, agentRouter)\"); if(m<0){console.error(\"FAIL: 未挂载 /api/agent/machines\");process.exit(1)} if(a>=0 && m>a){console.error(\"FAIL: machines 挂载晚于 agentRouter，会被吞\");process.exit(1)}"; echo OK'

- [ ] [ARTIFACT] 前端机器管理页 + 接入智能获客 + 注册路由
  Test: manual:bash -c 'ls apps/dashboard/src/pages/*achine*anagement*.tsx >/dev/null 2>&1 || { echo "FAIL: 机器管理页缺"; exit 1; }; grep -q "machines" apps/dashboard/src/config/navigation.config.ts || { echo "FAIL: navigation 未接入 machines 路由"; exit 1; }; echo OK'

- [ ] [ARTIFACT] machines API vitest 单测存在且断言四端点
  Test: manual:bash -c 'f=apps/api/src/routes/agent-machines.test.ts; test -f "$f" || { echo "FAIL: 单测缺"; exit 1; }; grep -q "machines" "$f" && grep -Eq "PUT|put|machine_role" "$f" || { echo "FAIL: 单测未覆盖改角色"; exit 1; }; echo OK'

## BEHAVIOR 条目（内嵌可执行 manual:bash，打真实 API :5200 + 真库 psql，user_facing 模式A）

- [ ] [BEHAVIOR] Step1 列表 schema + tenant scope — GET /api/agent/machines 返当前租户机器，每行含 7 字段（Golden Path Step 1）
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; Q(){ psql -h "${PGHOST:-localhost}" -U "${PGUSER:-cecelia}" -d "${PGDATABASE:-cecelia}" -tAc "$1" | tr -d " " | sed -n '\''1p'\''; }; LK="mk-s1-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''MS1'\'','\''$LK'\'','\''matrix'\'') RETURNING id"); AID=$(Q "INSERT INTO zenithjoy.agents(tenant_id,agent_id,hostname,status,version) VALUES('\''$TID'\'','\''ag-s1-$(date +%s)$$'\'','\''pc-s1'\'','\''online'\'','\''1.0.70'\'') RETURNING id"); R=$(curl -sf "$A/api/agent/machines?tenant_id=$TID"); echo "$R" | jq -e ".success==true and (.data.machines|type==\"array\")" >/dev/null; echo "$R" | jq -e "[.data.machines[]|select(.id==\"$AID\")]|length==1" >/dev/null || { echo "FAIL: 机器未出现在列表"; exit 1; }; echo "$R" | jq -e ".data.machines[0]|has(\"nickname\") and has(\"hostname\") and has(\"status\") and has(\"version\") and has(\"machine_role\") and has(\"douyin_account_count\")" >/dev/null || { echo "FAIL: 行缺字段"; exit 1; }; echo OK'

- [ ] [BEHAVIOR] Step1 号数聚合对账 — douyin_account_count 等于该机器 douyin session 真实行数（Golden Path Step 1）
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; Q(){ psql -h "${PGHOST:-localhost}" -U "${PGUSER:-cecelia}" -d "${PGDATABASE:-cecelia}" -tAc "$1" | tr -d " " | sed -n '\''1p'\''; }; LK="mk-cnt-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''MCNT'\'','\''$LK'\'','\''matrix'\'') RETURNING id"); AID=$(Q "INSERT INTO zenithjoy.agents(tenant_id,agent_id,hostname,status) VALUES('\''$TID'\'','\''ag-cnt-$(date +%s)$$'\'','\''pc-cnt'\'','\''online'\'') RETURNING id"); Q "INSERT INTO zenithjoy.agent_platform_sessions(agent_id,platform,account_label,role,status,bound_at) VALUES('\''$AID'\'','\''douyin'\'','\''main'\'','\''main'\'','\''active'\'',NOW())" >/dev/null; Q "INSERT INTO zenithjoy.agent_platform_sessions(agent_id,platform,account_label,role,status,bound_at) VALUES('\''$AID'\'','\''douyin'\'','\''xh1'\'','\''burner'\'','\''active'\'',NOW())" >/dev/null; DBC=$(Q "SELECT count(*) FROM zenithjoy.agent_platform_sessions WHERE agent_id='\''$AID'\'' AND platform='\''douyin'\''"); APIC=$(curl -sf "$A/api/agent/machines?tenant_id=$TID" | jq -r "[.data.machines[]|select(.id==\"$AID\")][0].douyin_account_count"); [ "$APIC" = "$DBC" ] || { echo "FAIL: 号数 api=$APIC db=$DBC 不一致"; exit 1; }; [ "$DBC" = "2" ] || { echo "FAIL: 预期2号 实$DBC"; exit 1; }; echo OK'

- [ ] [BEHAVIOR] Step2 改名+标主副持久化 — PUT 改 nickname+machine_role，5 分钟内 DB 实写 + 列表反映（Golden Path Step 2）
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; Q(){ psql -h "${PGHOST:-localhost}" -U "${PGUSER:-cecelia}" -d "${PGDATABASE:-cecelia}" -tAc "$1" | tr -d " " | sed -n '\''1p'\''; }; LK="mk-s2-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''MS2'\'','\''$LK'\'','\''matrix'\'') RETURNING id"); AID=$(Q "INSERT INTO zenithjoy.agents(tenant_id,agent_id,hostname,status) VALUES('\''$TID'\'','\''ag-s2-$(date +%s)$$'\'','\''pc-s2'\'','\''online'\'') RETURNING id"); R=$(curl -sf -X PUT "$A/api/agent/machines/$AID" -H "Content-Type: application/json" -d "{\"nickname\":\"主力机A\",\"machine_role\":\"main\"}"); echo "$R" | jq -e ".success==true and .data.nickname==\"主力机A\" and .data.machine_role==\"main\"" >/dev/null || { echo "FAIL: PUT 响应不符 $R"; exit 1; }; OK=$(Q "SELECT count(*) FROM zenithjoy.agents WHERE id='\''$AID'\'' AND nickname='\''主力机A'\'' AND machine_role='\''main'\'' AND updated_at > NOW() - interval '\''5 minutes'\''"); [ "$OK" = "1" ] || { echo "FAIL: DB 未在时间窗内持久化"; exit 1; }; curl -sf "$A/api/agent/machines?tenant_id=$TID" | jq -e "[.data.machines[]|select(.id==\"$AID\")][0]|.nickname==\"主力机A\" and .machine_role==\"main\"" >/dev/null || { echo "FAIL: 列表未反映新值"; exit 1; }; echo OK'

- [ ] [BEHAVIOR] Step3 详情 + 号有效性派生 — GET /:id 返 machine+sessions，active→valid:true，expired→valid:false（Golden Path Step 3）
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; Q(){ psql -h "${PGHOST:-localhost}" -U "${PGUSER:-cecelia}" -d "${PGDATABASE:-cecelia}" -tAc "$1" | tr -d " " | sed -n '\''1p'\''; }; LK="mk-s3-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''MS3'\'','\''$LK'\'','\''matrix'\'') RETURNING id"); AID=$(Q "INSERT INTO zenithjoy.agents(tenant_id,agent_id,hostname,status) VALUES('\''$TID'\'','\''ag-s3-$(date +%s)$$'\'','\''pc-s3'\'','\''online'\'') RETURNING id"); Q "INSERT INTO zenithjoy.agent_platform_sessions(agent_id,platform,account_label,role,status,bound_at) VALUES('\''$AID'\'','\''douyin'\'','\''main'\'','\''main'\'','\''active'\'',NOW())" >/dev/null; Q "INSERT INTO zenithjoy.agent_platform_sessions(agent_id,platform,account_label,role,status,bound_at) VALUES('\''$AID'\'','\''douyin'\'','\''dead1'\'','\''burner'\'','\''expired'\'',NOW())" >/dev/null; R=$(curl -sf "$A/api/agent/machines/$AID?tenant_id=$TID"); echo "$R" | jq -e ".data.machine.id==\"$AID\" and (.data.sessions|type==\"array\")" >/dev/null || { echo "FAIL: 详情结构不符 $R"; exit 1; }; echo "$R" | jq -e ".data.sessions[0]|has(\"role\") and has(\"status\") and has(\"valid\")" >/dev/null || { echo "FAIL: 号缺字段"; exit 1; }; echo "$R" | jq -e "[.data.sessions[]|select(.account_label==\"main\")][0].valid==true" >/dev/null || { echo "FAIL: active 应 valid:true"; exit 1; }; echo "$R" | jq -e "[.data.sessions[]|select(.account_label==\"dead1\")][0].valid==false" >/dev/null || { echo "FAIL: expired 应 valid:false"; exit 1; }; echo OK'

- [ ] [BEHAVIOR] Step4 添加抖音号派单 + fake-agent 回写 + 新号出现（Golden Path Step 4，复用 qr-bind/burner 回写）
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; Q(){ psql -h "${PGHOST:-localhost}" -U "${PGUSER:-cecelia}" -d "${PGDATABASE:-cecelia}" -tAc "$1" | tr -d " " | sed -n '\''1p'\''; }; LK="mk-s4-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''MS4'\'','\''$LK'\'','\''matrix'\'') RETURNING id"); AID=$(Q "INSERT INTO zenithjoy.agents(tenant_id,agent_id,hostname,status) VALUES('\''$TID'\'','\''ag-s4-$(date +%s)$$'\'','\''pc-s4'\'','\''online'\'') RETURNING id"); LBL="xh-$(date +%s)$$"; TASK=$(curl -sf -X POST "$A/api/agent/machines/$AID/add-douyin" -H "Content-Type: application/json" -d "{\"account_label\":\"$LBL\"}" | jq -r ".data.task_id"); echo "$TASK" | grep -Eq "^[0-9a-f-]{36}$" || { echo "FAIL: task_id 非 uuid=$TASK"; exit 1; }; QD=$(Q "SELECT count(*) FROM zenithjoy.publish_tasks WHERE id='\''$TASK'\'' AND agent_id='\''$AID'\'' AND status='\''queued'\'' AND created_at > NOW() - interval '\''5 minutes'\''"); [ "$QD" = "1" ] || { echo "FAIL: 派单未入 publish_tasks"; exit 1; }; curl -sf -X POST "$A/api/agent/burner/qr-bind-result" -H "Content-Type: application/json" -d "{\"task_id\":\"$TASK\",\"agent_id\":\"$AID\",\"qr_login\":\"success\",\"account_nickname\":\"新号昵称\"}" | jq -e ".success==true" >/dev/null || { echo "FAIL: 回写失败"; exit 1; }; SC=$(Q "SELECT count(*) FROM zenithjoy.agent_platform_sessions WHERE agent_id='\''$AID'\'' AND account_label='\''$LBL'\'' AND role='\''burner'\'' AND status='\''active'\'' AND created_at > NOW() - interval '\''5 minutes'\''"); [ "$SC" = "1" ] || { echo "FAIL: 回写后 session 未入库"; exit 1; }; curl -sf "$A/api/agent/machines/$AID?tenant_id=$TID" | jq -e "[.data.sessions[]|select(.account_label==\"$LBL\")][0]|.status==\"active\" and .valid==true" >/dev/null || { echo "FAIL: 新号未出现在详情"; exit 1; }; echo OK'

- [ ] [BEHAVIOR] Step5 离线机器仍返回 + status=offline（Golden Path Step 5，边界：离线不消失）
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; Q(){ psql -h "${PGHOST:-localhost}" -U "${PGUSER:-cecelia}" -d "${PGDATABASE:-cecelia}" -tAc "$1" | tr -d " " | sed -n '\''1p'\''; }; LK="mk-s5-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''MS5'\'','\''$LK'\'','\''matrix'\'') RETURNING id"); AID=$(Q "INSERT INTO zenithjoy.agents(tenant_id,agent_id,hostname,status) VALUES('\''$TID'\'','\''ag-s5-$(date +%s)$$'\'','\''pc-s5'\'','\''offline'\'') RETURNING id"); curl -sf "$A/api/agent/machines?tenant_id=$TID" | jq -e "[.data.machines[]|select(.id==\"$AID\")][0].status==\"offline\"" >/dev/null || { echo "FAIL: 离线机器未返回或 status 不对"; exit 1; }; echo OK'

- [ ] [BEHAVIOR] Step6 租户隔离 — B 列表不含 A 机器 + 跨租户 GET 详情 404（NFR 租户隔离，AI_ADDED）
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; Q(){ psql -h "${PGHOST:-localhost}" -U "${PGUSER:-cecelia}" -d "${PGDATABASE:-cecelia}" -tAc "$1" | tr -d " " | sed -n '\''1p'\''; }; LKA="mk-a-$(date +%s)$$"; LKB="mk-b-$(date +%s)$$z"; TIDA=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''MTA'\'','\''$LKA'\'','\''matrix'\'') RETURNING id"); TIDB=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''MTB'\'','\''$LKB'\'','\''matrix'\'') RETURNING id"); AID=$(Q "INSERT INTO zenithjoy.agents(tenant_id,agent_id,hostname,status) VALUES('\''$TIDA'\'','\''ag-iso-$(date +%s)$$'\'','\''pc-iso'\'','\''online'\'') RETURNING id"); curl -sf "$A/api/agent/machines?tenant_id=$TIDB" | jq -e "all(.data.machines[]; .id != \"$AID\")" >/dev/null || { echo "FAIL: 跨租户泄漏机器"; exit 1; }; CODE=$(curl -s -o /dev/null -w "%{http_code}" "$A/api/agent/machines/$AID?tenant_id=$TIDB"); [ "$CODE" = "404" ] || { echo "FAIL: 跨租户详情应 404 实=$CODE"; exit 1; }; echo OK'

- [ ] [BEHAVIOR] Step7 schema 纯度禁用字段反向 + error path 非法 role 400 不改库（AI_ADDED 防 drift）
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; Q(){ psql -h "${PGHOST:-localhost}" -U "${PGUSER:-cecelia}" -d "${PGDATABASE:-cecelia}" -tAc "$1" | tr -d " " | sed -n '\''1p'\''; }; LK="mk-s7-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''MS7'\'','\''$LK'\'','\''matrix'\'') RETURNING id"); AID=$(Q "INSERT INTO zenithjoy.agents(tenant_id,agent_id,hostname,status,machine_role) VALUES('\''$TID'\'','\''ag-s7-$(date +%s)$$'\'','\''pc-s7'\'','\''online'\'','\''sub'\'') RETURNING id"); curl -sf "$A/api/agent/machines?tenant_id=$TID" | jq -e "[.data.machines[]|select(.id==\"$AID\")][0] | (has(\"name\") or has(\"role\") or has(\"account_count\")) | not" >/dev/null || { echo "FAIL: 机器行含禁用 drift 字段"; exit 1; }; CODE=$(curl -s -o /dev/null -w "%{http_code}" -X PUT "$A/api/agent/machines/$AID" -H "Content-Type: application/json" -d "{\"machine_role\":\"boss\"}"); [ "$CODE" = "400" ] || { echo "FAIL: 非法 role 应 400 实=$CODE"; exit 1; }; RB=$(Q "SELECT machine_role FROM zenithjoy.agents WHERE id='\''$AID'\''"); [ "$RB" = "sub" ] || { echo "FAIL: 非法 role 不应改库 实=$RB"; exit 1; }; echo OK'

## BEHAVIOR:E2E 条目（user_facing 专属，Mode B final-e2e windows_cloud 跑 — Playwright stub，截图存 SPRINT_DIR/screenshots/）

> **windows_cloud 限界声明**：干净 VM 无真后端 → spec 用 `page.route` stub `/api/agent/machines*` + `VITE_SKIP_AUTH` 注入身份，**只验 UI 渲染/交互/文案**，**不验** cookie→:5200 租户接缝（接缝#2，logic-done-pending）。后端逻辑由上方 mode-A 真 API+真库 BEHAVIOR 覆盖。

- [ ] [BEHAVIOR:E2E] 运营走完机器管理 Golden Path（列表→命名标主副→点进详情→添加号→离线标红），截图可视化验证
  Screenshots:
    - 01-initial.png   期望：机器列表渲染 ≥2 行，含名称/在线状态/角色(主机器/副机器)/抖音号数列；离线机器有标红样式或「离线」文案
    - 02-action.png    期望：给机器命名 + 选「主机器」+ 保存后，出现成功提示且列表名称更新为新名
    - 03-result.png    期望：点进机器详情见抖音号列表（主号「有效」），点「添加抖音号」后新号出现在该机器下
  路径格式：${SPRINT_DIR}/screenshots/<step>.png
  期望：所有截图与期望描述一致，Claude Read 图自验通过
