---
skeleton: false
journey_type: autonomous
---
# Contract DoD — Sprint: buildAssignments 真调度（在线+负载最少优先 + 待派发重试）

**范围**: `acquisition-dispatch.ts` buildAssignments 升级为在线感知 + 最少负载调度 + pending_dispatch 重试 + queued→pending 重标；DB migration 新增 `dispatch_reason` 列和 `pending_dispatch` status
**大小**: M

## ARTIFACT 条目

- [ ] [ARTIFACT] DB migration 文件存在，含 `dispatch_reason` 列 ADD 和 check constraint 更新（含 'pending_dispatch'）
  Test: node -e "const fs=require('fs'),path=require('path');const dir='apps/api/db/migrations';const files=fs.readdirSync(dir).filter(f=>f.includes('dispatch'));const latest=files.sort().pop();if(!latest){process.exit(1);}const c=fs.readFileSync(path.join(dir,latest),'utf8');if(!c.includes('dispatch_reason'))process.exit(1);if(!c.includes('pending_dispatch'))process.exit(1);console.log('OK migration:'+latest)"

- [ ] [ARTIFACT] `buildAssignments` 函数签名保持兼容，`BuildResult` 新增 `pending: number` 字段
  Test: node -e "const c=require('fs').readFileSync('apps/api/src/services/acquisition-dispatch.ts','utf8');if(!c.includes('pending'))process.exit(1);if(!c.includes('pending_dispatch'))process.exit(1);if(!c.includes('dispatch_reason'))process.exit(1);console.log('OK')"

- [ ] [ARTIFACT] `buildAssignments` burner 查询联接 `agents.last_heartbeat_at` 并按在线状态+当天负载排序
  Test: node -e "const c=require('fs').readFileSync('apps/api/src/services/acquisition-dispatch.ts','utf8');if(!c.includes('last_heartbeat_at'))process.exit(1);if(!c.includes('2 minutes')||!c.includes('INTERVAL'))process.exit(1);console.log('OK heartbeat join')"

- [ ] [ARTIFACT] `apps/api/tests/routes/acquisition-dispatch.test.ts` 存在，含本 sprint 新增的 4 类场景（dispatch_reason/least_load/pending_dispatch/queued_remap）
  Test: node -e "const c=require('fs').readFileSync('apps/api/tests/routes/acquisition-dispatch.test.ts','utf8');['dispatch_reason','least_load','pending_dispatch','queued_remap'].forEach(kw=>{if(!c.includes(kw)){console.error('MISSING test for:',kw);process.exit(1);}});console.log('OK tests')"

## BEHAVIOR 条目（内嵌可执行 manual: 命令，按 journey_type 选模板）

### B1: API 端点返回 pending 字段（schema 字段类型验证）

- [ ] [BEHAVIOR] POST /dispatch/build 响应 data 含 `pending` 字段，类型为 number
  Test: manual:bash -c 'RESP=$(curl -sf -X POST http://localhost:3000/api/acquisition/dispatch/build -H "X-Tenant-Id: e2e-dod-b1" -H "Content-Type: application/json" 2>/dev/null || echo "{}"); echo "$RESP" | jq -e ".data.pending | type == \"number\"" || { echo "FAIL: pending field missing or wrong type"; exit 1; }; echo OK'
  期望: OK

### B2: 在线小号按负载排序，dispatch_reason='least_load'（核心行为验证 — 含内联 seeding）

- [ ] [BEHAVIOR] 有在线小号时，dm_assignments 新行含 `dispatch_reason='least_load'` 且 status='queued'
  Test: manual:bash -c 'set -e; DB="${DATABASE_URL:-postgresql://localhost/zenithjoy}"; API="${API_URL:-http://localhost:3000}"; TID="t-b2-dod"; psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b2'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b2'"'"'" 2>/dev/null || true; AGID=$(psql "$DB" -t -c "INSERT INTO zenithjoy.agents (tenant_id,agent_id,hostname,status,last_heartbeat_at) VALUES ('"'"'$TID'"'"','"'"'ag-b2'"'"','"'"'h'"'"','"'"'online'"'"',now()) RETURNING id" | tr -d " "); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions (agent_id,platform,account_label,role,status) VALUES ('"'"'$AGID'"'"','"'"'douyin'"'"','"'"'brn-b2'"'"','"'"'burner'"'"','"'"'active'"'"')"; psql "$DB" -c "INSERT INTO zenithjoy.acquisition_leads (tenant_id,sec_uid,profile_url,relevance_score) VALUES ('"'"'$TID'"'"','"'"'s-b2'"'"','"'"'https://d/b2'"'"',90)"; RESP=$(curl -sf -X POST "$API/api/acquisition/dispatch/build" -H "X-Tenant-Id: $TID" -H "Content-Type: application/json") || { echo "FAIL: call error"; exit 1; }; echo "$RESP" | jq -e ".success == true" || { echo "FAIL: not success"; exit 1; }; C=$(psql "$DB" -t -c "SELECT count(*) FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"' AND dispatch_reason='"'"'least_load'"'"' AND status='"'"'queued'"'"' AND created_at > NOW() - interval '"'"'2 minutes'"'"'" | tr -d " "); psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b2'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b2'"'"'" 2>/dev/null || true; [ "$C" -ge 1 ] || { echo "FAIL: no least_load queued rows count=$C"; exit 1; }; echo OK'
  期望: OK

### B3: 全部小号离线时写 pending_dispatch，assigned=0，pending≥1（含内联 seeding）

- [ ] [BEHAVIOR] 全离线场景：data.assigned=0, data.pending≥1，DB 中 pending_dispatch status 行存在
  Test: manual:bash -c 'set -e; DB="${DATABASE_URL:-postgresql://localhost/zenithjoy}"; API="${API_URL:-http://localhost:3000}"; TID="t-b3-dod"; psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b3'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b3'"'"'" 2>/dev/null || true; AGID=$(psql "$DB" -t -c "INSERT INTO zenithjoy.agents (tenant_id,agent_id,hostname,status,last_heartbeat_at) VALUES ('"'"'$TID'"'"','"'"'ag-b3'"'"','"'"'h'"'"','"'"'offline'"'"',now()-interval '"'"'10 minutes'"'"') RETURNING id" | tr -d " "); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions (agent_id,platform,account_label,role,status) VALUES ('"'"'$AGID'"'"','"'"'douyin'"'"','"'"'brn-b3'"'"','"'"'burner'"'"','"'"'active'"'"')"; psql "$DB" -c "INSERT INTO zenithjoy.acquisition_leads (tenant_id,sec_uid,profile_url,relevance_score) VALUES ('"'"'$TID'"'"','"'"'s-b3'"'"','"'"'https://d/b3'"'"',80)"; RESP=$(curl -sf -X POST "$API/api/acquisition/dispatch/build" -H "X-Tenant-Id: $TID" -H "Content-Type: application/json") || { echo "FAIL: call error"; exit 1; }; echo "$RESP" | jq -e ".data.assigned == 0" || { echo "FAIL: assigned != 0"; exit 1; }; echo "$RESP" | jq -e ".data.pending >= 1" || { echo "FAIL: pending < 1"; exit 1; }; C=$(psql "$DB" -t -c "SELECT count(*) FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"' AND status='"'"'pending_dispatch'"'"' AND created_at > NOW() - interval '"'"'2 minutes'"'"'" | tr -d " "); psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b3'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b3'"'"'" 2>/dev/null || true; [ "$C" -ge 1 ] || { echo "FAIL: no pending_dispatch rows count=$C"; exit 1; }; echo OK'
  期望: OK

### B4: 离线小号不出现在 queued 行（含内联 seeding，修复 R1 假阳性）

- [ ] [BEHAVIOR] 全离线场景中，离线小号的 account_label 不出现在 status='queued' 的 dm_assignments 中
  Test: manual:bash -c 'set -e; DB="${DATABASE_URL:-postgresql://localhost/zenithjoy}"; API="${API_URL:-http://localhost:3000}"; TID="t-b4-dod"; psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b4'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b4'"'"'" 2>/dev/null || true; AGID=$(psql "$DB" -t -c "INSERT INTO zenithjoy.agents (tenant_id,agent_id,hostname,status,last_heartbeat_at) VALUES ('"'"'$TID'"'"','"'"'ag-b4'"'"','"'"'h'"'"','"'"'offline'"'"',now()-interval '"'"'10 minutes'"'"') RETURNING id" | tr -d " "); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions (agent_id,platform,account_label,role,status) VALUES ('"'"'$AGID'"'"','"'"'douyin'"'"','"'"'brn-b4'"'"','"'"'burner'"'"','"'"'active'"'"')"; psql "$DB" -c "INSERT INTO zenithjoy.acquisition_leads (tenant_id,sec_uid,profile_url,relevance_score) VALUES ('"'"'$TID'"'"','"'"'s-b4'"'"','"'"'https://d/b4'"'"',80)"; RESP=$(curl -sf -X POST "$API/api/acquisition/dispatch/build" -H "X-Tenant-Id: $TID" -H "Content-Type: application/json") || { echo "FAIL: dispatch call error"; exit 1; }; echo "$RESP" | jq -e ".success == true" || { echo "FAIL: not success"; exit 1; }; Q=$(psql "$DB" -t -c "SELECT count(*) FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"' AND account_label='"'"'brn-b4'"'"' AND status='"'"'queued'"'"'" | tr -d " "); psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b4'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b4'"'"'" 2>/dev/null || true; [ "$Q" = "0" ] || { echo "FAIL: offline burner in queued count=$Q"; exit 1; }; echo OK'
  期望: OK

### B5: pending_dispatch 重试补派 — 下周期有可用小号时升级为 queued（含内联 seeding，修复 R1 假阳性）

- [ ] [BEHAVIOR] pending_dispatch 行在下一轮有可用小号后 status 更新为 queued，pending 行清零
  Test: manual:bash -c 'set -e; DB="${DATABASE_URL:-postgresql://localhost/zenithjoy}"; API="${API_URL:-http://localhost:3000}"; TID="t-b5-dod"; psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b5'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b5'"'"'" 2>/dev/null || true; AGID=$(psql "$DB" -t -c "INSERT INTO zenithjoy.agents (tenant_id,agent_id,hostname,status,last_heartbeat_at) VALUES ('"'"'$TID'"'"','"'"'ag-b5'"'"','"'"'h'"'"','"'"'offline'"'"',now()-interval '"'"'10 minutes'"'"') RETURNING id" | tr -d " "); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions (agent_id,platform,account_label,role,status) VALUES ('"'"'$AGID'"'"','"'"'douyin'"'"','"'"'brn-b5'"'"','"'"'burner'"'"','"'"'active'"'"')"; psql "$DB" -c "INSERT INTO zenithjoy.acquisition_leads (tenant_id,sec_uid,profile_url,relevance_score) VALUES ('"'"'$TID'"'"','"'"'s-b5'"'"','"'"'https://d/b5'"'"',80)"; R1=$(curl -sf -X POST "$API/api/acquisition/dispatch/build" -H "X-Tenant-Id: $TID" -H "Content-Type: application/json") || { echo "FAIL: first call error"; exit 1; }; echo "$R1" | jq -e ".data.pending >= 1" || { echo "FAIL: first call did not produce pending_dispatch"; exit 1; }; psql "$DB" -c "UPDATE zenithjoy.agents SET last_heartbeat_at=now(), status='"'"'online'"'"' WHERE id='"'"'$AGID'"'"'"; R2=$(curl -sf -X POST "$API/api/acquisition/dispatch/build" -H "X-Tenant-Id: $TID" -H "Content-Type: application/json") || { echo "FAIL: second call error"; exit 1; }; echo "$R2" | jq -e ".data.assigned >= 1" || { echo "FAIL: retry assigned < 1"; exit 1; }; STILL=$(psql "$DB" -t -c "SELECT count(*) FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"' AND status='"'"'pending_dispatch'"'"'" | tr -d " "); psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b5'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b5'"'"'" 2>/dev/null || true; [ "$STILL" = "0" ] || { echo "FAIL: still pending_dispatch=$STILL after retry"; exit 1; }; echo OK'
  期望: OK

### B6: error path — 无租户上下文返 401 + error.code='NO_TENANT'（修复 R1 只验 HTTP 未验 body）

- [ ] [BEHAVIOR] POST /dispatch/build 无 X-Tenant-Id 返 401 且 body.error.code='NO_TENANT'
  Test: manual:bash -c 'RESP=$(curl -s -w "\n%{http_code}" -X POST http://localhost:3000/api/acquisition/dispatch/build -H "Content-Type: application/json"); CODE=$(echo "$RESP" | tail -1); BODY=$(echo "$RESP" | head -n -1); [ "$CODE" = "401" ] || { echo "FAIL: HTTP $CODE != 401"; exit 1; }; echo "$BODY" | jq -e ".error.code == \"NO_TENANT\"" || { echo "FAIL: error.code not NO_TENANT; body=$BODY"; exit 1; }; echo OK'
  期望: OK

### B7: keys 完整性 — data 字段集包含所有必填字段且类型正确（修复 R1 用 has() 未验类型）

- [ ] [BEHAVIOR] POST /dispatch/build 成功响应 data 同时含全部 6 个字段（scored/assigned/skipped_dedup/skipped_budget/burners/pending），类型分别为 number/number/number/number/array/number，且 data keys 恰好为这 6 个
  Test: manual:bash -c 'RESP=$(curl -sf -X POST http://localhost:3000/api/acquisition/dispatch/build -H "X-Tenant-Id: e2e-b7-keys" -H "Content-Type: application/json" 2>/dev/null || echo "{}"); echo "$RESP" | jq -e ".data.scored | type == \"number\"" || { echo "FAIL: scored not number"; exit 1; }; echo "$RESP" | jq -e ".data.assigned | type == \"number\"" || { echo "FAIL: assigned not number"; exit 1; }; echo "$RESP" | jq -e ".data.skipped_dedup | type == \"number\"" || { echo "FAIL: skipped_dedup not number"; exit 1; }; echo "$RESP" | jq -e ".data.skipped_budget | type == \"number\"" || { echo "FAIL: skipped_budget not number"; exit 1; }; echo "$RESP" | jq -e ".data.burners | type == \"array\"" || { echo "FAIL: burners not array"; exit 1; }; echo "$RESP" | jq -e ".data.pending | type == \"number\"" || { echo "FAIL: pending not number"; exit 1; }; echo "$RESP" | jq -e ".data | keys | sort == [\"assigned\",\"burners\",\"pending\",\"scored\",\"skipped_budget\",\"skipped_dedup\"]" || { echo "FAIL: data schema keys 不完整或含多余字段"; exit 1; }; echo OK'
  期望: OK

### B8: 租户隔离 — Tenant A 的 pending 积压不影响 Tenant B（含内联 seeding，修复 R1 假阳性）

- [ ] [BEHAVIOR] 租户 A 有 pending 积压时，租户 B 的 dm_assignments 无 pending_dispatch 行
  Test: manual:bash -c 'set -e; DB="${DATABASE_URL:-postgresql://localhost/zenithjoy}"; API="${API_URL:-http://localhost:3000}"; TA="t-b8a-dod"; TB="t-b8b-dod"; for T in "$TA" "$TB"; do psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$T'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$T'"'"'" 2>/dev/null || true; done; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b8a'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b8b'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b8a'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b8b'"'"'" 2>/dev/null || true; AGA=$(psql "$DB" -t -c "INSERT INTO zenithjoy.agents (tenant_id,agent_id,hostname,status,last_heartbeat_at) VALUES ('"'"'$TA'"'"','"'"'ag-b8a'"'"','"'"'ha'"'"','"'"'offline'"'"',now()-interval '"'"'10 minutes'"'"') RETURNING id" | tr -d " "); AGB=$(psql "$DB" -t -c "INSERT INTO zenithjoy.agents (tenant_id,agent_id,hostname,status,last_heartbeat_at) VALUES ('"'"'$TB'"'"','"'"'ag-b8b'"'"','"'"'hb'"'"','"'"'online'"'"',now()) RETURNING id" | tr -d " "); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions (agent_id,platform,account_label,role,status) VALUES ('"'"'$AGA'"'"','"'"'douyin'"'"','"'"'brn-b8a'"'"','"'"'burner'"'"','"'"'active'"'"')"; psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions (agent_id,platform,account_label,role,status) VALUES ('"'"'$AGB'"'"','"'"'douyin'"'"','"'"'brn-b8b'"'"','"'"'burner'"'"','"'"'active'"'"')"; psql "$DB" -c "INSERT INTO zenithjoy.acquisition_leads (tenant_id,sec_uid,profile_url,relevance_score) VALUES ('"'"'$TA'"'"','"'"'sa'"'"','"'"'https://d/a'"'"',70)"; psql "$DB" -c "INSERT INTO zenithjoy.acquisition_leads (tenant_id,sec_uid,profile_url,relevance_score) VALUES ('"'"'$TB'"'"','"'"'sb'"'"','"'"'https://d/b'"'"',70)"; curl -sf -X POST "$API/api/acquisition/dispatch/build" -H "X-Tenant-Id: $TA" -H "Content-Type: application/json" | jq -e ".success == true" > /dev/null || { echo "FAIL: tenant A dispatch error"; exit 1; }; RB=$(curl -sf -X POST "$API/api/acquisition/dispatch/build" -H "X-Tenant-Id: $TB" -H "Content-Type: application/json") || { echo "FAIL: tenant B dispatch error"; exit 1; }; echo "$RB" | jq -e ".data.assigned >= 1" || { echo "FAIL: tenant B assigned=0"; exit 1; }; CB=$(psql "$DB" -t -c "SELECT count(*) FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TB'"'"' AND status='"'"'pending_dispatch'"'"'" | tr -d " "); for T in "$TA" "$TB"; do psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$T'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$T'"'"'" 2>/dev/null || true; done; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b8a'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b8b'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b8a'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b8b'"'"'" 2>/dev/null || true; [ "$CB" = "0" ] || { echo "FAIL: tenant B has pending_dispatch=$CB (cross-tenant leak)"; exit 1; }; echo OK'
  期望: OK

### B9: pending_dispatch 去重 — 同 (tenant,lead) 不重复插入（新增，补 PRD 边界）

- [ ] [BEHAVIOR] 已存在 pending_dispatch 的 (tenant,lead) 二次调用 buildAssignments 不重复插入
  Test: manual:bash -c 'set -e; DB="${DATABASE_URL:-postgresql://localhost/zenithjoy}"; API="${API_URL:-http://localhost:3000}"; TID="t-b9-dod"; psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b9'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b9'"'"'" 2>/dev/null || true; AGID=$(psql "$DB" -t -c "INSERT INTO zenithjoy.agents (tenant_id,agent_id,hostname,status,last_heartbeat_at) VALUES ('"'"'$TID'"'"','"'"'ag-b9'"'"','"'"'h'"'"','"'"'offline'"'"',now()-interval '"'"'10 minutes'"'"') RETURNING id" | tr -d " "); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions (agent_id,platform,account_label,role,status) VALUES ('"'"'$AGID'"'"','"'"'douyin'"'"','"'"'brn-b9'"'"','"'"'burner'"'"','"'"'active'"'"')"; psql "$DB" -c "INSERT INTO zenithjoy.acquisition_leads (tenant_id,sec_uid,profile_url,relevance_score) VALUES ('"'"'$TID'"'"','"'"'s-b9'"'"','"'"'https://d/b9'"'"',75)"; curl -sf -X POST "$API/api/acquisition/dispatch/build" -H "X-Tenant-Id: $TID" -H "Content-Type: application/json" | jq -e ".data.pending >= 1" || { echo "FAIL: first call no pending"; exit 1; }; curl -sf -X POST "$API/api/acquisition/dispatch/build" -H "X-Tenant-Id: $TID" -H "Content-Type: application/json" | jq -e ".data.pending == 0 or .data.assigned == 0" > /dev/null; C=$(psql "$DB" -t -c "SELECT count(*) FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"' AND status='"'"'pending_dispatch'"'"'" | tr -d " "); psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b9'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b9'"'"'" 2>/dev/null || true; [ "$C" = "1" ] || { echo "FAIL: dedup violated, pending_dispatch count=$C (expected 1)"; exit 1; }; echo OK'
  期望: OK

### B10: queued→pending_dispatch 重标 — burner 掉线后已排队行自动重标（新增，补 PRD 边界情况）

- [ ] [BEHAVIOR] 已派 queued 行所属 burner 心跳超时后，buildAssignments 将其 status 更新为 pending_dispatch
  Test: manual:bash -c 'set -e; DB="${DATABASE_URL:-postgresql://localhost/zenithjoy}"; API="${API_URL:-http://localhost:3000}"; TID="t-b10-dod"; psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b10'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b10'"'"'" 2>/dev/null || true; AGID=$(psql "$DB" -t -c "INSERT INTO zenithjoy.agents (tenant_id,agent_id,hostname,status,last_heartbeat_at) VALUES ('"'"'$TID'"'"','"'"'ag-b10'"'"','"'"'h'"'"','"'"'online'"'"',now()) RETURNING id" | tr -d " "); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions (agent_id,platform,account_label,role,status) VALUES ('"'"'$AGID'"'"','"'"'douyin'"'"','"'"'brn-b10'"'"','"'"'burner'"'"','"'"'active'"'"')"; psql "$DB" -c "INSERT INTO zenithjoy.acquisition_leads (tenant_id,sec_uid,profile_url,relevance_score) VALUES ('"'"'$TID'"'"','"'"'s-b10'"'"','"'"'https://d/b10'"'"',80)"; R1=$(curl -sf -X POST "$API/api/acquisition/dispatch/build" -H "X-Tenant-Id: $TID" -H "Content-Type: application/json") || { echo "FAIL: first call error"; exit 1; }; echo "$R1" | jq -e ".data.assigned >= 1" || { echo "FAIL: first round did not assign lead"; exit 1; }; AID=$(psql "$DB" -t -c "SELECT id FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"' AND status='"'"'queued'"'"'" | tr -d " "); [ -n "$AID" ] || { echo "FAIL: no queued row created"; exit 1; }; psql "$DB" -c "UPDATE zenithjoy.agents SET last_heartbeat_at=now()-interval '"'"'10 minutes'"'"', status='"'"'offline'"'"' WHERE id='"'"'$AGID'"'"'"; R2=$(curl -sf -X POST "$API/api/acquisition/dispatch/build" -H "X-Tenant-Id: $TID" -H "Content-Type: application/json") || { echo "FAIL: second call error"; exit 1; }; echo "$R2" | jq -e ".success == true" || { echo "FAIL: not success"; exit 1; }; STATUS=$(psql "$DB" -t -c "SELECT status FROM zenithjoy.dm_assignments WHERE id='"'"'$AID'"'"'" | tr -d " "); psql "$DB" -c "DELETE FROM zenithjoy.dm_assignments WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.acquisition_leads WHERE tenant_id='"'"'$TID'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agent_platform_sessions WHERE account_label='"'"'brn-b10'"'"'" 2>/dev/null || true; psql "$DB" -c "DELETE FROM zenithjoy.agents WHERE agent_id='"'"'ag-b10'"'"'" 2>/dev/null || true; [ "$STATUS" = "pending_dispatch" ] || { echo "FAIL: queued row status=$STATUS (expected pending_dispatch)"; exit 1; }; echo OK'
  期望: OK
