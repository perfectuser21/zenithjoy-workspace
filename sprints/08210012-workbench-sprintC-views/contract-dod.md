---
skeleton: false
journey_type: user_facing
target_environment: windows_cloud
gp_anchor: line11/structured_workbench#step3
---
# Contract DoD — Sprint: 路③ 结构化工作台 · Sprint C（S3 视图切得开）

**范围**: 视图偏好四端点（`GET/POST/PATCH/DELETE`，落**既有** `zenithjoy.db_view_prefs`，**零新增 migration**）/ `GET /tables/:id/rows` 加 `filter`+`sort`（JSONB 路径查询 + `field_id` 白名单映射）/ 看板视图组件（dnd-kit，**仅 `single_select` 分组** + 未分组列 + 指针拖卡改值走 `PATCH /rows/:id` + 失败弹回）/ 视图切换器 + 隐藏列 / `GET /assigned-to-me` / 视图层跨组织正反双向 + 反查两分支 / 视图三动作落 `db_audit`
**不在范围**: 日历/画廊视图、跨表关联（Sprint D）、公式 rollup、CSV 导出、字段类型变更、字段 UPDATE/DELETE 端点、表改名与改可见性、行级权限、附件字段、多人实时协同、对外 API、服务端行模型、AG Grid v33 升级；**A26「视图组件独立挂载」本刀零门禁断言**（controller concern C3，见 contract-draft.md `## 本刀显式不实装`）
**大小**: L

> 全部 `manual:` 命令的工作目录 = repo 根。需 `E2E_DATABASE_URL`（或 `DATABASE_URL`），指 **zenithjoy** 库；未设时命令自身报错退出，**不落默认库**。
> **判定归 DoD、供给归脚本**（沿用 Sprint A/B「夹具供给协议」）：`bash "$S" --fixture-up` 只起真 `apps/api` + 种双企业 + 签三个真会话并写 `./.wb-fixture.env`（逐字导出 `API_PORT`/`SFX`/`ORGA_TENANT_ID`/`ORGB_TENANT_ID`/`COOKIE_A`(甲=Alice)/`COOKIE_A2`(乙=Bob，**同组织他人**)/`COOKIE_B`(丙=Carol，他企业)/`ALICE_OPENID`/`BOB_OPENID`/`CAROL_OPENID`/`EIGHT_FIELDS`，见 `structured-workbench-smoke.sh:540-552`）；一切 pass/fail 判定写在下面的命令里由 evaluator 直接执行，DB 断言的 `PG` 直接取 `${E2E_DATABASE_URL:-$DATABASE_URL}`，**不经脚本**。
> **变异一律外置判据**（沿用「变异证明执行协议」）：`--mutation-apply` 只改代码，判据是「被守卫的那一段自己 `exit≠0`」，不认脚本自述的 `proven-to-fire`。
> **接缝四条未真验前不得标 done**（见 contract-draft.md `## 接缝清单`）：dnd-kit 指针拖拽语义 / 视图偏好保存失败可见提示 / 看板分列渲染与卡片弹回 / JSONB 筛排的真 PG 排序语义；linux CI 绿 ≠ done，未真验一律标 `logic-done-pending`。

## ARTIFACT 条目

- [ ] [ARTIFACT] **ART-1 concern C1：零新增 migration** —— 本刀相对 `origin/main` 未新增任何 `apps/api/db/migrations/*.sql`，且既有两份 migration 未被回改
  Test: node -e "const{execSync}=require('child_process');const d=execSync('git diff --name-only origin/main...HEAD -- apps/api/db/migrations').toString().trim();if(d)process.exit(1);const fs=require('fs');for(const p of ['apps/api/db/migrations/20260820_120000_structured_workbench.sql','apps/api/db/migrations/20260820_190000_workbench_rows_version.sql']){const a=execSync('git show origin/main:'+p).toString();if(a!==fs.readFileSync(p,'utf8'))process.exit(1)}"

- [ ] [ARTIFACT] **ART-2 concern C1：不另建 `db_views`** —— 全仓源码零 `db_views` 字面量，且视图 service 真读写 `db_view_prefs` 三键
  Test: node -e "const fs=require('fs'),path=require('path');const walk=d=>fs.existsSync(d)?fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]):[];const files=[...walk('apps/api/src'),...walk('apps/staff-hub/src')].filter(f=>/\.(ts|tsx)$/.test(f));for(const f of files){if(/\bdb_views\b/.test(fs.readFileSync(f,'utf8')))process.exit(1)}const svc='apps/api/src/services/workbench-views.service.ts';if(!fs.existsSync(svc))process.exit(1);const c=fs.readFileSync(svc,'utf8');for(const s of ['zenithjoy.db_view_prefs','table_id','org_id','member_id','prefs'])if(!c.includes(s))process.exit(1)"

- [ ] [ARTIFACT] **ART-3 端点计数三条断言同步改到新值**（17→22 / 写 9→12 / 读 8→10，**改值不删断言**，旧值零残留），且四条新路由逐字在案
  Test: node -e "const c=require('fs').readFileSync('apps/api/src/routes/workbench.test.ts','utf8');for(const s of ['GET /tables/:id/views','POST /tables/:id/views','PATCH /views/:id','DELETE /views/:id','GET /assigned-to-me'])if(!c.includes(s))process.exit(1);for(const n of ['22','12','10'])if(!new RegExp('toBe\\\\('+n+'\\\\)').test(c))process.exit(1);if(/toBe\(17\)|toBe\(9\)|toBe\(8\)/.test(c))process.exit(1)"

- [ ] [ARTIFACT] **ART-4 视图路由挂在路③ router 上，源码零明文身份头字面量**（A2 家族口径），且**零 `req.headers` / `body.org_id` 读取**
  Test: node -e "const fs=require('fs');const files=['apps/api/src/routes/workbench.ts','apps/api/src/services/workbench-views.service.ts'].filter(p=>fs.existsSync(p));if(files.length!==2)process.exit(1);const c=files.map(p=>fs.readFileSync(p,'utf8')).join('\n');const need=[/router\.get\(['\"]\/tables\/:id\/views['\"]/,/router\.post\(['\"]\/tables\/:id\/views['\"]/,/router\.patch\(['\"]\/views\/:id['\"]/,/router\.delete\(['\"]\/views\/:id['\"]/,/router\.get\(['\"]\/assigned-to-me['\"]/];for(const r of need)if(!r.test(c))process.exit(1);if(/X-Tenant-Id|X-User-Email|X-Feishu-User-Id|X-Bypass-Tenant|tenantContextOptional|selfHealOwnerMember|staffGuard/i.test(c))process.exit(1);const s=fs.readFileSync('apps/api/src/services/workbench-views.service.ts','utf8');if(/req\.headers|body\.org_id|body\.tenant_id|body\.member_id/.test(s))process.exit(1)"

- [ ] [ARTIFACT] **ART-5 本刀 4 个合同测试文件有真实收集配置**：专用 vitest config 白名单含本 sprint tests 目录 + `apps/api` 暴露运行脚本 + setup 从连接串推导五个 `DATABASE_*`
  Test: node -e "const fs=require('fs');const p='apps/api/vitest.workbench-views.config.ts';if(!fs.existsSync(p))process.exit(1);const c=fs.readFileSync(p,'utf8');if(!c.includes('../../sprints/08210012-workbench-sprintC-views/tests/'))process.exit(1);if(!/singleFork:\s*true/.test(c))process.exit(1);if(!fs.existsSync('apps/api/vitest.workbench-views.setup.ts'))process.exit(1);const su=fs.readFileSync('apps/api/vitest.workbench-views.setup.ts','utf8');for(const k of ['DATABASE_HOST','DATABASE_PORT','DATABASE_NAME','DATABASE_USER','DATABASE_PASSWORD'])if(!su.includes(k))process.exit(1);const s=require('./apps/api/package.json').scripts||{};if(!/vitest\.workbench-views\.config\.ts/.test(s['test:workbench-views']||''))process.exit(1)"

- [ ] [ARTIFACT] **ART-6 dnd-kit 精确版本锁定**（合同 J3 + NFR 版本锁）：`@dnd-kit/core` 在 `dependencies` 里且版本**不含 `^`/`~`/`*`/`x`/范围符**；AG Grid 仍逐字 `32.2.1` 不跟 v33
  Test: node -e "const p=require('./apps/staff-hub/package.json');const d=p.dependencies||{};const v=d['@dnd-kit/core'];if(!v)process.exit(1);if(/[\^~*x><|\s-]/.test(v))process.exit(1);if(!/^\d+\.\d+\.\d+$/.test(v))process.exit(1);for(const k of ['ag-grid-community','ag-grid-react']){if(d[k]!=='32.2.1')process.exit(1)}"

- [ ] [ARTIFACT] **ART-7 看板组件用真 dnd-kit sensor（含键盘可达）+ A26 结构预留弱约束**：组件源码含 `PointerSensor` 与 `KeyboardSensor`，且**不出现 `useParams` / `useNavigate`**（不依赖路由上下文即可挂载），props 四件齐
  Test: node -e "const fs=require('fs');const p='apps/staff-hub/src/components/WorkbenchKanbanView.tsx';if(!fs.existsSync(p))process.exit(1);const c=fs.readFileSync(p,'utf8');for(const s of ['PointerSensor','KeyboardSensor','@dnd-kit/core','fields','rows','groupFieldId','onCardMoved'])if(!c.includes(s))process.exit(1);if(/useParams|useNavigate/.test(c))process.exit(1)"

- [ ] [ARTIFACT] **ART-8 拖卡落库映射抽成可变异的纯函数**：`workbenchKanban.ts` 导出 `resolveDropPatch` 与 `groupRowsByField`，且**「未分组」判据三态齐**（`null` / 缺键 / 空串，判定点 JC3）
  Test: node -e "const fs=require('fs');const p='apps/staff-hub/src/lib/workbenchKanban.ts';if(!fs.existsSync(p))process.exit(1);const c=fs.readFileSync(p,'utf8');for(const s of ['export function resolveDropPatch','export function groupRowsByField','__ungrouped__'])if(!c.includes(s))process.exit(1);if(!/===\s*null|== null/.test(c))process.exit(1);if(!/===\s*''|=== \"\"|length === 0/.test(c))process.exit(1);if(!/undefined|in row\.data|hasOwnProperty|\?\?/.test(c))process.exit(1)"

- [ ] [ARTIFACT] **ART-9 前端不走 `adminFetch`、不写死上限、视图保存分支零空吞异常**（INV 禁静默吞 + 禁写死环境假设值）
  Test: node -e "const fs=require('fs'),path=require('path');const roots=['apps/staff-hub/src/pages','apps/staff-hub/src/components','apps/staff-hub/src/lib'];const walk=d=>fs.existsSync(d)?fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]):[];const files=roots.flatMap(walk).filter(f=>/\.(ts|tsx)$/.test(f)&&/[Ww]orkbench|[Kk]anban|[Vv]iew/.test(f));if(!files.length)process.exit(1);for(const f of files){const c=fs.readFileSync(f,'utf8');if(/adminFetch/.test(c))process.exit(1);if(/\b5000\b/.test(c))process.exit(1);if(/catch\s*(\([^)]*\))?\s*\{\s*\}/.test(c))process.exit(1);if(/\.catch\(\s*\(\s*[^)]*\)\s*=>\s*\{\s*\}\s*\)/.test(c))process.exit(1)}"

- [ ] [ARTIFACT] **ART-10 E2E workflow 逐字接线**：windows job **仍无 job 级事件条件门**（A33(c)）；`paths` 含本刀 spec 与 sprint 目录；linux job 含本刀六段 flag + `test:workbench-views`；windows job 有**真调** views spec 的 run 命令；本刀截图有自己的 upload step
  Test: node -e "const c=require('fs').readFileSync('.github/workflows/e2e-knowledge-hub-path3.yml','utf8');if(!/structured-workbench-views\.spec\.ts/.test(c))process.exit(1);if(!/sprints\/08210012-workbench-sprintC-views/.test(c))process.exit(1);const parts=c.split(/\n  windows-real-browser:\n/);const lin=parts[0],win=parts[1]||'';const head=win.split(/\n    steps:/)[0];if(/^\s{4}if:/m.test(head))process.exit(1);if(/github\.event_name|workflow_dispatch/.test(head))process.exit(1);for(const f of ['--a20-only','--a21-only','--a22-only','--a25-only','--a1-a3-views-only','--view-delete-only'])if(!lin.includes(f))process.exit(1);if(!/test:workbench-views/.test(lin))process.exit(1);const runs=(win.match(/run:[\s\S]*?(?=\n      - |$)/g)||[]).join('\n');if(!/e2e-views-run\.ps1/.test(runs))process.exit(1);if(!/path3-views-screenshots/.test(win))process.exit(1)"

- [ ] [ARTIFACT] **ART-11 视图 E2E spec 存在、零请求拦截/改写**（变体C 死规则），且逐字含指针拖拽序列 / **两条失败分支各自的弹回断言**（断网 `kanban-drop-error` + 409 `kanban-drop-conflict` 及其冲突文案）/ 未分组列**三态计数 `toHaveCount(3)`** / 视图偏好失败提示
  Test: node -e "const c=require('fs').readFileSync('apps/staff-hub/e2e/structured-workbench-views.spec.ts','utf8');if(/page\.route|context\.route|fulfill\(/.test(c))process.exit(1);for(const s of ['@views-kanban','@views-prefs','mouse.down','mouse.up','steps:','setOffline','kanban-column-__ungrouped__','toHaveCount(3)','kanban-drop-error','kanban-drop-conflict','该行已被他人修改，你的改动未保存','view-prefs-error'])if(!c.includes(s))process.exit(1)"

- [ ] [ARTIFACT] **ART-12 smoke 已登记本刀 6 个新段与 7 个变异开关**（否则变异证明无处施加、段无处调用）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; L=$(bash "$S" --mutation-list) || exit 1; for m in A25-field-whitelist-off A20-group-type-nocheck A20-ungrouped-null-only A1V-view-org-bypass A1V-view-member-bypass VIEW-lastview-off A24-drag-wrong-row; do printf "%s\n" "$L" | grep -q "$m" || { echo "FAIL: 变异未登记 $m"; exit 1; }; done; for f in --a20-only --a21-only --a22-only --a25-only --a1-a3-views-only --view-delete-only --a24-pure-only; do grep -q -- "$f" "$S" || { echo "FAIL: smoke 缺段 $f"; exit 1; }; done; echo OK'
  期望: OK

## BEHAVIOR 条目

> 每条都能回答「这是 Golden Path 哪一步的用户可观察输出」，且对应代码一行没写时必然 FAIL —— 视图端点族与 `filter`/`sort` 参数在 `origin/main` 上都不存在（`GET /tables/:id/views` 现在返 404，`--a20-only` 现在返「未知参数」exit 1）。

### Step0 — 合同测试文件真被 vitest 收集执行（TDD 红绿的机械载体）

- [ ] [BEHAVIOR] 本刀 4 个测试文件被 vitest **真收集真跑**：4 个 suite 文件全绿、用例数 ≥ 20（r2 新增 JC3 三态纯函数 + member 维度隔离两条后的实际下限）、失败数 0，且跑的**恰是**本刀那 4 个文件（零收集时 vitest 自己报错，不会假绿成 exit 0）
  Test: manual:bash -c 'PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; [ -n "$PG" ] || { echo "FAIL: 缺 E2E_DATABASE_URL/DATABASE_URL"; exit 1; }; O=/tmp/wb-views-vitest.json; rm -f "$O"; (cd apps/api && npx vitest run --config vitest.workbench-views.config.ts --reporter=json --outputFile="$O") >/tmp/wb-views-vitest.log 2>&1; RC=$?; [ -f "$O" ] || { echo "FAIL: vitest 未产出报告（多半是零收集）"; tail -30 /tmp/wb-views-vitest.log; exit 1; }; jq -e "(.testResults | length) == 4 and .numTotalTests >= 20 and .numFailedTests == 0 and .success == true" < "$O" >/dev/null || { echo "FAIL: 收集/通过数不符"; jq -c "{files:(.testResults|length),tests:.numTotalTests,failed:.numFailedTests,ok:.success}" < "$O"; exit 1; }; jq -e "[.testResults[].name | select(test(\"views-(crud|group-type|filter-sort|isolation-degrade)\\\\.test\\\\.ts$\"))] | length == 4" < "$O" >/dev/null || { echo "FAIL: 跑的不是本刀那 4 个文件"; exit 1; }; [ "$RC" = "0" ] || { echo "FAIL: vitest exit=$RC"; exit 1; }; echo OK'
  期望: OK

### Step1 — 打开表拿到视图列表（`GET /tables/:id/views` 纯读零副作用）

- [ ] [BEHAVIOR] 空表 `GET views` 返 200 且 `data` keys **恰好** `["active_view_id","views"]`、`views` 为空、`active_view_id` 为 `null`；连调三次后 `db_view_prefs` 行数**一行不增**（GET 纯读，判定点 JC6）；建视图返 201 且 `View` keys **恰好十个**、十四个禁用字段名一个不出现
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; [ -n "$PG" ] || fail "缺 PG"; API="http://localhost:$API_PORT/api/knowledge/db"; TID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"WB-C1-$SFX\",\"visibility\":\"org\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"); [ -n "$TID" ] && [ "$TID" != "null" ] || fail "建表失败"; curl -sf -b "$COOKIE_A" "$API/tables/$TID/views" | jq -e "(.data|keys)==[\"active_view_id\",\"views\"] and (.data.views|length)==0 and .data.active_view_id==null" >/dev/null || fail "空表 views 形状不符"; N0=$(psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_view_prefs WHERE table_id = '"'"'$TID'"'"'"); for i in 1 2 3; do curl -sf -b "$COOKIE_A" "$API/tables/$TID/views" >/dev/null || fail "GET views 非 2xx"; done; N1=$(psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_view_prefs WHERE table_id = '"'"'$TID'"'"'"); [ "$N0" = "$N1" ] || fail "GET 有写入副作用（$N0 -> $N1）"; V=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TID/views" -d "{\"name\":\"默认视图\",\"view_type\":\"grid\",\"is_active\":true}") || fail "建视图非 2xx"; echo "$V" | jq -e "(.data|keys)==[\"degraded\",\"filters\",\"group_field_id\",\"hidden_field_ids\",\"is_active\",\"name\",\"sorts\",\"updated_at\",\"view_id\",\"view_type\"]" >/dev/null || fail "View keys 不是约定的十个 v=$V"; echo "$V" | jq -e ".data | ([has(\"id\"),has(\"viewId\"),has(\"type\"),has(\"prefs\"),has(\"config\"),has(\"active\"),has(\"groupBy\"),has(\"group_by\"),has(\"hiddenFields\"),has(\"hidden_fields\"),has(\"filterModel\"),has(\"sortModel\"),has(\"columns\"),has(\"fallback\")] | any | not)" >/dev/null || fail "View 出现禁用字段名"; VID=$(echo "$V" | jq -r ".data.view_id"); psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_view_prefs WHERE id = '"'"'$VID'"'"' AND table_id = '"'"'$TID'"'"' AND org_id = '"'"'$ORGA_TENANT_ID'"'"' AND member_id = '"'"'$ALICE_OPENID'"'"' AND created_at > NOW() - make_interval(mins => 5)" | grep -qx 1 || fail "视图未落 db_view_prefs 或三键不对"; bash "$S" --fixture-down; echo OK'
  期望: OK

### Step2 — 按文本字段筛 + 按数字字段排（上位合同 A20 前两件，⚠️ 接缝 S3-4）

- [ ] [BEHAVIOR] 三行数据（number = 9 / 10 / 2，text = 甲一 / 乙二 / 甲三）下：`filter` 按文本 `contains 甲` → 返回集合恰 2 行且逐行含「甲」；`sort` 按数字升序是**数值序** `[2,9]`（字典序会把 `10` 排 `9` 前）；全集升序为 `[2,9,10]`；**不传参数时响应与 Sprint B 逐字同形**（`rows[]` keys 仍恰好 6 个）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; TID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"WB-C2-$SFX\",\"visibility\":\"org\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"); FLD=$(curl -sf -b "$COOKIE_A" "$API/tables/$TID/fields"); FT=$(printf "%s" "$FLD" | jq -r ".data.fields[]|select(.field_type==\"text\")|.field_id"); FN=$(printf "%s" "$FLD" | jq -r ".data.fields[]|select(.field_type==\"number\")|.field_id"); I=0; for PAIR in "甲一:9" "乙二:10" "甲三:2"; do T=${PAIR%%:*}; N=${PAIR##*:}; RID=$(curl -sf -b "$COOKIE_A" -X POST "$API/tables/$TID/rows" | jq -r ".data.row_id"); curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X PATCH "$API/rows/$RID" -d "{\"version\":1,\"data\":{\"$FT\":\"$T\",\"$FN\":$N}}" >/dev/null || fail "预置行失败"; I=$((I+1)); done; R=$(curl -sf -b "$COOKIE_A" --get "$API/tables/$TID/rows" --data-urlencode "filter=[{\"field_id\":\"$FT\",\"op\":\"contains\",\"value\":\"甲\"}]" --data-urlencode "sort=[{\"field_id\":\"$FN\",\"dir\":\"asc\"}]") || fail "带筛排的 rows 非 2xx"; echo "$R" | jq -e --arg ft "$FT" --arg fn "$FN" "(.data.rows|length)==2 and .data.total==2 and ([.data.rows[].data[\$ft]]|all(contains(\"甲\"))) and [.data.rows[].data[\$fn]]==[2,9]" >/dev/null || fail "筛排结果不符 r=$R"; ALL=$(curl -sf -b "$COOKIE_A" --get "$API/tables/$TID/rows" --data-urlencode "sort=[{\"field_id\":\"$FN\",\"dir\":\"asc\"}]"); echo "$ALL" | jq -e --arg fn "$FN" "[.data.rows[].data[\$fn]]==[2,9,10]" >/dev/null || fail "数字升序不是数值序（字典序会把 10 排到 9 前）"; BASE=$(curl -sf -b "$COOKIE_A" "$API/tables/$TID/rows"); echo "$BASE" | jq -e "(.data|keys)==[\"row_limit\",\"rows\",\"total\"] and .data.total==3 and ((.data.rows[0]|keys)==[\"created_at\",\"data\",\"row_id\",\"row_order\",\"updated_at\",\"version\"])" >/dev/null || fail "不传参数时响应形状被改动了（Sprint B 回归）"; bash "$S" --fixture-down; echo OK'
  期望: OK

### Step3 — 切看板 + 单选字段分组 + 未分组列（上位合同 A20 后两件；判定点 JC3）

- [ ] [BEHAVIOR] `PATCH /views/:id {view_type:"kanban", group_field_id:<single_select>}` → 200、`degraded==false`、库中 `prefs` 的 `view_type`/`group_field_id` 逐字相符；**四行**前置真造出来（有值「甲」/ 值为 `null` / **缺键** / 空串各一行，psql 逐态点名核对，三态各恰 1 行）；**未分组归属由产品代码判定**——真跑本刀 vitest 的「未分组三态」纯函数用例（`groupRowsByField` 三态各一行 + 有值行，断言分列结果逐字），零通过或有失败即 FAIL
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; [ -n "$PG" ] || fail "缺 PG"; API="http://localhost:$API_PORT/api/knowledge/db"; TID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"WB-C3-$SFX\",\"visibility\":\"org\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"); FS=$(curl -sf -b "$COOKIE_A" "$API/tables/$TID/fields" | jq -r ".data.fields[]|select(.field_type==\"single_select\")|.field_id"); VID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TID/views" -d "{\"name\":\"看板\",\"view_type\":\"grid\",\"is_active\":true}" | jq -r ".data.view_id"); curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X PATCH "$API/views/$VID" -d "{\"view_type\":\"kanban\",\"group_field_id\":\"$FS\"}" | jq -e --arg fs "$FS" ".data.view_type==\"kanban\" and .data.group_field_id==\$fs and .data.degraded==false" >/dev/null || fail "切看板+分组未生效"; psql "$PG" -t -A -q -c "SELECT (prefs->>\$\$view_type\$\$) || \$\$|\$\$ || (prefs->>\$\$group_field_id\$\$) FROM zenithjoy.db_view_prefs WHERE id = \$\$$VID\$\$" | grep -qx "kanban|$FS" || fail "prefs 未逐字落库"; R1=$(curl -sf -b "$COOKIE_A" -X POST "$API/tables/$TID/rows" | jq -r ".data.row_id"); curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X PATCH "$API/rows/$R1" -d "{\"version\":1,\"data\":{\"$FS\":\"甲\"}}" >/dev/null || fail "预置有值行失败"; R2=$(curl -sf -b "$COOKIE_A" -X POST "$API/tables/$TID/rows" | jq -r ".data.row_id"); psql "$PG" -q -c "UPDATE zenithjoy.db_rows SET data = jsonb_set(data, ARRAY[\$\$$FS\$\$], \$\$null\$\$::jsonb) WHERE id = \$\$$R2\$\$" >/dev/null || fail "预置 null 行失败"; R3=$(curl -sf -b "$COOKIE_A" -X POST "$API/tables/$TID/rows" | jq -r ".data.row_id"); R4=$(curl -sf -b "$COOKIE_A" -X POST "$API/tables/$TID/rows" | jq -r ".data.row_id"); psql "$PG" -q -c "UPDATE zenithjoy.db_rows SET data = jsonb_set(data, ARRAY[\$\$$FS\$\$], to_jsonb(\$\$\$\$::text)) WHERE id = \$\$$R4\$\$" >/dev/null || fail "预置空串行失败"; NUL=$(psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_rows r WHERE r.table_id = \$\$$TID\$\$ AND r.data -> \$\$$FS\$\$ = \$\$null\$\$::jsonb"); MIS=$(psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_rows r WHERE r.table_id = \$\$$TID\$\$ AND NOT (r.data ? \$\$$FS\$\$)"); EMP=$(psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_rows r WHERE r.table_id = \$\$$TID\$\$ AND r.data ? \$\$$FS\$\$ AND char_length(r.data ->> \$\$$FS\$\$) = 0"); [ "${NUL:-0}" = "1" ] || fail "null 态未造出（实得 ${NUL:-0} 行）"; [ "${MIS:-0}" = "1" ] || fail "缺键态未造出（实得 ${MIS:-0} 行）"; [ "${EMP:-0}" = "1" ] || fail "空串态未造出（实得 ${EMP:-0} 行）"; O=/tmp/wb-ungrouped.json; rm -f "$O"; (cd apps/api && npx vitest run --config vitest.workbench-views.config.ts -t "未分组三态" --reporter=json --outputFile="$O") >/tmp/wb-ungrouped.log 2>&1; RC=$?; [ -f "$O" ] || { tail -20 /tmp/wb-ungrouped.log; fail "未分组三态用例零收集（-t 没匹配到）"; }; jq -e ".success == true and .numFailedTests == 0 and .numPassedTests >= 1" < "$O" >/dev/null || { jq -c "{passed:.numPassedTests,failed:.numFailedTests,ok:.success}" < "$O"; fail "groupRowsByField 三态用例未通过"; }; [ "$RC" = "0" ] || fail "未分组三态用例 vitest exit=$RC"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [ ] [BEHAVIOR] 变异证明（判据外置）：把 `groupRowsByField` 的未分组判据从三态改成**只判 `null`** 后，`--a20-only` 段必须 `exit ≠ 0`（缺键与空串的卡片凭空消失这件事有人红，判定点 JC3 的护栏非空）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --mutation-apply A20-ungrouped-null-only || exit 1; bash "$S" --a20-only; RC=$?; bash "$S" --mutation-revert A20-ungrouped-null-only; [ "$RC" -ne 0 ] || { echo "FAIL: 只判 null 下 A20 段仍 exit 0 —— 缺键/空串行的卡片凭空消失也没人红"; exit 1; }; echo OK'
  期望: OK

### Step4 — 分组列选非单选 → 400 可见报错（**controller concern C2 的落点**）

- [ ] [BEHAVIOR] 其余**七类**字段（`text`/`long_text`/`number`/`date`/`multi_select`/`person`/`url`）逐个做 `group_field_id` → 一律 **400 `GROUP_FIELD_TYPE_INVALID`**，且该视图 `prefs` **逐字未变**（不留半截状态）；**同一次运行内** `single_select` 正向 200（堵「一律 400」假绿）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; [ -n "$PG" ] || fail "缺 PG"; API="http://localhost:$API_PORT/api/knowledge/db"; TID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"WB-C4-$SFX\",\"visibility\":\"org\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"); FLD=$(curl -sf -b "$COOKIE_A" "$API/tables/$TID/fields"); VID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TID/views" -d "{\"name\":\"闸\",\"view_type\":\"grid\",\"is_active\":true}" | jq -r ".data.view_id"); BEFORE=$(psql "$PG" -t -A -q -c "SELECT prefs::text FROM zenithjoy.db_view_prefs WHERE id = '"'"'$VID'"'"'"); for T in text long_text number date multi_select person url; do FID=$(printf "%s" "$FLD" | jq -r --arg t "$T" ".data.fields[]|select(.field_type==\$t)|.field_id"); [ -n "$FID" ] && [ "$FID" != "null" ] || fail "$T 字段缺 field_id"; C=$(curl -s -o /tmp/wb-c4.json -w "%{http_code}" -b "$COOKIE_A" -H "Content-Type: application/json" -X PATCH "$API/views/$VID" -d "{\"view_type\":\"kanban\",\"group_field_id\":\"$FID\"}"); [ "$C" = "400" ] || fail "$T 做分组返 $C（应 400；multi_select 按 controller C2 也必须 400）"; jq -e ".error.code == \"GROUP_FIELD_TYPE_INVALID\"" < /tmp/wb-c4.json >/dev/null || fail "$T 的错误码不是 GROUP_FIELD_TYPE_INVALID"; done; AFTER=$(psql "$PG" -t -A -q -c "SELECT prefs::text FROM zenithjoy.db_view_prefs WHERE id = '"'"'$VID'"'"'"); [ "$BEFORE" = "$AFTER" ] || fail "400 却改了库（留半截状态）$BEFORE -> $AFTER"; FS=$(printf "%s" "$FLD" | jq -r ".data.fields[]|select(.field_type==\"single_select\")|.field_id"); curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X PATCH "$API/views/$VID" -d "{\"view_type\":\"kanban\",\"group_field_id\":\"$FS\"}" | jq -e --arg fs "$FS" ".data.group_field_id==\$fs" >/dev/null || fail "正向 single_select 未 200（一律 400 的假绿）"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [ ] [BEHAVIOR] 变异证明（判据外置）：摘掉分组字段类型判断后，`--a20-only` 段必须 `exit ≠ 0`
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --mutation-apply A20-group-type-nocheck || exit 1; bash "$S" --a20-only; RC=$?; bash "$S" --mutation-revert A20-group-type-nocheck; [ "$RC" -ne 0 ] || { echo "FAIL: 类型闸摘掉后 A20 段仍 exit 0 —— 分组类型闸是空的"; exit 1; }; echo OK'
  期望: OK

### Step5 — 拖卡换列落库（上位合同 A24 前半，⚠️ 接缝 S3-1/S3-3）

- [ ] [BEHAVIOR] 真浏览器**真指针拖拽**把卡片从「甲」列拖到「乙」列 → 目标列出现该卡、库中该行分组值变为「乙」且 `version` +1、**同表其余行 `data` 逐字未变**、刷新后仍在新列（判据 = windows job 的 conclusion + 「看板拖卡」step 真跑）
  Test: manual:bash -c 'WF=e2e-knowledge-hub-path3.yml; B=$(git rev-parse --abbrev-ref HEAD); R=$(gh run list --workflow "$WF" --branch "$B" --limit 1 --json databaseId,headSha,url) || { echo "FAIL: gh run list 失败"; exit 1; }; ID=$(echo "$R" | jq -r ".[0].databaseId"); [ -n "$ID" ] && [ "$ID" != "null" ] || { echo "FAIL: 分支 $B 上无 $WF 运行记录 —— 本刀 spec 成了孤儿"; exit 1; }; [ "$(echo "$R" | jq -r ".[0].headSha")" = "$(git rev-parse HEAD)" ] || { echo "FAIL: 拿到的是陈旧 run —— headSha 与本地 HEAD 不一致，上一次 push 的绿 run 冒充本轮"; exit 1; }; J=$(gh run view "$ID" --json jobs); echo "$J" | jq -e "[.jobs[] | select(.name | test(\"windows\")) | select(.conclusion == \"success\")] | length > 0" >/dev/null || { echo "FAIL: windows job 未成功（skipped 也算 FAIL）"; echo "$J" | jq -r ".jobs[] | \"  job=\(.name) conclusion=\(.conclusion)\""; exit 1; }; echo "$J" | jq -e "[.jobs[] | select(.name | test(\"windows\")) | .steps[] | select(.name | test(\"看板拖卡\")) | select(.conclusion == \"success\")] | length > 0" >/dev/null || { echo "FAIL: windows job 里没有跑看板拖卡那一段"; echo "$J" | jq -r "[.jobs[] | select(.name | test(\"windows\")) | .steps[].name] | @csv"; exit 1; }; echo OK'
  期望: OK

- [ ] [BEHAVIOR] 拖卡→写回的行映射是**被拖那一行**：纯函数 `resolveDropPatch` 单测真跑通过；变异 `A24-drag-wrong-row`（恒返 `rows[0]`）施加后 `--a24-pure-only` 必须 `exit ≠ 0`（拖卡改错行的守卫非空）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --a24-pure-only || { echo "FAIL: 拖卡纯函数单测本身没过"; exit 1; }; bash "$S" --mutation-apply A24-drag-wrong-row || exit 1; bash "$S" --a24-pure-only; RC=$?; bash "$S" --mutation-revert A24-drag-wrong-row; [ "$RC" -ne 0 ] || { echo "FAIL: 恒返 rows[0] 下拖卡纯函数段仍 exit 0 —— 拖到哪张卡都改第一行也没人红"; exit 1; }; echo OK'
  期望: OK

### Step6 — 拖卡失败弹回 + 并发 409（上位合同 A24 后半 / J2；⚠️ 接缝 S3-3）

- [ ] [BEHAVIOR] 双会话同基线 `version` 真并发改同一行的分组格 → 恰一个 200 一个 **409 `ROW_VERSION_CONFLICT`**，库中该格 = 先提交者写的值（不是后写的赢），`version` 恰 +1
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; [ -n "$PG" ] || fail "缺 PG"; API="http://localhost:$API_PORT/api/knowledge/db"; TID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"WB-C6-$SFX\",\"visibility\":\"org\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"); FS=$(curl -sf -b "$COOKIE_A" "$API/tables/$TID/fields" | jq -r ".data.fields[]|select(.field_type==\"single_select\")|.field_id"); RID=$(curl -sf -b "$COOKIE_A" -X POST "$API/tables/$TID/rows" | jq -r ".data.row_id"); VN=$(curl -sf -b "$COOKIE_A" "$API/tables/$TID/rows" | jq -r ".data.rows[0].version"); curl -s -o /tmp/wb-c6a.json -w "%{http_code}" -b "$COOKIE_A" -H "Content-Type: application/json" -X PATCH "$API/rows/$RID" -d "{\"version\":$VN,\"data\":{\"$FS\":\"甲\"}}" > /tmp/wb-c6a.code & curl -s -o /tmp/wb-c6b.json -w "%{http_code}" -b "$COOKIE_A2" -H "Content-Type: application/json" -X PATCH "$API/rows/$RID" -d "{\"version\":$VN,\"data\":{\"$FS\":\"乙\"}}" > /tmp/wb-c6b.code & wait; C1=$(cat /tmp/wb-c6a.code); C2=$(cat /tmp/wb-c6b.code); [ "$(printf "%s\n%s\n" "$C1" "$C2" | grep -c "^200$")" = "1" ] || fail "并发结果不是恰一个 200（$C1 / $C2）"; [ "$(printf "%s\n%s\n" "$C1" "$C2" | grep -c "^409$")" = "1" ] || fail "并发结果不是恰一个 409（$C1 / $C2）—— 静默覆盖"; if [ "$C1" = "200" ]; then LOSER=/tmp/wb-c6b.json; WINV="甲"; else LOSER=/tmp/wb-c6a.json; WINV="乙"; fi; jq -e ".error.code == \"ROW_VERSION_CONFLICT\"" < "$LOSER" >/dev/null || fail "409 体错误码不是 ROW_VERSION_CONFLICT"; GOT=$(psql "$PG" -t -A -q -c "SELECT r.data ->> '"'"'$FS'"'"' FROM zenithjoy.db_rows r WHERE r.id = '"'"'$RID'"'"'"); [ "$GOT" = "$WINV" ] || fail "库中值=$GOT 不等于先提交者的 $WINV（静默覆盖）"; psql "$PG" -t -A -q -c "SELECT r.version FROM zenithjoy.db_rows r WHERE r.id = '"'"'$RID'"'"'" | grep -qx "$((VN+1))" || fail "version 未恰好 +1"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [ ] [BEHAVIOR] 真浏览器真断网下拖卡 → 卡片**弹回原列**（两列卡片数逐字复原）+ `kanban-drop-error` 可见，绝不停在假位置（判据 = windows job 的「看板拖卡」step 真跑 + 该 step 产出弹回截图）
  Test: manual:bash -c 'WF=e2e-knowledge-hub-path3.yml; B=$(git rev-parse --abbrev-ref HEAD); R=$(gh run list --workflow "$WF" --branch "$B" --limit 1 --json databaseId,headSha) || { echo "FAIL: gh run list 失败"; exit 1; }; ID=$(echo "$R" | jq -r ".[0].databaseId"); [ -n "$ID" ] && [ "$ID" != "null" ] || { echo "FAIL: 分支 $B 上无 $WF 运行记录"; exit 1; }; [ "$(echo "$R" | jq -r ".[0].headSha")" = "$(git rev-parse HEAD)" ] || { echo "FAIL: 拿到的是陈旧 run —— headSha 与本地 HEAD 不一致，上一次 push 的绿 run 冒充本轮"; exit 1; }; J=$(gh run view "$ID" --json jobs); echo "$J" | jq -e "[.jobs[] | select(.name | test(\"windows\")) | .steps[] | select(.name | test(\"看板拖卡\")) | select(.conclusion == \"success\")] | length > 0" >/dev/null || { echo "FAIL: windows job 里没有跑看板拖卡那一段"; exit 1; }; D=$(mktemp -d); gh run download "$ID" -n path3-views-screenshots -D "$D" || { echo "FAIL: 下不到截图 artifact path3-views-screenshots"; exit 1; }; F=$(find "$D" -name "03-kanban-revert.png" | head -1); [ -n "$F" ] && [ -s "$F" ] || { echo "FAIL: artifact 里没有非空的弹回截图 03-kanban-revert.png"; exit 1; }; echo OK'
  期望: OK

### Step7/Step8 — 视图配置持久化（存 `field_id`）+ 换会话逐项一致 + 改显示名不失效（上位合同 A21）

- [ ] [BEHAVIOR] 设好筛+排+分组+隐藏列后：库中 `prefs` 里出现 `field_id` 而**字段显示名零命中**；同一员工换一个真会话重新 GET，`views` 经 `jq -S` 归一化后**逐字节全等**；把该字段的 `db_fields.name` 改掉后再 GET，`group_field_id` 不变且 `degraded==false`（改显示名视图不失效）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; [ -n "$PG" ] || fail "缺 PG"; API="http://localhost:$API_PORT/api/knowledge/db"; TID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"WB-C7-$SFX\",\"visibility\":\"org\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"); FLD=$(curl -sf -b "$COOKIE_A" "$API/tables/$TID/fields"); FS=$(printf "%s" "$FLD" | jq -r ".data.fields[]|select(.field_type==\"single_select\")|.field_id"); FN=$(printf "%s" "$FLD" | jq -r ".data.fields[]|select(.field_type==\"number\")|.field_id"); FT=$(printf "%s" "$FLD" | jq -r ".data.fields[]|select(.field_type==\"text\")|.field_id"); VID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TID/views" -d "{\"name\":\"我的视图\",\"view_type\":\"kanban\",\"group_field_id\":\"$FS\",\"sorts\":[{\"field_id\":\"$FN\",\"dir\":\"desc\"}],\"filters\":[{\"field_id\":\"$FT\",\"op\":\"contains\",\"value\":\"甲\"}],\"hidden_field_ids\":[\"$FN\"],\"is_active\":true}" | jq -r ".data.view_id"); [ -n "$VID" ] && [ "$VID" != "null" ] || fail "建视图失败"; P=$(psql "$PG" -t -A -q -c "SELECT prefs::text FROM zenithjoy.db_view_prefs WHERE id = '"'"'$VID'"'"'"); printf "%s" "$P" | grep -q "$FS" || fail "prefs 里没有 field_id"; printf "%s" "$P" | grep -q "字段-single_select" && fail "prefs 里存了字段显示名（改名即失效）"; V1=$(curl -sf -b "$COOKIE_A" "$API/tables/$TID/views" | jq -S ".data.views"); NEW=$(curl -sf -D - -o /dev/null -X POST "http://localhost:$API_PORT/api/staff/feishu-login" -H "Content-Type: application/json" -d "{\"code\":\"wb-code-$ALICE_OPENID\"}" | grep -i "^set-cookie:" | sed "s/^[Ss]et-[Cc]ookie: *//" | cut -d";" -f1 | paste -sd";" -); [ -n "$NEW" ] || fail "第二次会话签发失败"; psql "$PG" -q -c "UPDATE zenithjoy.db_fields SET name = '"'"'改过名的单选'"'"' WHERE id = '"'"'$FS'"'"'" >/dev/null || fail "改显示名失败"; V2=$(curl -sf -b "$NEW" "$API/tables/$TID/views" | jq -S ".data.views"); [ "$V1" = "$V2" ] || fail "换会话/改显示名后视图不逐项一致"; printf "%s" "$V2" | jq -e --arg fs "$FS" ".[0].group_field_id==\$fs and .[0].degraded==false and .[0].view_type==\"kanban\" and (.[0].hidden_field_ids|length)==1" >/dev/null || fail "视图逐项未复原"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [ ] [BEHAVIOR] 视图三种写动作（建/改/删）**全部落 `db_audit`** 且归属会话组织（5 分钟时间窗防历史行冒充）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; [ -n "$PG" ] || fail "缺 PG"; API="http://localhost:$API_PORT/api/knowledge/db"; TID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"WB-CAUD-$SFX\",\"visibility\":\"org\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"); V1=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TID/views" -d "{\"name\":\"甲视图\",\"view_type\":\"grid\",\"is_active\":true}" | jq -r ".data.view_id"); V2=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TID/views" -d "{\"name\":\"乙视图\",\"view_type\":\"grid\"}" | jq -r ".data.view_id"); curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X PATCH "$API/views/$V1" -d "{\"name\":\"改名后\"}" >/dev/null || fail "改视图失败"; curl -sf -b "$COOKIE_A" -X DELETE "$API/views/$V2" >/dev/null || fail "删视图失败"; N=$(psql "$PG" -t -A -q -c "SELECT count(DISTINCT a.action) FROM zenithjoy.db_audit a WHERE a.org_id = '"'"'$ORGA_TENANT_ID'"'"' AND a.action IN ('"'"'create_view'"'"','"'"'update_view'"'"','"'"'delete_view'"'"') AND a.created_at > NOW() - make_interval(mins => 5)"); [ "${N:-0}" = "3" ] || fail "db_audit 三个视图动作只落了 ${N:-0} 种"; bash "$S" --fixture-down; echo OK'
  期望: OK

### Step9 — 删视图只删偏好 + 至少保留一个（PRD 边界情况）

- [ ] [BEHAVIOR] 建 2 个视图后删掉一个 → 200 且 `data` keys **恰好** `["deleted_view_id","remaining"]`、`remaining==1`；`db_tables`/`db_fields`/`db_rows` 三表内容摘要 **md5 前后全等**；再删最后一个 → **400 `LAST_VIEW_PROTECTED`** 且该 prefs 行仍在
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; [ -n "$PG" ] || fail "缺 PG"; API="http://localhost:$API_PORT/api/knowledge/db"; SNAP="SELECT md5(string_agg(x, '"'"'|'"'"')) FROM (SELECT t.id::text || t.name AS x FROM zenithjoy.db_tables t WHERE t.org_id = '"'"'$ORGA_TENANT_ID'"'"' UNION ALL SELECT f.id::text || f.name FROM zenithjoy.db_fields f WHERE f.org_id = '"'"'$ORGA_TENANT_ID'"'"' UNION ALL SELECT r.id::text || r.data::text FROM zenithjoy.db_rows r WHERE r.org_id = '"'"'$ORGA_TENANT_ID'"'"' ORDER BY 1) s"; TID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"WB-C9-$SFX\",\"visibility\":\"org\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"); curl -sf -b "$COOKIE_A" -X POST "$API/tables/$TID/rows" >/dev/null || fail "建行失败"; V1=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TID/views" -d "{\"name\":\"视图一\",\"view_type\":\"grid\",\"is_active\":true}" | jq -r ".data.view_id"); V2=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TID/views" -d "{\"name\":\"视图二\",\"view_type\":\"grid\"}" | jq -r ".data.view_id"); S0=$(psql "$PG" -t -A -q -c "$SNAP"); curl -sf -b "$COOKIE_A" -X DELETE "$API/views/$V2" | jq -e "(.data|keys)==[\"deleted_view_id\",\"remaining\"] and .data.remaining==1 and .data.deleted_view_id==\"$V2\"" >/dev/null || fail "删视图响应形状不符"; S1=$(psql "$PG" -t -A -q -c "$SNAP"); [ "$S0" = "$S1" ] || fail "删视图动了表/字段/行（$S0 -> $S1）"; psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_view_prefs WHERE id = '"'"'$V2'"'"'" | grep -qx 0 || fail "偏好行未真删"; C=$(curl -s -o /tmp/wb-c9.json -w "%{http_code}" -b "$COOKIE_A" -X DELETE "$API/views/$V1"); [ "$C" = "400" ] || fail "删最后一个视图返 $C（应 400）"; jq -e ".error.code == \"LAST_VIEW_PROTECTED\"" < /tmp/wb-c9.json >/dev/null || fail "错误码不是 LAST_VIEW_PROTECTED"; psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_view_prefs WHERE id = '"'"'$V1'"'"'" | grep -qx 1 || fail "被拒的删除却把行删了"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [ ] [BEHAVIOR] 变异证明（判据外置）：摘掉「至少保留一个视图」判断后，`--view-delete-only` 段必须 `exit ≠ 0`
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --mutation-apply VIEW-lastview-off || exit 1; bash "$S" --view-delete-only; RC=$?; bash "$S" --mutation-revert VIEW-lastview-off; [ "$RC" -ne 0 ] || { echo "FAIL: 允许删空后该段仍 exit 0 —— 员工能把自己删到没有任何视图也没人红"; exit 1; }; echo OK'
  期望: OK

### Step10 — 视图偏好保存失败可见（上位合同 A23，⚠️ 接缝 S3-2）

- [ ] [BEHAVIOR] 真浏览器真断网下改视图配置 → `view-prefs-error` 可见且文案含「视图偏好未保存」、页面主体不白屏、恢复网络后就地重试成功（判据 = windows job 的「视图偏好」step 真跑 + 该段截图非空）
  Test: manual:bash -c 'WF=e2e-knowledge-hub-path3.yml; B=$(git rev-parse --abbrev-ref HEAD); R=$(gh run list --workflow "$WF" --branch "$B" --limit 1 --json databaseId,headSha) || { echo "FAIL: gh run list 失败"; exit 1; }; ID=$(echo "$R" | jq -r ".[0].databaseId"); [ -n "$ID" ] && [ "$ID" != "null" ] || { echo "FAIL: 分支 $B 上无 $WF 运行记录"; exit 1; }; [ "$(echo "$R" | jq -r ".[0].headSha")" = "$(git rev-parse HEAD)" ] || { echo "FAIL: 拿到的是陈旧 run —— headSha 与本地 HEAD 不一致，上一次 push 的绿 run 冒充本轮"; exit 1; }; J=$(gh run view "$ID" --json jobs); echo "$J" | jq -e "[.jobs[] | select(.name | test(\"windows\")) | .steps[] | select(.name | test(\"视图偏好\")) | select(.conclusion == \"success\")] | length > 0" >/dev/null || { echo "FAIL: windows job 里没有跑视图偏好那一段"; echo "$J" | jq -r "[.jobs[] | select(.name | test(\"windows\")) | .steps[].name] | @csv"; exit 1; }; D=$(mktemp -d); gh run download "$ID" -n path3-views-screenshots -D "$D" || { echo "FAIL: 下不到截图 artifact"; exit 1; }; F=$(find "$D" -name "05-view-prefs-error.png" | head -1); [ -n "$F" ] && [ -s "$F" ] || { echo "FAIL: artifact 里没有非空的 05-view-prefs-error.png"; exit 1; }; echo OK'
  期望: OK

### Step11 — 视图层跨组织正反双向 + 反查两分支（上位合同 A22 / A34 第五层）

- [ ] [BEHAVIOR] `db_view_prefs` 是 `(org_id, table_id, member_id)` 三键，隔离必须**三层同一次运行内成对**：① 他企业（丙 `COOKIE_B`）对 A 企业 `view_id` 的 `GET`/`PATCH`/`DELETE` 一律 404 且与随机 uuid **md5 全等** ② **同组织他人**（乙 `COOKIE_A2`，同 org 不同 member）对甲的 `view_id` 的 `GET`/`PATCH`/`DELETE` **同样 404 同形**、`GET /tables/:id/views` 里**零命中**甲的视图 ③ 本人（甲）四个视图端点全部 2xx 且拿到自己的数据；三层跑完甲的 prefs 串逐字未变
  Test: manual:bash .github/workflows/scripts/smoke/structured-workbench-smoke.sh --a1-a3-views-only
  期望: exit 0

- [ ] [BEHAVIOR] 变异证明（判据外置，**两个开关各摘一维**，防「摘 member 条件时另一维替它挡住」的半边空守卫）：`A1V-view-org-bypass`（去掉 `org_id` 条件）与 `A1V-view-member-bypass`（去掉 `member_id` 条件）**逐个**施加后，`--a1-a3-views-only` 段都必须 `exit ≠ 0`
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; for M in A1V-view-org-bypass A1V-view-member-bypass; do bash "$S" --mutation-apply "$M" || exit 1; bash "$S" --a1-a3-views-only; RC=$?; bash "$S" --mutation-revert "$M"; [ "$RC" -ne 0 ] || { echo "FAIL: 摘掉 $M 那一维后视图隔离段仍 exit 0 —— 该维隔离断言是空的"; exit 1; }; done; echo OK'
  期望: OK

- [ ] [BEHAVIOR] 反查两分支（合同 J6）：① 视图引用的字段被删掉后 `GET views` 返 **200**、该视图 `group_field_id` 为 `null`、失效 id 从 `sorts`/`filters`/`hidden_field_ids` 里被剔除、**`degraded==true`**（非白屏非 5xx）；② A 企业 `PATCH` 自己的视图但 `group_field_id` 传 **B 企业真实存在的 field_id** → **404 且体与随机 uuid 逐字节相同**
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; [ -n "$PG" ] || fail "缺 PG"; H(){ openssl dgst -md5 < "$1" | awk "{print \$NF}"; }; API="http://localhost:$API_PORT/api/knowledge/db"; TID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"WB-C11-$SFX\",\"visibility\":\"org\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"); FLD=$(curl -sf -b "$COOKIE_A" "$API/tables/$TID/fields"); FS=$(printf "%s" "$FLD" | jq -r ".data.fields[]|select(.field_type==\"single_select\")|.field_id"); FN=$(printf "%s" "$FLD" | jq -r ".data.fields[]|select(.field_type==\"number\")|.field_id"); VID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TID/views" -d "{\"name\":\"降级验\",\"view_type\":\"kanban\",\"group_field_id\":\"$FS\",\"sorts\":[{\"field_id\":\"$FS\",\"dir\":\"asc\"}],\"hidden_field_ids\":[\"$FS\",\"$FN\"],\"is_active\":true}" | jq -r ".data.view_id"); [ -n "$VID" ] && [ "$VID" != "null" ] || fail "建视图失败"; psql "$PG" -q -c "DELETE FROM zenithjoy.db_fields WHERE id = '"'"'$FS'"'"'" >/dev/null || fail "删字段（前置状态）失败"; G=$(curl -s -o /tmp/wb-c11a.json -w "%{http_code}" -b "$COOKIE_A" "$API/tables/$TID/views"); [ "$G" = "200" ] || fail "已删字段的视图反查返 $G（应 200 降级，非 5xx 非白屏）"; jq -e --arg v "$VID" --arg fs "$FS" --arg fn "$FN" "(.data.views | map(select(.view_id == \$v)) | length) == 1 and (.data.views[] | select(.view_id == \$v) | .group_field_id == null and .degraded == true and ([.sorts[].field_id] | index(\$fs) | not) and ([.hidden_field_ids[]] | index(\$fs) | not) and ([.hidden_field_ids[]] | index(\$fn)))" < /tmp/wb-c11a.json >/dev/null || fail "降级不彻底（失效 id 未剔除 / degraded 未置 true / 未失效的 id 被误删）"; BT=$(curl -sf -b "$COOKIE_B" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"WB-C11B-$SFX\",\"visibility\":\"org\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"); BFID=$(curl -sf -b "$COOKIE_B" "$API/tables/$BT/fields" | jq -r ".data.fields[]|select(.field_type==\"single_select\")|.field_id"); [ -n "$BFID" ] && [ "$BFID" != "null" ] || fail "B 企业字段种子失败"; RND=$(uuidgen | tr "A-Z" "a-z"); C1=$(curl -s -o /tmp/wb-c11b.json -w "%{http_code}" -b "$COOKIE_A" -H "Content-Type: application/json" -X PATCH "$API/views/$VID" -d "{\"group_field_id\":\"$BFID\"}"); C2=$(curl -s -o /tmp/wb-c11c.json -w "%{http_code}" -b "$COOKIE_A" -H "Content-Type: application/json" -X PATCH "$API/views/$VID" -d "{\"group_field_id\":\"$RND\"}"); [ "$C1" = "404" ] || fail "他企业 field_id 写入返 $C1（应 404）"; [ "$C2" = "404" ] || fail "随机 uuid field_id 返 $C2（应 404）"; [ "$(H /tmp/wb-c11b.json)" = "$(H /tmp/wb-c11c.json)" ] || fail "两个 404 体不同 —— 可比对字节分辨他企业 field 是否真实存在"; bash "$S" --fixture-down; echo OK'
  期望: OK

### Step12 — `field_id` 白名单：用户输入永不进标识符位（上位合同 A25，proven-to-fire）

- [ ] [BEHAVIOR] **标识符位三处全对抗**（`field_id` / `dir` / `op`）：① `sort`+`filter` 的 `field_id` 逐个传原始 SQL 片段（`id; DROP TABLE zenithjoy.db_rows; --` / `1) OR 1=1 --` / `data->>'x'`，**逐字保留引号**，用 `jq -nc` 构造绕开引号地狱）→ 一律 **400 `VALIDATION_FAILED`** ② **`dir` 落在 `ORDER BY … ASC|DESC` 关键字位**，用合法 `field_id` + 坏 `dir`（`asc; DROP TABLE zenithjoy.db_rows; --` / `asc NULLS FIRST, 1` / `ASC--`）→ 一律 400 ③ 合法 `field_id` + 非白名单 `op` → 400；另传合法但不属本表的 UUID → **404**；全程零 5xx；`information_schema` 的 `zenithjoy` 表清单前后**全等**且 `db_rows` 总行数不减
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; [ -n "$PG" ] || fail "缺 PG"; API="http://localhost:$API_PORT/api/knowledge/db"; TID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"WB-C12-$SFX\",\"visibility\":\"org\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"); FT=$(curl -sf -b "$COOKIE_A" "$API/tables/$TID/fields" | jq -r ".data.fields[]|select(.field_type==\"text\")|.field_id"); [ -n "$FT" ] && [ "$FT" != "null" ] || fail "取 text field_id 失败"; curl -sf -b "$COOKIE_A" -X POST "$API/tables/$TID/rows" >/dev/null || fail "建行失败"; SNAPT="SELECT string_agg(t.table_name, \$\$,\$\$ ORDER BY t.table_name) FROM information_schema.tables t WHERE t.table_schema = \$\$zenithjoy\$\$"; T0=$(psql "$PG" -t -A -q -c "$SNAPT"); R0=$(psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_rows"); for I in 1 2 3; do case $I in 1) BAD="id; DROP TABLE zenithjoy.db_rows; --";; 2) BAD="1) OR 1=1 --";; 3) BAD="data->>'"'"'x'"'"'";; esac; SP=$(jq -nc --arg f "$BAD" "[{field_id:\$f,dir:\"asc\"}]"); CS=$(curl -s -o /tmp/wb-c12s.json -w "%{http_code}" -b "$COOKIE_A" --get "$API/tables/$TID/rows" --data-urlencode "sort=$SP"); [ "$CS" = "400" ] || fail "sort.field_id 传 SQL 片段返 $CS（应 400）payload=$BAD"; jq -e ".error.code == \"VALIDATION_FAILED\"" < /tmp/wb-c12s.json >/dev/null || fail "sort 片段错误码不是 VALIDATION_FAILED"; FP=$(jq -nc --arg f "$BAD" "[{field_id:\$f,op:\"contains\",value:\"x\"}]"); CF=$(curl -s -o /dev/null -w "%{http_code}" -b "$COOKIE_A" --get "$API/tables/$TID/rows" --data-urlencode "filter=$FP"); [ "$CF" = "400" ] || fail "filter.field_id 传 SQL 片段返 $CF（应 400）payload=$BAD"; done; for J in 1 2 3; do case $J in 1) BADD="asc; DROP TABLE zenithjoy.db_rows; --";; 2) BADD="asc NULLS FIRST, 1";; 3) BADD="ASC--";; esac; DP=$(jq -nc --arg f "$FT" --arg d "$BADD" "[{field_id:\$f,dir:\$d}]"); CD=$(curl -s -o /tmp/wb-c12d.json -w "%{http_code}" -b "$COOKIE_A" --get "$API/tables/$TID/rows" --data-urlencode "sort=$DP"); [ "$CD" = "400" ] || fail "dir 传 $BADD 返 $CD（应 400 —— dir 直接落 ORDER BY 关键字位）"; jq -e ".error.code == \"VALIDATION_FAILED\"" < /tmp/wb-c12d.json >/dev/null || fail "dir 片段错误码不是 VALIDATION_FAILED"; done; OP=$(jq -nc --arg f "$FT" "[{field_id:\$f,op:\"nosuchop\",value:\"x\"}]"); CO=$(curl -s -o /dev/null -w "%{http_code}" -b "$COOKIE_A" --get "$API/tables/$TID/rows" --data-urlencode "filter=$OP"); [ "$CO" = "400" ] || fail "非白名单 op 返 $CO（应 400，且禁 5xx）"; RND=$(uuidgen | tr "A-Z" "a-z"); UP=$(jq -nc --arg f "$RND" "[{field_id:\$f,dir:\"asc\"}]"); CU=$(curl -s -o /dev/null -w "%{http_code}" -b "$COOKIE_A" --get "$API/tables/$TID/rows" --data-urlencode "sort=$UP"); [ "$CU" = "404" ] || fail "跨表合法 UUID 返 $CU（应 404）"; T1=$(psql "$PG" -t -A -q -c "$SNAPT"); [ "$T0" = "$T1" ] || fail "information_schema 表清单变了 —— 用户输入进了标识符位"; R1=$(psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_rows"); [ "$R0" -le "$R1" ] || fail "db_rows 行数减少了（$R0 -> $R1）"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [ ] [BEHAVIOR] 变异证明（判据外置）：摘掉 `field_id` → 本表字段集的白名单映射后，`--a25-only` 段必须 `exit ≠ 0`
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --mutation-apply A25-field-whitelist-off || exit 1; bash "$S" --a25-only; RC=$?; bash "$S" --mutation-revert A25-field-whitelist-off; [ "$RC" -ne 0 ] || { echo "FAIL: 摘掉白名单后 A25 段仍 exit 0 —— 白名单守卫是空的"; exit 1; }; echo OK'
  期望: OK

### Step13 — 「指派给我」全局视图（PRD 范围内）

- [ ] [BEHAVIOR] `GET /assigned-to-me` 返 200 且 `data` keys **恰好** `["items"]`、item keys **恰好** `["field_id","row_id","table_id","table_name"]`；本人 person 格命中自己的行 1 条；**他企业**同 openid 值的行零命中；**同组织他人的 `private` 表**的行零命中（表级可见性延伸）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; mk(){ curl -sf -b "$1" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"$2\",\"visibility\":\"$3\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"; }; put(){ P=$(curl -sf -b "$1" "$API/tables/$2/fields" | jq -r ".data.fields[]|select(.field_type==\"person\")|.field_id"); R=$(curl -sf -b "$1" -X POST "$API/tables/$2/rows" | jq -r ".data.row_id"); curl -sf -b "$1" -H "Content-Type: application/json" -X PATCH "$API/rows/$R" -d "{\"version\":1,\"data\":{\"$P\":\"$ALICE_OPENID\"}}" >/dev/null || return 1; echo "$R"; }; TA=$(mk "$COOKIE_A" "WB-C13A-$SFX" org); RA=$(put "$COOKIE_A" "$TA") || fail "A 自己的行预置失败"; TP=$(mk "$COOKIE_A2" "WB-C13P-$SFX" private); RP=$(put "$COOKIE_A2" "$TP") || fail "同组织他人 private 行预置失败"; TB=$(mk "$COOKIE_B" "WB-C13B-$SFX" org); RB=$(put "$COOKIE_B" "$TB") || fail "他企业行预置失败"; OUT=$(curl -sf -b "$COOKIE_A" "$API/assigned-to-me") || fail "assigned-to-me 非 2xx"; echo "$OUT" | jq -e "(.data|keys)==[\"items\"]" >/dev/null || fail "data keys 不是恰好 items"; echo "$OUT" | jq -e --arg r "$RA" "[.data.items[]|select(.row_id==\$r)]|length==1" >/dev/null || fail "自己的行未命中"; echo "$OUT" | jq -e --arg r "$RA" "(.data.items[]|select(.row_id==\$r)|keys)==[\"field_id\",\"row_id\",\"table_id\",\"table_name\"]" >/dev/null || fail "item keys 不是恰好四个"; echo "$OUT" | jq -e --arg r "$RB" "[.data.items[]|select(.row_id==\$r)]|length==0" >/dev/null || fail "他企业的行泄漏进了指派给我"; echo "$OUT" | jq -e --arg r "$RP" "[.data.items[]|select(.row_id==\$r)]|length==0" >/dev/null || fail "同组织他人的 private 表的行泄漏了"; bash "$S" --fixture-down; echo OK'
  期望: OK

### Invariant 覆盖（PRD 铁律逐条映射）

- [ ] [BEHAVIOR] INV-1 [组织归属] 视图写入的 `org_id`/`member_id` 只来自会话：请求体塞 B 企业 `org_id`/`member_id` + 伪造 `X-Tenant-Id` 头建视图，落库归属仍是会话所属组织与会话 memberId，B 企业零新增 prefs 行
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; [ -n "$PG" ] || fail "缺 PG"; API="http://localhost:$API_PORT/api/knowledge/db"; TID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"WB-CINV1-$SFX\",\"visibility\":\"org\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"); B0=$(psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_view_prefs WHERE org_id = '"'"'$ORGB_TENANT_ID'"'"'"); VID=$(curl -sf -b "$COOKIE_A" -H "X-Tenant-Id: $ORGB_TENANT_ID" -H "Content-Type: application/json" -X POST "$API/tables/$TID/views" -d "{\"name\":\"越权视图\",\"view_type\":\"grid\",\"is_active\":true,\"org_id\":\"$ORGB_TENANT_ID\",\"tenant_id\":\"$ORGB_TENANT_ID\",\"member_id\":\"$CAROL_OPENID\"}" | jq -r ".data.view_id"); [ -n "$VID" ] && [ "$VID" != "null" ] || fail "建视图失败"; psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_view_prefs WHERE id = '"'"'$VID'"'"' AND org_id = '"'"'$ORGA_TENANT_ID'"'"' AND member_id = '"'"'$ALICE_OPENID'"'"'" | grep -qx 1 || fail "视图归属被请求体/请求头带偏"; B1=$(psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_view_prefs WHERE org_id = '"'"'$ORGB_TENANT_ID'"'"'"); [ "$B0" = "$B1" ] || fail "B 企业 prefs 行数变了（$B0 -> $B1）"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [ ] [BEHAVIOR] INV-2 [禁明文身份头] 路③ 全部源码（含本刀新增的视图 service / 看板组件 / 拖卡纯函数）七个字面量零命中，且本刀新增文件**全部在 A2 扫描域内**（漏登记一个 = 空集假绿）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --a2-only || { echo "FAIL: A2 静态守卫未过"; exit 1; }; SC=$(bash "$S" --a2-print-scope) || { echo "FAIL: --a2-print-scope 非 0"; exit 1; }; for f in $(git diff --name-only origin/main...HEAD -- apps/api/src apps/staff-hub/src | grep -E "\.(ts|tsx)$"); do [ -f "$f" ] || continue; grep -qE "/api/knowledge/db|[Ww]orkbench" "$f" || continue; printf "%s\n" "$SC" | grep -qxF "$f" || { echo "FAIL: 本刀新增的路③ 文件未进 A2 扫描域 $f"; exit 1; }; done; echo OK'
  期望: OK

- [ ] [BEHAVIOR] INV-7 [无运行时 DDL] + INV-8 [用户输入不进标识符位]：视图 `name` 与 `filter.value` 逐个注入五个对抗 payload（`<img src=x onerror=alert(1)>` / `__proto__` / `constructor` / `"; DROP TABLE db_view_prefs; --` / 超长 emoji 串）→ 全部作为**数据值**处理（201 或 400，**零 5xx**），`information_schema` 表清单前后全等，`db_view_prefs` 物理表仍在
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; [ -n "$PG" ] || fail "缺 PG"; API="http://localhost:$API_PORT/api/knowledge/db"; T0=$(psql "$PG" -t -A -q -c "SELECT string_agg(table_name, '"'"','"'"' ORDER BY table_name) FROM information_schema.tables WHERE table_schema = '"'"'zenithjoy'"'"'"); TID=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"WB-CADV-$SFX\",\"visibility\":\"org\",\"fields\":$EIGHT_FIELDS}" | jq -r ".data.table_id"); FT=$(curl -sf -b "$COOKIE_A" "$API/tables/$TID/fields" | jq -r ".data.fields[]|select(.field_type==\"text\")|.field_id"); for I in 1 2 3 4 5; do case $I in 1) P="<img src=x onerror=alert(1)>";; 2) P="__proto__";; 3) P="constructor";; 4) P="\"; DROP TABLE db_view_prefs; --";; 5) P="🧨🧨🧨";; esac; BODY=$(jq -nc --arg p "$P" --arg ft "$FT" "{name:\$p,view_type:\"grid\",filters:[{field_id:\$ft,op:\"contains\",value:\$p}]}"); C=$(curl -s -o /tmp/wb-cadv.json -w "%{http_code}" -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TID/views" -d "$BODY"); case "$C" in 201|400) ;; *) fail "对抗 payload 触发 $C（禁 5xx）：$P";; esac; done; N=$(psql "$PG" -t -A -q -c "SELECT count(*) FROM zenithjoy.db_view_prefs WHERE table_id = '"'"'$TID'"'"' AND prefs::text LIKE '"'"'%onerror%'"'"'"); [ "${N:-0}" -ge 1 ] || fail "注入串未作为数据值落进 prefs（被 400 拒掉当合规不算数）"; T1=$(psql "$PG" -t -A -q -c "SELECT string_agg(table_name, '"'"','"'"' ORDER BY table_name) FROM information_schema.tables WHERE table_schema = '"'"'zenithjoy'"'"'"); [ "$T0" = "$T1" ] || fail "information_schema 表清单变了"; psql "$PG" -t -A -q -c "SELECT to_regclass('"'"'zenithjoy.db_view_prefs'"'"') IS NOT NULL" | grep -qx t || fail "db_view_prefs 表没了"; bash "$S" --fixture-down; echo OK'
  期望: OK

- INV-3 [正向对照] → 已由 Step11 的 `--a1-a3-views-only` 条覆盖（反向 404 与正向 2xx 同一次运行内成对），另由 Step4 的「七类 400 + `single_select` 正向 200」在类型闸上复用同一范式，不另设条目
- INV-4 [禁静默覆盖] → 已由 Step6 两条（真并发 409 + 库值 = 先提交者）覆盖；拖卡不新增写入路径，直接复用 Sprint B 的 `A13-version-nocheck` 变异（该变异已在 `--mutation-list` 里，本刀不重复登记）
- INV-5 [禁静默吞失败] → 已由 Step10（windows job 真断网段 + 截图）与 ART-9（源码零空 catch、错误分支零整表重拉）覆盖
- INV-6 [软删可还原] → **N/A**：本刀不新增删除语义。视图偏好是用户偏好不是数据，PRD 边界情况原文即「删视图只删偏好记录」；表/行的软删由 Sprint A/B 的 A9/A16 及其变异（`A9-hard-delete` / `A16-row-hard-delete`）持有，本刀 Step9 的「三表 md5 前后全等」正是它的护栏
- INV-9 [ViewPrefs 存 field_id] → 已由 Step7/Step8 条（`prefs` 含 field_id、字段显示名零命中、改显示名后视图不失效）覆盖
- INV-10 [变异证明] → 已由五条变异条目覆盖（`A20-group-type-nocheck` / `VIEW-lastview-off` / `A1V-view-org-bypass` / `A25-field-whitelist-off` / `A24-drag-wrong-row`），判据一律外置
- INV-11 [禁写死环境假设值] → 已由 ART-9（前端零 `5000` 字面量）+ 接缝清单 S3-1（拖拽步进从元素 `boundingBox` 现算，不写死坐标）覆盖；Step2 里的 `[2,9,10]` 是**期望输出**不是环境假设值
- INV-12 [多端完整性] → **N/A**：本刀只有一个展示面（`apps/staff-hub` 浏览器端），不涉及多设备/多 OS 类型；`apps/dashboard` 一行不改，不构成第二端

## BEHAVIOR:E2E 条目

- [ ] [BEHAVIOR:E2E] 员工完整走完 S3 Golden Path，六张截图从 windows job 的 artifact 真取回（不认宿主机上手工塞的图），且 linux job 与 windows job 双双 success
  Test: manual:bash -c 'WF=e2e-knowledge-hub-path3.yml; B=$(git rev-parse --abbrev-ref HEAD); R=$(gh run list --workflow "$WF" --branch "$B" --limit 1 --json databaseId,headSha) || { echo "FAIL: gh run list 失败"; exit 1; }; ID=$(echo "$R" | jq -r ".[0].databaseId"); [ -n "$ID" ] && [ "$ID" != "null" ] || { echo "FAIL: 分支 $B 上无 $WF 运行记录"; exit 1; }; [ "$(echo "$R" | jq -r ".[0].headSha")" = "$(git rev-parse HEAD)" ] || { echo "FAIL: 拿到的是陈旧 run —— headSha 与本地 HEAD 不一致，上一次 push 的绿 run 冒充本轮"; exit 1; }; J=$(gh run view "$ID" --json jobs); for N in linux windows; do echo "$J" | jq -e --arg n "$N" "[.jobs[] | select(.name | test(\$n)) | select(.conclusion == \"success\")] | length > 0" >/dev/null || { echo "FAIL: $N job 未成功"; echo "$J" | jq -r ".jobs[] | \"  job=\(.name) conclusion=\(.conclusion)\""; exit 1; }; done; D=$(mktemp -d); gh run download "$ID" -n path3-views-screenshots -D "$D" || { echo "FAIL: 下不到本刀截图 artifact path3-views-screenshots"; exit 1; }; DST=sprints/08210012-workbench-sprintC-views/screenshots; mkdir -p "$DST"; find "$D" -name "*.png" -exec cp {} "$DST"/ \; ; N=$(find "$D" -name "*.png" | wc -l | tr -d " "); [ "$N" -ge 6 ] || { echo "FAIL: artifact 里只有 $N 张截图（需 >=6）"; exit 1; }; for f in $(find "$D" -name "*.png"); do [ -s "$f" ] || { echo "FAIL: 空截图 $f"; exit 1; }; done; echo OK'
  Screenshots:
    - 01-kanban-columns.png    期望：看板视图打开，列 = 该单选字段 `options` 全集 +「未分组」列；**「未分组」列里恰好 3 张卡**（值为 `null` / 缺键 / 空串三态各一张，JC3 的可视护栏），有值行在其选项列
    - 02-kanban-dragged.png    期望：卡片已落在「乙」列，原列不再有它；工具条无错误提示
    - 03-kanban-revert.png     期望：断网拖拽后卡片**回到原列**，可见错误提示「拖动未保存」，两列卡片数与拖前逐字相同
    - 04-grid-filter-sort.png  期望：表格视图按文本字段筛后只剩符合行、按数字字段升序排列（`2` 在 `9` 在 `10` 之前），被隐藏的列不在表头
    - 05-view-prefs-error.png  期望：断网改视图后工具条出现「视图偏好未保存」，页面主体仍在（不白屏），当前视图仍可交互
    - 06-kanban-conflict.png   期望：同事先改过该行后再拖同一张卡 → 卡片**回到原列**（该列卡片数与拖前逐字相同），可见提示「该行已被他人修改，你的改动未保存」（**409 分支，与 03 的断网分支是两条不同前端路径**）
  路径格式：sprints/08210012-workbench-sprintC-views/screenshots/<step>.png
  期望：OK，且六张图已从 artifact 落到 sprints/08210012-workbench-sprintC-views/screenshots/
