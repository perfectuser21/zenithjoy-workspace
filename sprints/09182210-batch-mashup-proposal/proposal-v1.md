# 批量混剪 Golden Path v1

提案人：Cecelia（AI）。归属 Line05「视频剪辑」，journey_id `bb4f2154-d5d3-4836-af11-aeeaa3c2e8c8`。
一句话：客户已有一批（≥10段）自有素材时，系统自动批量组合出上百份候选短视频方案，客户挑中后导出通过内容安全自查的 1080P 高清成片。

本版基于 `sprints/09182210-batch-mashup-proposal/explore-report.md`（四态判定 **DONE_WITH_CONCERNS**）与主理人已判定过一轮、GAN 对抗过一轮的承诺式骨干四步表。探索报告结论：S1 地基（上传/存储/数据模型）已打一半，S2/S3/S4 核心壁垒（模板槶位、语义检索、候选生成、内容安全/水印 Gate、声音克隆、剪映导出）**全部为 0**；Line05/07 现有 `video-remake.service.ts`（单帧驱动翻拍）与本路机制完全不同，不可复用于核心链路。据此本版把探索发现的"全部为 0"直接翻译为三道前置 Gate，不假装骨干里的空白是"半成"。

## Gate 0 · 技术可行性探索（开工第一个任务，先于任何功能代码）

**问题**：S2/S3/S4 的核心壁垒——语义检索匹配引擎（向量库选型/embedding 模型）、候选生成算法、Seedance/Happy Horse 补拍、阿里云超分、CosyVoice 声音克隆——**全库零调用点**（`explore-report.md` §S2/§S3/§S4 各行），成本、延迟、可用性均未验证。

- 探索项：①embedding 检索候选生成在客户级素材规模（≥10 段起步）下的延迟与成本是否可接受；②Seedance/Happy Horse/阿里云超分/CosyVoice 四个第三方 API 的账号/额度/SLA 是否可开通并达到可用响应时间。
- 结论三选一：**A** = 全部可行 → 按现有骨干继续；**B** = 部分不可行（例如补拍 API 不可用）→ 降级方案（S2 槶位缺素材分支改为"直接跳过该候选"，去掉 AI 补拍），回主理人重新拍板缩水后的 scope；**C** = 语义检索本身不可行（成本/延迟不可接受）→ 暂停，本 GP 需重新设计核心机制，不得强行上线。
- 预授权规则：结论 A/B 按对应方案继续；结论 C 暂停并升级主理人。

## Gate 1 · 内容安全与声音克隆授权文书（碰对外发布 + 真实人物声音，S4）

- 客户授权协议模板必须含：①声音克隆对象是客户本人/员工/已获授权代言人的明确书面/电子确认，逐次勾选（非一次性总授权）；②内容安全自查未通过的素材/成片，客户授权系统转人工复核或删除的处置权；③对外发布责任划分（平台封号/法律追责归属，参照 Gate 2 资金门共用凭据管理规范）。
- 声音克隆功能首版**默认关闭**，客户逐次勾选授权后才开放该次生成；未过内容安全自查的成片**禁止导出**（fail-closed，见 A1/J7）。
- 无授权记录 = 功能入口不可点击，不是"生成后打免责水印"。

## Gate 2 · 第三方付费 API 预算门（S1/S2/S4 多处调用）

- 上线前必须完成：Gemini/Seedance/Happy Horse/阿里云超分/CosyVoice 各账号开通 + 额度预留 + 超时重试策略。团队已有真实付费调用先例可循（`content-judgment.ts` 调 Gemini、`video-remake.service.ts:369-373` 调 DashScope），但混剪需要的四个第三方能力**目前一个都没有接入代码**，凭据/额度从零搭建。
- `budget_guard` 生效：累计花费触达 `total_cost_cap_usd` 后续任务自动置为 `paused_budget`，人工确认前不再产生新调用，禁止静默继续扣费。

---

## Golden Path 步骤（承诺式骨干，4 步）

### Step 1：客户的素材变成系统认得出内容的可用库

**现状**：**半成**。上传/存储/数据模型三件已是真实生产代码，"系统认得内容"这一层（转码/切镜头/打标签/embedding/语义去重）全部缺失。

| 子项 | 现状 | 证据（`explore-report.md`） |
|---|---|---|
| 三端素材上传入口 | 已有 | `apps/api/src/routes/materials.ts` 全文件；已挂载 `apps/api/src/app.ts:10`（import）+ `:168`（`app.use('/api/materials', ...)`） |
| COS 私有桶存储/分片/签名URL | 已有 | `apps/api/src/services/material-storage-cos.ts` 全文件，`routes/materials.ts:120` 实际调用 |
| materials/contents/content_materials 三表模型 | 已有（已为跨作品混剪预留） | `apps/api/db/migrations/20260904_224500_materials_contents.sql`：注释明确写"混剪时从池子里跨作品挑素材...只是多插一条关联" |
| 转码流水线 | 缺失 | 全库无编码/转码流水线（仅 video-remake 抽单帧，非批量转码） |
| PySceneDetect/镜头切分 | 缺失 | `grep -rlni "scenedetect\|shot.detect\|cut.detect"` 全 repo 零命中 |
| AI 打标签（Gemini/视频理解） | 缺失（现有 Gemini 调用不可复用） | `content-judgment.ts:58,73` 是判客户画像匹配，`comment-grading.ts` 是评论分级，均非素材内容打标签 |
| Embedding/向量检索 | 缺失（明确注释否认） | `apps/api/src/services/wechat/business-kb.ts:5`："thin 阶段不用 embedding，纯关键词打分" |
| 语义级去重 | 缺失（`content_hash` 是死列） | migration 定义了 `content_hash` 列，但 `grep -rn "content_hash" apps/api/src` 零命中；实际用的 `dedupe_key` 是字面哈希，非语义级 |
| 版权/水印判不出拦人工复核 | 缺失 | 未找到任何版权/水印检测调用点 |

【挂片】转码(缺失)／PySceneDetect切镜头(缺失)／Gemini打标签(缺失)／embedding索引(缺失)／素材上传存储(已有，直接复用)
【分支/判定点】J1 语义级去重方法选型（误判后果见判定点登记表）；J2 版权/水印判不出默认拦人工复核（误判后果：侵权素材混入成片）；J3 追加素材增量索引更新方式；边界（Gate 前置条件）＝只接 ≥10 段自有素材，<10 段在入口拒绝并提示原因，不进入本路

**验证等级承诺**：**L3（真机真验）**。整步含真实第三方调用（Gemini 打标签/embedding 生成模型），按"就高不就低"整体标 L3；素材上传/存储子项本身可 L2（真实 DB/COS 写入验证）。

### Step 2：客户定下这批视频要按什么套路剪

**现状**：**缺失**。全库无槶位/slot 模板系统；唯一带"模板"字样的 `composeTemplate`（`ai-video-pipeline-ai.controller.ts` line ~601）是"脚本生成 HTML 动效视频"模板，语义与"素材分配到钩子/产品/证据/CTA 槶位"完全不同，不可复用。

| 子项 | 现状 | 证据 |
|---|---|---|
| 钩子/产品/证据/CTA 四类槶位模板库 | 缺失 | `explore-report.md` §S2：全库无槶位概念，`composeTemplate` 语义不同 |
| Seedance/Happy Horse 补拍生成 | 缺失 | `grep -rlni "seedance\|happyhorse\|快马"` 全 repo 零命中 |
| 阿里云超分 | 缺失（未验证到调用点） | `video-remake.service.ts:340` 的 DashScope 调用是 `wan-i2v-turbo`（图生视频），非超分模型 |

【挂片】钩子/产品/证据/CTA 槶位模板库(全新设计，缺失)／补拍生成能力(缺失，依赖 Gate 0 结论)
【分支/判定点】可与 Step 1 并行开发（非判定点，为调度关系）；J4 槶位缺素材时的处置方式（AI 生成补拍或降级，误判后果见判定点登记表）

**验证等级承诺**：**L3（真机真验）**。模板槶位选择本身可 L1（纯前端交互），但补拍分支调用真实第三方（Seedance/Happy Horse），按就高整体标 L3；Gate 0 结论为 B/C 时本步范围收缩。

### Step 3：客户刷到一屏候选方案任选

**现状**：**缺失**。核心壁垒语义检索引擎、候选生成/过滤逻辑全部为 0；`VideoRemakePipelinePage.tsx` 虽真实存在且已挂导航（`config/navigation.config.ts` 命中），但其交互是"单任务 9 节点状态流水线"（`NODE_DEFS`/`video-remake.service.ts:27-37`），不是"一屏铺开 50-300 个候选卡片任选一个"的网格选择器，只有"点节点展开看详情"的交互习惯可参照，网格/卡片选择器本身要新建。

| 子项 | 现状 | 证据 |
|---|---|---|
| 语义检索匹配引擎（核心壁垒） | 缺失 | 全库无 embedding/vector 检索基础设施 |
| 候选量控制（防组合爆炸） | 缺失 | 无任何候选生成/组合逻辑代码 |
| 候选质量过滤（含内容安全维度） | 缺失 | 无 |
| 候选预览/选择 UI | 缺失，UI 骨架可参照但需新建 | `VideoRemakePipelinePage.tsx` 全 381 行确认真实存在，机制不匹配，网格选择器要新建 |

【挂片】语义检索匹配引擎(缺失，依赖 Gate 0 结论)／候选去重(缺失)／低清代理预览(缺失)／候选网格选择 UI(缺失，新建)
【分支/判定点】J5 候选量控制阈值方法（50-300 条防组合爆炸，误判后果见判定点登记表）；J6 候选质量过滤含内容安全维度的初筛方法；中途失败进度可见（NFR，非判定点）；并发限流（NFR，非判定点）

**验证等级承诺**：**L3（真机真验）**。语义检索若调用外部 embedding/大模型 API 属真实第三方；候选生成计算本身为真实服务端计算（非 mock），按就高整体标 L3。

### Step 4：客户选中的方案变成能直接发的高清成片

**现状**：**半成**。FFmpeg/ffprobe 已有真实使用先例，但停在"抽帧/取元数据"级别；内容安全 Gate、声音克隆、剪映导出三项均为 0。

| 子项 | 现状 | 证据 |
|---|---|---|
| FFmpeg 高清合成渲染 | 半成 | `video-remake.service.ts:58`（抽单帧）、`:72`（ffprobe 取元数据）；真正"多段素材合成高清导出"的编排（转场/多轨/字幕烧录）未见 |
| 内容安全 + 水印自查 Gate（fail-closed） | 缺失 | `grep` 命中的 `staff-directory.ts`、`single-org-selfcheck.ts` 均为误命中（员工目录日志、启动自检文案），非内容审核；抖音发布器 1 处 "safety" 命中未验证为强制拦截 Gate |
| fail-closed 架构先例 | 有先例但非本领域 | `middleware/workbench-auth.ts`、`services/device-readiness.ts`、`services/feishu-orchestrator.ts` 是鉴权/设备就绪/飞书编排场景的 fail-closed，可抄范式不可抄逻辑 |
| CosyVoice 声音克隆 | 缺失 | `grep -rlni "cosyvoice\|voice.clone\|声音克隆"` 全 repo 零命中；`avatar` skill 里的技术选型是文档知识，非本 repo 已实现集成 |
| pyJianYingDraft 剪映草稿导出 | 缺失 | `grep -rlni "jianying\|capcut"` 全 repo 零命中 |

【挂片】FFmpeg 高清渲染编排(半成→需加厚)／字幕音乐叠加(缺失)／声音克隆(缺失，受 Gate 1 约束)／剪映精修导出(缺失，可选路径)
【分支/判定点】J7 下载前内容安全+水印自查 Gate（fail-closed，误判后果见判定点登记表）；J8 声音克隆客户明确授权确认（误判后果见判定点登记表）；J9 剪映路径失效自动降级 FFmpeg 直出的判定方法

**验证等级承诺**：**L3（真机真验）**。声音克隆/内容安全审核为真实第三方调用，FFmpeg 渲染/剪映导出为真实服务端处理与真实文件产出，按就高整体标 L3。本步为对外发布前最后一关，Gate 1/Gate 2 均在此步生效。

---

## 验收断言（Final E2E，冻结后 AI 不可改）

- **A1** 内容安全自查未通过或水印自查未通过的成片，导出接口恒拒绝返回下载文件；psql 查询对应 `content_id` 的 `safety_check_status`/`watermark_check_status` 字段，只要非 `passed`，`contents` 表上该条记录的 `export_url`/`download_url` 必须为 NULL（fail-closed，不允许"先给文件后补检查"）。
- **A2** 导出的高清成片必须通过 `ffprobe` 验证：存在视频流（`ffprobe -v error -select_streams v -show_entries stream=codec_type` 输出含 `video`）+ 分辨率 ≥ 1920x1080 + 时长与所选候选方案时长误差 ≤ 1 秒。
- **A3** 声音克隆功能被调用时，API 必须先校验 `voice_clone_consent` 字段：未授权（`consent!=true`）请求恒返回 403 并落审计日志；已授权请求成功后 `voice_clone_consent_record` 表存在对应授权凭据行（psql 查询存在性 + 授权时间早于生成任务创建时间）。
- **A4** 候选生成 API（如 `GET /api/mashup/candidates`）返回的候选列表长度必须落在 [50, 300] 区间（超出该区间视为组合爆炸/过少两类异常，测试用例分别构造素材数量边界触发并断言返回值）。
- **A5** 第三方付费 API（Seedance/Happy Horse/阿里云超分/CosyVoice/Gemini）累计调用花费达到 `budget_guard.total_cost_cap_usd` 后，新任务状态必须置为 `paused_budget`（psql 查询该状态行存在），且日志证明后续调用未真实发起（无对应 provider 的出站请求记录）。

---

## 判定点登记表

| ID | 判定点 | 候选方法 | 所选方法（REC） | 依据 | 误判后果 |
|---|---|---|---|---|---|
| J1 | 语义级去重（S1） | ①文件字面哈希（现状 `dedupe_key`，不够）②embedding 向量相似度阈值 ③感知哈希（pHash 视频关键帧） | **REC = embedding 向量相似度** | 目的是防止同一素材内容（经转码/裁切/加水印后）在多个候选方案里重复出现；文件哈希对转码后文件完全失效，感知哈希对语义级相似（同场景不同机位）识别力不足 | 漏判（该重复没查出）→ 同一素材在多个候选混剪里重复出现，观感差且浪费候选配额；错杀（不同素材误判为重复）→ 其中一段素材永远进不了候选库，浪费客户上传的有效素材 |
| J2 | 版权/水印判不出默认拦人工复核（S1） | ①AI 自动判定直接放行/拦截 ②AI 主判 + 低置信度默认转人工复核（fail-closed） ③上传时人工全量审核 | **REC = AI 主判 + 低置信度默认转人工复核** | 全量人工审核不可扩展；纯 AI 自动判定放行有漏判风险；客户自有素材理论风险低但仍可能含未授权转发素材，前置比 S4 末端拦截更省成本 | 漏判（判成无风险实际有）→ 侵权素材混入成片对外发布，法律/平台处罚风险；错杀（判成有风险实际无）→ 客户自己拍的素材被拦，人工复核队列积压、客户体感变慢 |
| J3 | 追加素材的索引更新方式（S1） | ①每次追加全量重建索引 ②仅对新增素材做增量索引 ③定时批量合并 | **REC = 增量索引** | 素材库会持续增长，全量重建成本随规模线性增长不可持续；定时批处理延迟高、客户体感差 | 增量索引出错导致新素材"隐身"（检索不到）→ 客户以为已加入实际永远不会出现在候选里，且无报错提示，难以发现 |
| J4 | 槶位缺素材处置（S2） | ①直接跳过该候选方案 ②AI 生成补拍（Seedance/Happy Horse + 超分）③复用其他槶位素材顶替 | **REC = 优先 AI 生成补拍，补拍失败/成本超限则跳过该候选** | 顶替会破坏模板设计的叙事逻辑（钩子槶位塞证据类素材导致叙事错乱）；补拍是骨干表明确写入的方案，符合 Gate 0 验证结论 A/B 路线 | 错误顶替 → 候选视频叙事混乱，直接影响候选可用率；补拍质量判不好又放行 → 候选里混入明显 AI 痕迹或穿帮镜头 |
| J5 | 候选量控制阈值（S3） | ①暴力枚举全部组合（组合爆炸）②按语义匹配分排序后截断到 50-300 ③固定条数硬截断 | **REC = 语义匹配分排序 + 动态截断到 50-300** | 候选过多既不可能全部渲染预览（成本），也让客户选择困难；过少可能覆盖不到客户满意的排列；固定硬截断不考虑素材/模板规模差异 | 截断算法有偏差（如总排除某类素材）→ 客户永远刷不到某些好排列，反馈"每次候选都差不多"，实质是隐藏的候选多样性缺陷 |
| J6 | 候选初筛的内容安全维度（S3） | ①不做初筛，全部留给 S4 末端 Gate ②候选生成阶段就做轻量初筛（过滤明显违规候选，不做终审） | **REC = 候选生成阶段轻量初筛 + S4 末端仍保留完整终审（双闸）** | 若把明显违规候选留到 S4 才拦，客户已经"选中"过一次体验浪费，且徒增末端 Gate 的复核负担 | 初筛漏判 → 违规候选混入候选池被客户选中后才在 S4 拦下，客户体验受损；初筛错杀 → 正常候选被提前排除，减少候选多样性 |
| J7 | 下载前内容安全+水印自查 Gate（S4，fail-closed） | ①全部依赖发布平台（抖音等）事后审核 ②接入第三方内容安全 API 主动预检，不确定即拦（fail-closed）③纯规则关键词过滤 | **REC = 接入第三方内容安全 API 主动预检（复用现有 ToAPIs/Gemini 代理调用先例），不确定默认拦截转人工复核** | 纯规则过滤覆盖不了视觉层违规（画面本身色情/暴力，非文字）；依赖平台事后审核=发布之后才发现，已造成实际暴露 | 漏判 → 违规内容对外发布，导致平台封号/法律责任；错杀 → 正常内容被拦，人工复核队列积压拖慢客户拿到成片的时间 |
| J8 | 声音克隆客户明确授权确认（S4） | ①默认允许，仅事后打"AI 合成"水印免责 ②客户逐次勾选授权后才开启该次生成 ③自动检测声纹相似度决定是否需要授权 | **REC = 客户逐次勾选授权后才开启（Gate 1）** | 事后水印不解决未授权克隆本身的法律风险；声纹相似度检测无法覆盖"克隆对象是谁、是否已获得其本人授权"这一法律事实，技术手段判不出授权关系 | 误判"已授权"实际未获得 → 对外发布使用他人声音，面临法律追责，责任严重且不可逆；误判"未授权"实际已获得 → 功能被误拦，客户体验受损但可申诉解决 |
| J9 | 剪映路径失效降级（S4） | ①失败直接报错阻断交付 ②自动降级为 FFmpeg 直出并提示客户"已用标准渠道生成，未做精修" ③排队重试剪映直到成功 | **REC = 自动降级 FFmpeg 直出 + 显式提示** | 剪映精修是增值项非刚需，交付优先于精修效果；排队重试会拖慢客户拿到成片的时间且剪映失效原因多为环境问题，重试未必能自愈 | 降级后若不显式提示，客户会认为"混剪功能质量不稳定"而非"这次走的是标准渠道"，误判来源导致客户对功能整体失去信任 |

---

## P2 记账（不阻塞，进账本留给实现期）

- 转码流水线的具体编码参数（分辨率梯度/码率）本版不拍死，实现期按客户素材实际分布校准。
- 候选量阈值 [50, 300] 的精确取值本版按 A4 断言区间给出，实现期用真实素材规模数据回填收窄。
- A1/A2 等断言涉及的字段名（`safety_check_status`/`export_url` 等）为提案期占位命名，实现期以实际迁移 DDL 为准，语义不变。
- 内容安全第三方 API 具体选型（复用现有 ToAPIs/Gemini 代理 vs 新增专用内容安全供应商）留给 Gate 0/Gate 2 探索阶段定案。
- Line05/07 现有 `video-remake.service.ts` 的 job/node 状态机模式、fixture 兜底模式、`NodeCard` 前端交互组件——工程惯例层面可参照，不构成本 GP 核心能力，实现期按需借鉴不强制复用。
