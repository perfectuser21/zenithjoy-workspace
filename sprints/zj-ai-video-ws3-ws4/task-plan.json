{
  "initiative_id": "init-3",
  "journey_type": "user_facing",
  "journey_type_reason": "涉及 apps/dashboard/ 前端 UI（比例选择器 + pollStatus）+ Windows ZenithJoy Agent E2E，起点为用户 UI 操作",
  "tasks": [
    {
      "task_id": "ws1",
      "title": "updateProgress → {ok:true} + getJob → {job:{...}} 响应格式修正 + pollStatus 适配",
      "scope": "apps/api/src/controllers/ai-video-pipeline.controller.ts — (1) updateProgress: res.json(updated) → res.json({ ok:true }); (2) getJob: res.json({...job}) → res.json({ job: { id: job.id, status: job.status, progress: job.progress ?? 0, detected_aspect: job.detected_aspect ?? null } }); apps/dashboard/src/pages/LocalVideoPipelinePage.tsx — (3) pollStatus: return { id, ...res.data } → return { id, status: res.data.job.status, progress: res.data.job.progress ?? 0, error: res.data.job.error }",
      "dod": [
        "[BEHAVIOR] updateProgress 函数含 res.json({ ok: true })（不含 res.json(updated)）",
        "[BEHAVIOR] 响应 keys 集合等于 ['ok']（no id/status 字段）",
        "[BEHAVIOR] 禁用字段 aspectRatio/aspect/ratio/orientation 不出现在 PATCH 响应",
        "[BEHAVIOR] 404 error path 未被破坏",
        "[BEHAVIOR] getJob 返回 {job:{...}} wrapper（不是 flat ...job spread）",
        "[BEHAVIOR] getJob .job 块显式含 detected_aspect key（snake_case，禁 camelCase detectedAspect）",
        "[BEHAVIOR] pollStatus 通过 res.data.job 访问（不用 flat res.data spread）",
        "[ARTIFACT] ai-video-pipeline.controller.ts 两处修改已保存",
        "[ARTIFACT] LocalVideoPipelinePage.tsx pollStatus 已更新为 res.data.job 路径"
      ],
      "files": [
        "apps/api/src/controllers/ai-video-pipeline.controller.ts",
        "apps/dashboard/src/pages/LocalVideoPipelinePage.tsx"
      ],
      "depends_on": [],
      "complexity": "S",
      "estimated_minutes": 30
    },
    {
      "task_id": "ws2",
      "title": "E2E spec 适配 {job:{...}} + W-G 模板 + 9:16 画幅 + detected_aspect 非空强断言",
      "scope": "e2e/agent-video-pipeline.spec.js — (1) step 3 后加 W-G 模板点击 + 9:16 按钮点击（在提交前）; (2) jobResp.detected_aspect → jobResp.job.detected_aspect（适配 WS1 新 getJob 形状）; (3) toContain([...,null]) → toMatch(/^(9:16|16:9)$/)",
      "dod": [
        "[BEHAVIOR] spec 含 W-G 模板选择点击代码",
        "[BEHAVIOR] spec 含 9:16 画幅按钮点击，出现在提交之前",
        "[BEHAVIOR] detected_aspect 断言使用 toMatch(/^(9:16|16:9)$/) 或 toBe('9:16')，不接受 null",
        "[BEHAVIOR] spec 不再含 ['9:16', '16:9', null].toContain 宽松断言",
        "[BEHAVIOR] spec 用 jobResp.job.detected_aspect 访问（适配新 {job:{...}} 形状）",
        "[ARTIFACT] e2e/agent-video-pipeline.spec.js 含 W-G 和 9:16"
      ],
      "files": [
        "e2e/agent-video-pipeline.spec.js"
      ],
      "depends_on": ["ws1"],
      "complexity": "M",
      "estimated_minutes": 35
    },
    {
      "task_id": "ws3",
      "title": "GHA agent-e2e-video.yml default version → 1.1.31",
      "scope": ".github/workflows/agent-e2e-video.yml — agent_version input 的 default: \"1.1.30\" 改为 \"1.1.31\"（对齐 services/agent/package.json version=1.1.31，修正 PRD 原写 1.1.29 降级问题）",
      "dod": [
        "[BEHAVIOR] GHA default 等于 agent package.json version（1.1.31）",
        "[BEHAVIOR] GHA default 具体值为 1.1.31",
        "[BEHAVIOR] GHA default 不再是旧版 1.1.30",
        "[BEHAVIOR] GHA workflow 含必要字段 workflow_dispatch/agent_version/default:",
        "[ARTIFACT] agent-e2e-video.yml default: 已改为 1.1.31"
      ],
      "files": [
        ".github/workflows/agent-e2e-video.yml"
      ],
      "depends_on": ["ws2"],
      "complexity": "S",
      "estimated_minutes": 20
    }
  ]
}
