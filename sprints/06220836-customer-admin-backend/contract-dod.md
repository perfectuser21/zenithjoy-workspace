---
skeleton: false
journey_type: user_facing
target_environment: windows_cloud
---
# Contract DoD — Sprint: 客户管理后台（公司名 / 子账号 / 客服-PC 绑定 / 诊断报告页）

**范围**: 公司名 CRUD（复用 tenants.name）/ 子账号 CRUD（含 role + plan 配额）/ 客服-PC 绑定（1:1 双唯一 + 机器配额）/ 诊断报告展示（复用 module-health）/ 软删 / 轻量审计 / 租户隔离。不动注册、计费、RBAC、登录鉴权。
**大小**: L

> **验证环境约定**：后端 BEHAVIOR 打**真实 ZenithJoy API** `API_BASE`（默认 `http://localhost:5200`，见 admin-customers-smoke.sh）+ **真实 Postgres**（PGHOST/PGUSER/PGDATABASE/PGPASSWORD，默认 cecelia）。**不是** Brain 5221（本 sprint 是 ZenithJoy 产品，非 Cecelia harness）。UI 行为见 ## BEHAVIOR:E2E（windows_cloud Playwright）。
>
> **super-admin 鉴权**（R2 补）：新端点挂 `superAdminGuard`。CI（ci-l4-e2e-smoke.yml）的 `smoke-api-contract` job 已设 `ZENITHJOY_INTERNAL_TOKEN` → 守卫 dev-fallback 关闭，无鉴权头真返 401。故所有**正向** BEHAVIOR/smoke 必须带 `-H "X-Internal-Token: $ZENITHJOY_INTERNAL_TOKEN"`（job env 已注入；本地 evaluator env 未设此 var 时为空串 → 守卫 dev-fallback 放行，两端可移植）。负向 403 用例故意发 `X-Feishu-User-Id: not-an-admin` 触发白名单拒绝。
>
> **psql 字符串字面量铁律**（R2 修正 round-1 潜伏 bug）：`manual:bash -c '...'` 内 psql 的 SQL 字符串字面量**必须**用 `'\''…'\''`（POSIX 单引号转义），实测经 `bash -c` 层后 psql 收到 `'matrix'`（合法字符串）；round-1 用 `\"…\"` 经层后 psql 收到 `"matrix"`（被当**标识符** → `column "..." does not exist` 报错）。**禁止**对 SQL 字符串字面量用双引号转义。JSON/jq 参数仍用 `\"`（它们要的就是双引号）。

## ARTIFACT 条目

- [ ] [ARTIFACT] migration 建 tenant_sub_accounts / service_agents 表 + 双唯一 + 软删字段
  Test: node -e "const fs=require('fs'),g=require('child_process').execSync('ls apps/api/db/migrations/').toString();const f=g.split('\n').find(x=>/customer_admin_backend\.sql$/.test(x));if(!f)process.exit(1);const c=fs.readFileSync('apps/api/db/migrations/'+f,'utf8');if(!/tenant_sub_accounts/.test(c)||!/service_agents/.test(c)||!/deleted_at/.test(c)||!/UNIQUE/i.test(c))process.exit(1)"

- [ ] [ARTIFACT] 新路由文件含 4 个新端点 + superAdminGuard
  Test: node -e "const fs=require('fs');const cand=['apps/api/src/routes/tenant-admin.ts','apps/api/src/routes/customer-admin.ts','apps/api/src/routes/admin-customers.ts'];const c=cand.filter(p=>fs.existsSync(p)).map(p=>fs.readFileSync(p,'utf8')).join('\n');if(!/accounts/.test(c)||!/service-agents/.test(c)||!/bind-device/.test(c)||!/superAdminGuard/.test(c))process.exit(1)"

- [ ] [ARTIFACT] Dashboard 客户管理页含公司/子账号/绑定/诊断 4 区
  Test: node -e "const{execSync}=require('child_process');const o=execSync('grep -rl \"service-agents\\|bind-device\\|module-health\" apps/dashboard/src/pages apps/dashboard/src/api 2>/dev/null || true').toString();if(!o.trim())process.exit(1)"

- [ ] [ARTIFACT] smoke 脚本 + Playwright spec + e2e-verify.ps1 存在
  Test: node -e "const fs=require('fs');['.github/workflows/scripts/smoke/customer-admin-backend-smoke.sh','apps/dashboard/e2e/customer-admin-backend.spec.ts','sprints/06220836-customer-admin-backend/e2e-verify.ps1'].forEach(p=>{if(!fs.existsSync(p))process.exit(1)})"

- [ ] [ARTIFACT] CI 接线：ci-l4-e2e-smoke.yml 的 smoke-api-contract job 显式 step 跑 customer-admin-backend-smoke.sh，且 workflow 在 PR 触发（阻塞1 CI_GAP 修复 — 让 8+ 条后端断言真进 CI gate）
  Test: node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-e2e-smoke.yml','utf8');if(!/customer-admin-backend-smoke\.sh/.test(c)){console.error('smoke 未接进 workflow');process.exit(1)}if(!/pull_request/.test(c)){console.error('workflow 未在 PR 触发，加 step 也不 gate');process.exit(1)}"

- [ ] [ARTIFACT] smoke 脚本含真实后端链路断言（非空壳 exit 0）— 覆盖 bind-device/service-agents/accounts + 真 psql + 配额
  Test: node -e "const fs=require('fs');const p='.github/workflows/scripts/smoke/customer-admin-backend-smoke.sh';if(!fs.existsSync(p))process.exit(1);const c=fs.readFileSync(p,'utf8');['bind-device','service-agents','/accounts','psql','配额','X-Internal-Token'].forEach(k=>{if(!c.includes(k)){console.error('smoke 缺 '+k);process.exit(1)}})"

## BEHAVIOR 条目（内嵌可执行 manual:bash，打真实 API+真库，user_facing 模式A）

- [ ] [BEHAVIOR] Step1 改公司名落库 — PUT /api/tenant/:id 改 tenants.name，DB 实际更新
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; PH="${PGHOST:-localhost}"; PU="${PGUSER:-cecelia}"; PD="${PGDATABASE:-cecelia}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; T="${ZENITHJOY_INTERNAL_TOKEN:-}"; Q(){ psql -h $PH -U $PU -d $PD -tAc "$1"; }; LK="lk-s1-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''Personal-old'\'','\''$LK'\'','\''matrix'\'') RETURNING id" | tr -d " "); R=$(curl -sf -X PUT "$A/api/tenant/$TID" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"name\":\"晨悦传媒\"}"); echo "$R" | jq -e ".success==true and .data.name==\"晨悦传媒\"" >/dev/null; N=$(Q "SELECT name FROM zenithjoy.tenants WHERE id='\''$TID'\''" | tr -d " "); [ "$N" = "晨悦传媒" ]; echo OK'
  期望: OK

- [ ] [BEHAVIOR] Step2 建子账号带 role — POST /api/tenant/:id/accounts，5 分钟内 DB 新增 service_agent 行
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; PH="${PGHOST:-localhost}"; PU="${PGUSER:-cecelia}"; PD="${PGDATABASE:-cecelia}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; T="${ZENITHJOY_INTERNAL_TOKEN:-}"; Q(){ psql -h $PH -U $PU -d $PD -tAc "$1"; }; LK="lk-s2-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''T2'\'','\''$LK'\'','\''matrix'\'') RETURNING id" | tr -d " "); Q "INSERT INTO zenithjoy.licenses(license_key,tier,max_machines,tenant_id,expires_at) VALUES('\''$LK'\'','\''matrix'\'',5,'\''$TID'\'',NOW()+interval '\''365 days'\'')" >/dev/null; R=$(curl -sf -X POST "$A/api/tenant/$TID/accounts" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"email\":\"svc@t2.test\",\"display_name\":\"客服\",\"role\":\"service_agent\"}"); AID=$(echo "$R" | jq -r ".data.account_id"); echo "$R" | jq -e ".success==true and .data.role==\"service_agent\"" >/dev/null; C=$(Q "SELECT count(*) FROM zenithjoy.tenant_sub_accounts WHERE id='\''$AID'\'' AND tenant_id='\''$TID'\'' AND role='\''service_agent'\'' AND deleted_at IS NULL AND created_at > NOW() - interval '\''5 minutes'\''" | tr -d " "); [ "$C" = "1" ]; echo OK'
  期望: OK

- [ ] [BEHAVIOR] Step2-err 非法 role 拒绝 — role=boss → 400 INVALID_ROLE，DB 无行
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; PH="${PGHOST:-localhost}"; PU="${PGUSER:-cecelia}"; PD="${PGDATABASE:-cecelia}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; T="${ZENITHJOY_INTERNAL_TOKEN:-}"; Q(){ psql -h $PH -U $PU -d $PD -tAc "$1"; }; LK="lk-s2e-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''T3'\'','\''$LK'\'','\''matrix'\'') RETURNING id" | tr -d " "); Q "INSERT INTO zenithjoy.licenses(license_key,tier,max_machines,tenant_id,expires_at) VALUES('\''$LK'\'','\''matrix'\'',5,'\''$TID'\'',NOW()+interval '\''365 days'\'')" >/dev/null; CODE=$(curl -s -o /tmp/r3.json -w "%{http_code}" -X POST "$A/api/tenant/$TID/accounts" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"email\":\"x@t3.test\",\"display_name\":\"x\",\"role\":\"boss\"}"); [ "$CODE" = "400" ] || { echo "FAIL code=$CODE"; exit 1; }; jq -e ".success==false and (.error.code|test(\"ROLE\"))" /tmp/r3.json >/dev/null; C=$(Q "SELECT count(*) FROM zenithjoy.tenant_sub_accounts WHERE tenant_id='\''$TID'\'' AND created_at > NOW() - interval '\''5 minutes'\''" | tr -d " "); [ "$C" = "0" ]; echo OK'
  期望: OK

- [ ] [BEHAVIOR] Step3 绑定落库 + 双唯一拒绝 — bind 写 service_agents；重复绑同客服/PC → 409 ALREADY_BOUND 无新行
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; PH="${PGHOST:-localhost}"; PU="${PGUSER:-cecelia}"; PD="${PGDATABASE:-cecelia}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; T="${ZENITHJOY_INTERNAL_TOKEN:-}"; Q(){ psql -h $PH -U $PU -d $PD -tAc "$1"; }; LK="lk-s3-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''T4'\'','\''$LK'\'','\''matrix'\'') RETURNING id" | tr -d " "); Q "INSERT INTO zenithjoy.licenses(license_key,tier,max_machines,tenant_id,expires_at) VALUES('\''$LK'\'','\''matrix'\'',5,'\''$TID'\'',NOW()+interval '\''365 days'\'')" >/dev/null; AID=$(curl -sf -X POST "$A/api/tenant/$TID/accounts" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"email\":\"svc@t4.test\",\"display_name\":\"客服\",\"role\":\"service_agent\"}" | jq -r ".data.account_id"); MID="pc-$(date +%s)$$"; curl -sf -X POST "$A/api/tenant/$TID/service-agents/$AID/bind-device" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"machine_id\":\"$MID\"}" | jq -e ".success==true" >/dev/null; C=$(Q "SELECT count(*) FROM zenithjoy.service_agents WHERE account_id='\''$AID'\'' AND machine_id='\''$MID'\'' AND deleted_at IS NULL AND created_at > NOW() - interval '\''5 minutes'\''" | tr -d " "); [ "$C" = "1" ]; CODE=$(curl -s -o /dev/null -w "%{http_code}" -X POST "$A/api/tenant/$TID/service-agents/$AID/bind-device" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"machine_id\":\"$MID\"}"); [ "$CODE" = "409" ] || { echo "FAIL dup code=$CODE"; exit 1; }; C2=$(Q "SELECT count(*) FROM zenithjoy.service_agents WHERE account_id='\''$AID'\'' AND deleted_at IS NULL AND created_at > NOW() - interval '\''5 minutes'\''" | tr -d " "); [ "$C2" = "1" ]; echo OK'
  期望: OK

- [ ] [BEHAVIOR] Step3-mq 机器配额超额硬拒（阻塞2）— license 满 max_machines 后 bind 新机器 → 4xx MACHINE_QUOTA_EXCEEDED，service_agents 无新行
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; PH="${PGHOST:-localhost}"; PU="${PGUSER:-cecelia}"; PD="${PGDATABASE:-cecelia}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; T="${ZENITHJOY_INTERNAL_TOKEN:-}"; Q(){ psql -h $PH -U $PU -d $PD -tAc "$1"; }; LK="lk-mq-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''MQ'\'','\''$LK'\'','\''basic'\'') RETURNING id" | tr -d " "); Q "INSERT INTO zenithjoy.licenses(license_key,tier,max_machines,tenant_id,expires_at) VALUES('\''$LK'\'','\''basic'\'',1,'\''$TID'\'',NOW()+interval '\''365 days'\'')" >/dev/null; LID=$(Q "SELECT id FROM zenithjoy.licenses WHERE license_key='\''$LK'\''" | tr -d " "); Q "INSERT INTO zenithjoy.license_machines(license_id,machine_id) VALUES('\''$LID'\'','\''mq-pre-$(date +%s)$$'\'')" >/dev/null; AID=$(curl -sf -X POST "$A/api/tenant/$TID/accounts" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"email\":\"svc@mq.test\",\"display_name\":\"客服\",\"role\":\"service_agent\"}" | jq -r ".data.account_id"); MID="mq-new-$(date +%s)$$"; CODE=$(curl -s -o /tmp/mq.json -w "%{http_code}" -X POST "$A/api/tenant/$TID/service-agents/$AID/bind-device" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"machine_id\":\"$MID\"}"); case "$CODE" in 409|400|403) : ;; *) echo "FAIL code=$CODE"; cat /tmp/mq.json; exit 1;; esac; jq -e ".error.code|test(\"MACHINE_QUOTA\")" /tmp/mq.json >/dev/null || { echo "FAIL: 错误码非 MACHINE_QUOTA_EXCEEDED"; cat /tmp/mq.json; exit 1; }; C=$(Q "SELECT count(*) FROM zenithjoy.service_agents WHERE account_id='\''$AID'\'' AND created_at > NOW() - interval '\''5 minutes'\''" | tr -d " "); [ "$C" = "0" ]; echo OK'
  期望: OK

- [ ] [BEHAVIOR] Step5 子账号配额超限硬拒 — 建满 limit 后第 limit+1 个 4xx「配额 N/M」且 DB 行数==limit
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; PH="${PGHOST:-localhost}"; PU="${PGUSER:-cecelia}"; PD="${PGDATABASE:-cecelia}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; T="${ZENITHJOY_INTERNAL_TOKEN:-}"; Q(){ psql -h $PH -U $PU -d $PD -tAc "$1"; }; LK="lk-s5-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''T5'\'','\''$LK'\'','\''basic'\'') RETURNING id" | tr -d " "); Q "INSERT INTO zenithjoy.licenses(license_key,tier,max_machines,tenant_id,expires_at) VALUES('\''$LK'\'','\''basic'\'',1,'\''$TID'\'',NOW()+interval '\''365 days'\'')" >/dev/null; LIM=$(curl -sf "$A/api/tenant/$TID/accounts" -H "X-Internal-Token: $T" | jq -r ".quota.limit"); [ "$LIM" -ge 1 ] || { echo "FAIL limit=$LIM"; exit 1; }; i=0; while [ "$i" -lt "$LIM" ]; do curl -sf -X POST "$A/api/tenant/$TID/accounts" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"email\":\"f$i@t5.test\",\"display_name\":\"f$i\",\"role\":\"operator\"}" >/dev/null; i=$((i+1)); done; CODE=$(curl -s -o /tmp/q5.json -w "%{http_code}" -X POST "$A/api/tenant/$TID/accounts" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"email\":\"over@t5.test\",\"display_name\":\"o\",\"role\":\"operator\"}"); case "$CODE" in 409|400|403) : ;; *) echo "FAIL over code=$CODE"; exit 1;; esac; jq -e ".error.message | test(\"配额\") and test(\"/\")" /tmp/q5.json >/dev/null; C=$(Q "SELECT count(*) FROM zenithjoy.tenant_sub_accounts WHERE tenant_id='\''$TID'\'' AND deleted_at IS NULL AND created_at > NOW() - interval '\''5 minutes'\''" | tr -d " "); [ "$C" = "$LIM" ]; echo OK'
  期望: OK

- [ ] [BEHAVIOR] Step6 租户隔离 + 软删 — A 账号不入 B 列表；软删后列表不含、deleted_at 置位、物理行保留
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; PH="${PGHOST:-localhost}"; PU="${PGUSER:-cecelia}"; PD="${PGDATABASE:-cecelia}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; T="${ZENITHJOY_INTERNAL_TOKEN:-}"; Q(){ psql -h $PH -U $PU -d $PD -tAc "$1"; }; LKA="lk-a-$(date +%s)$$"; LKB="lk-b-$(date +%s)$$z"; TIDA=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''TA'\'','\''$LKA'\'','\''matrix'\'') RETURNING id" | tr -d " "); TIDB=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''TB'\'','\''$LKB'\'','\''matrix'\'') RETURNING id" | tr -d " "); Q "INSERT INTO zenithjoy.licenses(license_key,tier,max_machines,tenant_id,expires_at) VALUES('\''$LKA'\'','\''matrix'\'',5,'\''$TIDA'\'',NOW()+interval '\''365 days'\'')" >/dev/null; Q "INSERT INTO zenithjoy.licenses(license_key,tier,max_machines,tenant_id,expires_at) VALUES('\''$LKB'\'','\''matrix'\'',5,'\''$TIDB'\'',NOW()+interval '\''365 days'\'')" >/dev/null; AID=$(curl -sf -X POST "$A/api/tenant/$TIDA/accounts" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"email\":\"iso@ta.test\",\"display_name\":\"iso\",\"role\":\"operator\"}" | jq -r ".data.account_id"); CROSS=$(curl -sf "$A/api/tenant/$TIDB/accounts" -H "X-Internal-Token: $T" | jq -r "[.data[].email]|map(select(test(\"@ta.test\")))|length"); [ "$CROSS" = "0" ] || { echo "FAIL leak=$CROSS"; exit 1; }; curl -sf -X DELETE "$A/api/tenant/$TIDA/accounts/$AID" -H "X-Internal-Token: $T" >/dev/null; IN=$(curl -sf "$A/api/tenant/$TIDA/accounts" -H "X-Internal-Token: $T" | jq -r "[.data[].account_id]|index(\"$AID\") // \"gone\""); [ "$IN" = "gone" ]; DEL=$(Q "SELECT count(*) FROM zenithjoy.tenant_sub_accounts WHERE id='\''$AID'\'' AND deleted_at IS NOT NULL" | tr -d " "); [ "$DEL" = "1" ]; echo OK'
  期望: OK

- [ ] [BEHAVIOR] GET /accounts schema 纯度（阻塞3）— 顶层 keys 子集卡 + 禁用字段反向 + data 项用 account_id 不泄漏 id/users
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; PH="${PGHOST:-localhost}"; PU="${PGUSER:-cecelia}"; PD="${PGDATABASE:-cecelia}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; T="${ZENITHJOY_INTERNAL_TOKEN:-}"; Q(){ psql -h $PH -U $PU -d $PD -tAc "$1"; }; LK="lk-pa-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''PURA'\'','\''$LK'\'','\''matrix'\'') RETURNING id" | tr -d " "); Q "INSERT INTO zenithjoy.licenses(license_key,tier,max_machines,tenant_id,expires_at) VALUES('\''$LK'\'','\''matrix'\'',5,'\''$TID'\'',NOW()+interval '\''365 days'\'')" >/dev/null; curl -sf -X POST "$A/api/tenant/$TID/accounts" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"email\":\"p@a.test\",\"display_name\":\"p\",\"role\":\"operator\"}" >/dev/null; R=$(curl -sf "$A/api/tenant/$TID/accounts" -H "X-Internal-Token: $T"); echo "$R" | jq -e ".success==true and (.data|type==\"array\")" >/dev/null; echo "$R" | jq -e "(keys - [\"data\",\"quota\",\"success\",\"total\"])|length==0" >/dev/null || { echo "FAIL: 顶层多余 key $(echo "$R"|jq -c keys)"; exit 1; }; for F in users clients members result id; do echo "$R" | jq -e "has(\"$F\")|not" >/dev/null || { echo "FAIL: 顶层禁用字段 $F"; exit 1; }; done; echo "$R" | jq -e ".data[0]|has(\"account_id\")" >/dev/null || { echo "FAIL: data 项缺 account_id"; exit 1; }; echo "$R" | jq -e ".data[0]|has(\"id\")|not" >/dev/null || { echo "FAIL: data 项泄漏 id"; exit 1; }; echo "$R" | jq -e ".data[0]|has(\"users\")|not" >/dev/null; echo OK'
  期望: OK

- [ ] [BEHAVIOR] GET /service-agents schema 纯度（阻塞3）— 顶层 keys 子集卡 + 禁用字段反向 + data 项用 binding_id 不泄漏 id
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; PH="${PGHOST:-localhost}"; PU="${PGUSER:-cecelia}"; PD="${PGDATABASE:-cecelia}"; export PGPASSWORD="${PGPASSWORD:-cecelia}"; T="${ZENITHJOY_INTERNAL_TOKEN:-}"; Q(){ psql -h $PH -U $PU -d $PD -tAc "$1"; }; LK="lk-ps-$(date +%s)$$"; TID=$(Q "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES('\''PURS'\'','\''$LK'\'','\''matrix'\'') RETURNING id" | tr -d " "); Q "INSERT INTO zenithjoy.licenses(license_key,tier,max_machines,tenant_id,expires_at) VALUES('\''$LK'\'','\''matrix'\'',5,'\''$TID'\'',NOW()+interval '\''365 days'\'')" >/dev/null; AID=$(curl -sf -X POST "$A/api/tenant/$TID/accounts" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"email\":\"svc@ps.test\",\"display_name\":\"c\",\"role\":\"service_agent\"}" | jq -r ".data.account_id"); MID="ps-$(date +%s)$$"; curl -sf -X POST "$A/api/tenant/$TID/service-agents/$AID/bind-device" -H "Content-Type: application/json" -H "X-Internal-Token: $T" -d "{\"machine_id\":\"$MID\"}" | jq -e ".success==true" >/dev/null; R=$(curl -sf "$A/api/tenant/$TID/service-agents" -H "X-Internal-Token: $T"); echo "$R" | jq -e ".success==true and (.data|type==\"array\")" >/dev/null; echo "$R" | jq -e "(keys - [\"data\",\"success\",\"total\"])|length==0" >/dev/null || { echo "FAIL: 顶层多余 key $(echo "$R"|jq -c keys)"; exit 1; }; for F in users clients members result id; do echo "$R" | jq -e "has(\"$F\")|not" >/dev/null || { echo "FAIL: 顶层禁用字段 $F"; exit 1; }; done; echo "$R" | jq -e ".data[0]|has(\"binding_id\")" >/dev/null || { echo "FAIL: data 项缺 binding_id"; exit 1; }; echo "$R" | jq -e ".data[0]|has(\"id\")|not" >/dev/null || { echo "FAIL: data 项泄漏 id"; exit 1; }; echo OK'
  期望: OK

- [ ] [BEHAVIOR] Step4 诊断端点 schema 复用 — GET /api/agent/module-health 返 {ok, data:array}
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; R=$(curl -s "$A/api/agent/module-health" -H "Authorization: Bearer ${E2E_LICENSE_TOKEN:-test-lic}"); echo "$R" | jq -e "has(\"ok\") and (.data|type==\"array\")" >/dev/null; echo OK'
  期望: OK

- [ ] [BEHAVIOR] error-path 非超管 403 — X-Feishu-User-Id: not-an-admin 访问新端点 → 403
  Test: manual:bash -c 'set -e; A="${API_BASE:-http://localhost:5200}"; CODE=$(curl -s -o /dev/null -w "%{http_code}" -H "X-Feishu-User-Id: not-an-admin" "$A/api/tenant/00000000-0000-0000-0000-000000000000/accounts"); [ "$CODE" = "403" ] || { echo "FAIL code=$CODE"; exit 1; }; echo OK'
  期望: OK

## BEHAVIOR:E2E 条目（user_facing 专属，Mode B final-e2e windows_cloud Playwright 跑）

- [ ] [BEHAVIOR:E2E] 管理员在 Dashboard 真走完 Golden Path，截图可视化验证
  Screenshots:
    - 01-customers-page.png   期望：「客户管理」页加载，公司列表可见，4 区入口（公司/子账号/绑定/诊断）可见
    - 02-company-named.png     期望：设公司名提交后，列表/详情显示新公司名（非 Personal-邮箱）
    - 03-accounts.png          期望：建 3 个子账号后列表出现 3 行，含 1 个 service_agent role 标签
    - 04-bound.png             期望：绑定后显示「客服 X @ PC Y ● 在线/离线」行
    - 05-diagnosis.png         期望：诊断区显示该机模块矩阵表格（✅/❌+原因+时间），或空态「该机暂无上报，请确认 Agent 已连中台」
  路径格式：sprints/06220836-customer-admin-backend/screenshots/<step>.png
  期望：evaluator 完成后截图已复制到 sprints/06220836-customer-admin-backend/screenshots/，每张与期望描述一致，Claude Read 图自验通过
