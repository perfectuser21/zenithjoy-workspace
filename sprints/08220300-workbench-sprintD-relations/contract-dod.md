---
skeleton: false
journey_type: user_facing
---
# Contract DoD — Sprint: 路③ Sprint D · S4 关联连得上（跨表 Relation）

**范围**: relation 字段类型 + 目标表配置(db_fields.options) + 关联值(db_rows.data JSONB 目标 row_id 数组) + 行选择器候选端点 + 反向引用端点 + 组织隔离三向(A27②/A28/A31) + 引用完整性三级(A30) + 关联接进既有 path3 workflow/vitest/smoke。**不重造** A33 workflow / A35 排除清单 / A32/A34 路由族+鉴权闸（已在 base）。
**大小**: L

## ARTIFACT 条目

- [x] [ARTIFACT] **ART-1 concern②：零新建关联表** —— 关联值落 `db_rows.data` JSONB、目标配置落 `db_fields.options`，全仓源码/ migration 零 `db_relations` 之类关联物理表建表 SQL
  Test: manual:bash -c 'grep -rIn "CREATE TABLE" apps/api/db/migrations/ | grep -iE "relation|db_ref|db_link" | grep -vi "exclusion" && { echo "FAIL: 出现关联物理表 migration"; exit 1; }; echo OK'
  期望: OK

- [x] [ARTIFACT] **ART-2 relation 字段类型三处同步登记** —— 新 migration 把 `relation` 加进 `db_fields` field_type CHECK；`workbench.service.ts` 的 `FIELD_TYPES` 含 `relation`；`workbench-rows.service.ts` 的 `cellTypeError` 有 relation case
  Test: manual:bash -c 'grep -rIlq "relation" apps/api/db/migrations/*relations*.sql 2>/dev/null || { echo "FAIL: 缺 relation migration"; exit 1; }; grep -q "relation" apps/api/src/services/workbench.service.ts || { echo "FAIL: FIELD_TYPES 缺 relation"; exit 1; }; grep -q "relation" apps/api/src/services/workbench-rows.service.ts || { echo "FAIL: cellTypeError 缺 relation case"; exit 1; }; echo OK'
  期望: OK

- [x] [ARTIFACT] **ART-3 两个新只读端点在路③ router 上，源码零明文身份头** —— `relation-candidates` 与 `backrefs` 挂 `workbench.ts`（自动继承 workbenchAuthGuard），且新增关联源码零 `req.headers`/`body.org_id`/`body.tenant_id` 读取、零七个禁用身份头字面量（A2 家族口径）
  Test: manual:bash -c 'grep -q "relation-candidates" apps/api/src/routes/workbench.ts || { echo "FAIL: 缺候选端点"; exit 1; }; grep -q "backrefs" apps/api/src/routes/workbench.ts || { echo "FAIL: 缺反查端点"; exit 1; }; for L in X-Tenant-Id X-User-Email X-Feishu-User-Id X-Bypass-Tenant tenantContextOptional selfHealOwnerMember staffGuard; do grep -rq "$L" apps/api/src/routes/workbench.ts apps/api/src/services/workbench-rows.service.ts apps/api/src/services/workbench.service.ts && { echo "FAIL: 出现禁用身份头 $L"; exit 1; }; done; echo OK'
  期望: OK

- [x] [ARTIFACT] **ART-4 db_fields.deleted_at 软删列 + 字段软删端点**（A30③）—— 新 migration 加 `db_fields.deleted_at`；`DELETE /tables/:id/fields/:fieldId` 端点在案；字段列表查询过滤 `deleted_at IS NULL`
  Test: manual:bash -c 'grep -riq "db_fields" apps/api/db/migrations/*relations*.sql && grep -riq "deleted_at" apps/api/db/migrations/*relations*.sql || { echo "FAIL: 缺 db_fields.deleted_at migration"; exit 1; }; grep -Eq "fields/:fieldId|fields/:id" apps/api/src/routes/workbench.ts || { echo "FAIL: 缺字段删除端点"; exit 1; }; echo OK'
  期望: OK

- [x] [ARTIFACT] **ART-5 本刀合同测试有真实收集配置** —— `vitest.workbench-relations.config.ts` 白名单含本 sprint tests 目录 + `apps/api` 暴露 `test:workbench-relations` + setup 从连接串推导五个 `DATABASE_*`
  Test: manual:bash -c 'grep -q "08220300-workbench-sprintD-relations/tests" apps/api/vitest.workbench-relations.config.ts || { echo "FAIL: config 未白名单本刀 tests"; exit 1; }; node -e "const p=require(\"./apps/api/package.json\");process.exit(p.scripts[\"test:workbench-relations\"]?0:1)" || { echo "FAIL: 缺 test:workbench-relations script"; exit 1; }; grep -q "DATABASE_HOST" apps/api/vitest.workbench-relations.setup.ts || { echo "FAIL: setup 未推导 DATABASE_*"; exit 1; }; echo OK'
  期望: OK

- [x] [ARTIFACT] **ART-6 E2E workflow 逐字接线**（concern①：接入不重造）—— `e2e-knowledge-hub-path3.yml` 的 `paths` 含本刀 spec 与 sprint 目录；linux job 含 `test:workbench-relations`；windows job 有真调 `e2e-relations-run.ps1` 的 step；本刀截图有独立 upload step（`path3-relations-screenshots`）；且 windows job **仍无 job 级事件条件门**（A33(c) 不回退）
  Test: manual:bash -c 'WF=.github/workflows/e2e-knowledge-hub-path3.yml; for P in "structured-workbench-relations.spec.ts" "08220300-workbench-sprintD-relations" "test:workbench-relations" "e2e-relations-run.ps1" "path3-relations-screenshots"; do grep -q "$P" "$WF" || { echo "FAIL: workflow 缺 $P"; exit 1; }; done; node -e "const y=require(\"fs\").readFileSync(\".github/workflows/e2e-knowledge-hub-path3.yml\",\"utf8\");const m=y.match(/windows-real-browser:[\s\S]*?(?=\n  [a-z]|$)/);if(!m)process.exit(1);const head=m[0].split(\"\n\").filter(l=>/^    [a-z_]+:/.test(l)).join(\"\n\");process.exit(/if:.*(workflow_dispatch|github\.event_name)/.test(head)?1:0)" || { echo "FAIL: windows job 加了事件条件门(A33c 回退)"; exit 1; }; echo OK'
  期望: OK

- [x] [ARTIFACT] **ART-7 relation E2E spec 存在、零请求拦截/改写**（变体C 死规则）—— `structured-workbench-relations.spec.ts` 存在、零 `page.route(`、含 ASCII 标签 `@relation-build`/`@relation-jump`/`@relation-backref`，且含跳转断言与反向面板断言
  Test: manual:bash -c 'SP=apps/staff-hub/e2e/structured-workbench-relations.spec.ts; [ -f "$SP" ] || { echo "FAIL: 缺 relation spec"; exit 1; }; grep -q "page.route(" "$SP" && { echo "FAIL: spec 出现请求拦截(变体C 死规则)"; exit 1; }; for T in "@relation-build" "@relation-jump" "@relation-backref"; do grep -q "$T" "$SP" || { echo "FAIL: spec 缺标签 $T"; exit 1; }; done; echo OK'
  期望: OK

- [x] [ARTIFACT] **ART-8 smoke 登记本刀 relation 7 段 + 7 个变异开关 + baseline 棘轮** —— `structured-workbench-smoke.sh` 含 relation 各段 flag + `mutation_list` 追加 7 个变异；smoke-baseline.txt 未回退既有登记
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; L=$(bash "$S" --mutation-list) || exit 1; for m in A27-rel-org-bypass A30R-rel-dangling-leak A28-rel-enum-leak A31-rel-private-leak A30F-field-hard-delete A29-backref-private-leak A30T-table-hard-delete; do printf "%s\n" "$L" | grep -q "$m" || { echo "FAIL: 变异未登记 $m"; exit 1; }; done; for f in --a27-only --a28-only --a29-backref-only --a30-row-only --a30-table-only --a30-field-only --a31-only; do grep -q -- "$f" "$S" || { echo "FAIL: smoke 缺段 $f"; exit 1; }; done; echo OK'
  期望: OK

## BEHAVIOR 条目（真 Postgres + 真会话，evaluator 直接跑）

- [x] [BEHAVIOR] 本刀 4 个测试文件被 vitest **真收集真跑**：4 suite 全绿、用例数 ≥ 18（r2 新增 A29② 私有反向 + P2 注入两条后的下限）、失败 0，且跑的**恰是**本刀那 4 个文件（零收集时 vitest 自报错，不假绿）
  Test: manual:bash -c 'PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; [ -n "$PG" ] || { echo "FAIL: 缺 E2E_DATABASE_URL/DATABASE_URL"; exit 1; }; O=/tmp/wb-rel-vitest.json; rm -f "$O"; (cd apps/api && npx vitest run --config vitest.workbench-relations.config.ts --reporter=json --outputFile="$O") >/tmp/wb-rel-vitest.log 2>&1; RC=$?; [ -f "$O" ] || { echo "FAIL: vitest 未产报告(多半零收集)"; tail -30 /tmp/wb-rel-vitest.log; exit 1; }; jq -e "(.testResults|length)==4 and .numTotalTests>=18 and .numFailedTests==0 and .success==true" < "$O" >/dev/null || { echo "FAIL: 收集/通过数不符"; jq -c "{files:(.testResults|length),tests:.numTotalTests,failed:.numFailedTests,ok:.success}" < "$O"; exit 1; }; jq -e "[.testResults[].name|select(test(\"relations-(field-and-build|bidirectional|isolation-enum|integrity)\\\\.test\\\\.ts$\"))]|length==4" < "$O" >/dev/null || { echo "FAIL: 跑的不是本刀那 4 个文件"; exit 1; }; [ "$RC" = "0" ] || { echo "FAIL: vitest exit=$RC"; exit 1; }; echo OK'
  期望: OK

- [x] [BEHAVIOR] 建 relation 字段：`POST /tables/A/fields {field_type:"relation",options:[B]}` → 201、`field_type` 逐字 `relation`、`options[0]==B`；跨企业目标表 → 400（不 2xx）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; TA=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"RA-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); TB=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"RB-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null || fail "建 relation 字段非 2xx"; RF=$(curl -sf -b "$COOKIE_A" "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); [ -n "$RF" ] && [ "$RF" != "null" ] || fail "relation 字段未落库"; curl -sf -b "$COOKIE_A" "$API/tables/$TA/fields" | jq -e --arg tb "$TB" ".data.fields[]|select(.field_type==\"relation\")|.options[0]==\$tb" >/dev/null || fail "options[0] 非目标表"; TC=$(curl -sf -b "$COOKIE_B" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"RC-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); CODE=$(curl -s -o /dev/null -w "%{http_code}" -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"跨企业\",\"field_type\":\"relation\",\"options\":[\"$TC\"],\"display_order\":8}]}"); case "$CODE" in 4*) :;; *) fail "跨企业目标未被拒(返 $CODE)";; esac; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] 建关联落 JSONB：`PATCH /rows/:aRow {data:{<relField>:[b1,b2]}}` → 200，真查 `db_rows.data->'<relField>'` = `["b1","b2"]` 数组
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; sf(){ curl -sf -b "$COOKIE_A" "$@"; }; TA=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"JA-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); TB=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"JB-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); B1=$(sf -X POST "$API/tables/$TB/rows" | jq -r ".data.row_id"); B2=$(sf -X POST "$API/tables/$TB/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null || fail "建字段失败"; RF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); AR=$(sf -X POST "$API/tables/$TA/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X PATCH "$API/rows/$AR" -d "{\"version\":1,\"data\":{\"$RF\":[\"$B1\",\"$B2\"]}}" >/dev/null || fail "建关联 PATCH 非 2xx"; psql "$PG" -t -A -q -c "SELECT (data -> \$\$$RF\$\$) = to_jsonb(ARRAY[\$\$$B1\$\$,\$\$$B2\$\$]) FROM zenithjoy.db_rows WHERE id = \$\$$AR\$\$" | grep -qx t || fail "关联值未以数组落 db_rows.data"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] 双向可见 + Response Schema：候选端点 `data` keys 恰 `[candidates,field_id,target_table_id]`、候选项 keys 恰 `[row_id,title]`、禁用字段零命中；backrefs `data` keys 恰 `[backrefs,row_id]`、项 keys 恰五键、A 关联 B 后 B 侧反查列出 A 行标题
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; sf(){ curl -sf -b "$COOKIE_A" "$@"; }; TA=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"BA-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); TB=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"BB-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); TF=$(sf "$API/tables/$TB/fields" | jq -r ".data.fields[0].field_id"); B1=$(sf -X POST "$API/tables/$TB/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X PATCH "$API/rows/$B1" -d "{\"version\":1,\"data\":{\"$TF\":\"目标甲\"}}" >/dev/null; sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null; RF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); ATF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"text\")|.field_id"); AR=$(sf -X POST "$API/tables/$TA/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X PATCH "$API/rows/$AR" -d "{\"version\":1,\"data\":{\"$ATF\":\"来源行\",\"$RF\":[\"$B1\"]}}" >/dev/null || fail "建关联失败"; CAND=$(sf "$API/tables/$TA/fields/$RF/relation-candidates") || fail "候选端点非 2xx"; echo "$CAND" | jq -e "(.data|keys)==[\"candidates\",\"field_id\",\"target_table_id\"]" >/dev/null || fail "候选 envelope keys 不符 c=$CAND"; echo "$CAND" | jq -e ".data.candidates[]|(keys)==[\"row_id\",\"title\"]" >/dev/null || fail "候选项 keys 不符"; echo "$CAND" | jq -e ".data.candidates[]|([has(\"id\"),has(\"label\"),has(\"name\"),has(\"text\"),has(\"value\"),has(\"display\")]|any|not)" >/dev/null || fail "候选项出现禁用字段"; BR=$(sf "$API/rows/$B1/backrefs") || fail "反查端点非 2xx"; echo "$BR" | jq -e "(.data|keys)==[\"backrefs\",\"row_id\"]" >/dev/null || fail "backrefs envelope keys 不符"; echo "$BR" | jq -e --arg ta "$TA" --arg ar "$AR" ".data.backrefs|length==1 and .[0].table_id==\$ta and .[0].row_id==\$ar and .[0].row_title==\"来源行\"" >/dev/null || fail "反查未命中来源 br=$BR"; echo "$BR" | jq -e ".data.backrefs[]|(keys)==[\"field_id\",\"row_id\",\"row_title\",\"table_id\",\"table_name\"]" >/dev/null || fail "backref 项 keys 不符"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] A28 反枚举：carol(B企业)以 A 真实表 id 与随机不存在 id 各调候选端点 20 次，两组状态码全 404、响应体逐字节相同、无 timestamp（404 优先于 400）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; sf(){ curl -sf -b "$COOKIE_A" "$@"; }; TA=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"EA-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); TB=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"EB-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null; RF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); RND="00000000-0000-4000-8000-000000000000"; declare -A seen; for i in $(seq 1 20); do CR=$(curl -s -o /tmp/rel-real.json -w "%{http_code}" -b "$COOKIE_B" "$API/tables/$TA/fields/$RF/relation-candidates"); CX=$(curl -s -o /tmp/rel-rnd.json -w "%{http_code}" -b "$COOKIE_B" "$API/tables/$RND/fields/$RF/relation-candidates"); [ "$CR" = "404" ] || fail "真实表 id 未 404(得 $CR)"; [ "$CX" = "404" ] || fail "随机 id 未 404(得 $CX)"; seen["$(cat /tmp/rel-real.json)"]=1; seen["$(cat /tmp/rel-rnd.json)"]=1; done; [ "${#seen[@]}" = "1" ] || fail "真实/随机响应体可区分(${#seen[@]} 种)"; jq -e "has(\"timestamp\")|not" < /tmp/rel-real.json >/dev/null || fail "404 体带 timestamp(可区分信号)"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] A27② 读路径二次校验：把目标行 org 直接改成 B 企业后展开候选 → 该行被剔除（不泄露对方数据）；A31① bob 对 alice 私有表建关联被拒
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; sf(){ curl -sf -b "$COOKIE_A" "$@"; }; TA=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"SA-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); TB=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"SB-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); B1=$(sf -X POST "$API/tables/$TB/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null; RF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); sf "$API/tables/$TA/fields/$RF/relation-candidates" | jq -e --arg b "$B1" "[.data.candidates[].row_id]|index(\$b)!=null" >/dev/null || fail "篡改前候选应含 B1"; psql "$PG" -q -c "UPDATE zenithjoy.db_rows SET org_id = \$\$$ORGB_TENANT_ID\$\$ WHERE id = \$\$$B1\$\$" >/dev/null || fail "篡改 org 失败"; sf "$API/tables/$TA/fields/$RF/relation-candidates" | jq -e --arg b "$B1" "[.data.candidates[].row_id]|index(\$b)==null" >/dev/null || fail "读路径二次校验未剔除越权行(泄露)"; psql "$PG" -q -c "UPDATE zenithjoy.db_rows SET org_id = \$\$$ORGA_TENANT_ID\$\$ WHERE id = \$\$$B1\$\$" >/dev/null; PV=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"PVT-$SFX\",\"visibility\":\"private\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); BOBT=$(curl -sf -b "$COOKIE_A2" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"BOBS-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); CODE=$(curl -s -o /dev/null -w "%{http_code}" -b "$COOKIE_A2" -H "Content-Type: application/json" -X POST "$API/tables/$BOBT/fields" -d "{\"fields\":[{\"name\":\"偷指私有\",\"field_type\":\"relation\",\"options\":[\"$PV\"],\"display_order\":9}]}"); case "$CODE" in 4*) :;; *) fail "bob 指 alice 私有表未被拒(返 $CODE)";; esac; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] A30① 删被引用行安全失效：软删目标行 → 候选剔除它、来源读路径不 5xx、来源关联值仍是合法数组（不悬空可跳）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; sf(){ curl -sf -b "$COOKIE_A" "$@"; }; TA=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"DA-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); TB=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"DB-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); B1=$(sf -X POST "$API/tables/$TB/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null; RF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); AR=$(sf -X POST "$API/tables/$TA/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X PATCH "$API/rows/$AR" -d "{\"version\":1,\"data\":{\"$RF\":[\"$B1\"]}}" >/dev/null; sf -X DELETE "$API/rows/$B1" >/dev/null || fail "软删目标行失败"; sf "$API/tables/$TA/fields/$RF/relation-candidates" | jq -e --arg b "$B1" "[.data.candidates[].row_id]|index(\$b)==null" >/dev/null || fail "已删行未从候选剔除(悬空)"; CODE=$(curl -s -o /dev/null -w "%{http_code}" -b "$COOKIE_A" "$API/rows/$B1/backrefs"); case "$CODE" in 200|404) :;; *) fail "已删行反查返 $CODE(应 200/404 非 5xx)";; esac; psql "$PG" -t -A -q -c "SELECT jsonb_typeof(data -> \$\$$RF\$\$) FROM zenithjoy.db_rows WHERE id = \$\$$AR\$\$" | grep -qx array || fail "来源关联值不再是数组(结构被写坏)"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] A30② 删表还原引用恢复：软删目标表 → 候选 404；还原 → 候选恢复且含被引用行
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; sf(){ curl -sf -b "$COOKIE_A" "$@"; }; TA=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"TA-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); TBN="TB2-$SFX"; TB=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"$TBN\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); B1=$(sf -X POST "$API/tables/$TB/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null; RF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); sf -H "Content-Type: application/json" -X DELETE "$API/tables/$TB" -d "{\"confirm_name\":\"$TBN\"}" >/dev/null || fail "软删表失败"; CODE=$(curl -s -o /dev/null -w "%{http_code}" -b "$COOKIE_A" "$API/tables/$TA/fields/$RF/relation-candidates"); [ "$CODE" = "404" ] || fail "删表后候选未 404(得 $CODE)"; sf -X POST "$API/trash/$TB/restore" >/dev/null || fail "还原表失败"; sf "$API/tables/$TA/fields/$RF/relation-candidates" | jq -e --arg b "$B1" "[.data.candidates[].row_id]|index(\$b)!=null" >/dev/null || fail "还原后候选未恢复被引用行"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] A30③ 删字段软删+二次确认+值保留：confirm_name 不符→400字段不删；相符→200 软删（db_fields 行仍在+deleted_at 置位+db_rows.data 旧值保留）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; sf(){ curl -sf -b "$COOKIE_A" "$@"; }; TA=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"FA-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); TB=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"FB-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); B1=$(sf -X POST "$API/tables/$TB/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null; RF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); AR=$(sf -X POST "$API/tables/$TA/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X PATCH "$API/rows/$AR" -d "{\"version\":1,\"data\":{\"$RF\":[\"$B1\"]}}" >/dev/null; WRONG=$(curl -s -o /dev/null -w "%{http_code}" -b "$COOKIE_A" -H "Content-Type: application/json" -X DELETE "$API/tables/$TA/fields/$RF" -d "{\"confirm_name\":\"乱填\"}"); [ "$WRONG" = "400" ] || fail "confirm 不符未返 400(得 $WRONG)"; sf "$API/tables/$TA/fields" | jq -e --arg f "$RF" "[.data.fields[].field_id]|index(\$f)!=null" >/dev/null || fail "confirm 不符时字段被误删"; sf -H "Content-Type: application/json" -X DELETE "$API/tables/$TA/fields/$RF" -d "{\"confirm_name\":\"关联\"}" >/dev/null || fail "软删字段非 2xx"; sf "$API/tables/$TA/fields" | jq -e --arg f "$RF" "[.data.fields[].field_id]|index(\$f)==null" >/dev/null || fail "软删后字段仍在列表"; psql "$PG" -t -A -q -c "SELECT deleted_at IS NOT NULL FROM zenithjoy.db_fields WHERE id = \$\$$RF\$\$" | grep -qx t || fail "db_fields 行不在或 deleted_at 未置位(物理删?)"; psql "$PG" -t -A -q -c "SELECT (data ? \$\$$RF\$\$) FROM zenithjoy.db_rows WHERE id = \$\$$AR\$\$" | grep -qx t || fail "字段软删后 db_rows.data 旧值丢失"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] 无运行时 DDL：建 relation 字段+建关联+反查全程 zenithjoy schema 的表/列集合逐字节不变，且零 %relation% 物理表（relation 不进标识符位）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; sf(){ curl -sf -b "$COOKIE_A" "$@"; }; snap(){ psql "$PG" -t -A -q -c "SELECT string_agg(table_name||\$\$.\$\$||column_name, \$\$,\$\$ ORDER BY table_name,column_name) FROM information_schema.columns WHERE table_schema=\$\$zenithjoy\$\$"; }; B0=$(snap); TA=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"NA-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); TB=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"NB-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); B1=$(sf -X POST "$API/tables/$TB/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null; RF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); AR=$(sf -X POST "$API/tables/$TA/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X PATCH "$API/rows/$AR" -d "{\"version\":1,\"data\":{\"$RF\":[\"$B1\"]}}" >/dev/null; sf "$API/rows/$B1/backrefs" >/dev/null; B1S=$(snap); [ "$B0" = "$B1S" ] || fail "运行时 information_schema 变了(疑似运行时 DDL)"; N=$(psql "$PG" -t -A -q -c "SELECT count(*) FROM information_schema.tables WHERE table_schema=\$\$zenithjoy\$\$ AND table_name LIKE \$\$%relation%\$\$"); [ "$N" = "0" ] || fail "出现 $N 张 relation 命名物理表"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] A29②「仅自己」表反向面板仅表主可见：alice 用私有表 P 引用同组织可见行 X → bob 看 X 的 backrefs **不含** P 来源（零泄露）；alice 表主本人**能看到** P 来源（正反成对，堵「一律空」假绿）。判定口径：backref 来源表 visibility='private' 且 owner_member_id ≠ 请求者 → 剔除
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; a(){ curl -sf -b "$COOKIE_A" "$@"; }; T=$(a -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"29T-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); TF=$(a "$API/tables/$T/fields" | jq -r ".data.fields[0].field_id"); X=$(a -X POST "$API/tables/$T/rows" | jq -r ".data.row_id"); a -H "Content-Type: application/json" -X PATCH "$API/rows/$X" -d "{\"version\":1,\"data\":{\"$TF\":\"行X\"}}" >/dev/null; P=$(a -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"29P-$SFX\",\"visibility\":\"private\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); a -H "Content-Type: application/json" -X POST "$API/tables/$P/fields" -d "{\"fields\":[{\"name\":\"私有引用\",\"field_type\":\"relation\",\"options\":[\"$T\"],\"display_order\":9}]}" >/dev/null; RF=$(a "$API/tables/$P/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); PR=$(a -X POST "$API/tables/$P/rows" | jq -r ".data.row_id"); a -H "Content-Type: application/json" -X PATCH "$API/rows/$PR" -d "{\"version\":1,\"data\":{\"$RF\":[\"$X\"]}}" >/dev/null || fail "私有表建关联失败"; curl -sf -b "$COOKIE_A2" "$API/rows/$X/backrefs" | jq -e --arg p "$P" "[.data.backrefs[].table_id]|index(\$p)==null" >/dev/null || fail "bob 看到了 alice 私有表反向来源(面客泄露)"; a "$API/rows/$X/backrefs" | jq -e --arg p "$P" "[.data.backrefs[].table_id]|index(\$p)!=null" >/dev/null || fail "alice 表主本人看不到自己私有表来源(一律空假绿)"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] P2 注入对抗面：relation 值塞非 uuid / SQL 片段 → 400 VALIDATION_FAILED（用户输入不进标识符位，深校验拒越权/非法 row_id）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; a(){ curl -sf -b "$COOKIE_A" "$@"; }; T=$(a -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"INJT-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); TB=$(a -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"INJB-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); a -H "Content-Type: application/json" -X POST "$API/tables/$T/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null; RF=$(a "$API/tables/$T/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); AR=$(a -X POST "$API/tables/$T/rows" | jq -r ".data.row_id"); R=$(curl -s -b "$COOKIE_A" -o /tmp/inj.json -w "%{http_code}" -H "Content-Type: application/json" -X PATCH "$API/rows/$AR" -d "{\"version\":1,\"data\":{\"$RF\":[\"'"'"'; DROP TABLE zenithjoy.db_rows; --\",\"not-a-uuid\"]}}"); [ "$R" = "400" ] || fail "非法 relation 值未返 400(得 $R)"; jq -e ".error.code==\"VALIDATION_FAILED\"" < /tmp/inj.json >/dev/null || fail "错误码非 VALIDATION_FAILED"; psql "${E2E_DATABASE_URL:-$DATABASE_URL}" -t -A -q -c "SELECT to_regclass(\$\$zenithjoy.db_rows\$\$) IS NOT NULL" | grep -qx t || fail "db_rows 表被注入删了(标识符位失守)"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] 变异 A27-rel-org-bypass proven-to-fire：去掉关联目标 org 校验后 `--a27-only` 必须 exit≠0（跨企业/私有可关联有人红）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --mutation-apply A27-rel-org-bypass || exit 1; bash "$S" --a27-only; RC=$?; bash "$S" --mutation-revert A27-rel-org-bypass; [ "$RC" -ne 0 ] || { echo "FAIL: 去掉目标 org 校验后 A27 段仍 exit 0(隔离护栏空)"; exit 1; }; echo OK'
  期望: OK

- [x] [BEHAVIOR] 其余 6 个变异 proven-to-fire（覆盖 GP §6 A29②/A30②③/A28/A31 各维度）：各自让对应段 exit≠0
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; prove(){ M="$1"; SEG="$2"; bash "$S" --mutation-apply "$M" || return 1; bash "$S" "$SEG"; RC=$?; bash "$S" --mutation-revert "$M"; [ "$RC" -ne 0 ] || { echo "FAIL: $M 下 $SEG 仍 exit 0"; return 1; }; }; prove A30R-rel-dangling-leak --a30-row-only || exit 1; prove A28-rel-enum-leak --a28-only || exit 1; prove A30F-field-hard-delete --a30-field-only || exit 1; prove A31-rel-private-leak --a31-only || exit 1; prove A29-backref-private-leak --a29-backref-only || exit 1; prove A30T-table-hard-delete --a30-table-only || exit 1; echo OK'
  期望: OK

## BEHAVIOR:E2E 条目（user_facing 专属，Mode B final-e2e 跑 windows_cloud）

- [x] [BEHAVIOR:E2E] 用户在真浏览器走完 S4 关联 Golden Path（windows-latest），本刀 relation step + 截图可视化验证
  Screenshots:
    - 01-relation-build.png   期望：建 relation 字段配目标表后，表A某行单元格显示所选目标记录标题
    - 02-relation-jump.png    期望：点击关联项后，跳转到目标表该记录详情面板（关键元素可见）
    - 03-relation-backref.png 期望：打开被引用记录详情，反向面板列出引用来源（来源表名+行标题）
  路径格式：sprints/08220300-workbench-sprintD-relations/screenshots/<step>.png
  期望：e2e-knowledge-hub-path3.yml 的 windows job conclusion==success + 本刀 relation step success + 从 artifact path3-relations-screenshots 取回 ≥3 张非空截图（判据见 contract-draft.md `## E2E 验收` bash 块）
