---
skeleton: false
journey_type: autonomous
---
# Contract DoD — Sprint: 抖音私信主动触达 · Android 执行路径

**范围**: Android 无障碍服务私信发送执行面（新增，非替换 Windows 路径）+ 本地频控计数器 + 点击后重抓快照纪律 + 中台派单按能力标记 device_platform + `/dm-outreach-result` 按 `dm_assignment_id` 幂等回传（带 device_platform=android）+ 修复 `dm_outreach_log` 缺 `assignment_id` 列导致触达记录页 outreach-history 长期返回空列表的既有断点。真机无障碍点击/真送达确认仅人工在 adb+Tailscale 环境补验（PRD `target_environment_reason` 显式声明），不计入自动裁决。
**大小**: M

> 所有 [BEHAVIOR] 在 local_api 跑，依赖 env：`API_BASE`（默认 `http://localhost:5200`）、`DB`/`DATABASE_URL`（默认 `postgresql://postgres:postgres@localhost:5432/zenithjoy_test`）。Android 端 [BEHAVIOR] 依赖 `ANDROID_HOME`（本地 gradle 单测），CI windows/linux runner 上必须可跑；本地开发机缺 SDK 时该条降级为 `logic-done-pending`，不得直接标 done。

## ARTIFACT 条目

- [ ] [ARTIFACT] Android 频控计数器模块存在（纯函数，可脱离 Android 框架单测）
  Test: manual:bash -c 'f=$(grep -rl "object DmOutreachRateLimiter" services/agent-android/app/src/main/kotlin 2>/dev/null | head -1); [ -n "$f" ] || { echo FAIL; exit 1; }; echo OK'
  期望: OK

- [ ] [ARTIFACT] Android 快照重抓纪律辅助模块存在
  Test: manual:bash -c 'f=$(grep -rl "SnapshotDiscipline" services/agent-android/app/src/main/kotlin 2>/dev/null | head -1); [ -n "$f" ] || { echo FAIL; exit 1; }; echo OK'
  期望: OK

- [ ] [ARTIFACT] `dm_outreach_log` 新增 `assignment_id` 列的迁移文件存在（修复既有 outreach-history 断点）
  Test: manual:bash -c 'f=$(grep -rl "ADD COLUMN IF NOT EXISTS assignment_id" apps/api/db/migrations 2>/dev/null | head -1); [ -n "$f" ] || { echo FAIL; exit 1; }; echo OK'
  期望: OK

- [ ] [ARTIFACT] `/dm-outreach-result` handler 接受 `device_platform` + `dm_assignment_id` 字段
  Test: manual:bash -c 'f=apps/api/src/routes/agent-burner.ts; grep -q "device_platform" "$f" && grep -q "dm_assignment_id" "$f" || { echo FAIL; exit 1; }; echo OK'
  期望: OK

## BEHAVIOR 条目（manual:bash，自带 seed，独立可跑）

- [ ] [BEHAVIOR] 频控计数器：10 分钟窗口内已发 ≥3 条判定为拒绝，窗口外历史不计入（Android 单元测试真跑，非 mock）
  Test: manual:bash -c 'set -e; cd services/agent-android; [ -n "${ANDROID_HOME:-}" ] || { echo "SKIP(no ANDROID_HOME) — logic-done-pending"; exit 0; }; gradle :app:testDebugUnitTest --tests "*DmOutreachRateLimiterTest*" --rerun; f=app/build/test-results/testDebugUnitTest/TEST-com.zenithjoy.agent.collect.DmOutreachRateLimiterTest.xml; grep -q "failures=\"0\" errors=\"0\"" "$f" || { echo FAIL; exit 1; }; echo OK'
  期望: OK

- [ ] [BEHAVIOR] 点击后必须重新抓取快照，不得复用旧引用（Android 单元测试真跑，非 mock）
  Test: manual:bash -c 'set -e; cd services/agent-android; [ -n "${ANDROID_HOME:-}" ] || { echo "SKIP(no ANDROID_HOME) — logic-done-pending"; exit 0; }; gradle :app:testDebugUnitTest --tests "*DmOutreachSnapshotDisciplineTest*" --rerun; f=app/build/test-results/testDebugUnitTest/TEST-com.zenithjoy.agent.collect.DmOutreachSnapshotDisciplineTest.xml; grep -q "failures=\"0\" errors=\"0\"" "$f" || { echo FAIL; exit 1; }; echo OK'
  期望: OK

- [ ] [BEHAVIOR] 派单按 agent 能力标记 device_platform：capabilities 含 android 的 agent 拿到的任务 payload.device_platform=android
  Test: manual:bash -c 'set -e; API_BASE="${API_BASE:-http://localhost:5200}"; DB="${DB_URL:-${DATABASE_URL:-postgresql://postgres:postgres@localhost:5432/zenithjoy_test}}"; T=$(psql "$DB" -At -c "INSERT INTO zenithjoy.tenants (name, license_key, plan) VALUES ('"'"'dmand-b3-$RANDOM'"'"','"'"'dmand-k-$RANDOM'"'"','"'"'free'"'"') RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); A=$(psql "$DB" -At -c "INSERT INTO zenithjoy.agents (tenant_id, agent_id, capabilities, status) VALUES ('"'"'$T'"'"','"'"'dmand-a-$RANDOM'"'"', ARRAY['"'"'android'"'"'], '"'"'online'"'"') RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions (agent_id, platform, account_label, role, status, bound_at) VALUES ('"'"'$A'"'"','"'"'douyin'"'"','"'"'andr-b3'"'"','"'"'burner'"'"','"'"'active'"'"', NOW())" >/dev/null; L=$(psql "$DB" -At -c "INSERT INTO zenithjoy.acquisition_leads (tenant_id, sec_uid, nickname, profile_url, relevance_score) VALUES ('"'"'$T'"'"','"'"'sec-b3'"'"','"'"'客户b3'"'"','"'"'https://www.douyin.com/user/sec-b3'"'"', 88) RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); curl -fsS -X PUT -H "X-Tenant-Id: $T" -H "Content-Type: application/json" -d "{\"dm_per_day\":30,\"dm_per_hour\":10,\"burner_count\":2,\"dm_active_start\":\"00:00\",\"dm_active_end\":\"23:59\",\"dm_interval_min_sec\":1,\"dm_interval_max_sec\":2}" "$API_BASE/api/acquisition/config" >/dev/null; psql "$DB" -c "INSERT INTO zenithjoy.dm_assignments (tenant_id, lead_id, account_label, status, scheduled_for) VALUES ('"'"'$T'"'"','"'"'$L'"'"','"'"'andr-b3'"'"','"'"'queued'"'"', NOW() - interval '"'"'1 minute'"'"')" >/dev/null; curl -fsS -X POST -H "X-Tenant-Id: $T" "$API_BASE/api/acquisition/dispatch/run" >/dev/null; DP=$(psql "$DB" -At -c "SELECT payload->>'"'"'device_platform'"'"' FROM zenithjoy.publish_tasks WHERE task_type='"'"'dm_outreach'"'"' AND agent_id='"'"'$A'"'"' ORDER BY created_at DESC LIMIT 1"); [ "$DP" = "android" ] || { echo "FAIL: device_platform=$DP"; exit 1; }; echo OK'
  期望: OK

- [ ] [BEHAVIOR] /dm-outreach-result 幂等：同一 dm_assignment_id 重复 POST 两次，dm_outreach_log 只计一次（不重复计数）
  Test: manual:bash -c 'set -e; API_BASE="${API_BASE:-http://localhost:5200}"; DB="${DB_URL:-${DATABASE_URL:-postgresql://postgres:postgres@localhost:5432/zenithjoy_test}}"; T=$(psql "$DB" -At -c "INSERT INTO zenithjoy.tenants (name, license_key, plan) VALUES ('"'"'dmand-b4-$RANDOM'"'"','"'"'dmand-k4-$RANDOM'"'"','"'"'free'"'"') RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); A=$(psql "$DB" -At -c "INSERT INTO zenithjoy.agents (tenant_id, agent_id, capabilities, status) VALUES ('"'"'$T'"'"','"'"'dmand-a4-$RANDOM'"'"', ARRAY['"'"'android'"'"'], '"'"'online'"'"') RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions (agent_id, platform, account_label, role, status, bound_at) VALUES ('"'"'$A'"'"','"'"'douyin'"'"','"'"'andr-b4'"'"','"'"'burner'"'"','"'"'active'"'"', NOW())" >/dev/null; L=$(psql "$DB" -At -c "INSERT INTO zenithjoy.acquisition_leads (tenant_id, sec_uid, nickname, profile_url, relevance_score) VALUES ('"'"'$T'"'"','"'"'sec-b4'"'"','"'"'客户b4'"'"','"'"'https://www.douyin.com/user/sec-b4'"'"', 88) RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); ASSIGN=$(psql "$DB" -At -c "INSERT INTO zenithjoy.dm_assignments (tenant_id, lead_id, account_label, status, scheduled_for) VALUES ('"'"'$T'"'"','"'"'$L'"'"','"'"'andr-b4'"'"','"'"'queued'"'"', NOW() - interval '"'"'1 minute'"'"') RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); curl -fsS -X PUT -H "X-Tenant-Id: $T" -H "Content-Type: application/json" -d "{\"dm_per_day\":30,\"dm_per_hour\":10,\"burner_count\":2,\"dm_active_start\":\"00:00\",\"dm_active_end\":\"23:59\",\"dm_interval_min_sec\":1,\"dm_interval_max_sec\":2}" "$API_BASE/api/acquisition/config" >/dev/null; curl -fsS -X POST -H "X-Tenant-Id: $T" "$API_BASE/api/acquisition/dispatch/run" >/dev/null; TID=$(psql "$DB" -At -c "SELECT id FROM zenithjoy.publish_tasks WHERE task_type='"'"'dm_outreach'"'"' AND agent_id='"'"'$A'"'"' ORDER BY created_at DESC LIMIT 1"); BEFORE=$(psql "$DB" -At -c "SELECT count(*) FROM zenithjoy.dm_outreach_log WHERE tenant_id='"'"'$T'"'"' AND account_label='"'"'andr-b4'"'"' AND status='"'"'sent'"'"'"); curl -sf -X POST "$API_BASE/api/agent/burner/dm-outreach-result" -H "Content-Type: application/json" -d "{\"task_id\":\"$TID\",\"agent_id\":\"$A\",\"account_label\":\"andr-b4\",\"status\":\"sent\",\"profile_url\":\"https://www.douyin.com/user/sec-b4\",\"device_platform\":\"android\",\"dm_assignment_id\":\"$ASSIGN\"}" >/dev/null; curl -sf -X POST "$API_BASE/api/agent/burner/dm-outreach-result" -H "Content-Type: application/json" -d "{\"task_id\":\"$TID\",\"agent_id\":\"$A\",\"account_label\":\"andr-b4\",\"status\":\"sent\",\"profile_url\":\"https://www.douyin.com/user/sec-b4\",\"device_platform\":\"android\",\"dm_assignment_id\":\"$ASSIGN\"}" >/dev/null; AFTER=$(psql "$DB" -At -c "SELECT count(*) FROM zenithjoy.dm_outreach_log WHERE tenant_id='"'"'$T'"'"' AND account_label='"'"'andr-b4'"'"' AND status='"'"'sent'"'"'"); [ "$AFTER" = "$((BEFORE + 1))" ] || { echo "FAIL before=$BEFORE after=$AFTER"; exit 1; }; echo OK'
  期望: OK

- [ ] [BEHAVIOR] outreach-history 联表修复后可见该记录 status=sent（既有 assignment_id 缺列断点已修）
  Test: manual:bash -c 'set -e; API_BASE="${API_BASE:-http://localhost:5200}"; DB="${DB_URL:-${DATABASE_URL:-postgresql://postgres:postgres@localhost:5432/zenithjoy_test}}"; COL=$(psql "$DB" -At -c "SELECT column_name FROM information_schema.columns WHERE table_schema='"'"'zenithjoy'"'"' AND table_name='"'"'dm_outreach_log'"'"' AND column_name='"'"'assignment_id'"'"'"); [ "$COL" = "assignment_id" ] || { echo "FAIL: 列未补齐"; exit 1; }; T=$(psql "$DB" -At -c "INSERT INTO zenithjoy.tenants (name, license_key, plan) VALUES ('"'"'dmand-b5-$RANDOM'"'"','"'"'dmand-k5-$RANDOM'"'"','"'"'free'"'"') RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); A=$(psql "$DB" -At -c "INSERT INTO zenithjoy.agents (tenant_id, agent_id, capabilities, status) VALUES ('"'"'$T'"'"','"'"'dmand-a5-$RANDOM'"'"', ARRAY['"'"'android'"'"'], '"'"'online'"'"') RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions (agent_id, platform, account_label, role, status, bound_at) VALUES ('"'"'$A'"'"','"'"'douyin'"'"','"'"'andr-b5'"'"','"'"'burner'"'"','"'"'active'"'"', NOW())" >/dev/null; L=$(psql "$DB" -At -c "INSERT INTO zenithjoy.acquisition_leads (tenant_id, sec_uid, nickname, profile_url, relevance_score) VALUES ('"'"'$T'"'"','"'"'sec-b5'"'"','"'"'客户b5'"'"','"'"'https://www.douyin.com/user/sec-b5'"'"', 88) RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); ASSIGN=$(psql "$DB" -At -c "INSERT INTO zenithjoy.dm_assignments (tenant_id, lead_id, account_label, status, scheduled_for) VALUES ('"'"'$T'"'"','"'"'$L'"'"','"'"'andr-b5'"'"','"'"'queued'"'"', NOW() - interval '"'"'1 minute'"'"') RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); curl -fsS -X PUT -H "X-Tenant-Id: $T" -H "Content-Type: application/json" -d "{\"dm_per_day\":30,\"dm_per_hour\":10,\"burner_count\":2,\"dm_active_start\":\"00:00\",\"dm_active_end\":\"23:59\",\"dm_interval_min_sec\":1,\"dm_interval_max_sec\":2}" "$API_BASE/api/acquisition/config" >/dev/null; curl -fsS -X POST -H "X-Tenant-Id: $T" "$API_BASE/api/acquisition/dispatch/run" >/dev/null; TID=$(psql "$DB" -At -c "SELECT id FROM zenithjoy.publish_tasks WHERE task_type='"'"'dm_outreach'"'"' AND agent_id='"'"'$A'"'"' ORDER BY created_at DESC LIMIT 1"); curl -sf -X POST "$API_BASE/api/agent/burner/dm-outreach-result" -H "Content-Type: application/json" -d "{\"task_id\":\"$TID\",\"agent_id\":\"$A\",\"account_label\":\"andr-b5\",\"status\":\"sent\",\"profile_url\":\"https://www.douyin.com/user/sec-b5\",\"device_platform\":\"android\",\"dm_assignment_id\":\"$ASSIGN\"}" >/dev/null; HIST=$(curl -sf -H "X-Tenant-Id: $T" "$API_BASE/api/acquisition/dispatch/outreach-history"); echo "$HIST" | jq -e "[.data.items[] | select(.id==\"$ASSIGN\")] | any(.status==\"sent\")" >/dev/null || { echo "FAIL: $HIST"; exit 1; }; echo OK'
  期望: OK

- [ ] [BEHAVIOR] error path：无 android capability 的 agent 不会被打上 device_platform=android（禁止误标）
  Test: manual:bash -c 'set -e; API_BASE="${API_BASE:-http://localhost:5200}"; DB="${DB_URL:-${DATABASE_URL:-postgresql://postgres:postgres@localhost:5432/zenithjoy_test}}"; T=$(psql "$DB" -At -c "INSERT INTO zenithjoy.tenants (name, license_key, plan) VALUES ('"'"'dmand-b6-$RANDOM'"'"','"'"'dmand-k6-$RANDOM'"'"','"'"'free'"'"') RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); A=$(psql "$DB" -At -c "INSERT INTO zenithjoy.agents (tenant_id, agent_id, capabilities, status) VALUES ('"'"'$T'"'"','"'"'dmand-w6-$RANDOM'"'"', ARRAY[]::text[], '"'"'online'"'"') RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions (agent_id, platform, account_label, role, status, bound_at) VALUES ('"'"'$A'"'"','"'"'douyin'"'"','"'"'win-b6'"'"','"'"'burner'"'"','"'"'active'"'"', NOW())" >/dev/null; L=$(psql "$DB" -At -c "INSERT INTO zenithjoy.acquisition_leads (tenant_id, sec_uid, nickname, profile_url, relevance_score) VALUES ('"'"'$T'"'"','"'"'sec-b6'"'"','"'"'客户b6'"'"','"'"'https://www.douyin.com/user/sec-b6'"'"', 88) RETURNING id" | grep -oE "[0-9a-f-]{36}" | head -1); curl -fsS -X PUT -H "X-Tenant-Id: $T" -H "Content-Type: application/json" -d "{\"dm_per_day\":30,\"dm_per_hour\":10,\"burner_count\":2,\"dm_active_start\":\"00:00\",\"dm_active_end\":\"23:59\",\"dm_interval_min_sec\":1,\"dm_interval_max_sec\":2}" "$API_BASE/api/acquisition/config" >/dev/null; psql "$DB" -c "INSERT INTO zenithjoy.dm_assignments (tenant_id, lead_id, account_label, status, scheduled_for) VALUES ('"'"'$T'"'"','"'"'$L'"'"','"'"'win-b6'"'"','"'"'queued'"'"', NOW() - interval '"'"'1 minute'"'"')" >/dev/null; curl -fsS -X POST -H "X-Tenant-Id: $T" "$API_BASE/api/acquisition/dispatch/run" >/dev/null; DP=$(psql "$DB" -At -c "SELECT payload->>'"'"'device_platform'"'"' FROM zenithjoy.publish_tasks WHERE task_type='"'"'dm_outreach'"'"' AND agent_id='"'"'$A'"'"' ORDER BY created_at DESC LIMIT 1"); [ "$DP" != "android" ] || { echo "FAIL: 非 android agent 被误标 android"; exit 1; }; echo OK'
  期望: OK
