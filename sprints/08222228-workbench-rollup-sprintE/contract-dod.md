---
skeleton: false
journey_type: user_facing
---
# Contract DoD — Sprint: 路③ Sprint E · S4 加厚 rollup/lookup 聚合

**范围**: rollup/lookup 字段类型（配置三元组存 db_fields.options）+ 六函数(count/sum/min/max/concat/lookup) + 类型×函数校验 + 读时聚合服务（钉死 workbench-rollup.service.ts）+ 组织隔离（聚合基数 org 二次校验）+ 失效降级三支（删 relation 字段/删目标字段/软删目标表）+ 数值规整 + 多值格式化 + rollup 只读单元格渲染 + 配置器 UI + rollups 只读端点 + 墙裁定守卫 + 接进既有 path3 workflow/vitest/smoke。**不重造** A33 workflow / A35 排除清单 / A32/A34 路由族+鉴权闸 / Sprint D relation 解析链（均在 base）。
**大小**: L

## ARTIFACT 条目

- [x] [ARTIFACT] **ART-1 rollup 读服务落钉死路径（A41 grep 靶固定，不并入他文件）** —— `apps/api/src/services/workbench-rollup.service.ts` 存在且含聚合读函数；rollup 聚合逻辑不并进 `workbench-relations.service.ts`
  Test: manual:bash -c '[ -f apps/api/src/services/workbench-rollup.service.ts ] || { echo "FAIL: rollup 服务未落钉死路径"; exit 1; }; grep -qE "count|sum|min|max|concat|lookup" apps/api/src/services/workbench-rollup.service.ts || { echo "FAIL: rollup 服务无聚合函数"; exit 1; }; echo OK'
  期望: OK

- [x] [ARTIFACT] **ART-2 rollup/lookup 三处同步登记** —— migration 把 `rollup`/`lookup` 加进 `db_fields` field_type CHECK；`workbench.service.ts` 的 ACCEPTED_FIELD_TYPES 含 rollup/lookup；rollups 端点挂 `workbench.ts`
  Test: manual:bash -c 'grep -rIlq "rollup" apps/api/db/migrations/*rollup*.sql 2>/dev/null || { echo "FAIL: 缺 rollup migration"; exit 1; }; grep -q "rollup" apps/api/src/services/workbench.service.ts || { echo "FAIL: ACCEPTED_FIELD_TYPES 缺 rollup"; exit 1; }; grep -q "lookup" apps/api/src/services/workbench.service.ts || { echo "FAIL: 缺 lookup"; exit 1; }; grep -q "rollups" apps/api/src/routes/workbench.ts || { echo "FAIL: 缺 rollups 端点"; exit 1; }; echo OK'
  期望: OK

- [x] [ARTIFACT] **ART-3 零新建表 + 源码零明文身份头** —— migration 零 rollup 物理表建表 SQL；`retrieval-exclusions.ts` 仍恰五张表名（rollup 零新表无需改）；rollup 服务零禁用身份头字面量（A2 家族口径）
  Test: manual:bash -c 'grep -rIn "CREATE TABLE" apps/api/db/migrations/ | grep -iE "rollup|db_agg" | grep -vi "exclusion" && { echo "FAIL: 出现 rollup 物理表"; exit 1; }; for L in X-Tenant-Id X-User-Email X-Feishu-User-Id X-Bypass-Tenant tenantContextOptional selfHealOwnerMember staffGuard; do grep -rq "$L" apps/api/src/services/workbench-rollup.service.ts 2>/dev/null && { echo "FAIL: rollup 服务出现禁用身份头 $L"; exit 1; }; done; echo OK'
  期望: OK

- [x] [ARTIFACT] **ART-4 rollup 合同测试有真实收集配置** —— `vitest.workbench-rollup.config.ts` 白名单含本 sprint tests 目录 + `apps/api` 暴露 `test:workbench-rollup` + setup 从连接串推导 `DATABASE_*`
  Test: manual:bash -c 'grep -q "08222228-workbench-rollup-sprintE/tests" apps/api/vitest.workbench-rollup.config.ts || { echo "FAIL: config 未白名单本刀 tests"; exit 1; }; node -e "const p=require(\"./apps/api/package.json\");process.exit(p.scripts[\"test:workbench-rollup\"]?0:1)" || { echo "FAIL: 缺 test:workbench-rollup script"; exit 1; }; grep -q "DATABASE_HOST" apps/api/vitest.workbench-rollup.setup.ts || { echo "FAIL: setup 未推导 DATABASE_*"; exit 1; }; echo OK'
  期望: OK

- [x] [ARTIFACT] **ART-5 E2E workflow 逐字接线（接入不重造）** —— `e2e-knowledge-hub-path3.yml` 的 `paths` 含本刀 spec/config/sprint 目录；linux job 含 `test:workbench-rollup`；windows job 有真调 `e2e-rollup-run.ps1` 的 step；本刀截图有独立 upload step（`path3-rollup-screenshots`）；windows job **仍无 job 级事件条件门**（A33(c) 不回退）
  Test: manual:bash -c 'WF=.github/workflows/e2e-knowledge-hub-path3.yml; for P in "structured-workbench-rollup.spec.ts" "08222228-workbench-rollup-sprintE" "test:workbench-rollup" "e2e-rollup-run.ps1" "path3-rollup-screenshots"; do grep -q "$P" "$WF" || { echo "FAIL: workflow 缺 $P"; exit 1; }; done; node -e "const y=require(\"fs\").readFileSync(\".github/workflows/e2e-knowledge-hub-path3.yml\",\"utf8\");const m=y.match(/windows-real-browser:[\s\S]*?(?=\n  [a-z]|$)/);if(!m)process.exit(1);const head=m[0].split(\"\n\").filter(l=>/^    [a-z_]+:/.test(l)).join(\"\n\");process.exit(/if:.*(workflow_dispatch|github\.event_name)/.test(head)?1:0)" || { echo "FAIL: windows job 加了事件条件门(A33c 回退)"; exit 1; }; echo OK'
  期望: OK

- [x] [ARTIFACT] **ART-6 rollup E2E spec 存在、零请求拦截**（变体C 死规则）—— `structured-workbench-rollup.spec.ts` 存在、零 `page.route(`、含 ASCII 标签 `@rollup-build`/`@rollup-lookup`/`@rollup-degrade`
  Test: manual:bash -c 'SP=apps/staff-hub/e2e/structured-workbench-rollup.spec.ts; [ -f "$SP" ] || { echo "FAIL: 缺 rollup spec"; exit 1; }; grep -q "page.route(" "$SP" && { echo "FAIL: spec 出现请求拦截"; exit 1; }; for T in "@rollup-build" "@rollup-lookup" "@rollup-degrade"; do grep -q "$T" "$SP" || { echo "FAIL: spec 缺标签 $T"; exit 1; }; done; echo OK'
  期望: OK

- [x] [ARTIFACT] **ART-7 smoke 登记 rollup 段 + 4 变异开关 + A41 守卫脚本** —— `structured-workbench-smoke.sh` 含 rollup 各段 flag + `mutation_list` 追加 4 变异；`scripts/rollup-wall-guard.sh` 存在
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; L=$(bash "$S" --mutation-list) || exit 1; for m in A38-rollup-org-bypass A39-rollup-degrade-bypass A40-rollup-typecheck-off A41-rollup-retrieval-import; do printf "%s\n" "$L" | grep -q "$m" || { echo "FAIL: 变异未登记 $m"; exit 1; }; done; for f in --rollup-a37-only --rollup-a38-only --rollup-a39-only --rollup-a40-only --rollup-ddl-only; do grep -q -- "$f" "$S" || { echo "FAIL: smoke 缺段 $f"; exit 1; }; done; [ -f sprints/08222228-workbench-rollup-sprintE/scripts/rollup-wall-guard.sh ] || { echo "FAIL: 缺 A41 守卫脚本"; exit 1; }; echo OK'
  期望: OK

## BEHAVIOR 条目（真 Postgres + 真会话，evaluator 直接跑）

- [x] [BEHAVIOR] 本刀 4 个测试文件被 vitest **真收集真跑**：4 suite 全绿、用例数 ≥ 14、失败 0，且跑的**恰是**本刀那 4 个文件（零收集时 vitest 自报错，不假绿）
  Test: manual:bash -c 'PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; [ -n "$PG" ] || { echo "FAIL: 缺 E2E_DATABASE_URL/DATABASE_URL"; exit 1; }; O=/tmp/wb-rollup-vitest.json; rm -f "$O"; (cd apps/api && npx vitest run --config vitest.workbench-rollup.config.ts --reporter=json --outputFile="$O") >/tmp/wb-rollup-vitest.log 2>&1; RC=$?; [ -f "$O" ] || { echo "FAIL: vitest 未产报告(多半零收集)"; tail -30 /tmp/wb-rollup-vitest.log; exit 1; }; jq -e "(.testResults|length)==4 and .numTotalTests>=14 and .numFailedTests==0 and .success==true" < "$O" >/dev/null || { echo "FAIL: 收集/通过数不符"; jq -c "{files:(.testResults|length),tests:.numTotalTests,failed:.numFailedTests,ok:.success}" < "$O"; exit 1; }; jq -e "[.testResults[].name|select(test(\"rollup-(aggregate|isolation|degrade|type-check-ddl)\\\\.test\\\\.ts$\"))]|length==4" < "$O" >/dev/null || { echo "FAIL: 跑的不是本刀那 4 个文件"; exit 1; }; [ "$RC" = "0" ] || { echo "FAIL: vitest exit=$RC"; exit 1; }; echo OK'
  期望: OK

- [x] [BEHAVIOR] A37 聚合值+数值规整+多值格式化：双企业种子 count=3、sum=52（string "12" 规整计入，绝不字符串拼接冒充）、concat="甲, 乙, 丙"；rollups envelope keys 恰 [cells,table_id]、cell keys 恰五键、禁用字段零命中
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; sf(){ curl -sf -b "$COOKIE_A" "$@"; }; TB=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"RT-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0},{\"name\":\"金额\",\"field_type\":\"number\",\"options\":[],\"display_order\":1}]}" | jq -r ".data.table_id"); TITLEF=$(sf "$API/tables/$TB/fields" | jq -r ".data.fields[]|select(.field_type==\"text\")|.field_id"); AMTF=$(sf "$API/tables/$TB/fields" | jq -r ".data.fields[]|select(.field_type==\"number\")|.field_id"); mkrow(){ R=$(sf -X POST "$API/tables/$TB/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X PATCH "$API/rows/$R" -d "{\"version\":1,\"data\":{\"$TITLEF\":\"$1\",\"$AMTF\":$2}}" >/dev/null; echo "$R"; }; R0=$(mkrow 甲 10); R1=$(mkrow 乙 30); R2=$(mkrow 丙 99); psql "$PG" -q -c "UPDATE zenithjoy.db_rows SET data = jsonb_set(data, ARRAY[\$\$$AMTF\$\$], to_jsonb(\$\$12\$\$::text)) WHERE id = \$\$$R2\$\$" >/dev/null || fail "force string 失败"; TA=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"RS-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null; RELF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"rsum\",\"field_type\":\"rollup\",\"options\":[\"$RELF\",\"$AMTF\",\"sum\"],\"display_order\":20},{\"name\":\"rcount\",\"field_type\":\"rollup\",\"options\":[\"$RELF\",\"\",\"count\"],\"display_order\":21},{\"name\":\"rconcat\",\"field_type\":\"rollup\",\"options\":[\"$RELF\",\"$TITLEF\",\"concat\"],\"display_order\":22}]}" >/dev/null || fail "建 rollup 字段失败"; SUMF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.name==\"rsum\")|.field_id"); CNTF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.name==\"rcount\")|.field_id"); CCTF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.name==\"rconcat\")|.field_id"); AR=$(sf -X POST "$API/tables/$TA/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X PATCH "$API/rows/$AR" -d "{\"version\":1,\"data\":{\"$RELF\":[\"$R0\",\"$R1\",\"$R2\"]}}" >/dev/null || fail "建关联失败"; RU=$(sf "$API/tables/$TA/rollups") || fail "rollups 端点非 2xx"; echo "$RU" | jq -e --arg r "$AR" --arg f "$SUMF" ".data.cells[]|select(.row_id==\$r and .field_id==\$f)|.value==52" >/dev/null || fail "sum 非 52(string 12 未规整 或 字符串拼接冒充)"; echo "$RU" | jq -e --arg r "$AR" --arg f "$CNTF" ".data.cells[]|select(.row_id==\$r and .field_id==\$f)|.value==3" >/dev/null || fail "count 非 3"; echo "$RU" | jq -e --arg r "$AR" --arg f "$CCTF" ".data.cells[]|select(.row_id==\$r and .field_id==\$f)|.value==\"甲, 乙, 丙\"" >/dev/null || fail "concat 非 甲, 乙, 丙"; echo "$RU" | jq -e "(.data|keys)==[\"cells\",\"table_id\"]" >/dev/null || fail "rollups envelope keys 不符"; echo "$RU" | jq -e ".data.cells[]|(keys)==[\"degraded\",\"field_id\",\"fn\",\"row_id\",\"value\"]" >/dev/null || fail "cell keys 不符"; echo "$RU" | jq -e ".data.cells[]|([has(\"id\"),has(\"name\"),has(\"title\"),has(\"result\"),has(\"cell\")]|any|not)" >/dev/null || fail "cell 出现禁用字段"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] A38 聚合隔离只跨本 org：把某目标行 org 直接改成 B 企业后读 rollup → 该行不进聚合基数（count 减 1、sum 不含该行值），聚合值等于仅本 org 存活行期望
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; sf(){ curl -sf -b "$COOKIE_A" "$@"; }; TB=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"IT-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0},{\"name\":\"金额\",\"field_type\":\"number\",\"options\":[],\"display_order\":1}]}" | jq -r ".data.table_id"); TITLEF=$(sf "$API/tables/$TB/fields" | jq -r ".data.fields[]|select(.field_type==\"text\")|.field_id"); AMTF=$(sf "$API/tables/$TB/fields" | jq -r ".data.fields[]|select(.field_type==\"number\")|.field_id"); mkrow(){ R=$(sf -X POST "$API/tables/$TB/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X PATCH "$API/rows/$R" -d "{\"version\":1,\"data\":{\"$TITLEF\":\"$1\",\"$AMTF\":$2}}" >/dev/null; echo "$R"; }; R0=$(mkrow 甲 10); R1=$(mkrow 乙 20); R2=$(mkrow 丙 30); TA=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"IS-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null; RELF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"rsum\",\"field_type\":\"rollup\",\"options\":[\"$RELF\",\"$AMTF\",\"sum\"],\"display_order\":20},{\"name\":\"rcount\",\"field_type\":\"rollup\",\"options\":[\"$RELF\",\"\",\"count\"],\"display_order\":21}]}" >/dev/null; SUMF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.name==\"rsum\")|.field_id"); CNTF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.name==\"rcount\")|.field_id"); AR=$(sf -X POST "$API/tables/$TA/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X PATCH "$API/rows/$AR" -d "{\"version\":1,\"data\":{\"$RELF\":[\"$R0\",\"$R1\",\"$R2\"]}}" >/dev/null; RU=$(sf "$API/tables/$TA/rollups"); echo "$RU" | jq -e --arg r "$AR" --arg f "$SUMF" ".data.cells[]|select(.row_id==\$r and .field_id==\$f)|.value==60" >/dev/null || fail "篡改前 sum 非 60"; psql "$PG" -q -c "UPDATE zenithjoy.db_rows SET org_id = \$\$$ORGB_TENANT_ID\$\$ WHERE id = \$\$$R2\$\$" >/dev/null || fail "篡改 org 失败"; RU2=$(sf "$API/tables/$TA/rollups"); echo "$RU2" | jq -e --arg r "$AR" --arg f "$CNTF" ".data.cells[]|select(.row_id==\$r and .field_id==\$f)|.value==2" >/dev/null || fail "越权行未剔除(count 未减 1)"; echo "$RU2" | jq -e --arg r "$AR" --arg f "$SUMF" ".data.cells[]|select(.row_id==\$r and .field_id==\$f)|.value==30" >/dev/null || fail "sum 含越权行(泄露)"; psql "$PG" -q -c "UPDATE zenithjoy.db_rows SET org_id = \$\$$ORGA_TENANT_ID\$\$ WHERE id = \$\$$R2\$\$" >/dev/null; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] 反枚举 error path：carol(B企业) 以 A 企业真实表 id 与随机不存在 id 各调 `/rollups` 多次 → 两组状态码全 404、响应体逐字节相同、无 timestamp（404 优先 400，不泄露存在性）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; TA=$(curl -sf -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"ENUM-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); RND="00000000-0000-4000-8000-000000000000"; declare -A seen; for i in $(seq 1 10); do CR=$(curl -s -o /tmp/roll-real.json -w "%{http_code}" -b "$COOKIE_B" "$API/tables/$TA/rollups"); CX=$(curl -s -o /tmp/roll-rnd.json -w "%{http_code}" -b "$COOKIE_B" "$API/tables/$RND/rollups"); [ "$CR" = "404" ] || fail "真实表 id 未 404(得 $CR)"; [ "$CX" = "404" ] || fail "随机 id 未 404(得 $CX)"; seen["$(cat /tmp/roll-real.json)"]=1; seen["$(cat /tmp/roll-rnd.json)"]=1; done; [ "${#seen[@]}" = "1" ] || fail "真实/随机响应体可区分(${#seen[@]} 种)"; jq -e "has(\"timestamp\")|not" < /tmp/roll-real.json >/dev/null || fail "404 体带 timestamp(可区分信号)"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] A39 失效降级：删 rollup 依赖的 relation 字段 → 该 rollup 单元格 value 置 null + degraded=true（不悬空、rollups 端点不 5xx、单元格仍在）
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; sf(){ curl -sf -b "$COOKIE_A" "$@"; }; TB=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"DT-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0},{\"name\":\"金额\",\"field_type\":\"number\",\"options\":[],\"display_order\":1}]}" | jq -r ".data.table_id"); TITLEF=$(sf "$API/tables/$TB/fields" | jq -r ".data.fields[]|select(.field_type==\"text\")|.field_id"); AMTF=$(sf "$API/tables/$TB/fields" | jq -r ".data.fields[]|select(.field_type==\"number\")|.field_id"); BR=$(sf -X POST "$API/tables/$TB/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X PATCH "$API/rows/$BR" -d "{\"version\":1,\"data\":{\"$TITLEF\":\"甲\",\"$AMTF\":10}}" >/dev/null; TA=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"DS-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null; RELF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"rsum\",\"field_type\":\"rollup\",\"options\":[\"$RELF\",\"$AMTF\",\"sum\"],\"display_order\":20}]}" >/dev/null; SUMF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.name==\"rsum\")|.field_id"); AR=$(sf -X POST "$API/tables/$TA/rows" | jq -r ".data.row_id"); sf -H "Content-Type: application/json" -X PATCH "$API/rows/$AR" -d "{\"version\":1,\"data\":{\"$RELF\":[\"$BR\"]}}" >/dev/null; sf "$API/tables/$TA/rollups" | jq -e --arg r "$AR" --arg f "$SUMF" ".data.cells[]|select(.row_id==\$r and .field_id==\$f)|.value==10" >/dev/null || fail "基线 sum 非 10"; sf -H "Content-Type: application/json" -X DELETE "$API/tables/$TA/fields/$RELF" -d "{\"confirm_name\":\"关联\"}" >/dev/null || fail "删 relation 字段失败"; CODE=$(curl -s -o /tmp/rollup-deg.json -w "%{http_code}" -b "$COOKIE_A" "$API/tables/$TA/rollups"); [ "$CODE" = "200" ] || fail "删依赖后 rollups 端点返 $CODE(应 200 非 5xx)"; jq -e --arg r "$AR" --arg f "$SUMF" ".data.cells[]|select(.row_id==\$r and .field_id==\$f)|(.value==null and .degraded==true)" < /tmp/rollup-deg.json >/dev/null || fail "删 relation 字段后 rollup 未降级(value null+degraded)"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] A40 类型×函数校验：sum 配 text 目标字段建 rollup → 400 VALIDATION_FAILED + 文案（非静默返错值/NaN）；count 无需目标字段可建 201
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; sf(){ curl -sf -b "$COOKIE_A" "$@"; }; TB=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"TT-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0},{\"name\":\"金额\",\"field_type\":\"number\",\"options\":[],\"display_order\":1}]}" | jq -r ".data.table_id"); TITLEF=$(sf "$API/tables/$TB/fields" | jq -r ".data.fields[]|select(.field_type==\"text\")|.field_id"); TA=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"TS-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null; RELF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); CODE=$(curl -s -o /tmp/rollup-bad.json -w "%{http_code}" -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"badsum\",\"field_type\":\"rollup\",\"options\":[\"$RELF\",\"$TITLEF\",\"sum\"],\"display_order\":20}]}"); [ "$CODE" = "400" ] || fail "sum 配 text 未 400(得 $CODE)"; jq -e ".error.code==\"VALIDATION_FAILED\" and (.error.message|type==\"string\" and length>0)" < /tmp/rollup-bad.json >/dev/null || fail "非 VALIDATION_FAILED 或缺文案(疑静默 NaN)"; C2=$(curl -s -o /dev/null -w "%{http_code}" -b "$COOKIE_A" -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"okcount\",\"field_type\":\"rollup\",\"options\":[\"$RELF\",\"\",\"count\"],\"display_order\":21}]}"); [ "$C2" = "201" ] || fail "count 合法配置未 201(得 $C2)"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] 无运行时 DDL + 读时计算不落库：建 rollup 字段+读聚合全程 zenithjoy schema 表/列集合逐字节不变，零 %rollup% 物理表，配置以三元组落 db_fields.options
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; bash "$S" --fixture-up || exit 1; . ./.wb-fixture.env; PG="${E2E_DATABASE_URL:-${DATABASE_URL:-}}"; fail(){ echo "FAIL: $1"; bash "$S" --fixture-down; exit 1; }; API="http://localhost:$API_PORT/api/knowledge/db"; sf(){ curl -sf -b "$COOKIE_A" "$@"; }; snap(){ psql "$PG" -t -A -q -c "SELECT string_agg(table_name||\$\$.\$\$||column_name, \$\$,\$\$ ORDER BY table_name,column_name) FROM information_schema.columns WHERE table_schema=\$\$zenithjoy\$\$"; }; B0=$(snap); TB=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"NT-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0},{\"name\":\"金额\",\"field_type\":\"number\",\"options\":[],\"display_order\":1}]}" | jq -r ".data.table_id"); AMTF=$(sf "$API/tables/$TB/fields" | jq -r ".data.fields[]|select(.field_type==\"number\")|.field_id"); TA=$(sf -H "Content-Type: application/json" -X POST "$API/tables" -d "{\"name\":\"NS-$SFX\",\"visibility\":\"org\",\"fields\":[{\"name\":\"标题\",\"field_type\":\"text\",\"options\":[],\"display_order\":0}]}" | jq -r ".data.table_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"关联\",\"field_type\":\"relation\",\"options\":[\"$TB\"],\"display_order\":9}]}" >/dev/null; RELF=$(sf "$API/tables/$TA/fields" | jq -r ".data.fields[]|select(.field_type==\"relation\")|.field_id"); sf -H "Content-Type: application/json" -X POST "$API/tables/$TA/fields" -d "{\"fields\":[{\"name\":\"rsum\",\"field_type\":\"rollup\",\"options\":[\"$RELF\",\"$AMTF\",\"sum\"],\"display_order\":20}]}" >/dev/null || fail "建 rollup 字段失败"; sf "$API/tables/$TA/rollups" >/dev/null; B1=$(snap); [ "$B0" = "$B1" ] || fail "运行时 information_schema 变了(疑似运行时 DDL/落库)"; N=$(psql "$PG" -t -A -q -c "SELECT count(*) FROM information_schema.tables WHERE table_schema=\$\$zenithjoy\$\$ AND table_name LIKE \$\$%rollup%\$\$"); [ "$N" = "0" ] || fail "出现 $N 张 rollup 命名物理表"; sf "$API/tables/$TA/fields" | jq -e --arg rel "$RELF" --arg amt "$AMTF" ".data.fields[]|select(.name==\"rsum\")|.options==[\$rel,\$amt,\"sum\"]" >/dev/null || fail "rollup 配置未以三元组落 options"; bash "$S" --fixture-down; echo OK'
  期望: OK

- [x] [BEHAVIOR] A41 墙裁定守卫：rollup 读服务落钉死路径 + 检索特征文件零 workbench-rollup 命中（源码级，无需 DB）
  Test: manual:bash -c 'bash sprints/08222228-workbench-rollup-sprintE/scripts/rollup-wall-guard.sh || { echo "FAIL: A41 守卫报红"; exit 1; }; echo OK'
  期望: OK

- [x] [BEHAVIOR] 4 个变异 proven-to-fire：A38 去 org 条件 / A39 降级分支旁路 / A40 去类型校验 各自让对应 rollup 段 exit≠0；A41 检索特征文件 import rollup 服务令守卫报红
  Test: manual:bash -c 'S=.github/workflows/scripts/smoke/structured-workbench-smoke.sh; prove(){ M="$1"; SEG="$2"; bash "$S" --mutation-apply "$M" || return 1; bash "$S" "$SEG"; RC=$?; bash "$S" --mutation-revert "$M"; [ "$RC" -ne 0 ] || { echo "FAIL: $M 下 $SEG 仍 exit 0"; return 1; }; }; prove A38-rollup-org-bypass --rollup-a38-only || exit 1; prove A39-rollup-degrade-bypass --rollup-a39-only || exit 1; prove A40-rollup-typecheck-off --rollup-a40-only || exit 1; G=sprints/08222228-workbench-rollup-sprintE/scripts/rollup-wall-guard.sh; bash "$S" --mutation-apply A41-rollup-retrieval-import || exit 1; bash "$G"; RC=$?; bash "$S" --mutation-revert A41-rollup-retrieval-import; [ "$RC" -ne 0 ] || { echo "FAIL: A41 import 变异下守卫仍 exit 0"; exit 1; }; echo OK'
  期望: OK

## BEHAVIOR:E2E 条目（user_facing 专属，Mode B final-e2e 跑 windows_cloud）

- [x] [BEHAVIOR:E2E] 用户在真浏览器走完 S4 rollup Golden Path（windows-latest），本刀 rollup step + 截图可视化验证
  Screenshots:
    - 01-rollup-build.png    期望：建 relation 字段 + 配 rollup 字段(sum/count) 后，源表某行单元格显示聚合数值
    - 02-rollup-lookup.png   期望：配 lookup 字段后，单元格显示关联行目标字段多值逗号拼接（含「, 」分隔）
    - 03-rollup-degrade.png  期望：删 rollup 依赖字段后，单元格显示可见降级占位（不显示旧值、不白屏）
  路径格式：sprints/08222228-workbench-rollup-sprintE/screenshots/<step>.png
  期望：e2e-knowledge-hub-path3.yml 的 windows job conclusion==success + 本刀 rollup step success + 从 artifact path3-rollup-screenshots 取回 ≥3 张非空截图（判据见 contract-draft.md `## E2E 验收` bash 块）
