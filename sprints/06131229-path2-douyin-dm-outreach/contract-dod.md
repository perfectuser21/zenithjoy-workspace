---
skeleton: false
journey_type: agent_remote
---
# Contract DoD — Sprint: Path 2 抖音私信主动触达 thin v1

**范围**: 中台 dm_outreach 派单 + agent 回报（sent/limited/failed+error_code）+ 飞书 Lead 触达状态回写 + 单号停用不连坐 + fake-agent smoke。真发 CDP 手验不入自动 E2E。
**大小**: M

> 所有 [BEHAVIOR] 在 local_api 跑，依赖 env：`API_BASE`（中台 localhost）、`DB`、`FEISHU_API_BASE`（fake-feishu）、`SMOKE_TOKEN`。每条自带最小 seed，独立可跑。
> 状态值约定：publish_tasks.status ∈ queued/running/done/failed；agent_platform_sessions.status 停用用 `expired`（受 chk_aps_status CHECK 约束，合法值 pending/active/connected/offline/expired，无 disabled）。

## ARTIFACT 条目

- [ ] [ARTIFACT] smoke 脚本存在且含真链路（派单 + fake-agent 回报 + DB/飞书断言）
  Test: manual:bash -c 'f=.github/workflows/scripts/smoke/golden-path-2-dm-smoke.sh; grep -q "dm-outreach" "$f" && grep -q "dm-outreach-result" "$f" && grep -q "__test/seen-records" "$f" || { echo FAIL; exit 1; }; echo OK'
  期望: OK

- [ ] [ARTIFACT] agent 端 douyin-dm-outreach handler 存在
  Test: manual:bash -c 'f=services/agent/src/handlers/douyin-dm-outreach.ts; grep -q "handleDouyinDmOutreach" "$f" || { echo FAIL; exit 1; }; echo OK'
  期望: OK

- [ ] [ARTIFACT] agent index 注册 dm-outreach handler
  Test: manual:bash -c 'grep -q "douyin-dm-outreach" services/agent/src/index.ts || { echo FAIL; exit 1; }; echo OK'
  期望: OK

- [ ] [ARTIFACT] agent 版本 bump 2.0.13 → 2.0.14
  Test: manual:bash -c 'v=$(node -p "require(\"./services/agent/package.json\").version"); [ "$v" = "2.0.14" ] || { echo "FAIL: version=$v"; exit 1; }; echo OK'
  期望: OK

## BEHAVIOR 条目（manual:bash，自带 seed，独立可跑）

- [ ] [BEHAVIOR] 派单 → DB 落 task_type=dm_outreach/platform=douyin/status=queued + 返 data.task_id（string）+ schema 完整性（data keys=={task_id} + 顶层无禁用字段 id/taskId/result/dm_id）
  Test: manual:bash -c 'set -e; T=$(psql "$DB" -At -c "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES ('"'"'dm-$RANDOM'"'"','"'"'k-$RANDOM'"'"','"'"'free'"'"') RETURNING id"|grep -oE "[0-9a-f-]{36}"|head -1); psql "$DB" -c "INSERT INTO zenithjoy.tenant_feishu_bindings(tenant_id,tenant_access_token,expires_at,app_token,table_id_leads,bound_at) VALUES ('"'"'$T'"'"','"'"'fake_t'"'"',NOW()+interval'"'"'1 hour'"'"','"'"'bascn_x'"'"','"'"'tbl_b1_leads'"'"',NOW())">/dev/null; A=$(psql "$DB" -At -c "INSERT INTO zenithjoy.agents(tenant_id,agent_id,hostname,status) VALUES ('"'"'$T'"'"','"'"'a-$RANDOM'"'"','"'"'xian-pc'"'"','"'"'online'"'"') RETURNING id"|grep -oE "[0-9a-f-]{36}"|head -1); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions(agent_id,platform,account_label,role,status,bound_at,created_at) VALUES ('"'"'$A'"'"','"'"'douyin'"'"','"'"'号1'"'"','"'"'burner'"'"','"'"'active'"'"',NOW(),NOW())">/dev/null; R=$(curl -sf -X POST "$API_BASE/api/agent/burner/dm-outreach" -H "Content-Type: application/json" -d "{\"tenant_id\":\"$T\",\"agent_id\":\"$A\",\"account_label\":\"号1\",\"profile_url\":\"https://www.douyin.com/user/MS4w1\",\"message\":\"hi\"}"); echo "$R" | jq -e ".success==true and (.data.task_id|type==\"string\") and (.id|not) and (.taskId|not) and (.result|not) and (.dm_id|not) and ((.data|keys|sort)==[\"task_id\"])"; ID=$(echo "$R"|jq -r .data.task_id); C=$(psql "$DB" -At -c "SELECT count(*) FROM zenithjoy.publish_tasks WHERE id='"'"'$ID'"'"' AND task_type='"'"'dm_outreach'"'"' AND platform='"'"'douyin'"'"' AND status='"'"'queued'"'"' AND created_at>NOW()-interval'"'"'5 minutes'"'"'"); [ "$C" = "1" ] || { echo "FAIL count=$C"; exit 1; }; echo OK'
  期望: OK

- [ ] [BEHAVIOR] sent 全链：触达中→回报 sent → task done + dm_status=sent + 飞书新增「已私信」(含触达小号/触达时间) + 查状态终态
  Test: manual:bash -c 'set -e; curl -fsS -X POST "${FEISHU_API_BASE}/__test/reset">/dev/null||true; T=$(psql "$DB" -At -c "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES ('"'"'dm-$RANDOM'"'"','"'"'k-$RANDOM'"'"','"'"'free'"'"') RETURNING id"|grep -oE "[0-9a-f-]{36}"|head -1); psql "$DB" -c "INSERT INTO zenithjoy.tenant_feishu_bindings(tenant_id,tenant_access_token,expires_at,app_token,table_id_leads,bound_at) VALUES ('"'"'$T'"'"','"'"'fake_t'"'"',NOW()+interval'"'"'1 hour'"'"','"'"'bascn_x'"'"','"'"'tbl_b1_leads'"'"',NOW())">/dev/null; A=$(psql "$DB" -At -c "INSERT INTO zenithjoy.agents(tenant_id,agent_id,hostname,status) VALUES ('"'"'$T'"'"','"'"'a-$RANDOM'"'"','"'"'xian-pc'"'"','"'"'online'"'"') RETURNING id"|grep -oE "[0-9a-f-]{36}"|head -1); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions(agent_id,platform,account_label,role,status,bound_at,created_at) VALUES ('"'"'$A'"'"','"'"'douyin'"'"','"'"'号1'"'"','"'"'burner'"'"','"'"'active'"'"',NOW(),NOW())">/dev/null; ID=$(curl -sf -X POST "$API_BASE/api/agent/burner/dm-outreach" -H "Content-Type: application/json" -d "{\"tenant_id\":\"$T\",\"agent_id\":\"$A\",\"account_label\":\"号1\",\"profile_url\":\"https://www.douyin.com/user/MS4wS\",\"message\":\"hi\"}"|jq -r .data.task_id); curl -fsS -X POST "$API_BASE/api/_smoke/fake-agent-burner-progress" -H "X-Smoke-Token: $SMOKE_TOKEN" -H "Content-Type: application/json" -d "{\"task_id\":\"$ID\",\"phase\":\"dm_in_progress\"}">/dev/null; R=$(curl -sf -X POST "$API_BASE/api/agent/burner/dm-outreach-result" -H "X-Smoke-Token: $SMOKE_TOKEN" -H "Content-Type: application/json" -d "{\"task_id\":\"$ID\",\"agent_id\":\"$A\",\"account_label\":\"号1\",\"status\":\"sent\",\"profile_url\":\"https://www.douyin.com/user/MS4wS\",\"screenshot_path\":\"/tmp/s.png\"}"); echo "$R"|jq -e ".data.status==\"sent\" and .data.lead_write_status==\"success\" and (.dm_status|not) and (.ok|not) and (.written|not) and (.negation|not) and ((.data|keys|sort)==[\"feishu_bitable_url\",\"lead_write_status\",\"status\",\"task_id\"])"; ST=$(psql "$DB" -At -c "SELECT status FROM zenithjoy.publish_tasks WHERE id='"'"'$ID'"'"' AND updated_at>NOW()-interval'"'"'5 minutes'"'"'"); [ "$ST" = "done" ]||{ echo "FAIL st=$ST"; exit 1; }; sleep 1; curl -sf "${FEISHU_API_BASE}/__test/seen-records?table_id=tbl_b1_leads"|jq -e "[.records[]|select(.[\"触达状态\"]==\"已私信\")]|any((.[\"触达小号\"]|length>0) and (.[\"触达时间\"]|length>0))"; curl -sf "$API_BASE/api/agent/burner/dm-tasks/$ID"|jq -e ".data.status==\"done\" and .data.dm_status==\"sent\" and (.data.feishu_bitable_url|type==\"string\")"; echo OK'
  期望: OK

- [ ] [BEHAVIOR] limited（仅互关）→ dm_status=limited + 飞书该主页写「未送达-仅互关」且**不含**「已私信」（禁止假 sent）
  Test: manual:bash -c 'set -e; curl -fsS -X POST "${FEISHU_API_BASE}/__test/reset">/dev/null||true; T=$(psql "$DB" -At -c "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES ('"'"'dm-$RANDOM'"'"','"'"'k-$RANDOM'"'"','"'"'free'"'"') RETURNING id"|grep -oE "[0-9a-f-]{36}"|head -1); psql "$DB" -c "INSERT INTO zenithjoy.tenant_feishu_bindings(tenant_id,tenant_access_token,expires_at,app_token,table_id_leads,bound_at) VALUES ('"'"'$T'"'"','"'"'fake_t'"'"',NOW()+interval'"'"'1 hour'"'"','"'"'bascn_x'"'"','"'"'tbl_b1_leads'"'"',NOW())">/dev/null; A=$(psql "$DB" -At -c "INSERT INTO zenithjoy.agents(tenant_id,agent_id,hostname,status) VALUES ('"'"'$T'"'"','"'"'a-$RANDOM'"'"','"'"'xian-pc'"'"','"'"'online'"'"') RETURNING id"|grep -oE "[0-9a-f-]{36}"|head -1); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions(agent_id,platform,account_label,role,status,bound_at,created_at) VALUES ('"'"'$A'"'"','"'"'douyin'"'"','"'"'号1'"'"','"'"'burner'"'"','"'"'active'"'"',NOW(),NOW())">/dev/null; U="https://www.douyin.com/user/MS4wL"; ID=$(curl -sf -X POST "$API_BASE/api/agent/burner/dm-outreach" -H "Content-Type: application/json" -d "{\"tenant_id\":\"$T\",\"agent_id\":\"$A\",\"account_label\":\"号1\",\"profile_url\":\"$U\",\"message\":\"hi\"}"|jq -r .data.task_id); curl -sf -X POST "$API_BASE/api/agent/burner/dm-outreach-result" -H "X-Smoke-Token: $SMOKE_TOKEN" -H "Content-Type: application/json" -d "{\"task_id\":\"$ID\",\"agent_id\":\"$A\",\"account_label\":\"号1\",\"status\":\"limited\",\"profile_url\":\"$U\"}"|jq -e ".data.status==\"limited\""; sleep 1; AL=$(curl -sf "${FEISHU_API_BASE}/__test/seen-records?table_id=tbl_b1_leads"); echo "$AL"|jq -e "[.records[]|select(.[\"触达主页 URL\"]==\"$U\")]|any(.[\"触达状态\"]==\"未送达-仅互关\")"; echo "$AL"|jq -e "[.records[]|select(.[\"触达主页 URL\"]==\"$U\")]|any(.[\"触达状态\"]==\"已私信\")|not"; echo OK'
  期望: OK

- [ ] [BEHAVIOR] failed+error_code=SESSION_EXPIRED → task failed + 该号 status=expired + 同 agent 另一号仍 active（不连坐）+ 飞书失败原因
  Test: manual:bash -c 'set -e; curl -fsS -X POST "${FEISHU_API_BASE}/__test/reset">/dev/null||true; T=$(psql "$DB" -At -c "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES ('"'"'dm-$RANDOM'"'"','"'"'k-$RANDOM'"'"','"'"'free'"'"') RETURNING id"|grep -oE "[0-9a-f-]{36}"|head -1); psql "$DB" -c "INSERT INTO zenithjoy.tenant_feishu_bindings(tenant_id,tenant_access_token,expires_at,app_token,table_id_leads,bound_at) VALUES ('"'"'$T'"'"','"'"'fake_t'"'"',NOW()+interval'"'"'1 hour'"'"','"'"'bascn_x'"'"','"'"'tbl_b1_leads'"'"',NOW())">/dev/null; A=$(psql "$DB" -At -c "INSERT INTO zenithjoy.agents(tenant_id,agent_id,hostname,status) VALUES ('"'"'$T'"'"','"'"'a-$RANDOM'"'"','"'"'xian-pc'"'"','"'"'online'"'"') RETURNING id"|grep -oE "[0-9a-f-]{36}"|head -1); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions(agent_id,platform,account_label,role,status,bound_at,created_at) VALUES ('"'"'$A'"'"','"'"'douyin'"'"','"'"'号1'"'"','"'"'burner'"'"','"'"'active'"'"',NOW(),NOW()),('"'"'$A'"'"','"'"'douyin'"'"','"'"'号2'"'"','"'"'burner'"'"','"'"'active'"'"',NOW(),NOW())">/dev/null; U="https://www.douyin.com/user/MS4wF"; ID=$(curl -sf -X POST "$API_BASE/api/agent/burner/dm-outreach" -H "Content-Type: application/json" -d "{\"tenant_id\":\"$T\",\"agent_id\":\"$A\",\"account_label\":\"号1\",\"profile_url\":\"$U\",\"message\":\"hi\"}"|jq -r .data.task_id); curl -sf -X POST "$API_BASE/api/agent/burner/dm-outreach-result" -H "X-Smoke-Token: $SMOKE_TOKEN" -H "Content-Type: application/json" -d "{\"task_id\":\"$ID\",\"agent_id\":\"$A\",\"account_label\":\"号1\",\"status\":\"failed\",\"error_code\":\"SESSION_EXPIRED\",\"profile_url\":\"$U\"}"|jq -e ".data.status==\"failed\" and .data.session_disabled==true"; ST=$(psql "$DB" -At -c "SELECT status FROM zenithjoy.publish_tasks WHERE id='"'"'$ID'"'"' AND updated_at>NOW()-interval'"'"'5 minutes'"'"'"); EC=$(psql "$DB" -At -c "SELECT response->>'"'"'error_code'"'"' FROM zenithjoy.publish_tasks WHERE id='"'"'$ID'"'"'"); [ "$ST" = "failed" ] && [ "$EC" = "SESSION_EXPIRED" ]||{ echo "FAIL st=$ST ec=$EC"; exit 1; }; S1=$(psql "$DB" -At -c "SELECT status FROM zenithjoy.agent_platform_sessions WHERE agent_id='"'"'$A'"'"' AND account_label='"'"'号1'"'"'"); S2=$(psql "$DB" -At -c "SELECT status FROM zenithjoy.agent_platform_sessions WHERE agent_id='"'"'$A'"'"' AND account_label='"'"'号2'"'"'"); [ "$S1" = "expired" ]||{ echo "FAIL 未停用 s1=$S1"; exit 1; }; [ "$S2" = "active" ]||{ echo "FAIL 连坐 s2=$S2"; exit 1; }; sleep 1; curl -sf "${FEISHU_API_BASE}/__test/seen-records?table_id=tbl_b1_leads"|jq -e "[.records[]|select(.[\"触达主页 URL\"]==\"$U\")]|any(.[\"触达状态\"]==\"失败\" and (.[\"失败原因\"]==\"SESSION_EXPIRED\"))"; echo OK'
  期望: OK

- [ ] [BEHAVIOR] 查状态端点 schema + 未知 task_id 返 404 NO_DM_TASK
  Test: manual:bash -c 'set -e; CODE=$(curl -s -o /tmp/dm404.json -w "%{http_code}" "$API_BASE/api/agent/burner/dm-tasks/00000000-0000-0000-0000-000000000000"); [ "$CODE" = "404" ]||{ echo "FAIL code=$CODE"; exit 1; }; jq -e ".error.code==\"NO_DM_TASK\"" /tmp/dm404.json||{ echo FAIL; exit 1; }; echo OK'
  期望: OK

- [ ] [BEHAVIOR] error path：派单缺 profile_url→400 MISSING_PROFILE_URL；缺 message→400 MISSING_MESSAGE；无 burner→400 NO_BURNER_SESSION
  Test: manual:bash -c 'set -e; T=$(psql "$DB" -At -c "INSERT INTO zenithjoy.tenants(name,license_key,plan) VALUES ('"'"'dm-$RANDOM'"'"','"'"'k-$RANDOM'"'"','"'"'free'"'"') RETURNING id"|grep -oE "[0-9a-f-]{36}"|head -1); psql "$DB" -c "INSERT INTO zenithjoy.tenant_feishu_bindings(tenant_id,tenant_access_token,expires_at,app_token,table_id_leads,bound_at) VALUES ('"'"'$T'"'"','"'"'fake_t'"'"',NOW()+interval'"'"'1 hour'"'"','"'"'bascn_x'"'"','"'"'tbl_b1_leads'"'"',NOW())">/dev/null; A=$(psql "$DB" -At -c "INSERT INTO zenithjoy.agents(tenant_id,agent_id,hostname,status) VALUES ('"'"'$T'"'"','"'"'a-$RANDOM'"'"','"'"'xian-pc'"'"','"'"'online'"'"') RETURNING id"|grep -oE "[0-9a-f-]{36}"|head -1); psql "$DB" -c "INSERT INTO zenithjoy.agent_platform_sessions(agent_id,platform,account_label,role,status,bound_at,created_at) VALUES ('"'"'$A'"'"','"'"'douyin'"'"','"'"'号1'"'"','"'"'burner'"'"','"'"'active'"'"',NOW(),NOW())">/dev/null; C1=$(curl -s -o /tmp/e1.json -w "%{http_code}" -X POST "$API_BASE/api/agent/burner/dm-outreach" -H "Content-Type: application/json" -d "{\"tenant_id\":\"$T\",\"agent_id\":\"$A\",\"account_label\":\"号1\",\"message\":\"x\"}"); [ "$C1" = "400" ] && jq -e ".error.code==\"MISSING_PROFILE_URL\"" /tmp/e1.json||{ echo FAIL1; exit 1; }; C2=$(curl -s -o /tmp/e2.json -w "%{http_code}" -X POST "$API_BASE/api/agent/burner/dm-outreach" -H "Content-Type: application/json" -d "{\"tenant_id\":\"$T\",\"agent_id\":\"$A\",\"account_label\":\"号1\",\"profile_url\":\"https://www.douyin.com/user/x\"}"); [ "$C2" = "400" ] && jq -e ".error.code==\"MISSING_MESSAGE\"" /tmp/e2.json||{ echo FAIL2; exit 1; }; C3=$(curl -s -o /tmp/e3.json -w "%{http_code}" -X POST "$API_BASE/api/agent/burner/dm-outreach" -H "Content-Type: application/json" -d "{\"tenant_id\":\"$T\",\"agent_id\":\"$A\",\"account_label\":\"没有的号\",\"profile_url\":\"https://www.douyin.com/user/x\",\"message\":\"x\"}"); [ "$C3" = "400" ] && jq -e ".error.code==\"NO_BURNER_SESSION\"" /tmp/e3.json||{ echo FAIL3; exit 1; }; echo OK'
  期望: OK
