---
id: dod-works-database-schema
version: 1.0.0
created: 2026-02-10
branch: cp-20260210-works-database-schema
prd: .prd-works-management.md
---

# Definition of Done: 数据库 Migration 脚本

## 📋 验收清单

### 1. Migration 脚本文件

- [ ] **创建 migration 脚本文件**
  - Test: 文件存在于 `docs/database/migrations/` 目录
  - Test: 文件命名为 `002_create_works_tables.sql`
  - Test: 文件包含 SQL 语句

### 2. zenithjoy.works 表

- [ ] **works 表结构正确**
  - Test: 包含所有必要字段（id, title, body, content_type 等）
  - Test: id 字段为 UUID PRIMARY KEY
  - Test: custom_fields 字段为 JSONB 类型
  - Test: 包含时间戳字段（created_at, updated_at 等）
  - Test: 包含索引定义

### 3. zenithjoy.publish_logs 表

- [ ] **publish_logs 表结构正确**
  - Test: 包含所有必要字段（id, work_id, platform 等）
  - Test: work_id 外键关联到 works(id)
  - Test: 包含 UNIQUE 约束（work_id + platform）
  - Test: 包含索引定义

### 4. zenithjoy.field_definitions 表

- [ ] **field_definitions 表结构正确**
  - Test: 包含所有必要字段（id, field_name, field_type 等）
  - Test: field_name 有 UNIQUE 约束
  - Test: options 字段为 JSONB 类型
  - Test: 包含初始数据（预设字段）

### 5. platform_posts 表扩展

- [ ] **platform_posts 表增加 work_id 字段**
  - Test: 添加 work_id UUID 字段
  - Test: 创建索引 idx_platform_posts_work
  - Test: 字段可为 NULL（兼容历史数据）

### 6. 文档完整性

- [ ] **README 或说明文档**
  - Test: 包含表结构说明
  - Test: 包含字段说明
  - Test: 包含执行方法

### 7. SQL 语法正确性

- [ ] **SQL 可执行**
  - Test: 使用 `IF NOT EXISTS` 避免重复创建
  - Test: 事务包裹（BEGIN/COMMIT）
  - Test: 包含错误处理

## 🧪 测试方法

### 本地测试

```bash
# 1. 检查文件存在
ls docs/database/migrations/002_create_works_tables.sql

# 2. 语法检查（干跑）
psql -h localhost -U postgres -d cecelia --dry-run -f docs/database/migrations/002_create_works_tables.sql

# 3. 实际执行（测试环境）
psql -h localhost -U postgres -d cecelia -f docs/database/migrations/002_create_works_tables.sql

# 4. 验证表创建
psql -h localhost -U postgres -d cecelia -c "\dt zenithjoy.*"
psql -h localhost -U postgres -d cecelia -c "\d zenithjoy.works"
psql -h localhost -U postgres -d cecelia -c "\d zenithjoy.publish_logs"
psql -h localhost -U postgres -d cecelia -c "\d zenithjoy.field_definitions"
psql -h localhost -U postgres -d cecelia -c "\d platform_posts"
```

## 📊 完成标准

- ✅ 所有表创建成功
- ✅ 所有索引创建成功
- ✅ 外键约束正确
- ✅ 初始数据插入成功
- ✅ 文档完整清晰

## 🚫 不在范围内

- ❌ 不包含 API 实现
- ❌ 不包含前端代码
- ❌ 不包含数据迁移（历史数据）
- ❌ 不包含性能优化

## 📝 相关文件

- PRD: `.prd-works-management.md`（数据模型章节）
- Schema 参考: `/home/xx/dev/perfect21-platform/docs/database/migrations/001_init.sql`
