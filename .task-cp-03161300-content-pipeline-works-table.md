---
id: task-cp-03161300-content-pipeline-works-table
type: task-card
branch: cp-03161300-content-pipeline-works-table
created: 2026-03-16
---

# Task Card: 内容流水线串联 — works 表 + content-id 接口

## 需求（What & Why）

**功能描述**：打通内容自动化流水线的三个核心断点：
1. 创建 `zenithjoy.works` + `zenithjoy.publish_logs` 数据库表（migration）
2. 新增 `nas-fetch-content.sh` 脚本：接受 `content-id`，从 NAS 读取内容路径，供 publisher 使用
3. 更新 `longform-creator` skill：创作完成后自动 INSERT 到 `zenithjoy.works` 表

**背景**：当前创作→发布→数据采集三个环节完全孤立，没有中央注册表，无法追踪内容生命周期。`zenithjoy.works` 表是整个流水线的枢纽。

**不做什么**：不改动现有 publisher 脚本的发布逻辑，不改动 scraper，不做前端 UI。

## 成功标准

1. [ARTIFACT] `docs/database/migrations/003_apply_works_tables.sql` 文件存在（包含建表 + 执行的幂等脚本）
2. [ARTIFACT] `infrastructure/scripts/nas-fetch-content.sh` 脚本存在，接受 `--content-id` 参数
3. [ARTIFACT] `cecelia` repo 的 `longform-creator` SKILL.md 包含 Step 5.5 写库步骤
4. [BEHAVIOR] 调用 `nas-fetch-content.sh --content-id 2025-11-03-009a0b` 能返回 NAS 路径
5. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] `docs/database/migrations/003_apply_works_tables.sql` 存在且包含 CREATE TABLE zenithjoy.works
  Test: manual:bash -c "grep -q 'zenithjoy.works' docs/database/migrations/003_apply_works_tables.sql"

- [x] [ARTIFACT] `infrastructure/scripts/nas-fetch-content.sh` 存在且可执行
  Test: manual:bash -c "test -f infrastructure/scripts/nas-fetch-content.sh"

- [x] [ARTIFACT] nas-fetch-content.sh 包含 --content-id 参数处理
  Test: manual:bash -c "grep -q 'content-id\|content_id' infrastructure/scripts/nas-fetch-content.sh"

- [x] [BEHAVIOR] nas-fetch-content.sh 能读取 NAS manifest 返回路径信息
  Test: manual:node -e "const fs=require('fs');if(!fs.existsSync('infrastructure/scripts/nas-fetch-content.sh'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:node -e "const fs=require('fs');['docs/database/migrations/003_apply_works_tables.sql','infrastructure/scripts/nas-fetch-content.sh'].forEach(f=>{if(!fs.existsSync(f))process.exit(1)})"

## 实现方案

**要改的文件**：
- 新增 `docs/database/migrations/003_apply_works_tables.sql` （zenithjoy repo）
- 新增 `infrastructure/scripts/nas-fetch-content.sh` （zenithjoy repo）

**Scope 锚定**：zenithjoy repo 内改动。longform-creator skill 更新走 cecelia repo 单独 PR。
