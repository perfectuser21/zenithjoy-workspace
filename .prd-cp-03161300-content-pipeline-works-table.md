# PRD: 内容流水线串联 — works 表 + content-id 接口

## 目标
打通内容自动化流水线三个断点，建立 zenithjoy.works 为中心的内容注册表。

## 范围
1. 执行 migration 创建 zenithjoy.works + zenithjoy.publish_logs 表
2. 新增 nas-fetch-content.sh：接受 content-id，返回 NAS 路径
3. longform-creator skill 写库（cecelia repo 单独 PR）

## 成功标准
- works 表存在，含 content_type / status / nas_path 字段
- nas-fetch-content.sh 可通过 content-id 查找 NAS 内容
- 整个创作→NAS→works表→publisher 流程可以端到端跑通
